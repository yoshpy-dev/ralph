# Verify report: local-branch-secret-scan

- Date: 2026-09-22
- Plan: `docs/plans/active/2026-09-20-local-branch-secret-scan.md` (issue #169)
- Verifier: `verifier` subagent (Claude Code), pipeline cycle 1/2
- Scope: spec compliance + static analysis for `git diff main...HEAD` at `186183c` (branch `feat/local-branch-secret-scan`). Not in scope: diff quality (`/self-review`, already run — `docs/reports/self-review-2026-09-21-local-branch-secret-scan.md`, 2 MEDIUM + 10 LOW, all fixed in `3afac6e`) or a test verdict (`/test`).

## Deterministic checks run

| Command | Result | Notes |
| --- | --- | --- |
| `RALPH_VERIFY_SCOPE=full ./scripts/run-static-verify.sh` | OK (exit 0) | `check-sync.sh` (IDENTICAL 159 / DRIFTED 0 / KNOWN_DIFF 5), `check-pipeline-sync.sh`, `check-skill-sync.sh` (13 skills in lock-step), `check-template-purity.sh`, golang verifier (`gofmt: ok`, `0 issues.`), then the new branch secret scan step itself (`secret-scan-branch: scanned a0d5bf5..186183c against origin/main: clean`). Evidence: `docs/evidence/verify-2026-09-21-214633.log` |
| `sh -n` on all 7 changed/new shell files (`secret-scan-branch.sh`, `run-verify.sh`, `secret-scan.sh`, `check-template.sh`, `xreview-helpers.sh`, both new test files) | OK | no output |
| `shellcheck -s sh` on the 5 production scripts | OK | `secret-scan-branch.sh`: only SC2329 (info, cleanup invoked via trap) and SC1091 (info, sourced file not followed) — both expected for this pattern. `run-verify.sh`, `secret-scan.sh`, `xreview-helpers.sh`: clean. `check-template.sh`: 3× SC2044 (warning, for-loop over `find`) — confirmed pre-existing on `main` (`git show main:scripts/check-template.sh \| shellcheck -s sh -` reproduces the same 3 warnings), not introduced by this diff |
| `shellcheck -s sh` on both new test files | OK | zero findings |
| `./scripts/check-sync.sh` (standalone) | OK — IDENTICAL 159, DRIFTED 0, ROOT_ONLY 0, TEMPLATE_ONLY 11, KNOWN_DIFF 5 | |
| `./scripts/check-template.sh` (standalone) | OK — "Template structure looks good.", exit 0 | prints 7 `FAIL: ... references missing hook: ./.claude/hooks/ralph-dispatch.sh <event>` lines but still exits 0; reproduced byte-for-byte against `main`'s copy of the script run in this same worktree — a pre-existing false-positive in the hook-reference check, not something this diff introduced or regresses |
| `./scripts/check-template-purity.sh` (standalone) | OK — no meta-repo-specific references in templates | |
| `./scripts/check-skill-sync.sh` (standalone) | OK — 13 skills in lock-step | |
| `gofmt -l internal/scaffold` | OK (no output) | |
| `go vet ./internal/scaffold/...` | OK (no output) | also proves the package compiles with the new `secret-scan-branch.sh` entry in `TestTemplateBaseScriptsExist` |
| `cmp` across all mirror pairs | OK, byte-identical | `.claude/skills/pr/SKILL.md` ↔ `.agents/skills/pr/SKILL.md` ↔ both `templates/base/` copies; `scripts/{secret-scan-branch.sh,run-verify.sh,secret-scan.sh,check-template.sh,xreview-helpers.sh}` ↔ their `templates/base/scripts/` copies. New script mode `755` on both sides |
| `./scripts/secret-scan-branch.sh --strict` (this branch, item 6) | OK — `scanned a0d5bf5..186183c against origin/main: clean`, exit 0 | |
| `./scripts/secret-scan.sh --range "$(git merge-base HEAD origin/main)..HEAD"` (item 6) | OK, exit 0, no output | |

No test suite was run (`go test`/`run-test.sh`), per this task's scope — that is `/test`'s responsibility.

## Spec compliance (acceptance criteria)

| AC | Status | Evidence |
| --- | --- | --- |
| AC-1 | Met | `tests/test-secret-scan-branch.sh:191-` (`AC-1: deleted secret still found by history scan`) reads a commit sequence where an early commit adds a secret-shaped line and a later commit deletes it; asserts exit 1 and a `findings` line. Read-confirmed against `scripts/secret-scan-branch.sh:141-152`: the scanner is invoked with `--range "$merge_base..HEAD"`, i.e. `git log -p` over the whole range, which by construction re-reads every commit's diff including the deleted line's introduction — this is the same mechanism `scripts/secret-scan.sh`'s pre-existing `--range` mode already uses, not new logic in this diff. |
| AC-2 | Met | Default-mode exit-0 paths verified in code and tests for all five listed conditions: base branch (`nothing_to_scan "HEAD is the base branch ..."`, `secret-scan-branch.sh:114-116`; test lines ~259-266), no base ref (`cannot_scan "no base ref for ..."`, `:120-125`; test ~445-450), no merge-base (`cannot_scan "no merge-base ..."`, `:127-129`; test ~466-472), empty range (`nothing_to_scan "no commits between ..."`, `:131-134`; test ~271-277), outside git (`cannot_scan "not inside a git work tree"`, `:96-98`; test ~477-482). Every path prints exactly one `secret-scan-branch: <reason>` line via `report_and_exit`/`report` and, in default mode, exits 0 (`strict=0` branch, `:75-80`). |
| AC-2b | Met | All six listed conditions (scanner missing, outside git worktree, no base ref either way, no merge-base, HEAD is base branch, empty range) route through `report_and_exit`, which exits 3 under `--strict` (`secret-scan-branch.sh:73-80`) — confirmed by 6 paired default/strict test blocks in `tests/test-secret-scan-branch.sh` (each asserts exit 0 in default mode and exit 3 in `--strict` mode for the same fixture, e.g. lines 259-266 base-branch, 271-277 empty-range, 304-307 base-override-to-self bypass, 331-334 fast-forward bypass, 445-450 no-base-ref, 466-472 unrelated-histories, 477-482 outside-git, 507-512 scanner-missing). The two Codex-advisory bypass scenarios the self-review reviewer demonstrated (base override naming the current branch; a local base fast-forwarded onto HEAD with no `origin/<base>`) are the two now covered by `test_base_override_reporting`/`test_fast_forward_bypass` — both now correctly exit 3 under `--strict` per the self-review-cycle-1 revision. `report_and_exit` exit-0-under-strict only happens on the `scanned ...: clean` line at `:161`, a separate code path from `report_and_exit`, satisfying "strict exit 0 means scanned and clean." |
| AC-2c | Met | `secret-scan-branch.sh:135-140` builds `tmp_allowlist` from `git show HEAD:.gitallowed` (empty file if HEAD has none) and always passes `RALPH_SECRET_ALLOWLIST="$tmp_allowlist"` to the scanner (`:159`), overriding any caller-set value. The ignore notice fires on `[ -n "${RALPH_SECRET_ALLOWLIST:-}" ] \|\| [ "$worktree_differs" -eq 1 ]` (`:145-147`). Tests: `tests/test-secret-scan-branch.sh` AC-2c block (~line 560-600) — finding present before any allowlist (exit 1), uncommitted worktree `.gitallowed` + `RALPH_SECRET_ALLOWLIST` override still finds (exit 1) with the ignore notice asserted, then committing the allowlist line suppresses it (exit 0, `assert_scanned_clean`). |
| AC-3 | Met | `secret-scan-branch.sh:99-108`: `GITHUB_BASE_REF` first, else `. xreview-helpers.sh; detect_base_branch`. Base-ref preference at `:120-125`: `refs/remotes/origin/$base_name` checked before `refs/heads/$base_name`. `test_origin_preference_and_fallback` (~line 336-390) proves the preference with opposing verdicts (`git update-ref refs/remotes/origin/main <leak-commit>` vs. a clean local `main`) — the strongest form of this proof, not just presence/absence. `test_base_override_reporting` (~396-430) covers `GITHUB_BASE_REF` and `RALPH_XREVIEW_BASE` (via `detect_base_branch`) each selecting `develop`. |
| AC-4 | Met | `scripts/run-verify.sh:212-227`: `case "$HARNESS_VERIFY_MODE" in static\|all)` gates the whole block; `test` mode is excluded by omission. `RALPH_VERIFY_SKIP_BRANCH_SECRET_SCAN` only skips when the value is exactly `"1"` (`:215`, `[ "${RALPH_VERIFY_SKIP_BRANCH_SECRET_SCAN:-}" = "1" ]`), with the skip line printed (`:216`). `tests/test-run-verify-branch-secret-scan.sh` confirms: static/all run it and test mode does not (lines ~144-153); a finding fails the run and prints an explicit `Branch secret scan failed` line even when `ran_any=0` and the run would otherwise close on the docs-only summary (`test_finding_fails_even_with_ran_any_zero`, ~163-176 — this is the self-review M1 regression test, and its own comment correctly states what it asserts this time, unlike the inverted comment self-review flagged in cycle 0); the same failure line also appears when a language verifier ran and passed (`test_finding_fails_with_ran_any_one`, ~184-204); the skip var is tested for `=1` (skips), and — per the self-review LOW D3 fix — for `=0`/unset (does not skip, code around ~207+); the docs-only clean-run summary text is asserted unchanged (`test_docs_only_output_unchanged`, ~235-241). |
| AC-5 | Met | `scripts/secret-scan.sh:145-147` adds the three guidance lines (bracket-expression example `api_ke[y]`) with no change to the scanner's regex patterns or `is_allowed` logic (confirmed by reading the full diff of `secret-scan.sh`: only the three `printf` lines were added, nothing else). `tests/test-secret-scan-branch.sh` AC-5 block (~line 602-650): a commit whose `.gitallowed` line self-matches the generic-secret-assignment pattern is still a finding (exit 1) in the branch-history scan (because a *later* commit changes the line, reproducing the PR #168 shape per the plan's documented two-commit test construction), and the bracket-expression form of the same intent is not a finding (exit 0). |
| AC-6 | Met | `.claude/skills/pr/SKILL.md` Step 3 (between commit staging Step 2 and push Step 4) runs `./scripts/secret-scan-branch.sh --strict` and states: exit 0 = continue, exit 1 = do not push (rewrite or add a bracket-expression `.gitallowed` line), exit 3 = do not push (fix the cause, re-run). Completion gate adds `[ ] ./scripts/secret-scan-branch.sh --strict exited 0 before every push`. Step 8 (plan archive) explicitly requires re-running the strict scan against the new `HEAD` before its own push. All internal step-number cross-references in the skill (Step 1, Step 3, Step 8) are self-consistent with the renumbered 1-10 sequence — confirmed by reading the full file. `cmp` confirms byte-identity across all four faces (`.claude/`, `.agents/`, both `templates/base/` copies). `check-skill-sync.sh` (13 skills in lock-step), `check-sync.sh` (DRIFTED 0), and `check-template-purity.sh` (clean) all pass, as run above. No other repo file references a specific `/pr` step number (`grep -rn "pr/SKILL.md.*Step\|pr skill.*Step [0-9]"` across `.claude`, `.agents`, `docs`, `README.md`, `AGENTS.md` returns nothing outside the skill itself), so the renumbering has no external cross-reference to update. |
| AC-7 | Partially met (static half only) | `scripts/secret-scan-branch.sh` and `templates/base/scripts/secret-scan-branch.sh` are byte-identical (`cmp`, above), both mode 755. `scripts/check-template.sh:25-26` lists both `scripts/secret-scan-branch.sh` and `scripts/xreview-helpers.sh` in `required_files` (the latter added per self-review LOW fix, closing the dependency gap the self-review flagged). `internal/scaffold/embed_test.go:78` lists `"secret-scan-branch.sh"` in `TestTemplateBaseScriptsExist`. `gofmt`/`go vet` on `internal/scaffold` are clean, confirming the package compiles with this change. The "scaffold / upgrade の Go テストが通る" half of this AC is a test-execution claim and is out of `/verify`'s scope — deferred to `/test`. |
| AC-8 | Not applicable to `/verify` | `run-verify.sh`/`run-test.sh` green is a test-execution claim (`/test`'s responsibility); `Closes #169` in the PR body is a `/pr`-time requirement. Neither is evaluated here, per the task's own framing. |

## Plan-wording drift check (task item 1, second half)

Scope row 1 layers three revisions in sequence — the original rule, then "**Codex advisory による改訂**", then "**self-review cycle 1 による改訂**" — inside one table cell, rather than stating only the final contract. Read start-to-finish it does land on an unambiguous final statement ("既定の mode では従来どおり理由を出して exit 0"), and the shipped code matches that final statement exactly (confirmed above under AC-2/AC-2b). This is the same changelog-in-place convention the plan uses elsewhere (Deviation notes), so it is not incorrect, but a reader skimming only the first sentence of the row would see the pre-revision contract. Minor readability note, not a spec-compliance gap — no fix needed for this verdict.

## CI parity (task item 3, last bullet)

Compared `.github/workflows/verify.yml`'s dedicated `Secret scan` step against what `secret-scan-branch.sh` does when it runs inside the same job's later `Run verification` step (`./scripts/run-verify.sh`, `RALPH_VERIFY_SCOPE: full`):

- **Range**: CI's dedicated step computes `base="$(git merge-base HEAD "origin/${GITHUB_BASE_REF}")"` unconditionally (relies on `actions/checkout`'s `fetch-depth: 0` to guarantee `origin/${GITHUB_BASE_REF}` exists). `GITHUB_BASE_REF` is set automatically by GitHub Actions for `pull_request` events, so `secret-scan-branch.sh`'s own base resolution (`GITHUB_BASE_REF` first, then prefer `refs/remotes/origin/$base_name`) lands on the identical `origin/${GITHUB_BASE_REF}` and the identical `merge-base..HEAD` range. Same range, both steps.
- **Allowlist**: CI's dedicated step calls `secret-scan.sh --range` directly, which defaults to reading `$repo_root/.gitallowed` from the checked-out worktree (`secret-scan.sh:18`). `secret-scan-branch.sh` instead explicitly reads `git show HEAD:.gitallowed`. In a normal CI checkout these are the same content (the worktree has no uncommitted diff from `HEAD`); the two approaches only diverge locally, where a developer might have an uncommitted `.gitallowed` edit — which is the intended divergence per AC-2c and the plan's design decision ("CI が読まないので無視し"). No parity gap.
- **Duplication, not a gap**: because `secret-scan-branch.sh` resolves to the same range and (in CI) the same allowlist content, CI now runs the same range scan twice per PR — once in the dedicated step, once inside `run-verify.sh`'s `Run verification` step. Since GitHub Actions steps stop the job on first failure by default, the dedicated step would already fail the job before `Run verification` runs, so the second scan inside `run-verify.sh` only ever executes when the first has already passed. This matches the self-review's own "Coverage gaps" note (duplicated work, not a defect); I independently re-derived the same conclusion by reading both scripts.
- **No case found where a local strict-clean run could still fail in CI, or vice versa**, under the conditions actually reachable by following the `/pr` skill in this repo (an `origin/main` ref that exists and nothing overriding `GITHUB_BASE_REF`/`RALPH_XREVIEW_BASE`) — consistent with the self-review's own reachability analysis (finding 2's "reachable by an agent... is empty").

## Documentation drift (task item 4)

Checked all surfaces named in the task plus the ones the plan's own Scope row 7 names:

- `.claude/skills/pr/SKILL.md` + 3 mirrors, `docs/quality/quality-gates.md` + template copy, `AGENTS.md` scripts bullet: all updated and consistent with the shipped code (see AC-6 evidence and the diffs read directly).
- `scripts/secret-scan-branch.sh` header/usage text and `scripts/xreview-helpers.sh` header: both read in full; accurate against the code (the self-review's LOW-severity header-wording finding — "could not determine what to scan" vs. the scanner-missing case — was fixed; header now says "could not scan").
- `README.md`: no update, and none needed. The "Deterministic hooks" table row mentions "commit-msg secret scan" — a different mechanism (a git hook) from this diff's `run-verify.sh`/`/pr`-invoked script; the `scripts/` tree summary already ends in "etc." and doesn't enumerate scripts individually elsewhere either, so this is not stale.
- `docs/quality/definition-of-done.md`: no mention of secret scanning at all (neither the pre-existing CI scan nor the new local one) — this is a generic per-phase checklist that was already this abstract before this branch; not a regression.
- `.claude/rules/ralph/git-commit-strategy.md`: its "Enforcement" sentence (`pre_bash_guard.sh` blocks dangerous patterns at commit time; `commit-msg-guard.sh` scans for leaked secrets at commit time) describes the pre-existing per-commit guard, a different mechanism from this branch's branch-history range scan; no drift.
- `.claude/skills/work/SKILL.md` steps 37/43/77: all say "run `./scripts/run-verify.sh` (or equivalent)" generically, without enumerating what it checks — the branch scan is now included transparently by design (that is the whole point of wiring it into `run-verify.sh` rather than every individual consumer), so no update is needed there.
- `docs/recipes/`: only `adding-a-language-pack.md` matches a `secret` grep, and that hit is an unrelated "secret-bearing files" gitignore-block note — not in scope for this diff.
- No file outside `.claude/skills/pr/SKILL.md`/`.agents/skills/pr/SKILL.md` references a specific `/pr` step number (checked above under AC-6), so the Steps renumbering (1→10) needed no external update.

No missing or stale documentation found beyond the Scope-row-1 readability note above, which is not a doc-drift defect (the shipped code matches the doc's own stated final contract).

## Non-goals held (task item 5)

- `.github/workflows/`: `git diff main...HEAD --stat -- .github templates/base/.github` is empty.
- `.gitallowed`: `git diff main...HEAD --stat -- .gitallowed templates/base/.gitallowed` is empty.
- No pre-push git hook added: `grep -rl "secret-scan-branch" .claude/hooks .codex/hooks.json` returns nothing; the new script is invoked only from `run-verify.sh` and the `/pr` skill text, as the plan states.
- Scanner patterns and `is_allowed` allowlist logic in `scripts/secret-scan.sh` are unchanged — the only diff is the three added guidance-text `printf` lines (confirmed by reading the full file diff).

## Secret hygiene of the branch itself (task item 6)

- `./scripts/secret-scan-branch.sh --strict` at `186183c`: `secret-scan-branch: scanned a0d5bf5..186183c against origin/main: clean`, exit 0.
- `./scripts/secret-scan.sh --range "$(git merge-base HEAD origin/main)..HEAD"`: exit 0, no output.
- Confirmed by reading (not by grepping literal patterns, per instructions): `add_secret_commit` in `tests/test-run-verify-branch-secret-scan.sh:128-129` builds its fixture token via `printf 'ghp_%s' '<split-suffix>'` at runtime, matching the same split-assembly discipline the self-review already confirmed for `tests/test-secret-scan-branch.sh`'s full fixture set.

## Coverage gaps / what remains unverified

- Test execution — whether the 65 assertions across the two new test files (44 in `test-secret-scan-branch.sh` per the plan's deviation note, 21 in `test-run-verify-branch-secret-scan.sh`) actually pass, and whether the pre-existing `tests/test-secret-scan.sh` and scaffold/upgrade Go regression tests still pass — is `/test`'s responsibility, not evaluated here. I read every assertion referenced in the AC table above but did not execute `go test`/`run-test.sh`.
- AC-7's "scaffold / upgrade Go tests pass" clause and AC-8 in full — both are test-execution or `/pr`-time claims, explicitly out of `/verify` scope.
- I did not independently reproduce the self-review's scratch-repo probes (base override naming the current branch, fast-forward bypass, stub-scanner non-1 exit) myself; I verified the *code* now handles all these paths correctly (AC-2b table row) and that the two bypass scenarios now have dedicated regression tests, but did not re-run the probes as a second, independent confirmation.
- Timing of `git log -p` on a long-lived branch: not measured, matching the self-review's own stated gap. Not relevant to this branch's own size (11 commits).

## Verdict

**Pass.** AC-1 through AC-6 are fully met, with the two Codex-advisory bypass scenarios and both self-review MEDIUM findings resolved and independently re-confirmed against the code and tests at HEAD `186183c`. AC-7 and AC-8 are correctly deferred in their test-execution and `/pr`-time halves. Static analysis is clean across shell syntax, shellcheck, `check-sync.sh`, `check-pipeline-sync.sh`, `check-skill-sync.sh`, `check-template-purity.sh`, `check-template.sh`, and the golang verifier. CI parity holds — no case found where a local strict-clean run could disagree with CI's dedicated Secret scan step under normal `/pr` usage. Documentation drift: none found beyond a minor Scope-row-1 readability note (not a defect). Both required secret-hygiene commands (item 6) came back clean against this branch's own history.
