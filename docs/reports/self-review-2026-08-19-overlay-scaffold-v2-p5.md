# Self-review — overlay-scaffold-v2-p5

- Date: 2026-08-19
- Plan: `docs/plans/active/2026-08-19-overlay-scaffold-v2-p5.md`
- Cycle: 1
- Scope: `git diff main...HEAD` (implementation commits `9bf09e2`, `b884a38`, `a61dbb5`, `742260f`, `a164b2c` + plan bookkeeping)
- Dimension: diff quality only (naming, readability, unnecessary changes, comment accuracy, error-message quality, test quality, security, maintainability). No test execution, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL). Merge recommendation: **hold for H1** — see below.

## Findings

| ID | Severity | Finding | Evidence | Recommendation |
|----|----------|---------|----------|----------------|
| H1 | HIGH | The purity guard's regex branch is silently dead. `pattern="${entry%%\|*}"` truncates each `REGEX_PATTERNS` entry at its **first** `\|`, but the only entry contains `(reports\|plans)`, so the pattern becomes `docs/(reports`. `grep -E` then exits 2 with "parentheses not balanced", which `2>/dev/null \|\| true` swallows, so the dated-report/plan-path detection never runs and never reports an error. No fixture covers this branch (cases A–D all exercise fixed strings) — the exact omission the plan cites as tech-debt row 106's lesson. | `scripts/check-template-purity.sh:53-55` (pattern), `:117` (split), `:105` (silenced grep). Probed: a fixture file containing `docs/reports/2026-08-19-overlay-x.md` and `docs/plans/active/2026-01-02-foo.md` passes with `exit=0`; running the split by hand yields `PATTERN=[docs/(reports]` and `grep: parentheses not balanced` (exit 2). | Use a delimiter that cannot appear in a regex (tab, or `:::`), or keep patterns and reasons in parallel arrays. Separately: treat grep exit >1 as a script failure instead of `\|\| true`, and add a fixture case for the dated-path pattern. |
| M1 | MEDIUM | `doctor --strict` disagrees with itself about what "integrity violation" means. FR-9(a) deliberately tolerates a pending upgrade ("a pending-but-expected update … is not a violation"), while (b) and (c) treat exactly that state as a violation: any `outcome != BlockUnchanged` is an offender, and `mergeResult.Changed` is true whenever the template side moved. A project that has not yet run `ralph upgrade` after a ralph release fails the gate README calls "the CI-grade integrity gate" — for the same reason (a) forgives. | `internal/cli/doctor.go:731-757` (a, tolerant) vs `:771` (b) and `:841` (c); `README.md` Commands table `ralph doctor [--strict]` row. | Pick one semantic. Either compare (b)/(c) against the manifest-recorded state so only genuine damage fails, or state the asymmetry in the `--strict` flag help and the README row so CI users are not surprised by a version-skew failure. |
| M2 | MEDIUM | An unreadable/corrupt `.ralph/manifest.toml` silently disables all five FR-9 checks. `checkScaffoldIntegrity` returns `nil` on any `ReadManifest` error, and its doc comment justifies this with "the latter is already surfaced by the pre-existing 'Manifest version' check" — but that check reports `Status: "warn"` with the misleading detail `"no manifest found"`, which never affects the exit code. So `--strict` exits 0 on a corrupted manifest, i.e. the integrity gate fails open exactly when the manifest is untrustworthy. | `internal/cli/doctor.go:678-683` and its doc block at `:666-677`; `internal/cli/doctor.go:401-408` (`checkManifestVersion`). | Distinguish `os.IsNotExist` (return nil, correct) from a parse error (return a `scaffoldViolationStatus`-eligible result naming the parse failure). |
| M3 | MEDIUM | The AC-7 preflight regression test does not pin the guarantee it names. `--all` targets are sorted by path, so the corrupted target `packs/languages/golang/verify.sh` is always processed **first**; `writeFileV2` performs its own `ValidateRealParentChain`, so an implementation with no preflight loop at all would fail on that same first target and likewise leave `scripts/run-verify.sh` and the manifest untouched. Every assertion in the test passes against the no-preflight implementation. | `internal/cli/eject_adopt_test.go:384-440` (test + its stated intent); `internal/cli/adopt.go:299` (sort); `internal/cli/upgrade_v2.go:709-722` (`writeFileV2`'s own chain check). | Corrupt the parent of the **later**-sorting target (`scripts/`) and keep `packs/languages/golang/verify.sh` valid, so a write-as-you-go implementation would have written the first target before failing — then the untouched-file assertion actually distinguishes the two designs. |
| M4 | MEDIUM | `ralph adopt` prompts with the wrong operation name: `confirmMigration` hardcodes `"Proceed with migration? [y/N] "`, and adopt reuses it verbatim. On the one destructive new command in this PR, the confirmation the user reads before discarding their own edits says "migration". The sibling extraction in the same slice (`checkGitCleanForDestructiveOp`) was parameterized by `opName` for exactly this reason, so the asymmetry is internal to one commit. | `internal/cli/migrate.go:1054`; `internal/cli/adopt.go:147`; contrast `internal/cli/migrate.go:1018` (`checkGitCleanForDestructiveOp(absDir, opName)`). | Add an `opName`/prompt parameter to `confirmMigration` the same way, and pass `"adopt"`. |
| M5 | MEDIUM | Two new whole-file `KNOWN_DIFFS` suppressions were added with no tech-debt row. `is_known_diff` matches the path, not a hunk, so root ↔ template divergence in `docs/quality/quality-gates.md` and `docs/recipes/adding-a-language-pack.md` is now permanently unreported. The repo has filed precisely this concern before — the (now RESOLVED) `AGENTS.md` row in the register says so in as many words — and this PR's register diff adds no equivalent row. | `scripts/check-sync.sh:95-106` (two new entries); `docs/tech-debt/README.md:97` (the AGENTS.md precedent row); register diff contains no new `KNOWN_DIFFS` row. | Either keep the two files mirrored and allowlist the purity hits instead, or add one tech-debt row per suppression with a concrete removal trigger. |
| M6 | MEDIUM | The purity fix satisfies the banned substring, not the guard's stated goal. `check-template-purity.sh` bans `check-sync.sh` because "any reference to it in template content is a broken instruction for downstream users" — but the template copy of the recipe still instructs the reader to run `scripts/new-language-pack.sh` and to `diff -rq packs/languages/terraform templates/packs/terraform`, and none of `scripts/new-language-pack.sh`, `packs/languages/`, or `templates/packs/` ship downstream. The added preamble ("this is a ralph CLI contributor workflow … not something you do inside your own scaffolded project") is honest, but it describes a document that arguably should not be in the shipped template set at all. | `templates/base/docs/recipes/adding-a-language-pack.md` (preamble + steps 1, 3, 6); verified absent from `templates/base/`: `scripts/new-language-pack.sh`, `packs/languages/`, `templates/packs/`. | Decide the intent: drop the recipe from `templates/base/` (and from the shipped docs index), or rewrite the whole file for the downstream reader. Rewording only the guarded substring leaves the same broken instruction under a different name. |
| M7 | MEDIUM | `ralph status` gained a new hard-failure path. `runStatus` now aborts the whole command when `buildScaffoldStatus` errors, which propagates from `resolveOwnershipPlan` (template load, desired-state build, and the disk reads `PlanCoreReplaceDesired` performs). Before this PR `ralph status` was a read-only org report with no scaffold failure mode; now a single unreadable tracked file takes the org roster down with it. The function already degrades gracefully for the "no manifest" case, so the asymmetry is within one function. | `internal/cli/status.go:76-79`; `internal/cli/status.go` `buildScaffoldStatus` (returns `(nil, nil)` for a missing manifest but wraps planning errors). | Degrade instead: print `Scaffold ownership: unavailable (<err>)` and continue to the org section, matching the no-manifest behavior. |
| M8 | MEDIUM | User-facing docs now assert a Codex behavior the same PR records as unverified. README states the `.ralph/local/hooks/<event>.d/` drop-in "runs under either CLI", and AGENTS.md / repo-map.md say the wiring "runs under both Claude Code and Codex" — while the new tech-debt row in this same PR says live firing of project-scoped `[[hooks.*]]` under Codex could not be reproduced (the `codex trust` grant has no scriptable equivalent in codex-cli 0.147.0). The wiring is correct and the reasoning for it is sound; the docs overstate it from "wired" to "verified working". | `README.md` Hooks section; `AGENTS.md` `.ralph/local/` bullet; `docs/architecture/repo-map.md` `.ralph/local/` bullet; the new unverified-trust row in `docs/tech-debt/README.md`. | Qualify one of the three (README is enough): "…both CLIs route through the same dispatcher; live firing under Codex has not yet been confirmed on a trusted checkout (see tech-debt)". |
| L1 | LOW | The RESOLVED note for tech-debt row 100 claims `tests/test-pre-bash-guard.sh` adds "15 `jq`-related assertions". The file has 4 cases, of which 2 run on the jq path. | `docs/tech-debt/README.md:100`; `tests/test-pre-bash-guard.sh:82-96`. | Replace the number with what the file actually asserts ("both the jq and sed-fallback paths, deny and allow, against the real nested payload shape"). |
| L2 | LOW | Comments left behind by the Slice-4 → Slice-5 allowlist emptying. Test case A explains it must avoid "false failures on the known, allowlisted leaks"; case D says it uses "the same pattern as the real allowlisted leak (check-sync.sh)" to prove the allowlist "is exact-path scoped" — there is no allowlisted leak any more. The script's own header likewise says the allowlist "exists for pre-existing, tracked leaks … (see each entry's reason)" while the declaration two dozen lines down says it is empty by design. | `tests/test-template-purity.sh:74-81`, `:128-131`; `scripts/check-template-purity.sh:11-16` vs `:57-64`. | Reword all three to describe the current state (empty allowlist; case D proves path scoping generically). |
| L3 | LOW | The new dispatcher-parity guard is narrower than its header claims. It extracts only quoted strings beginning `./`, so a reintroduced direct call written without the prefix (`command = [".claude/hooks/check_mojibake.sh"]`) is invisible to both the existence check and the direct-invocation check — and the "parsed zero hook commands" backstop only fires when *every* entry lacks `./`. The header says "every `.codex/config.toml` `[[hooks.*]]` command must route through `ralph-dispatch.sh`". | `tests/test-hook-wiring.sh:151` (and the same expression at `:102`); header at `:133-139`. | Match any quoted string ending in `.sh` on a `command =` line, then normalize the leading `./`. |
| L4 | LOW | `tests/test-template-purity.sh` writes its scratch output to the predictable path `/tmp/test-template-purity-out.$$`, while the sibling suites in this PR use `mktemp -d`. | `tests/test-template-purity.sh:31`, `:53`, `:59`, `:70`. | Use `mktemp` for consistency with `tests/test-pre-bash-guard.sh`. |

Counts: CRITICAL 0, HIGH 1, MEDIUM 8, LOW 4.

## Security review

This slice set was weighted for security (a destructive command, a bash guard, hook wiring). Three targeted probes, all with a negative result:

1. **`pre_bash_guard.sh` payload extraction is genuinely fixed on both paths.** Ran the real hook against a real-shape payload (`{"tool_name":"Bash","tool_input":{"command":"sudo …"}}`) with jq present and with a minimal PATH containing only `cat`/`sed`/`tr`/`dirname`. Both emit `permissionDecision: deny`. The change uses `lib_json.sh`'s already-documented dotted-path support rather than introducing a new extraction mechanism.
2. **No deny-rule bypass via a crafted payload.** I attempted the obvious shadowing attack — a Bash command whose text embeds `", "command": "echo safe` so the sed fallback's greedy `.*` prefers a later, benign `"command"` key. It does not work: JSON encoding escapes the injected quotes (`\"command\"`), and the escaped closing quote breaks the fallback pattern's `"command"[[:space:]]*:` requirement, so sed backtracks to the real key and still sees `sudo`. The jq path parses properly and is unaffected. Verified against the real hook, not by reading.
3. **No deny/ask pattern was weakened.** The `case` arms in `pre_bash_guard.sh` are untouched; only the extraction argument changed. Root and `templates/base/` copies are byte-identical (`cmp` clean), as are the two `.codex/config.toml` copies.

Residual (not filed as a finding, since it is pre-existing and out of this diff's scope): the guard fails open whenever extraction yields an empty string — which is how the row-100 defect stayed invisible for so long. If a future payload shape changes again, the failure mode is silence. A "no command extracted" diagnostic on stderr would make the next regression self-announcing.

`ralph adopt`'s destructive path is well ordered: git-clean precondition → preflight every target → print the full target list → confirm → write → manifest commit barrier. The clean-tree requirement is checked before anything is printed or written, so the documented "git is the only rollback" guarantee holds. The residual partial-write window (files written, manifest not) is documented in the error text and is git-recoverable by construction. M3 is about the test, not the implementation.

## Positive notes

- **Mirror discipline held.** `cmp` confirms `.claude/hooks/pre_bash_guard.sh` ↔ `templates/base/…` and `.codex/config.toml` ↔ `templates/base/…` are byte-identical after the change.
- **The multi-surface deferral from Phase 3 was closed at all three surfaces.** The "Codex still calls hook scripts directly" claim was corrected in `README.md`, `AGENTS.md`, and `docs/architecture/repo-map.md` in the same commit — the widening-blast-radius shape that bit an earlier phase did not recur here (M8 is about tone, not coverage).
- **Tests assert the absence of side effects, not just return values.** `TestRunAdoptIO_PreflightFailure_AllBatchZeroWrites`, `…_All_ZeroTargets_NoOp`, and `…_ConfirmationDeclined_ZeroWrites` all compare manifest bytes and directory hash snapshots before/after. Test names carry the AC they pin.
- **The register was updated in both directions.** Rows 100 and 103 are struck through with accurate RESOLVED notes (L1 is a numeric detail inside one of them), and the genuinely unverified part — Codex project-hook firing behind the interactive `trust` gate — was filed as a new row with a concrete trigger rather than quietly dropped.
- **New shell suites need no enumeration.** `run_hook_tests()` globs `tests/test-*.sh`, so `test-template-purity.sh` and `test-pre-bash-guard.sh` are picked up automatically; both are committed mode `100755`, and `check-template-purity.sh` is too (its `[ -x ]` wiring in `verify.local.sh` would otherwise skip silently).
- **`adopt --all` enumerates forks safely.** `classifyFork` appends an `AdvisoryEntry` unconditionally, including for a fork whose disk file is missing, so enumerating from `plan.Advisories` really does cover every fork rather than only those with a non-empty diff.
- **Shared classification instead of a parallel definition.** eject, adopt, `doctor --strict`, and `status` all consume `resolveOwnershipPlan`, so "fork"/"drift"/"retired" cannot diverge from what `ralph upgrade` reports. Each consumer's doc comment says so and the code matches.

## Merge recommendation

**Do not merge as-is.** H1 is a one-line fix (delimiter choice) plus a fixture, and it matters because the PR ships the guard as a CI gate while one of its two detection mechanisms never executes. M1–M4 are worth fixing in the same cycle: M1 and M2 change what `--strict` means in CI, M3 means the destructive command's headline safety property is unpinned, and M4 is user-facing text on a destructive confirmation. M5–M8 and the LOWs are acceptable as tracked follow-ups if the cycle budget is tight, but M5 in particular is cheap (one register row each) and the repo has been burned by unwritten `KNOWN_DIFFS` suppressions before.

## Known gaps in this review

- No tests were executed and no static analysis was run (verifier/tester own those). M3's conclusion is derived by reading the sort order and `writeFileV2`'s own parent-chain check, not by mutating the code and re-running the suite.
- Spec compliance (FR-2/3/9/10/12 wording vs implementation) and documentation drift were deliberately out of scope.
- The Codex trust gap recorded in the register is a runtime-verification gap, not a diff-quality finding; it is referenced here only where the docs overstate it (M8).

---

# Cycle 2

- Date: 2026-08-19
- Cycle: 2 (final under the default `RALPH_STANDARD_MAX_PIPELINE_CYCLES=2` cap)
- Scope: the cycle-2 delta only — `git diff cb2e00d..HEAD`, i.e. `7cf4f47` (cycle-1 finding fixes), `33c1166` / `38b7c63` / `348801c` (verify/test/triage reports), `74d28ef` (spec AC ticks + quality-gates tech-debt row), `b9a956f` (drift guidance → eject/adopt, touches `internal/cli/upgrade_v2.go` + `internal/upgrade/report.go`), `7286251` (sync-docs insight event), `e01939e` (cross-review AR#1/AR#2 fixes in `doctor.go`). Commit DAG confirmed linear (`git log --format='%h parent=%p'`).
- Dimension: diff quality only. No tests run, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL, no HIGH). Merge recommendation: **merge**, with one batched tech-debt row for the 7 unfixed findings below.

## 1. Cycle-1 finding re-classification

All 13 cycle-1 findings are fixed at the site each finding cited. Three fixes
left a residual at a sibling site or introduced a new inaccuracy; those are
filed as new cycle-2 findings (C2-*) rather than as partial fixes, and are
cross-referenced here.

| Cycle-1 ID | Fixed at HEAD? | Evidence | Residual |
|----|----|----|----|
| H1 (regex branch silently dead) | **Fixed** | `REGEX_PATTERNS` / `REGEX_REASONS` are now parallel arrays (`scripts/check-template-purity.sh:63-68`); the loop indexes them together (`:164-166`); `grep_scan_or_fail` (`:106-122`) treats grep exit >1 as a whole-script failure instead of `\|\| true`, and its doc comment records why it must not be called through process substitution. Fixture cases E and F (`tests/test-template-purity.sh:150-171`) walk both halves of the `reports\|plans` alternation; against the pre-fix code the pattern truncates to `docs/(reports`, `grep -E` exits 2, the hit count stays 0, the guard exits 0, and `run_case_expect_output` fails — so both cases genuinely discriminate. | **Yes → C2-M2.** The identical split-on-first-`\|` bug survives in `is_allowlisted`, one function down in the same script. |
| M1 (`--strict` (a) vs (b)/(c) asymmetry) | **Fixed** | The "state the asymmetry" option was taken at all three surfaces the finding named: `--strict` flag help (`internal/cli/doctor.go:38-47`), README's `ralph doctor [--strict]` row, and the doc comments on `checkScaffoldCoreHashes` / `checkManagedBlocks` / `checkSettingsOwnedKeys`. | **Yes → C2-M1.** The same flag help and README row enumerate exactly five strict-eligible checks, which two fixes in this cycle have since falsified. |
| M2 (corrupt manifest fails open) | **Fixed** | `checkScaffoldIntegrity` now splits `errors.Is(err, fs.ErrNotExist)` (return nil) from any other read error (`scaffoldViolationStatus(true, strict)`), and the doc comment explains why the earlier "the Manifest version check already surfaces it" reasoning was wrong. Pinned by `TestCheckScaffoldIntegrity_CorruptManifest_StrictFails`. | — |
| M3 (AC-7 preflight test does not pin its guarantee) | **Fixed** | The test now corrupts the **later**-sorting target (`scripts/`) and leaves the earlier-sorting `packs/languages/golang/verify.sh` valid, and calls `writeMigrationDiskFile` first so that fork's content genuinely diverges from the template — otherwise the byte-equality assertion could not tell "untouched" from "overwritten with the identical template". The comment block states the sort-order reasoning explicitly. Under a write-as-you-go implementation the earlier target would have been written before the batch aborted, so the assertion now discriminates. | — |
| M4 (`ralph adopt` prompts "migration") | **Fixed** | `confirmDestructiveOp(in, out, autoYes, opName)` extracted; `confirmMigration` kept as a thin wrapper, mirroring the `checkGitCleanForMigration` → `checkGitCleanForDestructiveOp` convention the finding cited. `adopt.go:147` passes `"adopt"`, matching the `"adopt"` already passed at `adopt.go:130`. Minor wording nit (the prompt reads "Proceed with adopt?", a verb where "migration" was a noun); left as-is because consistency with the sibling call site's `opName` is worth more than the grammar. | — |
| M5 (two `KNOWN_DIFFS` suppressions, no register row) | **Fixed** | One register row covering both entries, naming each file's reason for diverging, and carrying a concrete removal trigger ("the next template-content restructure of either file, or the next purity-guard pattern change that lets root and template converge"). | — |
| M6 (purity fix satisfied the substring, not the goal) | **Fixed** | `templates/base/docs/recipes/adding-a-language-pack.md` was rewritten end to end for the downstream reader. Every command and path it now names ships downstream — verified: `ralph pack list` / `ralph pack add` (`internal/cli/pack.go:26,37`), `ralph eject`, `ralph upgrade`, `scripts/detect-languages.sh` and `scripts/detect-changed-languages.sh` (both present in `templates/base/scripts/`), `scripts/run-verify.sh`, `.ralph/manifest.toml`. The contributor workflow is now a four-sentence pointer upstream rather than a set of steps the reader cannot run. | — |
| M7 (`ralph status` gained a hard-failure path) | **Fixed** | `runStatus` sets `scaffoldStatus{Err: ...}` instead of returning; `printScaffoldSection` renders `Scaffold ownership: unavailable (<err>)`; `buildStatusScaffoldJSON` emits an additive `error` key. `TestStatusCmd_ScaffoldSection_ComputationError_DegradesGracefully` asserts exit 0, the degraded line, **and** that both org rows still render. The `json:"layout,omitempty"` change is not a wire regression: `buildScaffoldStatus` returns the literal `"legacy"`, never `""`, on the non-v2 path. | — |
| M8 (docs assert unverified Codex behavior) | **Fixed** | README's Hooks paragraph now reads "…both Claude Code and Codex route through the same dispatcher, though live firing under Codex has not yet been confirmed on a trusted checkout (see `docs/tech-debt/README.md`)". | — |
| L1 (stale "15 jq-related assertions") | **Fixed** | Replaced with a description of what the file asserts rather than a count. | — |
| L2 (comments left behind by the allowlist emptying) | **Fixed** | All three sites the finding named were reworded: script header (`:11-17`), case A comment, case D comment. | **Yes → C2-L1.** Case D's replacement comment credits case C with proving something case C does not prove. |
| L3 (dispatcher-parity guard narrower than its header) | **Fixed** | Both extraction sites now use `grep -oE '"[^"]*\.sh"'` with `sed 's#^\./##'` normalization, so `command = [".claude/hooks/check_mojibake.sh"]` is caught by the existence check and the direct-invocation guard. | — |
| L4 (predictable `/tmp` scratch path) | **Fixed** | `OUT_FILE="$(mktemp)"`, cleaned in the existing `trap cleanup EXIT`. | — |

Cycle-1 totals: 1 HIGH + 8 MEDIUM + 4 LOW = 13. Fixed at the cited site: 13/13.

## 2. Cycle-2 findings

| ID | Severity | Finding | Evidence | Recommendation |
|----|----------|---------|----------|----------------|
| C2-M1 | MEDIUM | `--strict`'s user-facing contract still enumerates exactly five checks, which this cycle's own fixes falsified twice. The flag help says `--strict only elevates these five scaffold checks — every other doctor check (e.g. missing claude/codex CLI) keeps its existing pass/warn/fail semantics`, and README's doctor row says `--strict exits 1 on any core-hash mismatch, block damage, settings-key drift, conflict marker, or manifest/disk mismatch`. But `checkScaffoldIntegrity` now returns a strict-eligible `"Scaffold integrity"` result — which is none of the five — for two additional conditions: an unreadable/corrupt manifest (M2, `7cf4f47`) and an ownership-planning failure (AR#1, `e01939e`). Both were `"warn"` before this PR, so "every other doctor check keeps its existing semantics" is literally false at HEAD. A CI operator reading either surface would not expect exit 1 from a corrupt `.ralph/manifest.toml`. The direction is fail-closed, so this is a documentation gap, not a behavioral one. | `internal/cli/doctor.go:38-47` (flag help, the "five" enumeration and the "every other doctor check" sentence); `internal/cli/doctor.go` `checkScaffoldIntegrity` — the corrupt-manifest and `resolveOwnershipPlan`-error early returns, both `scaffoldViolationStatus(true, strict)`; `README.md` `ralph doctor [--strict]` row. Both new paths are pinned as `--strict` failures by `TestCheckScaffoldIntegrity_CorruptManifest_StrictFails` and `TestCheckScaffoldIntegrity_OwnershipPlanningError_StrictFails`. | Add one clause to both surfaces: `--strict` also fails when the scaffold cannot be classified at all (unreadable manifest, ownership-planning error), reported as `Scaffold integrity`. Cheapest at the flag help, since the README row already links onward. |
| C2-M2 | MEDIUM | H1's delimiter collision survives one function down, in the same script the fix rewrote. `is_allowlisted` still parses each `ALLOWLIST` entry as `path\|pattern\|reason` by truncating at the first `\|` — so the only `REGEX_PATTERNS` entry, whose `\|` is alternation, can never be matched by an allowlist entry. Probed: for the entry `templates/base/docs/x.md\|docs/(reports\|plans)/[a-zA-Z_/-]*[0-9]{4}-[0-9]{2}-[0-9]{2}\|intentional`, `entry_pattern` resolves to `docs/(reports`, which never equals the pattern `scan_regex` passes in. The script's header, `ALLOWLIST`'s own comment ("pattern is the exact `FIXED_PATTERNS`/`REGEX_PATTERNS` entry it applies to"), and the failure message ("add a path+pattern pair to ALLOWLIST") all advertise a mechanism that cannot work for the regex branch. Harmless today only because `ALLOWLIST` is empty; the first person who needs to allowlist a dated-path hit will get a silent no-op, which is exactly H1's failure mode. No fixture covers it — case C injects the fixed-string pattern `yoshpy-dev`, the same "no fixture for this branch" omission H1 was about. | `scripts/check-template-purity.sh:79-90` (`is_allowlisted`, the two-step `%%\|*` / `#*\|` split), `:70-77` (the ALLOWLIST comment's promise), `:63-65` (the regex entry whose `\|` is alternation), `:179-180` (the failure message); `tests/test-template-purity.sh:100-135` (case C uses a fixed-string pattern only). Probe transcript: `entry_pattern = [docs/(reports]` vs `actual pattern = [docs/(reports\|plans)/…]` → no match. | Apply H1's own lesson here: give `ALLOWLIST` parallel arrays too (`ALLOWLIST_PATHS` / `ALLOWLIST_PATTERNS` / `ALLOWLIST_REASONS`), or index allowlist entries by the pattern's array index rather than its text. Then extend case C to cover a regex-pattern allowlist entry. |
| C2-L1 | LOW | Case D's replacement comment makes a new false claim. Cycle-1 L2 correctly flagged the old comment ("proving the allowlist is exact-path scoped"); the fix moved that claim onto case C — "case C above proves separately that an entry, once added, is scoped to its exact (path, pattern) pair and does not blanket-suppress the pattern everywhere". Case C's fixture root contains exactly one file, which *is* allowlisted, and no unallowlisted control at another path, so it proves suppression works — not that it is scoped. Case D itself uses a different pattern (`check-sync.sh`) in a different fixture root, so it does not close the gap either. No case in the file currently demonstrates path scoping. | `tests/test-template-purity.sh:137-142` (case D's comment), `:100-135` (case C: single-file fixture, `C_DIR/templates/base/docs/leak.md`, no second path). | Either add a second, unallowlisted file with the same pattern to case C's fixture (then the claim becomes true), or reword the comment to say what the two cases actually establish. |
| C2-L2 | LOW | `grep_scan_or_fail` merges grep's stderr into the stream that is parsed as hits. The fix replaced `2>/dev/null` with `2>&1` — correct on the error path, where `_scan_output` is echoed as the diagnostic, but on the success path (`status == 0`) any warning grep wrote to stderr becomes an extra line in `_scan_output`, and both `scan_fixed` and `scan_regex` will parse it as a hit with `path="grep"`, which cannot be allowlisted. Realistic triggers (recursive symlink loop, unreadable file alongside a match) do not exist under `templates/` today, so exposure is nil — but the previous code could not do this. | `scripts/check-template-purity.sh:109` (`_scan_output="$(grep … 2>&1)"`), `:129-137` / `:145-153` (both loops parse `_scan_output` verbatim). | Capture stderr separately (e.g. into a temp file or a second variable) and only surface it on the exit >1 branch, so the parsed stream stays stdout-only. |
| C2-L3 | LOW | `checkManifestConsistency` hand-copies `v2SkipPaths()`'s body minus its last line, with no compile-time link back to it. If a fifth exception face is ever added to `v2SkipPaths()`, this copy silently misses it and the doc comment's "Two of `v2SkipPaths()`'s four exception faces" goes stale — the doc comment even anticipates the risk ("Do not widen this back to the full `v2SkipPaths()` set without re-verifying…") but the code shape does not enforce it. Count is accurate today: `v2SkipPaths()` holds `v2SettingsPath`, the two `blockSurfaces`, and `upgrade.SettingsSnapshotRelPath`. | `internal/cli/doctor.go:980-985` (the hand-built `skip` map) vs `internal/cli/upgrade_v2.go:43-50` (`v2SkipPaths`). | Derive rather than duplicate: `skip := v2SkipPaths(); delete(skip, upgrade.SettingsSnapshotRelPath)`. That reads as exactly what the doc comment says and auto-inherits any future face. |
| C2-L4 | LOW | The drift-guidance sentence is duplicated as a literal at four sites. All four read correctly in context (real-run error tail, no-op tail, `--dry-run` preview, and the markdown report section between the `## Unresolved drift` heading and its table) — this is a maintainability note, not a correctness one: the next wording change has to find all four, and three of them are in one file. | `internal/cli/upgrade_v2.go:229`, `:309`, `:790`; `internal/upgrade/report.go:124`. | Hoist a package-level const in `internal/cli` for the three CLI sites (the fourth is in `internal/upgrade` and would need an exported const to share). |
| C2-L5 | LOW | Same-PR report pointers were invalidated by a later commit in the same PR. `e01939e` inserted ~17 lines of comment above `resolveOwnershipPlan` and ~21 above `checkManifestConsistency`, shifting every `doctor.go` line number cited by two artifacts written earlier in this delta. All four pointers were correct when written and land on unrelated comment prose at HEAD: the triage report's `internal/cli/doctor.go:726 付近` (AR#1) now lands mid-doc-comment, and `:954-956` (AR#2) now lands in `checkManifestConsistency`'s prose rather than the `skip[p]` exclusion it named; the verify report's `checkConflictMarkers` `:899` and `checkManifestConsistency` `:929` are likewise +17/+50 off. The Go comments `e01939e` itself added avoid this correctly — they cite function names and are cycle-qualified ("AR#1, cycle 1, docs/reports/cross-review-triage-…-p5.md"). | `docs/reports/cross-review-triage-overlay-scaffold-v2-p5.md:25-26`; `docs/reports/verify-2026-08-19-overlay-scaffold-v2-p5.md:44`. Verified by diffing `git show 348801c:internal/cli/doctor.go` against HEAD at those offsets. | Prefer function name + behavior over `file:line` in reports, matching what the code comments in this same commit already do. Not worth a re-edit of merged reports on its own; fold into the batched tech-debt row. |

Counts: CRITICAL 0, HIGH 0, MEDIUM 2, LOW 5.

## 3. Positive notes

- **The AR#1 and AR#2 fixes do not pre-empt each other's regression tests.** They landed in one commit and touch the same function family, which is where a sibling fix usually neutralizes the other's test. Checked both directions: deleting the settings snapshot does not make `resolveOwnershipPlan` error (`readFinalDiskContent` returns `nil, nil` for `ErrNotExist`), so AR#2's test still reaches `checkManifestConsistency` rather than short-circuiting on AR#1's new early return; and AR#1's test asserts `len(results) == 1`, so it cannot pass by way of AR#2's sweep. Mutating away either fix alone still fails its own test.
- **The two exception-face tests are labelled honestly.** `TestCheckScaffoldIntegrity_SettingsJSONDeleted_OwnedKeysCatchesIt` and `…_AgentsMdDeleted_ManagedBlocksCatchesIt` pass against both pre- and post-fix code — they are audits of the narrowed skip set, not regression guards, and their doc comments say exactly that ("the exception-face audit behind AR#2's narrowed … skip set"). Neither is vacuous: all three faces are real manifest-tracked template files (`templates/base/.ralph/core/settings.ralph.json` exists; `AGENTS.md` and `.claude/settings.json` are rendered by `RenderFS` and land in `hashes`), so the exclusions they assert are load-bearing.
- **AR# pointers are cycle-qualified.** The six `AR#1`/`AR#2` citations added in `e01939e` all carry "cycle 1" plus the report path, so they stay resolvable after `/cross-review` rewrites the per-slug triage file in place for cycle 2. This is the convention `internal/cli/upgrade_v2.go` adopted in Phase 3 after the unqualified form caused trouble, applied here without being asked.
- **The register was maintained in both directions.** Row 100's RESOLVED note had its wrong assertion count corrected rather than left standing (L1), and two new rows were added — the `KNOWN_DIFFS` suppression pair (M5) and the template `quality-gates.md` wiring mismatch that `/verify` surfaced. The latter's pointer, "`docs/reports/verify-…-p5.md`(judgment (b))", resolves: the verify report does have a section `(b)`, and it names that file.
- **AC-10's tick is caveated rather than overstated.** `74d28ef` ticked the spec AC while appending "Codex 側は dispatcher 配線済みだが project-scoped hooks の実発火確認が trust 制約で未了" and pointing at the tech-debt row — consistent with the M8 README fix rather than contradicting it.
- **No debug code, no secrets, no stray output.** Swept the whole delta's `+` lines for `TODO`/`FIXME`/`fmt.Print`/`set -x`/credential-shaped tokens: nothing.
- **All four drift-guidance surfaces read correctly.** Checked each in context, including the `--dry-run` preview (where the surrounding text is "would be left untouched") and the markdown report (heading → guidance → table). Both `ralph eject` and `ralph adopt` genuinely apply to the unresolved-drift population per AC-2 and AC-5, so the pointer is not just well-worded but correct.

## 4. Merge recommendation

**Merge.** No CRITICAL and no HIGH. Every cycle-1 finding is fixed at the site
it named, the two cross-review ACTION_REQUIREDs are fixed with regression
tests that genuinely discriminate against the pre-fix code, and the two
MEDIUMs found here are both documentation/robustness issues with zero
behavioral exposure at HEAD (`ALLOWLIST` is empty, so C2-M2 cannot bite until
someone adds an entry; C2-M1 fails closed, so the risk is operator surprise,
not a missed violation).

This is the final cycle under the default cap, so the seven findings above
will not be fixed in-PR unless the cap is raised. Per the repo's own
convention that "recommended but not fixed at the cap" is a deferral, they
need **one batched tech-debt row** before `/pr`, with a trigger along the
lines of: *the next touch to `scripts/check-template-purity.sh` or
`ralph doctor --strict`'s user-facing help*. C2-M1 is the one worth
considering for an in-cycle fix instead — it is a single clause in the flag
help and a single clause in the README row, and it is the only finding that
misdescribes a CI gate's exit semantics to an operator.

## 5. Known gaps in this review

- No tests were executed and no static analysis was run. The claim that cases E/F and the AR#1/AR#2 regression tests fail against pre-fix code is derived by walking the pre-fix control flow (`git show <pre-fix>:<path>`), not by checking out the old code and running the suite.
- C2-M2 was confirmed by an isolated shell probe replicating `is_allowlisted`'s parameter expansions, not by running `check-template-purity.sh` with a regex allowlist entry injected.
- Spec compliance (including whether the AC ticks in `74d28ef` are earned) and documentation drift remain `/verify` and `/sync-docs` territory and were not audited here.

---

# Cycle 3

- Date: 2026-08-19
- Cycle: 3 (cap raised from 2 to 3 by the operator after cycle-2 `/cross-review` reported 4 ACTION_REQUIRED)
- Scope: the cycle-3 delta only — `git diff 2bc988f..HEAD`: `e39d436` (C2-M1/M2/L1–L3 fixes), `c805cf5` / `5312a55` / `7e6ddcb` / `dfbc061` (cycle-2 verify/test/sync-docs artifacts), `1f50407` (stale allowlist name in the purity FAIL message), `3836a7e` (cycle-2 triage, 4 AR), `25b4f79` (AR#1–#4 fixes + class-closing warn-path audit), `fd136ae` (conflict-scan unreadable-file defense in depth). Commit DAG confirmed linear via `git log --format='%h parent=%p'`; note `1f50407` is the direct child of `c805cf5`, which matters for C3-L7 below.
- Dimension: diff quality only. No tests run, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL, no HIGH). Merge recommendation: **merge**, with C3-M1 and C3-L1 fixed in-cycle if the operator accepts ~10 more lines of churn; everything else folds into the existing batched tech-debt row (which itself needs its "cap 2/2" framing corrected — C3-L7).

## 1. Cycle-2 finding re-classification

All seven cycle-2 findings are resolved as intended: five fixed at the site
each named, two deferred into the batched tech-debt row and still accurate at
HEAD. Four fixes left a residual at a sibling site or in the surrounding
prose; those are filed as new C3-* findings and cross-referenced here.

| Cycle-2 ID | Status at HEAD | Evidence | Residual |
|----|----|----|----|
| C2-M1 (`--strict` help/README enumerate exactly five checks) | **Fixed** | `newDoctorCmd`'s `--strict` help dropped "these five" for "these scaffold checks" and now names the meta-failure class explicitly ("an unparseable manifest, an ownership-planning error such as an unreadable tracked file, or a per-check computation error (e.g. an unreadable block surface, an invalid-JSON settings.json)"). Re-derived the strict-eligible set from the code at HEAD (not at `e39d436`): every `scaffoldViolationStatus(true, strict)` site is covered by that wording, including the two `25b4f79` added. Parentheses balance. | **Yes → C3-L1.** `25b4f79` widened the flag help but not the README row, which is the other surface C2-M1 named. |
| C2-M2 (`is_allowlisted` splits on the first `\|`) | **Fixed** | `ALLOWLIST` became `ALLOWLIST_PATHS` / `ALLOWLIST_PATTERNS`; `is_allowlisted` is now an index loop with no delimiter parsing, so a regex pattern containing `\|` compares by value. The bash-3.2 justification for the length loop over `"${!arr[@]}"` is recorded at the site. | **Yes → C3-L2.** Three bare `ALLOWLIST` references survive in the script header — which `25b4f79` rewrote — and the allowlist's own comment still enumerates only `FIXED_PATTERNS`/`REGEX_PATTERNS` although `scan_path` is now a third consumer. |
| C2-L1 (case D's comment credits case C with proving path scoping) | **Fixed** | New case C3 writes a second occurrence of the same pattern at a non-allowlisted path in the same fixture root and asserts the run fails naming `second.md` **and not** `leak.md`. That is the assertion the old comment claimed and did not have; case D's comment now points at C3. | — |
| C2-L2 (`2>&1` merges grep stderr into the parsed hit stream) | **Fixed** | `grep_scan_or_fail` dropped `2>&1`; grep's stderr goes to the script's stderr and can no longer be parsed as a hit line with `path="grep"`. | **Yes → C3-L3.** The error branch still `echo "$_scan_output" >&2` under a "FAIL: grep failed" heading, which now prints partial hits rather than the error; `scan_path` was written with the same shape. |
| C2-L3 (`checkManifestConsistency` hand-copies `v2SkipPaths()`) | **Fixed** | `skip := v2SkipPaths(); delete(skip, upgrade.SettingsSnapshotRelPath)` — exactly the derivation the finding recommended, with a comment recording why the snapshot stays in the sweep. A fifth exception face would now be inherited automatically. | **Yes → C3-L4.** The function's doc header still opens "Two of `v2SkipPaths()`'s four exception faces are excluded here" and then enumerates three; the fix's own new inline comment says three. |
| C2-L4 (drift guidance duplicated at four sites) | **Deferred, accurate** | Re-checked at HEAD: the literal is still at `internal/cli/upgrade_v2.go:229`, `:309`, `:790` and `internal/upgrade/report.go:124` — four sites, unchanged. The batched register row describes it correctly. | **Yes → C3-L7** (row framing only, not the finding). |
| C2-L5 (cycle-1 report pointers shifted by `e01939e`) | **Deferred, accurate but superseded** | The row's mitigation ("read the reports at `348801c`/`33c1166`") is still the right advice, but `3836a7e` rewrote the triage report in place for cycle 2, so the cycle-1 pointers the row describes no longer exist in that file at all. | **Yes → C3-L7.** The cycle-2 pointers that replaced them were invalidated by the row's own grandchild commit. |

Cycle-2 totals: 2 MEDIUM + 5 LOW = 7. Fixed at the cited site: 5/5 of the
in-cycle set; 2/2 deferrals still hold at HEAD.

## 2. Cycle-3 findings

| ID | Severity | Finding | Evidence | Recommendation |
|----|----------|---------|----------|----------------|
| C3-M1 | MEDIUM | `fd136ae`'s new branch is not reached by any test in the diff, and the test named for it passes against the pre-fix code. `checkConflictMarkers` now collects `unreadable` paths and reports them as a strict-eligible violation. `TestCheckScaffoldIntegrity_UnreadableTrackedFile_StrictFails` chmods `docs/notes.md` to 0000 and asserts only that *some* result fails naming that path — but `docs/notes.md` is a path the ownership planner reads itself, so `resolveOwnershipPlan` errors first and `checkScaffoldIntegrity` returns a single `Scaffold integrity` fail before `checkConflictMarkers` is ever called. The test's own doc comment says exactly this, so it is labelled honestly (an audit, like the cycle-2 exception-face pair) — but it means deleting the whole `unreadable` branch leaves the suite green, and the repo's testing rule asks for an edge case whenever branching changes. The branch is genuinely reachable in production: the planner skips `v2SkipPaths()` members and `PreservePrefixes` paths (`resolveOwnershipPlan` → `upgrade.ReplaceOptions`), and FR-9(e) uses `os.Stat`, which succeeds on a mode-0000 file — so an unreadable `AGENTS.md`/`.gitignore`/settings snapshot, or an unreadable preserved path from an unavailable pack, is exactly what this branch exists for. | `internal/cli/doctor.go` `checkConflictMarkers` (the `unreadable` slice and its early return); `internal/cli/doctor_scaffold_test.go` `TestCheckScaffoldIntegrity_UnreadableTrackedFile_StrictFails` (gate-level assertion + its own "the ownership-planning pass reads the path first and fails there" comment); `internal/cli/adopt.go` `resolveOwnershipPlan` (`SkipPaths: v2SkipPaths()`, `PreservePrefixes`); `internal/upgrade/replaceplan.go` `PlanCoreReplaceDesired` (`opts.SkipPaths[path] → continue`, `hasPreservePrefix → continue` — both before `readDiskFile`). | Add one test that reaches the branch: chmod `AGENTS.md` to 0000 (a `v2SkipPaths()` face the planner never reads) and assert the **`Scaffold: conflict markers`** result specifically is `fail` under `--strict` and names `AGENTS.md`. Against the pre-`fd136ae` code that result is `pass`, so the test discriminates. Skip-as-root guard as in the existing test. |
| C3-L1 | LOW | C2-M1's fix reached one of the two surfaces it named. `25b4f79` widened the `--strict` flag help with the per-check computation-error class but left the README `ralph doctor [--strict]` row at `e39d436`'s wording: "…and on the meta-failures that make those checks impossible (unparseable manifest, unreadable tracked file)". At HEAD an invalid-JSON `.claude/settings.json` and an unreadable managed-block surface are also strict-eligible, and neither is an unparseable manifest nor an unreadable tracked file. The generic clause ("meta-failures that make those checks impossible") still covers them semantically, so this is weaker than C2-M1 — the parenthetical is incomplete rather than false — but it is the same surface pair and the same fix that just corrected it. | `README.md` Commands table, `ralph doctor [--strict]` row (unchanged since `e39d436`); `internal/cli/doctor.go` `newDoctorCmd` `--strict` help (widened in `25b4f79`); the two new `scaffoldViolationStatus(true, strict)` sites in `checkManagedBlocks` and `checkSettingsOwnedKeys`. | One word: make the README parenthetical illustrative — "(e.g. an unparseable manifest, an unreadable tracked file, an invalid-JSON `settings.json`)" — so it cannot drift again the next time a meta-failure is added. |
| C3-L2 | LOW | The purity script's allowlist documentation was not carried through either the rename or the new third dimension. Three bare `ALLOWLIST` references survive in the header block — "listed in ALLOWLIST for that exact (path, pattern) pair", "ALLOWLIST is empty today", "see ALLOWLIST's own comment below" — naming a variable that `e39d436` removed, so `grep -n 'ALLOWLIST=' ` now matches nothing. `1f50407` swept the FAIL-path message for exactly this and stopped there, and `25b4f79` then *rewrote* this header paragraph to add the path dimension while carrying all three stale names forward. Separately, the allowlist's own comment still says `ALLOWLIST_PATTERNS` "is the exact FIXED_PATTERNS/REGEX_PATTERNS entry each applies to" although `scan_path` passes `PATH_PATTERNS` entries into the same `is_allowlisted`, and no fixture allowlists a path pattern (case C covers a fixed-string pattern only) — the same "new branch, no allowlist fixture" omission H1 and C2-M2 were each about, now on its third occurrence. | `scripts/check-template-purity.sh` header block (the three `ALLOWLIST` mentions, rewritten in `25b4f79`), the `ALLOWLIST_PATHS`/`ALLOWLIST_PATTERNS` comment, `scan_path` (`is_allowlisted "$line" "$pattern"`); `tests/test-template-purity.sh` header case list and case A's comment carry the same stale name; `git show 1f50407` (partial sweep). Mechanism itself verified correct: for a path hit, `ALLOWLIST_PATHS` takes the `find`-printed path and `ALLOWLIST_PATTERNS` the `PATH_PATTERNS` entry. | Sweep `ALLOWLIST` → `ALLOWLIST_PATHS`/`ALLOWLIST_PATTERNS` across both files, and add `PATH_PATTERNS` to the allowlist comment's enumeration. If the mechanism is meant to be usable for path hits, one fixture case (case C with a `PATH_PATTERNS` entry) closes the branch. |
| C3-L3 | LOW | The C2-L2 fix removed `2>&1` but left the line that consumed it. `grep_scan_or_fail`'s exit >1 branch still does `echo "$_scan_output" >&2` under the heading `FAIL: grep $mode failed (exit $status)…`. With stderr no longer merged, `_scan_output` on that branch holds whatever matches grep printed before erroring (usually nothing), never the error text — so the diagnostic block prints hits, or an empty line, immediately below a line promising the failure reason. The actual grep error does reach the terminal, just unlabelled and possibly interleaved. `scan_path`, written later in the same commit family, copied the identical shape. | `scripts/check-template-purity.sh` `grep_scan_or_fail` (the `*)` case) and `scan_path` (the `*)` case); the adjacent comment ("stderr … flows to the script's own stderr, and exit >1 still hard-fails") is accurate — only the echo is orphaned. | Drop the `echo "$_scan_output" >&2` from both error branches, or capture stderr into a separate variable/temp file and echo *that*. |
| C3-L4 | LOW | `checkManifestConsistency`'s doc header undercounts its own exclusions, and the C2-L3 fix made the arithmetic visible. The header opens "Two of `v2SkipPaths()`'s four exception faces are excluded here" and then enumerates three paths ("settings.json (v2SettingsPath) and the two managed-block surfaces (AGENTS.md, .gitignore)"). `v2SkipPaths()` returns four paths; the code is now literally `v2SkipPaths()` minus one, i.e. three. The fix's own new inline comment three lines below gets it right ("settings.json's existence is guaranteed by FR-9(c) and the block faces' by FR-9(b)"), so the header contradicts a comment added in the same commit. | `internal/cli/doctor.go` `checkManifestConsistency` doc header vs its inline `skip := v2SkipPaths(); delete(skip, upgrade.SettingsSnapshotRelPath)` comment; `internal/cli/upgrade_v2.go` `v2SkipPaths` (settings.json + 2 block surfaces + snapshot = 4). | "Three of `v2SkipPaths()`'s four exception faces". |
| C3-L5 | LOW | Two nits inside `fd136ae`'s new branch. (a) It classifies absence with `os.IsNotExist(err)` while the two functions this PR standardized — `checkScaffoldIntegrity` and `checkManifestConsistency` in the same file — use `errors.Is(err, fs.ErrNotExist)`; behaviour is identical here because `os.ReadFile` returns an unwrapped `*PathError`, so this is idiom drift, not a bug. (b) The `unreadable` early return discards `offenders` computed so far, so a run with both an unreadable file and a real conflict marker reports only the unreadable one; the operator fixes permissions, re-runs, and only then learns about the conflict marker. Status is a violation either way. | `internal/cli/doctor.go` `checkConflictMarkers` (`if os.IsNotExist(err)` and the `if len(unreadable) > 0 { … return r }` block) vs `checkScaffoldIntegrity` / `checkManifestConsistency`'s `errors.Is(..., fs.ErrNotExist)`. | Use `errors.Is(err, fs.ErrNotExist)`; and either fall through so both detail fragments are joined, or say "…; conflict-marker scan incomplete" in the detail. |
| C3-L6 | LOW | Purity test case H is a duplicate of case A, and its own comment says so. Both run `run_case "…real templates/ tree…" 0 "templates"` against the same scan root with the same expectation; H's comment reads "Explicit companion to A (which already covers this)". Adding the path dimension does not change what A exercises — A runs the whole script, path scan included. The file-header case list carries the duplicate too. | `tests/test-template-purity.sh` case A and case H (identical `run_case` invocation), header case list entries A and H. | Delete case H and note the path dimension in case A's comment, or make H a genuinely different assertion (e.g. `PATH_PATTERNS` is non-empty, the way C1 pins the allowlist arrays). |
| C3-L7 | LOW | Same-PR artifacts describing each other went stale again, in three places, all within this delta. (a) The cycle-2 triage's three `file:line` pointers — `doctor.go:889-892` (AR#1), `doctor.go:810-813` (AR#2), `status.go:487-489` (AR#3) — were invalidated by its own direct child `25b4f79`, which inserted the fix comments above each site; at HEAD all three land in doc-comment prose, and `doctor.go:810-813` specifically lands in the (a)/(b) asymmetry paragraph rather than the `applyBlockUpdatesV2` error branch it names. This is C2-L5 recurring one cycle later on the same artifact class. (b) The cycle-2 verify report's closing disposition note ("the stale `ALLOWLIST` reference in the FAIL-path message … flagging here for the orchestrator's disposition") was resolved by its own direct child `1f50407`, so the merged PR ships a report listing an open item that is closed. (c) The batched tech-debt row and the plan's new deviation line both justify deferring C2-L4/C2-L5 as "cap 2/2 到達につき" / "最終 pipeline cycle(2/2)" — the cap was raised and a third cycle ran, so the stated reason is no longer the situation. The Go comments `25b4f79`/`fd136ae` added avoid all of this correctly: they cite function names, are cycle-qualified, and name the report path. | `docs/reports/cross-review-triage-overlay-scaffold-v2-p5.md` ACTION_REQUIRED table vs `sed -n 886,895p` / `808,816p internal/cli/doctor.go` and `485,492p internal/cli/status.go` at HEAD; `docs/reports/verify-2026-08-19-overlay-scaffold-v2-p5.md:217` vs `git show 1f50407`; `docs/tech-debt/README.md` (last row) and `docs/plans/active/2026-08-19-overlay-scaffold-v2-p5.md` Deviations. | Aim the fix at the producer, not the rows: have `/cross-review` triage cite `function (file)` instead of `file:line`, the way the Go comments in this PR already do. Concretely before `/pr`: reword the tech-debt row's and plan line's "cap 2/2" justification to "deferred through cycle 3", and fold (a)/(b) into that same row rather than re-editing merged reports. |

Counts: CRITICAL 0, HIGH 0, MEDIUM 1, LOW 7.

## 3. Positive notes

- **The warn-path audit's headline claim holds.** `grep -n '"warn"' internal/cli/doctor.go` returns no literal inside `checkScaffoldIntegrity`, `checkScaffoldCoreHashes`, `checkManagedBlocks`, `checkSettingsOwnedKeys`, `checkConflictMarkers`, or `checkManifestConsistency` — every remaining match is either another doctor check (CLI presence, hooks, packs, codex probe) or comment prose. Every FR-9 status now flows through `scaffoldViolationStatus`. The two silent `continue`s inside `applyBlockUpdatesV2` (missing `AGENTS.core.md`, missing `.gitignore` template) were checked separately and fail closed: neither writes an `outcomes` entry, so `checkManagedBlocks`' `!ok` test makes the surface an offender.
- **`resolveOwnershipPlan`'s discarded warnings are covered where the comment says they are.** Its "warnings … are discarded here — doctor's own checks below report their own findings at the right granularity" is verifiable: an unavailable pack recorded in `Meta.Packs` is reported by the manifest-based pack check as `Pack: <name>` / "pack not found in templates". The swallowed warning is a duplicate, not a lost finding.
- **`fs.ErrNotExist` really is wrappable here.** `scaffold.ReadManifest` returns `os.ReadFile`'s `*fs.PathError` unwrapped on the missing-file path and the TOML error on the parse path, so `errors.Is(err, fs.ErrNotExist)` in both `buildScaffoldStatus` and `checkScaffoldIntegrity` splits exactly the two cases intended; `status.go`'s `fmt.Errorf("reading manifest: %w", err)` preserves the chain for any caller that wants it.
- **The AR#1 test set is honest about a confound and works around it rather than pretending.** `TestCheckScaffoldIntegrity_SettingsJSONInvalidJSON_StrictFails` deliberately carries no `runDoctorFull` pin, with a comment explaining that `checkHooks` fails unconditionally on invalid `settings.json` and would confound it; the doctor-level pin is placed on the settings *snapshot* variant instead, which drives the identical `MergeOwnedSettings` branch through a file `checkHooks` never reads. That is the right call and it is written down at both sites.
- **The AR#2 test targets the error return, not the outcome path.** Swapping the embedded `.gitignore` template for one without markers makes `extractBlockInterior` fail inside `applyBlockUpdatesV2`, which is a genuine `err != nil` — distinct from the missing-surface case the pre-existing `AgentsMdDeleted` test already covers, and the doc comment says exactly that. `scaffold.EmbeddedFS` is assigned without restore, which matches the established convention in this package (every test that cares sets it via `initV2Project` first).
- **Case C3 is a real assertion, not a change-detector.** It asserts `second.md` appears **and** `leak.md` does not, in a fixture where both files carry the same pattern and only one is allowlisted. That is the path-scoping claim C2-L1 said no case established.
- **PATH_PATTERNS' five entries and their reasons are all true.** Verified against `templates/base/` directly: no `.claude/skills/release/`, `.github/workflows/` holds only `verify.yml`, and neither `check-sync.sh` nor `check-template-purity.sh` is under `templates/base/scripts/`. All five are genuine `ROOT_ONLY_EXCLUSIONS` members, so the "seeded from check-sync.sh's own list" provenance claim checks out too.
- **Citations are cycle-qualified throughout.** Every `AR#` added in `25b4f79`/`fd136ae` carries a cycle number plus the triage report path (`grep -rn "AR#" internal/ scripts/ tests/`), so the pointers survive `/cross-review` rewriting the per-slug triage file in place — the convention Phase 3 adopted after the unqualified form caused trouble.
- **The insight-event ledger is complete and correctly labelled.** Ten events, five phases × two cycles, each with the right `cycle` value and the cross-review rows carrying `action_required` 2 then 4. No phase forgot `insights-append.sh`, and no event repeats a stale cycle number.
- **No debug code, secrets, or stray output.** Swept every `+` line in the delta across `internal/`, `scripts/`, `tests/`, `README.md` for `TODO`/`FIXME`/`fmt.Print`/`set -x`/credential-shaped tokens: nothing. No `templates/base/` file was touched, so there is no mirror obligation in this delta.

## 4. Merge recommendation

**Merge.** No CRITICAL, no HIGH. All four cross-review ACTION_REQUIREDs are
fixed at the sites they named, with regression tests that discriminate
against pre-fix code in three of the four cases; the class-closing audit that
accompanied them is real rather than asserted (see the first positive note).
The single MEDIUM is about the fourth case — `fd136ae`'s defense-in-depth
branch is correct and reachable, but nothing in the suite would notice if it
were deleted, and the one-test fix is mechanical.

Worth fixing in this cycle, if churn is acceptable: **C3-M1** (one test,
~20 lines) and **C3-L1** (three words in the README row, closing the surface
C2-M1's fix missed). The remaining LOWs are prose and test-hygiene items; they
belong in the existing batched tech-debt row, which needs its justification
line corrected either way (C3-L7c) since the "cap 2/2, final cycle" premise it
was written under no longer describes this PR.

If cycle 3 is the last cycle, treat C3-L2 through C3-L7 as deliberate
deferrals and extend the existing row rather than opening a new one — the
repo's convention is that "recommended but not fixed at the cap" is a
deferral, and the plan is archived at `/pr` with nothing else carrying them.

## 5. Known gaps in this review

- No tests were executed and no static analysis was run. C3-M1's central
  claim — that `TestCheckScaffoldIntegrity_UnreadableTrackedFile_StrictFails`
  passes against the pre-`fd136ae` code — is derived by reading
  `PlanCoreReplaceDesired`'s skip/preserve early returns and the test's own
  gate-level assertion, not by checking out the parent commit and running it.
  The test's own doc comment states the same causal chain, so the two agree.
- The claim that a mode-0000 file still passes `os.Stat` (so FR-9(e) misses
  it) is standard POSIX behaviour, taken as read rather than probed here.
- Spec compliance (whether the AR fixes satisfy FR-9's wording) and
  documentation drift beyond the README row named in C3-L1 remain `/verify`
  and `/sync-docs` territory.

# Cycle 4

- Date: 2026-08-19
- Cycle: 4 (cap raised from 3 to 4 by the operator after cycle-3 `/cross-review` reported 2 ACTION_REQUIRED)
- Scope: the cycle-4 delta only — `git diff e5329f4..HEAD`: `6353a07` (C3-M1, C3-L1–L6, L7(c) fixes), `71236d8` / `aba2eda` / `ea00dcd` / `22f6212` (cycle-3 verify/test/sync-docs artifacts), `836162b` (cycle-3 triage, 2 AR), `de36e45` (AR#1 untracked-drift guidance + `writeDriftGuidanceV2` + report.go sentence + 2 tests; AR#2 codex config comment; C2-L4 register update).
- Dimension: diff quality only. No tests run, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL, no HIGH). Merge recommendation: **merge**, with C4-M1 and C4-M2 worth fixing in-cycle — both are a few lines and both are the same class the cycle-4 commit itself was written to close.

## 1. Cycle-3 finding re-classification

Seven of the eight cycle-3 findings are fixed at the site each named, with
`6353a07` applying the recommended fix rather than a nearby approximation in
every case. Two fixes applied the recommendation's *items* without its
*mechanism* (C3-L1) or reached one of the two files named (C3-L2); those are
filed as new C4-* findings and cross-referenced here.

| Cycle-3 ID | Status at HEAD | Evidence | Residual |
|----|----|----|----|
| C3-M1 (defense-in-depth branch no test reaches) | **Fixed** | `TestCheckScaffoldIntegrity_UnreadableSkipPathFace_ConflictScanBranchFires` chmods `AGENTS.md` to 0000 and asserts the **`Scaffold: conflict markers`** result specifically is `fail` and names the path — exactly the fixture the finding recommended, including the root guard. It discriminates: `resolveOwnershipPlan` passes `SkipPaths: v2SkipPaths()`, whose members `PlanCoreReplaceDesired` `continue`s past before `readDiskFile`, so the planner never reads `AGENTS.md` and pre-`fd136ae` that result is `pass`. `checkConflictMarkers`' own doc comment ("Every manifest.Files path is scanned (including v2 exception faces)") confirms the path is in the scan population. | — |
| C3-L1 (README `--strict` row narrower than the flag help) | **Fixed at the site, recommendation half-applied** | The README row now reads "(unparseable manifest, unreadable tracked file, unreadable block surface, invalid-JSON settings.json)" — the two missing items were added. | **Yes → C4-L2.** The recommendation was to make the parenthetical *illustrative* ("e.g. …") so it cannot drift again; the fix added items and kept the closed form. Re-deriving from HEAD: nine `scaffoldViolationStatus(true, strict)` sites exist and four meta-failures are outside the list. |
| C3-L2 (`ALLOWLIST` rename + third-consumer doc) | **Partially fixed** | The substantive half landed: the allowlist comment now says `FIXED_PATTERNS/REGEX_PATTERNS/PATH_PATTERNS` "(all three dimensions share this one mechanism)", and new purity test case I allowlists a `PATH_PATTERNS` entry and asserts the run passes — the suppression-path fixture the finding said was missing on its third occurrence. | **Yes → C4-L3.** Of the three bare `ALLOWLIST` mentions the finding enumerated, one was rewritten and two survive; all four in `tests/test-template-purity.sh` (the other file the finding named) are untouched. |
| C3-L3 (orphaned `echo "$_scan_output" >&2`) | **Fixed** | Both error branches (`grep_scan_or_fail` and `scan_path`) dropped the echo and each carries a comment saying why stderr is deliberately not captured. | — |
| C3-L4 (`checkManifestConsistency` header undercounts) | **Fixed** | "Three of `v2SkipPaths()`'s four exception faces are excluded here" — header now agrees with the enumeration below it and with the inline comment added in the C2-L3 fix. | — |
| C3-L5 (idiom drift + discarded offenders) | **Fixed** | (a) `errors.Is(err, fs.ErrNotExist)`, matching the two sibling functions. (b) The unreadable branch now appends "; conflict markers found in: …" when `offenders` is non-empty, with a comment stating the operator-facing reason. | — |
| C3-L6 (duplicate purity case H) | **Fixed** | Case H deleted; its distinct claim (the path dimension introduces no false positive on the real tree) folded into case A's comment. | **Yes → C4-L4.** The file-header case list lost H and never gained I. |
| C3-L7 (same-PR pointer decay, three places) | **(a)(b) deliberately not fixed; (c) fixed then re-staled** | (a)(b) are merged historical artifacts; the plan's cycle-3 deviation line records the decision explicitly, which is the right disposition. (c) The tech-debt row and plan line dropped the "cap 2/2 / 最終 cycle" justification for a time-stamped one. | **Yes → C4-L6.** The replacement narrative stops at "cap は 3 に引き上げられ cycle 3 が実施された" and asserts "両件は … deferral 判断を維持" — both falsified by `de36e45`, the fifth commit in the same PR, which resolved item (1) in cycle 4. |

Cycle-3 totals: 1 MEDIUM + 7 LOW = 8. Fixed at the cited site: 7/8 (C3-L7(a)(b)
deferred by design). Two fixes left a residual, both filed below.

## 2. Cycle-4 findings

| ID | Severity | Finding | Evidence | Recommendation |
|----|----------|---------|----------|----------------|
| C4-M1 | MEDIUM | **AR#1's untracked-drift distinction reached three CLI stderr sites and the markdown report, but not the fourth surface that prints the same guidance — `ralph doctor`'s FR-9(a) check, added by this same PR.** `checkScaffoldCoreHashes` builds its detail from `plan.Drift` and ends "— resolve with `ralph eject` (keep the change) or `ralph adopt` (discard it)", with no untracked annotation. That is verbatim the circle AR#1 was filed about: for an untracked entry both verbs refuse (`runEjectIO` / `resolveAdoptSingleTarget` return "not tracked in the manifest"). The same detail also calls every entry a "core path(s)", and the function's doc comment claims "`plan.Drift` is exactly this population … every core-owned path" — contradicted inside the same package by `buildScaffoldStatus`'s own comment, written in this PR: "plan.Drift can include genuinely untracked paths too (classifyUntracked, internal/upgrade/replaceplan.go) -- listed here in full, matching doctor's checkScaffoldCoreHashes (FR-9(a))". So the codebase already knows the population is mixed; only the message and the doc header do not. `ralph doctor --strict` is the CI-grade gate, i.e. the surface most likely to be the *first* place a user meets untracked drift. | `internal/cli/doctor.go` `checkScaffoldCoreHashes` (detail string + doc comment) vs `internal/cli/status.go` `buildScaffoldStatus` (the `driftPaths` comment) and `internal/upgrade/replaceplan.go` `classifyUntracked` (`DriftEntry{… RecordedHash: ""}`); `internal/cli/eject.go` and `internal/cli/adopt.go` `resolveAdoptSingleTarget` (the two "not tracked in the manifest" refusals); `internal/cli/upgrade_v2.go` `writeDriftGuidanceV2` (the fix that was applied everywhere else). | Reuse the same `d.RecordedHash == ""` test the helper uses: annotate those paths and append the manual-resolution clause, or at minimum drop "core" from the count phrase and say "…; paths with no manifest entry must be moved aside instead". A one-line variant is acceptable — the point is that the three unified sites and the fourth no longer disagree. |
| C4-M2 | MEDIUM | **The new `report.go` sentence ships a meta-repo-internal pointer into the downstream user's project — the same class of leak the other half of this very commit removed.** `renderDriftSection`'s guidance paragraph now ends "…(AR#1, cycle 3, docs/reports/cross-review-triage-overlay-scaffold-v2-p5.md)". `WriteUpgradeReport` writes this file to `docs/reports/upgrade-<version>-<date>.md` **inside the target project**, so a downstream operator resolving drift is pointed at a ralph-repo cross-review artifact that does not exist in their checkout — and the path shape (`docs/reports/…`) makes it look like it should. AR#2, in the same commit, generalized the codex config comment for exactly this reason (FR-10: shipped surfaces must not carry meta-repo history). The three CLI sites got this right: their citations live in Go comments, not in the emitted strings. | `internal/upgrade/report.go` `renderDriftSection` (the `b.WriteString` guidance paragraph) and `WriteUpgradeReport`'s `docs/reports/upgrade-<version>-<date>.md` destination; contrast `internal/cli/upgrade_v2.go` `writeDriftGuidanceV2`, where the identical citation sits in the doc comment and never in `writef` output; `.codex/config.toml` (AR#2 half of the same commit). | Move the parenthetical into a Go comment above the `WriteString`, leaving the user-facing sentence to end at "…re-run `ralph upgrade` instead." |
| C4-L1 | LOW | The report sentence tells the reader to look for "an **empty** Recorded column", but the table never renders one: `emptyDash("")` returns `—`, so an untracked row shows an em dash. A reader scanning for a blank cell finds none. | `internal/upgrade/report.go` `renderDriftSection` (the guidance string) vs its own row `fmt.Fprintf(… emptyDash(e.RecordedHash) …)` and `emptyDash`. | "Rows whose Recorded column is `—` …". |
| C4-L2 | LOW | C3-L1's fix added the two missing items but kept the closed parenthetical the finding asked to open, and the list is already incomplete again at HEAD. Nine `scaffoldViolationStatus(true, strict)` sites exist; four are meta-failures outside the README's four: resolving the target dir (`checkScaffoldIntegrity`), reading the settings snapshot, the templates not shipping `settings.json`, and a `MergeOwnedSettings` error (all `checkSettingsOwnedKeys`). The flag help avoids this by naming a *class* with examples ("a per-check computation error (e.g. …)"); the README is now the only surface that enumerates. This is the third cycle in which this one sentence has been widened. | `README.md` Commands table, `ralph doctor [--strict]` row; `internal/cli/doctor.go` — the nine `scaffoldViolationStatus(true, strict)` sites and `newDoctorCmd`'s `--strict` help. | Insert "e.g." and stop maintaining the list, as C3-L1 recommended. |
| C4-L3 | LOW | The `ALLOWLIST` → `ALLOWLIST_PATHS`/`ALLOWLIST_PATTERNS` rename is now on its **third** partial sweep inside one PR (`1f50407` fixed the FAIL message, `25b4f79` rewrote the header and carried the old name, `6353a07` rewrote one sentence of that header and carried the rest). Surviving at HEAD: two in the script header — "ALLOWLIST is empty today", "see ALLOWLIST's own comment below" — and four in the test file, including the header's case-C line and case A's comment, which is the other file C3-L2 named. `grep -n 'ALLOWLIST='` still matches nothing, so "see ALLOWLIST's own comment below" is an instruction that cannot be followed by search. | `scripts/check-template-purity.sh` (the two bare mentions in the header block); `tests/test-template-purity.sh` (case-C header line and the three in case A's comment); `git log -S ALLOWLIST` showing the three-commit sweep. | One `grep -n 'ALLOWLIST' scripts/check-template-purity.sh tests/test-template-purity.sh \| grep -v _PATHS \| grep -v _PATTERNS` and fix what it prints. |
| C4-L4 | LOW | The purity test's file-header case list went out of sync with the file in the same commit that edited both: C3-L6's fix deleted case H from the list and the body, and the new case I was added to the body only. The suite now prints A–G then I while the header documents A–G, and the letter H is a silent gap. | `tests/test-template-purity.sh` header "Cases:" block (A–G) vs the `run_case` / inline invocations in the body (A–G, I). | Add the I entry to the header list; either renumber I→H or note in its comment that H was retired. |
| C4-L5 | LOW | `writeDriftGuidanceV2`'s doc comment cites the register row by a quoted phrase that is not in the register: it says `(resolves tech-debt "drift 案内文言の重複")`, and `grep -c 'drift 案内文言の重複' docs/tech-debt/README.md` returns 0 — the row's wording is "未解決 drift 解消案内文が … 重複". A quoted string is a promise that it greps; this one is a paraphrase. The repo's own convention elsewhere (e.g. `TestRunUpgradeV2_UntrackedSeedPathCollision_AdoptedNotDrift`) quotes the row text verbatim. | `internal/cli/upgrade_v2.go` `writeDriftGuidanceV2` doc comment; `docs/tech-debt/README.md` (last row, item (1)). | Quote the row's actual opening words, or cite it positionally ("the batched Phase-5 cycle-2 row, item (1)"). |
| C4-L6 | LOW | The tech-debt row was updated in the description cell only, so the row now contradicts itself, and its C3-L7(c) rewrite went stale one commit later. Item (1) is struck through and annotated "(RESOLVED 2026-08-19 … cycle 4)", while the same cell's preamble still reads "その後 cap は 3 に引き上げられ cycle 3 が実施されたが、**両件**は … deferral 判断を維持"; the Risk cell still lists "(1) 文言更新漏れで surface 間の案内が食い違う", the Justification cell still explains why (1) was deferred, and the Trigger cell still schedules "(1) drift 案内文言への次の変更". The plan's cycle-3 deviation line carries the same now-false claim ("C2-L4/L5 の deferral 判断は cap 引き上げ後も維持"). This is C3-L7(c) recurring exactly one cycle later, from the same cause: a narrative pinned to a cap value the operator then raised. | `docs/tech-debt/README.md` last row (all five cells); `docs/plans/active/2026-08-19-overlay-scaffold-v2-p5.md` line 131; `git show 6353a07 -- docs/tech-debt/README.md` (the C3-L7(c) rewrite) vs `git show de36e45 -- docs/tech-debt/README.md`. | Narrow the row to item (2) only and move item (1) out entirely (it is resolved, not deferred), rather than editing four more cells. State the deferral without a cycle count: "個別修正の churn が見合わないため". |
| C4-L7 | LOW | The AR#2 rewrite removed the phase/session history correctly but also hardened a hedge and dropped the most operationally useful fact. The old text said hooks "did not fire in a scratch repo **even with `--dangerously-bypass-hook-trust`**, **likely because**" the trust gate needs an interactive grant; the new text asserts flatly that "project-scoped `[[hooks.*]]` entries **do not fire** in an untrusted checkout, **and** Codex's project hook trust gate needs the interactive `codex trust` grant" — a hypothesis promoted to a stated cause, with the failed-bypass datum gone. A downstream reader's first move on this comment is to try the bypass flag; the removed sentence is what would stop them. Neither the hedge nor the flag name is phase- or session-specific, so keeping both is compatible with AR#2. The register row still records the hedged version, so the two now disagree. Minor wording nit in the same sentence: "Live verification … has not been confirmed" (the payload shape is what is unconfirmed). | `.codex/config.toml` and `templates/base/.codex/config.toml` (identical, lines ~83–92) vs `git show 836162b^:.codex/config.toml`; `docs/tech-debt/README.md` (the codex-hooks row, still "実機…で確認できなかった"). | Restore "likely because" and the `--dangerously-bypass-hook-trust` clause; neither mentions a phase, slice, or session. |

Counts: CRITICAL 0, HIGH 0, MEDIUM 2, LOW 7.

## 3. Positive notes

- **The AR#1 fix's central factual claim is true, and checkable at both ends.** `classifyUntracked` is the only site that constructs a `DriftEntry` with `RecordedHash: ""` (the other two pass the manifest's recorded hash), and both verbs the guidance names do refuse such a path: `runEjectIO` returns "not tracked in the manifest; nothing to eject" and `resolveAdoptSingleTarget` "…nothing to adopt", with `driftPathSet`'s doc comment already recording AC-3. The "move the local file aside … then re-run `ralph upgrade`" advice is also correct — with the local file gone, `classifyUntracked`'s `!hasDisk` branch plans an `OpCreate`.
- **The new untracked test discriminates, and its fixture reaches the branch by construction.** It deletes the manifest entry for `scripts/run-verify.sh`, writes content matching neither template, and asserts on two strings only the new code emits (`"scripts/run-verify.sh (untracked)"` and `"cannot resolve them"`). Both `classifyUntracked` guards it must pass are visible in the fixture: the disk hash differs from the template hash, and the path is not owner=seed. The paired negative assertion on the tracked-drift test (`must not be annotated (untracked)`) is vacuous on its own, but the positive test is what holds the annotation in place, so the pair is honest.
- **The heading parameterization reads naturally at all three sites.** "Unresolved drift (left untouched; see report for detail):", "(left untouched; no report written this run — tree already converged):", "(would be left untouched):" — each renders as a grammatical parenthetical, and the em dash inside the second heading survives the `%s` unchanged. No site lost information relative to its previous literal.
- **The two `.codex/config.toml` copies are still byte-identical after the AR#2 edit** (`cmp` clean), and `grep -niE "phase|slice|session"` returns nothing in either — AR#2's stated scope is fully met. The retained `codex-cli 0.147.0` version pin is a fact, not session history, and is the right thing to keep.
- **The C2-L4 register update names the deliberate exception rather than hiding it.** Both the row and `writeDriftGuidanceV2`'s doc comment state that `internal/upgrade/report.go` keeps its own markdown-formatted variant on purpose, so a future contributor unifying the last site knows it was a decision. (The doc comment's "three stderr sites" count is accurate for the `writef` guidance; C4-M1 is about a different surface that prints its own.)
- **The insight-event ledger is complete and correctly labelled through cycle 3.** Fifteen events, five phases × three cycles, each carrying the right `cycle` value; no phase skipped `insights-append.sh`, and no event repeats a stale cycle number.
- **No debug code, secrets, or stray output in the delta.** Swept every `+` line across `internal/`, `scripts/`, `tests/`, `.codex/`, `templates/base/`, `README.md` for `TODO`/`FIXME`/`fmt.Print`/`set -x`/credential-shaped tokens: nothing. The only `templates/base/` file touched is the codex config, and its root twin was changed identically.

## 4. Merge recommendation

**Merge.** No CRITICAL, no HIGH. Both cross-review ACTION_REQUIREDs are fixed
at the sites they named, with an accurate factual premise and one genuinely
discriminating test.

Worth fixing in this cycle: **C4-M1** (one condition and a clause in
`checkScaffoldCoreHashes` — the fix is already written three files away, and
leaving it means the PR ships a surface that contradicts the fix it just
landed) and **C4-M2** (delete a parenthetical from a user-facing string,
which is also the cheapest item in the delta). C4-L1 is a two-word edit in
the same sentence as C4-M2 and should ride along.

The remaining LOWs are prose, register, and test-hygiene items. Note that
three of them — C4-L2, C4-L3, C4-L6 — are each the *third* pass at a
sentence this PR has already rewritten twice, which is a signal about the
edit style rather than about any one sentence: additive edits to closed
lists and cap-pinned narratives keep going stale. If this is the last cycle,
the batched register row is the right home for them, but it should be
rewritten rather than extended (see C4-L6).

## 5. Known gaps in this review

- No tests were executed and no static analysis was run. C3-M1's fix is
  judged discriminating by reading `PlanCoreReplaceDesired`'s `SkipPaths`
  early return and `checkConflictMarkers`' scan population, not by running
  the test against the pre-`fd136ae` tree.
- C4-M1's user-facing impact assumes an operator can reach `ralph doctor`
  with untracked drift present; that follows from `classifyUntracked` feeding
  the same `plan.Drift` the check reads, but was not reproduced end to end.
- Whether the AR#1/AR#2 fixes satisfy the spec's wording (FR-4, FR-10, AC-3)
  and whether any shipped doc still describes drift resolution without the
  untracked exception remain `/verify` and `/sync-docs` territory.
