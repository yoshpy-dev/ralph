# Verify report: org-runtime-retire-loop

- Date: 2026-08-03
- Plan: `docs/plans/active/2026-08-03-org-runtime-retire-loop.md`
- Verifier: `verifier` subagent (spec compliance + static analysis)
- Scope: `git diff d074838..a046b41` on `refactor/org-runtime-retire-loop` (merge-base d074838, HEAD a046b41). Includes self-review fix commit 746b35b.
- Evidence: `docs/evidence/verify-2026-08-02-204012.log` (auto-generated by `run-static-verify.sh`, full scope; gitignored, referenced here by path)

## Spec compliance

| Acceptance criterion | Status | Evidence |
| --- | --- | --- |
| AC-1: deletion of Loop scripts/Go packages/skill/templates from both root and templates/base; `go build ./...` green | Met | `scripts/{ralph-orchestrator,ralph-pipeline,ralph-loop-init,ralph-loop,ralph-status-helpers,build-tui,new-ralph-plan,ralph}`, `cmd/ralph-tui`, `internal/{ui,state,action,watcher}` all absent (`ls`/`find` check, no hits) on both root and `templates/base/`. `go build ./...` exit 0. |
| AC-2: `ralph status` org-manifest based (roster + watch-status, `--org-id` filter, no-manifest guidance) | Met | `internal/cli/status.go` rewritten; `TestStatusCmd_ListsAllOrgsWithRosterAndActiveCounts`, `TestStatusCmd_OrgIDFilterShowsOnlyThatOrg`, `TestStatusCmd_ShowsWatchHeartbeatAndPendingAlertCount`, `TestStatusCmd_EmptyStateDirShowsFriendlyNoteAndDoctorHint` all pass. Fix commit additionally closed a data-integrity gap (`TestStatusCmd_EmptyRosterFromFullyCorruptManifestStillWarns`) and unified the `--json` schema (`TestStatusCmd_JSONSchemaIsIdenticalEmptyVsPopulated`). |
| AC-3: `[loop]`/`[pipeline]` config + env removed from 3 surfaces; `defaults_sync_test` green; surviving env (`RALPH_STANDARD_MAX_PIPELINE_CYCLES`, `RALPH_CLAUDE_REVIEWER_MODEL`) unchanged | Met | `internal/config/config.go` has no `Loop`/`Pipeline` fields; `templates/base/ralph.toml` has no `[loop]`/`[pipeline]`; `scripts/ralph-config.sh` (+ template mirror) only exports the two survivors. `TestDefaultsLockStep` passes. |
| AC-4: `/cross-review` functions via moved helpers (`detect_base_branch`/`pick_reviewer`/`count_triage_findings` in `scripts/xreview-helpers.sh`), tests moved | Met | `tests/test-xreview-helpers.sh`: 26/26 pass. Fix commit went further than "moved but unused" (a self-review MEDIUM finding) — `.claude/skills/cross-review/SKILL.md` Step 2 now actually sources the file and calls `pick_reviewer`, and Step 6 calls `count_triage_findings` to cross-check the triage summary line. All 4 mirrors (`{.claude,.agents}/skills/cross-review/SKILL.md` × root/`templates/base`) are byte-identical (`diff` clean) and `check-skill-sync.sh` reports 13/13 in lock-step. |
| AC-5: zero-reference grep (historical docs excluded) passes; check-sync / check-skill-sync / check-pipeline-sync all green; `tests/test-no-loop-references.sh` exists and is CI-wired | Met | `sh tests/test-no-loop-references.sh` → `PASS` (pattern widened by the fix commit to also cover `ralph-cli-driver`, `ralph-loop.sh`, `ralph-status-helpers`, `new-ralph-plan`, `build-tui`, `ralph-tui`, per-phase `RALPH_*_MODEL`, `checkStaleOrchestratorState` — closing self-review's MEDIUM "guard narrower than its own header" finding). `scripts/verify.local.sh`'s `tests/test-*.sh` glob (enumeration-drift-proof) picks it up; `check-sync.sh` (DRIFTED=0), `check-pipeline-sync.sh` (all consumers referenced), `check-skill-sync.sh` (13/13) all `OK` in the full static-verify run. |
| AC-6: AGENTS/CLAUDE/README/rules/DoD/spec coherent with the new two-surface structure; spec FR-11 states harness-skills survive; no residual "/plan or /work abolished" text; `/work` has no `/loop` redirect | Met | `grep -n "廃止"` in the spec shows only the FR-6/FR-11 "does NOT abolish" language, no stale abolition claims. `.claude/skills/work/SKILL.md` guards directory plans with an explicit message pointing to `/org`/`/plan`, not `/loop`. `grep -in loop` across `AGENTS.md`/`CLAUDE.md`/`README.md`/`docs/quality/definition-of-done.md` shows only benign "operating loop"/"evaluator loops" prose, no Ralph Loop residue. |
| AC-6b: historical insights read-compat regression test exists; AC-5 guard excludes insights historical vocabulary | Met | `TestInsightsCmd_HistoricalLoopFlowEventStillReadable` (`internal/cli/insights_test.go:196`) passes. `tests/test-no-loop-references.sh`'s `EXCLUDE_REGEX` explicitly excludes `internal/insights/`, `internal/cli/insights_test.go`, and `docs/insights/`. |
| AC-7: PR④ known gaps #5/#6 fixed with tests; tech-debt rows (incl. legacy shell CLI) closed | Met | `internal/org/watch.go`: probe-recovery gating (`pending.LeadAgentGet != ""` / `HistoryLeadLines >= 0`) and `ensureWatchdogJoined` join-success gating both present, each with dedicated regression tests (`TestWatch_Deadman_ProbeOutageRecoveryAlone_DoesNotClearPendingAlert`, `TestWatch_Deadman_ProbeOutageThenGenuineManifestActivity_ClearsPendingAlert`, `TestWatch_EnsureWatchdogJoined_TransientFailure_RetriesUntilSuccess`). `docs/tech-debt/README.md` shows 8 rows struck through with `RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop` annotations and traceability pointers, including the two rows the self-review flagged as left-open-but-invalidated (`ralph status --json` plain text; Phase 6b Go native migration) and the legacy shell CLI row. |
| AC-8: `go test ./...` / `run-verify.sh` green; `ralph init` scaffold smoke shows no loop artifacts | Likely met (build/vet/static all green; full `go test ./...` is `/test`'s responsibility, not run here) | `go build ./...` exit 0, `go vet ./...` exit 0 (silent success). Scaffold smoke evidence at `docs/evidence/org-retire-loop-smoke-2026-08-03.txt`: no loop skill/scripts/config sections, `.claude/skills/org` + `.claude/rules/agent-messaging.md` + `ralph.toml [org]` present. **Caveat:** the smoke was built at commit 303380d (before the self-review fix commit 746b35b), so it does not reflect the fix commit's template-mirror edits — but those edits are doc/prose fixes only (cross-review SKILL.md, model-routing.md, .codex/README.md wording), not new loop-artifact reintroductions, so the verdict is unaffected. Full `go test ./...` was not run here (belongs to `/test`). |

## Self-review fix-commit crosscheck (746b35b)

Walked every finding row in `docs/reports/self-review-2026-08-03-org-runtime-retire-loop.md` against `git show 746b35b`:

| Finding | Fixed? | Evidence |
| --- | --- | --- |
| HIGH #1 `checkStaleOrchestratorState` dead check + stale `ralph abort` remediation text | Yes | `internal/cli/doctor.go` check + function removed; `internal/cli/doctor_stale_state_test.go` deleted (99 lines); comment renumbering (Check 6→7→8→9→10→11, no gaps) closes the related LOW finding too. |
| MEDIUM (guard pattern narrower than header) | Yes | `tests/test-no-loop-references.sh` PATTERN widened; header/EXCLUDE_REGEX comments rewritten to match; all 4 surfaced leftovers (doctor.go, `internal/org/watcher.go` comments, `docs/research/approach-comparison.md`, `.codex/README.md` ×2) fixed in the same commit. |
| MEDIUM (`count_triage_findings`/`pick_reviewer` no executable consumer) | Yes | `.claude/skills/cross-review/SKILL.md` Step 2/6 (+ 3 mirrors) now source `xreview-helpers.sh` and call both functions for real, rather than deleting them or leaving the false "already consumed" claim. |
| MEDIUM (`model-routing.md` stale `RALPH_MODEL`/`RALPH_EFFORT`/`ralph.toml` mirror claims) | Yes | "Where the values live" section trimmed to the two surviving vars only. |
| MEDIUM (`status.go` silently drops `CorruptLines` on empty path) | Yes | `printStatusEmpty` now takes and surfaces `corruptLines`; `TestStatusCmd_EmptyRosterFromFullyCorruptManifestStillWarns` added. |
| MEDIUM (`--json` two divergent schemas) | Yes | Unified `statusJSON` struct used by both `printStatusEmpty` and `printStatusJSONAllOrgs`; `TestStatusCmd_JSONSchemaIsIdenticalEmptyVsPopulated` added. |
| MEDIUM (cross-review SKILL.md stale "exported by `ralph run`" claim) | Yes | Clause rewritten to "set it directly or source `scripts/ralph-config.sh`" in all 4 mirrors. |
| MEDIUM (2 tech-debt rows left open despite being invalidated) | Yes | Both rows struck through with RESOLVED annotations in `docs/tech-debt/README.md`. |
| MEDIUM (`docs/research/approach-comparison.md` + `.codex/README.md` describe removed features as live) | Yes | Research doc gets a historical-document banner; `.codex/README.md` (+ template mirror) permission-policy row rewritten to `[org.permissions]`. |
| LOW (Japanese/English mixed `ralph status` output) | Yes | `printStatusEmpty` messages now English-only; `TestStatusCmd_EmptyStateDirShowsFriendlyNoteAndDoctorHint` updated to match. |
| LOW (doctor.go comment numbering gap) | Yes | Renumbered 1–11 contiguous, no gaps. |
| LOW (`checkDeadman` doc points to wrong doc-comment location) | Yes | Reworded to cite `leadProbeSnapshot`/`historyLeadLineCount` (the actual producers). |
| LOW (`xreview-helpers.sh` `count_triage_findings` bare-name collision risk, `_file`/`_category`/`_summary`/`_n`) | **Not fixed** | `scripts/xreview-helpers.sh:81-93` still uses bare names, not the `_ctf_` prefix the sibling functions use. Low-risk, self-contained; not in the recommendation's "fix before merge" or "strongly recommended" set. |
| LOW (README directory-tree column misalignment for `config/`/`insights/`) | **Not fixed** | `README.md:299,301` still carry one extra space before `#` relative to `cli/`/`scaffold/`/`upgrade/`/`org/`. Cosmetic only. |
| LOW (`ralph-config.sh` header overstates that `/cross-review` reads the exported env for the cycle cap) | Yes | Header reworded: cycle cap only via sourcing, reviewer-model var also works via plain export. |
| LOW (guard test hardcodes this plan's exact filename, becomes dead after archival) | Yes | `EXCLUDE_REGEX` widened from the exact filename to `docs/plans/active/` (wildcard), so it survives archival. |

Net: 14 of 16 findings (1 HIGH + 8 MEDIUM + 7 LOW, counting all rows in the self-review's Findings table) are fixed, each with either a code change plus a new regression test or a targeted doc correction. The 2 unfixed items are both LOW, cosmetic, self-contained, and were not named in the self-review's "fix before merge" / "strongly recommended" set — recommend carrying them as a short follow-up rather than blocking.

## Static analysis

| Command | Result | Notes |
| --- | --- | --- |
| `RALPH_VERIFY_SCOPE=full ./scripts/run-static-verify.sh` | Pass | Ran the settings.json validity check, Codex hook/PR-provenance guards, `check-sync.sh` (IDENTICAL=143, DRIFTED=0, ROOT_ONLY=0, TEMPLATE_ONLY=10 expected `.gitkeep`/`ralph.toml`, KNOWN_DIFF=3 expected), `check-pipeline-sync.sh` (all 7 consumer surfaces reference every pipeline step), `check-skill-sync.sh` (13/13 lock-step), golang verifier (`gofmt: ok`, `go vet` silent/clean, `staticcheck` silent/clean, `golangci-lint`: `0 issues.`). Full evidence at `docs/evidence/verify-2026-08-02-204012.log`. |
| `go build ./...` | Pass | Exit 0, no output. |
| `go vet ./...` | Pass | Exit 0, silent (expected on success). |
| `sh tests/test-no-loop-references.sh` | Pass | `PASS: no live references to the retired Ralph Loop execution system outside historical documents`. |
| `sh tests/test-xreview-helpers.sh` | Pass | 26/26 assertions pass. |
| `go test ./internal/config/... ./internal/cli/... -run "TestDefaultsLockStep\|TestStatusCmd\|TestInsightsCmd_Historical"` | Pass | Targeted subset for AC-2/3/6b/7 claims (full suite is `/test`'s scope, not re-run here). |

## Documentation drift

| Doc / contract | In sync? | Notes |
| --- | --- | --- |
| `docs/specs/2026-08-01-org-runtime.md` FR-6 / FR-11 | Yes | Both FRs state the harness-skill-persistence decision explicitly; no stale "/plan or /work abolished" language remains (`grep -n 廃止` shows only the correct "does not abolish" phrasing). |
| `AGENTS.md` / `CLAUDE.md` / `README.md` / `docs/quality/definition-of-done.md` | Yes | Two-surface structure (development harness + org runtime) consistently described; repo map lists `internal/org`, drops `internal/{ui,state,action,watcher}`; no residual Ralph Loop mentions outside benign "operating loop" prose. |
| `.claude/rules/post-implementation-pipeline.md`, `subagent-policy.md`, `model-routing.md` (+ mirrors) | Yes | Loop-specific sections removed; `model-routing.md`'s "Where the values live" corrected by the fix commit to name only surviving vars. |
| `docs/tech-debt/README.md` | Yes | 8 rows struck through with dated RESOLVED annotations and cross-references; both self-review-flagged "invalidated but left open" rows now closed. |
| `docs/research/approach-comparison.md`, `.codex/README.md` (+ template mirror) | Yes (post-fix) | Research doc now carries a historical-document banner; `.codex/README.md` permission-policy row rewritten to the surviving `[org.permissions]` mechanism. |
| `tests/test-no-loop-references.sh` header vs. `PATTERN`/`EXCLUDE_REGEX` | Yes (post-fix) | Header now accurately describes the (widened) pattern's actual coverage. |
| `.claude/skills/cross-review/SKILL.md` (+ 3 mirrors) | Yes (post-fix) | No longer claims `RALPH_CLAUDE_REVIEWER_MODEL` is "exported by `ralph run`"; Step 2/6 now genuinely invoke the moved helpers. |
| `scripts/xreview-helpers.sh` internal naming (`count_triage_findings`) | Minor drift (LOW, unfixed) | Bare `_file`/`_category`/`_summary`/`_n` locals, inconsistent with the `_dbb_*`/`_pr_*` prefixing convention used by its two siblings in the same file. Collision risk is low (single-file sourcing pattern) but worth a follow-up. |
| `README.md` repo-tree column alignment | Minor drift (LOW, unfixed) | `config/` and `insights/` rows carry one extra space before their trailing `#` comment relative to sibling rows. Cosmetic only. |
| `docs/plans/active/2026-08-03-org-runtime-retire-loop.md` Acceptance criteria checkboxes | Minor drift (pre-existing pattern, non-blocking) | All 8 ACs (+ AC-6b) are checked `[x]` in the plan and match the verified implementation state; "Progress checklist" (review/verify/test artifact rows) is stale as expected since this report is being written now — will self-correct once this and the test report land. |

## Observational checks

- `docs/evidence/org-retire-loop-smoke-2026-08-03.txt`: `ralph init --yes` scaffold smoke into a fresh temp dir, confirming absence of loop skill/scripts/config and presence of `.claude/skills/org`, `.claude/rules/agent-messaging.md`, `ralph.toml [org]`. Built at commit 303380d (predates the self-review fix commit); see AC-8 caveat above.
- No new screenshots/traces needed — this is a CLI/backend refactor with no UI surface remaining after `internal/ui` removal.

## Coverage gaps

- Full `go test ./...` was not executed here (out of `/verify` scope per the pipeline contract — belongs to `/test`). Only the targeted subset covering AC-2/3/6b/7 claims was run to corroborate spec-compliance evidence.
- AC-8's scaffold smoke evidence predates the self-review fix commit (746b35b); the delta between the two is doc/prose-only in template mirrors, but a fresh scaffold-smoke re-run after 746b35b would close this gap definitively rather than by inference.
- Two LOW self-review findings remain unfixed (`xreview-helpers.sh` bare local-variable names in `count_triage_findings`; `README.md` repo-tree column alignment for `config/`/`insights/`). Both are cosmetic/self-contained; recommend a short follow-up rather than blocking `/pr`.
- The `ralph upgrade` remove-path behavior for downstream projects (plan risk row 3, "破壊的変更として PR body に明記") was not exercised here — it is a manual/PR-body documentation obligation, not a static-analysis check, and belongs to `/pr` or a downstream smoke test.

## Verdict

**PASS** — all 8 acceptance criteria (AC-1 through AC-8, including AC-6b) are met with concrete evidence; the self-review's HIGH finding and 8 of 8 MEDIUM findings are confirmed fixed in commit 746b35b, each backed by a new regression test or a targeted doc correction; 5 of 7 LOW findings are also fixed. Full static verification (`RALPH_VERIFY_SCOPE=full`) is clean: `go build`/`go vet` pass, `gofmt`/`golangci-lint`/`staticcheck` clean, and all three sync gates (`check-sync.sh`, `check-skill-sync.sh`, `check-pipeline-sync.sh`) are green. The new `tests/test-no-loop-references.sh` guard passes and is wired into CI via `scripts/verify.local.sh`'s enumeration-drift-proof glob.

- **Verified:** AC-1–AC-7 (code deletion completeness, `go build` green, `ralph status` org-manifest rewrite with unified JSON schema, config/env retirement with `defaults_sync_test` green, cross-review helper migration now genuinely wired into SKILL.md, zero-reference guard passing with CI wiring, doc/spec coherence with no stale abolition claims, tech-debt row closures, PR④ gap fixes with regression tests). Self-review fix-commit crosscheck (HIGH #1 + 8 MEDIUM + 5/7 LOW).
- **Partially verified:** AC-8 — build/vet/static analysis and scaffold-smoke evidence all support it, but the scaffold smoke predates the fix commit (doc-only delta, low risk) and full `go test ./...` is deferred to `/test`.
- **Not verified (explicitly out of `/verify` scope):** full behavioral test suite (`/test`'s responsibility); downstream `ralph upgrade` remove-path smoke for existing projects (manual/PR-body obligation); the 2 unfixed LOW self-review findings (tracked above as minor, non-blocking drift).
