# Self-review report: overlay-scaffold-v2 Phase 4 (legacy→v2 migration + owner-aware untracked classification)

- Date: 2026-08-18
- Plan: `docs/plans/active/2026-08-18-overlay-scaffold-v2-p4.md`
- Reviewer: `reviewer` subagent (Claude Code, `/self-review`)
- Scope: diff quality only — `git diff main...HEAD` on `feat/overlay-scaffold-v2-p4` (HEAD `c8b47bd`, base `main` `1025d13`), 16 files, +3,815/−61. No tests, static analysis, spec-compliance, or doc-drift checking was performed (those belong to `/verify` and `/test`).

## Evidence reviewed

- Full diff of every non-test file: `internal/cli/migrate.go` (new, 1,361 lines), `internal/cli/upgrade.go`, `internal/cli/upgrade_v2.go`, `internal/cli/init.go`, `internal/cli/pack.go`, `internal/upgrade/replaceplan.go`.
- Test files read selectively for behavior the production code does not state on its own: `internal/cli/migrate_test.go` (rerun-stability and collision-matrix cases), and the added test function list for `internal/upgrade/replaceplan_test.go` / `internal/cli/upgrade_v2_test.go`.
- Surrounding (unchanged) code the new code contracts against: `internal/upgrade/replaceplan.go` (`classifyCore`, `classifySeed`, `ApplyOps`, `ValidateRealParentChain`), `internal/cli/upgrade_v2.go` (`runUpgradeV2`, `rebuildManifestV2`, `buildDesiredStateV2`), `internal/upgrade/settingsmerge.go` (`MergeOwnedSettings`), `internal/scaffold/manifest.go` (`ManifestFile`, `SetFileOwned`, `SetFileFork`).
- Historical evidence for the legacy hook list: `git show 24a611a^:templates/base/.claude/settings.json` — the pre-dispatcher template's `hooks` block references exactly the 8 `./.claude/hooks/<name>.sh` commands hardcoded in `legacyRalphHookCommands`, across 7 events. That list is correct and complete.
- Docs touched by the diff: `README.md`, `AGENTS.md`, `docs/tech-debt/README.md`, `docs/specs/2026-08-17-overlay-scaffold-v2.md`, `docs/recipes/adding-a-language-pack.md` (+ its `templates/base/` mirror).

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | **An already-resolved relocation of a *modified* file silently loses its fork attribution, turning the user's content into permanent unresolved drift.** Two paths reach this. (1) Rerun after a partial failure: `ClassifyMigration`'s pre-check drops the entry entirely when the old path is gone, without looking at what the destination holds or at the legacy entry's state. (2) Collision-matrix case (a) with a modified source: `relocationOutcome` returns `OpDeleteOldPath`, and `classifyForkCandidate` then builds an entry with no `Owner` and no `ForkedFromVersion`. In both cases the destination is not in `forkByPath`, so `buildMigratedManifest`'s generic desired sweep records it as `owner=core` with `DiskHash = templateHash` — a hash the disk does not have. The chained `runUpgradeV2` then hits `classifyCore`'s `default` branch (disk ≠ template, disk ≠ recorded) and files the path as **Drift**: left untouched forever, exit 3 on every future `ralph upgrade`, absent from the migration report's "Fork diffs" section. Content is not destroyed, but the fork semantics the migration promises are. The trigger is the *documented* resume path (plan AC-14) and the *documented* collision case (a) (AC-15). | `internal/cli/migrate.go:290-299` (rerun pre-check `if relocated && !hasDisk { continue }`); `internal/cli/migrate.go:532-544` (`kind == OpDeleteOldPath` branch leaves `Owner`/`ForkedFromVersion` unset); `internal/cli/migrate.go:566-583` (`relocationOutcome` case (a) does not branch on `sourceUnmodified`); `internal/cli/migrate.go:1230-1246` (generic sweep records `owner=core`, `diskHash = templateHash`); `internal/upgrade/replaceplan.go:340-350` (`classifyCore` → `plan.Drift`). The behavior is pinned by `internal/cli/migrate_test.go:1886-1893`, whose comment states the intent explicitly: "Case (a) resolves as 'already relocated', not a live fork: the destination is adopted as an ordinary core-owned path going forward, not recorded with Owner=fork." | Split case (a) on `sourceUnmodified`. When the source is modified/unmanaged and the destination already holds that same content, the old path should still be deleted **and** the destination recorded as a fork (`Owner=scaffold.OwnerFork` + `ForkedFromVersion`) — a new `MigrationOpKind` (e.g. `OpDeleteOldPathAdoptFork`) or an `Owner`-carrying `OpDeleteOldPath` both work. Apply the same treatment in the rerun pre-check: when the old path is absent but the relocation destination exists, classify the destination from the legacy entry's recorded state instead of dropping the entry. Then update `migrate_test.go:1886-1893` — it currently locks in the defect. |
| MEDIUM | exception-handling | **The chained v2 upgrade's drift signal is swallowed, so `ralph upgrade` exits 0 on a migrated project that still has unresolved drift.** `runMigrateLegacy` treats *any* error from `runUpgradeV2` as a warning and returns `nil`. But `runUpgradeV2` returns `ErrUpgradeDriftRemaining` for the ordinary drift outcome, which `cmd/ralph/main.go` maps to the dedicated exit code 3 (spec FR-4). Conflating "the chained engine blew up" with "the chained engine finished and reported drift" means the machine-detectable drift signal disappears for exactly one run — the migration run, which is the run most likely to surface drift. This also masks the HIGH finding above from any CI gate. | `internal/cli/migrate.go:795-798`; `internal/cli/upgrade_v2.go:26` (`ErrUpgradeDriftRemaining`) and `:229`, `:308` (both return sites). | Branch on the sentinel: `if errors.Is(err, ErrUpgradeDriftRemaining) { return err }` before falling through to the warning path, so the drift exit code survives the migration. Keep the warning-only treatment for genuine execution errors, which is the design decision the plan actually recorded. |
| MEDIUM | security | **Delete operations skip the parent-chain symlink guard that every other write site applies.** `validateMigrationOp` routes `OpDeleteOldPath` to `validateMigrationLeaf`, which only `Lstat`s the leaf; `validateMigrationDirOp` (`.ralph/baseline/`, executed via `os.RemoveAll`) likewise only `Lstat`s the leaf. Neither calls `upgrade.ValidateRealParentChain`. A symlinked intermediate directory (e.g. `.claude/rules` → outside the project) therefore lets migration `os.Remove`/`os.RemoveAll` through the link and delete outside `absDir`. `validateMigrationWriteTarget` gets this right, and `ApplyOps` validates the chain for *every* op including deletes — so this is a divergence from the engine `migrate.go` deliberately mirrors, and from the plan's own AC-16 wording ("移設元/先・削除対象" all validated with `ValidateRealParentChain`). | `internal/cli/migrate.go:852-876` (`validateMigrationOp` dispatch), `:882-902` (`validateMigrationLeaf`, no chain check), `:922-942` (`validateMigrationDirOp`, no chain check), `:909-917` (`validateMigrationWriteTarget`, chain check present); contrast `internal/upgrade/replaceplan.go:516-520` (`ApplyOps` validates the chain for all ops). | Call `upgrade.ValidateRealParentChain(absDir, relPath)` in `validateMigrationLeaf` and `validateMigrationDirOp` too — simplest is to hoist it into `validateMigrationOp` for every kind that names a path, so no future op kind can be added without it. |
| MEDIUM | typo | **README claims the migration covers "manifest-less" projects; the code refuses them.** Both new README passages say `ralph upgrade` migrates "legacy (pre-v2 **or manifest-less**)" projects. `runUpgradeIOWithOptions` returns `no .ralph/manifest.toml found — run 'ralph init' first` before any layout dispatch, so a manifest-less directory never reaches `runMigrateLegacy`. A user following the README on an un-scaffolded project gets a contradicting error. | `README.md:131` and `README.md:145` vs `internal/cli/upgrade.go:143-145`. | Drop "or manifest-less" from both sentences (a manifest-less project is `ralph init`'s case, not `ralph upgrade`'s). |
| MEDIUM | maintainability | **`PrunedHookCommands` is computed by substring match but applied by exact match, so the migration report can claim a prune that never happened.** `prunedLegacyHookCommands` uses `bytes.Contains` over the raw settings JSON; `pruneLegacySettingsHooks` removes entries only when `innerEntry["command"]` equals the listed string exactly. A user whose settings carry `"./.claude/hooks/pre_bash_guard.sh --verbose"` gets that command listed under "Settings prune" in `docs/reports/ralph-migration-<date>.md` as removed, while the entry actually survives and double-fires alongside the dispatcher — the precise failure AC-13 exists to prevent, now reported as prevented. | `internal/cli/migrate.go:451-459` (`bytes.Contains`) vs `:1123-1126` (`cmdSet[cmd]` exact equality); report rendering at `:1315-1327`. | Make the two agree. Simplest correct direction: have `pruneLegacySettingsHooks` return the commands it actually removed and render *that* in the report, rather than pre-computing a list from a looser match. If the intent is also to prune argument-carrying variants, decide that explicitly and match on a prefix in both places. |
| LOW | maintainability | `OpSettingsPrune` re-marshals and rewrites a user-modified `.claude/settings.json` even when `PrunedHookCommands` is empty and nothing changed, losing the user's key order (Go map marshal sorts keys) and HTML-escaping `<`/`>`/`&` in strings. The justification comment says this is harmless because "the chained v2 upgrade's own 3-way settings merge ... always re-canonicalizes the result immediately afterward" — but `runUpgradeV2` only writes the merge result when `mergeResult.Changed` is true, so the guarantee the comment leans on does not hold unconditionally. In practice the merge does change something on a legacy migration, so impact is small; the comment is still overstated. | `internal/cli/migrate.go:977-991` (unconditional write), `:1062-1073` (the comment), `:1074-1088` (early `return content, nil` paths that do preserve bytes); `internal/cli/upgrade_v2.go:121-126` (conditional write). | Skip the read/parse/write entirely when `len(e.PrunedHookCommands) == 0`, and soften the comment to describe the conditional write accurately. |
| LOW | maintainability | `OpKeepInPlace` entries are carried into the v3 manifest by direct struct copy (`old.Owner = ke.Owner; nm.Files[path] = old`), which drags the legacy `BaselineStatus`/`BaselinePath` fields along. `BaselinePath` can point into `.ralph/baseline/…`, the directory this same migration deletes (`OpDeleteDir`). Every other v3 write path (`SetFileOwned`, `SetFileFork`) normalizes these to `BaselineStatusMissing` with no path. So the only v3 entries that can carry a dangling baseline pointer are the ones this function hand-rolls. | `internal/cli/migrate.go:1212-1225`; `internal/scaffold/manifest.go` (`SetFileOwned`/`SetFileFork` both set `BaselineStatus: BaselineStatusMissing`, never `BaselinePath`); `internal/cli/migrate.go:328-334` (baseline dir deletion). | Clear `old.BaselineStatus`/`old.BaselinePath` (or set `BaselineStatus = scaffold.BaselineStatusMissing`) when carrying the entry forward, so no v3 manifest references a directory migration just removed. |
| LOW | maintainability | `buildMigratedManifest` records `DiskHash = templateHash` for every desired path it does not otherwise handle, asserting a disk state that does not exist yet — the chained `runUpgradeV2` is what creates those files. This is safe only because the chained call rebuilds the manifest; since a chained failure is warning-only (by design), a failed chain leaves the false hash committed. Contrast `rebuildManifestV2`, which makes the same assertion *after* `ApplyOps` has already made it true. | `internal/cli/migrate.go:1230-1246` vs `internal/cli/upgrade_v2.go:650-662`. | At minimum, note the dependency in the doc comment (it currently explains the fork/keep exceptions but not why the optimistic `diskHash` is acceptable). Reading disk for core paths too would make the record true unconditionally at negligible cost. |
| LOW | readability | `classifyUnmodifiedGeneric`'s `OpKeepInPlace` reason string says "content converges via the chained v2 upgrade". That is true for `owner=core` paths and false for `owner=seed` ones (`docs/…`, `ralph.toml`, `.github/workflows/verify.yml`), which `classifySeed` never converges — it only ever emits an advisory. The string is surfaced verbatim in the operator-facing preview and migration report. | `internal/cli/migrate.go:467-472`; `internal/upgrade/replaceplan.go:396-420` (`classifySeed`); `internal/cli/init.go:289-304` (`ownerForScaffoldPath` seed set). | Word the reason per resolved owner, or make it neutral ("left in place; the chained v2 upgrade handles it per its ownership attribute"). |
| LOW | maintainability | `writeMigrationFile` hardcodes `os.MkdirAll(…, 0755)`, adding a sixth site to the hardcoded-permission item already tracked as tech debt (item (3) of the batched LOW row, which this diff edits for other reasons but leaves saying "now-5 hardcoded-permission sites"). | `internal/cli/migrate.go:1007-1017`; `docs/tech-debt/README.md` (batched LOW row, item (3)). | Update the existing row's count and file list rather than opening a new one — the row is edited in this same diff, so the drift is one line away. |
| LOW | readability | `buildDesiredStateV2` is called twice per migration run (once by `runMigrateLegacy`, once by the chained `runUpgradeV2`), so any pack warning it prints ("pack %q: … — preserving its disk/manifest entries untouched") appears twice on stderr for a single command. | `internal/cli/migrate.go:727` and `internal/cli/upgrade_v2.go:80`; warning emitted at `internal/cli/upgrade_v2.go:372-378`. | Acceptable as-is; if it bothers, pass the already-built `desired`/`preservePrefixes` into the chained call rather than recomputing. |
| LOW | exception-handling | `checkGitCleanForMigration` uses `cmd.Output()`, which discards git's stderr. On the `git status --porcelain` failure branch the operator gets `checking git status before migration: exit status 128` with no reason. | `internal/cli/migrate.go:819-823`. | Use `CombinedOutput()`, or capture `exec.ExitError.Stderr` into the wrapped error. |
| LOW | readability | The migration report is rendered and written *before* the chained v2 upgrade runs, but its block-face section speaks in the past tense — "the chained v2 upgrade's block engine **appended** the ralph-managed block". If the chained upgrade fails (warning-only), the committed report describes work that did not happen. | `internal/cli/migrate.go:778-798` (report written, then chain), `:1329-1336` (past-tense guidance text). | Phrase it as pending ("will append … on the upgrade that follows this migration"), or move the report write after the chained call. |

## Positive notes

- No secrets, credentials, debug prints, commented-out code, or `TODO`/`FIXME` markers anywhere in the new 1,361-line file (grepped).
- Error strings are unusually disciplined: every abort path before the commit barrier states "no files were changed", and the post-barrier failure message names `git status`/`git diff` as the rollback mechanism explicitly, matching the plan's "no backups" design decision.
- `legacyRalphHookCommands` is exactly right. Verified against `git show 24a611a^:templates/base/.claude/settings.json`: the pre-dispatcher template wired precisely those 8 commands across 7 events, and nothing else (no `*.d/` drop-ins, no `lib_json.sh`, no `ralph-dispatch.sh`).
- `relocatedRulePath`'s single desired-state membership check genuinely subsumes the "is this an installed pack rule" question, as the resolved Open question claims — `buildDesiredStateV2` maps each installed pack's rule to `.claude/rules/ralph/<lang>.md`, so no second lookup is needed.
- `relocateMigrationFile` writes the destination before removing the source, and says why in a comment ("so a failure between the write and the delete leaves both paths present rather than losing content"). The ordering is the safe one.
- `ReplaceOptions.OwnerForPath` is a well-scoped seam: nil preserves prior behavior, the doc comment explains the package-boundary reason for the callback, and `classifyUntracked`'s new branch sits after the equal→refresh check so it cannot mask the settled case.
- The `docs/tech-debt/README.md` edits are honest in both directions — the two struck-through rows (`.codex/AGENTS.override.md` re-attribution, untracked seed collision) are genuinely closed by code in this diff, the `in io.Reader` sub-item is genuinely now read by `confirmMigration`, and the path-shape-transition row was updated to record that Phase 4 explicitly did *not* address it rather than quietly left stale.
- `TestRunMigrateLegacy_PartialFailure_…` uses an `os.Geteuid() == 0` skip alongside its `chmod 0444` injection — the root-CI trap called out in this repo's prior review history is handled.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| _(none proposed)_ | — | — | — | — |

The HIGH and the four MEDIUM findings above are all fixable within this cycle and should not be converted into register rows. The LOW findings are individually small; if the fix cycle chooses not to take them, batch them into a single row at that point rather than opening rows now. LOW item 10 (`writeMigrationFile`'s hardcoded `0755`) is an update to an existing row, not a new one.

## Recommendation

- Merge: **No — fix HIGH-1 first.** The fork-attribution loss converts the migration's own documented resume path into a permanently drifted, exit-3 project state, and MEDIUM-1 (swallowed `ErrUpgradeDriftRemaining`) hides that outcome from the exit code, so neither one is self-announcing in the field. MEDIUM-2 (missing parent-chain guard on deletes) contradicts a stated acceptance criterion and the engine this file mirrors; MEDIUM-3 and MEDIUM-4 are both cases of an artifact asserting something the code does not do.
- Follow-ups:
  1. Fix HIGH-1 in `relocationOutcome` + the `ClassifyMigration` rerun pre-check, and correct the test at `migrate_test.go:1886-1893` that currently pins the defect.
  2. Fix MEDIUM-1 through MEDIUM-4 (drift sentinel pass-through, parent-chain guard on delete/dir ops, README wording, prune-report/prune-behavior agreement).
  3. Decide as a batch whether to take the 8 LOW items now or defer them into one register row.
  4. Re-run the full pipeline from `/self-review` after the fixes, per `.claude/rules/ralph/post-implementation-pipeline.md`.

---

# Cycle 2 (final, 2/2)

- Date: 2026-08-18
- Reviewer: `reviewer` subagent (Claude Code, `/self-review`)
- Scope: **delta only** — `git diff 8fc6260..c51497e` (HEAD `c51497e`, base `main` `1025d13`). Commits: `b1babe7` (cycle-1 self-review fixes), `1dfba22`/`b1515ea`/`851e906` (verify/test/sync-docs artifacts), `6ad6e16` (cross-review triage), `c51497e` (cross-review AR#1–AR#3 fixes). Cycle-1 findings re-classified against HEAD, not against the tree they were written for.
- Delta shape: `internal/cli/migrate.go` +378/−69, `internal/cli/migrate_test.go` +554, `README.md` (2 lines), `docs/tech-debt/README.md` (1 row), plan + 4 report artifacts.
- Commit DAG is linear and in date order (`git log --format='%h parent=%p'`), so no report in this delta was written against a tree later than its own parent.

Finding IDs are published here so later artifacts can cite them checkably (cycle-1 IDs are referenced as HIGH-1 / MEDIUM-n / LOW-n in the order of the cycle-1 table above; cycle-2 IDs are C2-n).

## Cycle-1 findings re-classified

| Cycle-1 finding | Status at HEAD | Evidence |
| --- | --- | --- |
| HIGH-1 fork attribution lost on already-resolved relocation | **Fixed, both items.** Item (2) (collision case (a), modified source): `relocationOutcome` now splits on `sourceUnmodified` and returns the new `OpDeleteOldPathAdoptFork` kind. Item (1) (rerun pre-check): `classifyRerunRelocatedDestination` classifies the destination from the legacy entry's recorded state instead of dropping the entry, returning `nil, nil` only when the destination is absent or already equals the template. The new kind is wired into all five consumers (preview group, `validateMigrationOp`, `executeMigrationEntries`, `buildMigratedManifest`'s `forkByPath`, `renderMigrationReport`'s fork diffs) — I grepped every `switch e.Kind` / `entriesForKind` site, none was missed. | `internal/cli/migrate.go:677-682` (`relocationOutcome`), `:407-424` (`classifyRerunRelocatedDestination`), `:628-635` (`classifyForkCandidate`), `:1443`, `:1004`, `:1141`, `:1582` |
| HIGH-1's defect-pinning test (`migrate_test.go:1886-1893`) | **Corrected, not merely renamed around.** The `if e.Owner == scaffold.OwnerFork { t.Errorf("Owner should not be fork …") }` inversion is gone; both the unit case and the e2e case now assert `Owner == OwnerFork` **and** `ForkedFromVersion`. A second test covers the rerun path end to end, including a second `runUpgradeIOWithOptions` call asserting the path does not drift on the following upgrade. | `internal/cli/migrate_test.go:618-648`, `:1892-1907`, `:2003-2059` (`TestRunMigrateLegacy_RerunAfterPartialRelocation_ModifiedSource_DestAdoptsFork`) |
| MEDIUM-1 chained drift sentinel swallowed | **Fixed, and the sentinel survives the whole call chain.** `runMigrateLegacy` returns `err` on `errors.Is(err, ErrUpgradeDriftRemaining)` before the warning path. The chained call is the last statement in the function, so the early return skips no bookkeeping. `runUpgradeIOWithOptions` returns `runMigrateLegacy`'s error unwrapped, so `cmd/ralph/main.go:28`'s `errors.Is` still matches and exit 3 is reached. | `internal/cli/migrate.go:909-912`, `internal/cli/upgrade.go:154`, `cmd/ralph/main.go:28`, test at `internal/cli/migrate_test.go:2061-2104` |
| MEDIUM-2 delete ops skip the parent-chain guard | **Fixed at the dispatch point, which is stronger than the recommendation.** `upgrade.ValidateRealParentChain(absDir, e.OldPath)` is hoisted ahead of the `switch`, so the delete kinds, `OpDeleteDir`, and `OpForkRelocate`'s *source* are all covered and no future kind can be added without it. | `internal/cli/migrate.go:1000-1002` |
| MEDIUM-3 README "manifest-less" claim | **Fixed at both sites, and no third site exists.** `grep -rn "manifest-less"` over the tree returns only `docs/plans/archive/2026-07-13-audit-findings.md:76` (an unrelated `ralph doctor` note). | `README.md:131`, `:145` |
| MEDIUM-4 prune reported by substring, applied by exact match | **Fixed at the producer, not the row.** `pruneLegacySettingsHooks` now returns `(pruned, removed, nearMisses, err)`; `executeMigrationEntries` iterates by index and writes both back into the caller's `plan.Entries`; the report is rendered after execution, so it reads the actual removals. Near-misses get their own "may still double-fire — review manually" section. The empty-case string was changed from "were found" to "were removed", which is the claim the code can actually back. | `internal/cli/migrate.go:1286-1376` (`pruneLegacySettingsHooks`, `legacyHookNearMiss`), `:1185-1186` (write-back into `entries[i]`), `:1616-1628` (report rendering); tests `internal/cli/migrate_test.go:2192` (unit), `:2230` (end to end) |
| LOW-1 `OpSettingsPrune` rewrites an unchanged settings.json | **Partially fixed** — see C2-2 below. |
| LOW-2 `OpKeepInPlace` carries a dangling `BaselinePath` | Fixed: `old.BaselineStatus = scaffold.BaselineStatusMissing; old.BaselinePath = ""` immediately before the `nm.Files[path] = old` carry-forward. | `internal/cli/migrate.go:1485-1487` |
| LOW-3 optimistic `DiskHash = templateHash` | Addressed as recommended (documented, not changed): `buildMigratedManifest`'s doc now states the dependency on the chained call and what a failed chain leaves behind. | `internal/cli/migrate.go:1420-1432` |
| LOW-4 `OpKeepInPlace` reason string wrong for seed paths | Fixed: reworded to "left in place, the chained v2 upgrade converges it per its ownership attribute". | `internal/cli/migrate.go:556` |
| LOW-5 `writeMigrationFile`'s hardcoded `0755` | Deferred **and tracked**: the existing batched register row's item (3) now reads "now-6 hardcoded-permission sites", names `internal/cli/migrate.go` (`writeMigrationFile`) in its file list, and adds this report to its references. No new row opened, as recommended. | `docs/tech-debt/README.md` (batched LOW row) |
| LOW-6 duplicate pack warning (`buildDesiredStateV2` called twice) | **Not fixed, not documented, not tracked** — see C2-3 below. |
| LOW-7 `checkGitCleanForMigration` discards git stderr | Fixed: `statusCmd.Stderr` is captured into a `bytes.Buffer` and appended to the wrapped error when non-empty. | `internal/cli/migrate.go:937-944` |
| LOW-8 block-face report guidance in the past tense | Fixed: "will append the ralph-managed block onto them later in this same run". | `internal/cli/migrate.go:1635` |

Net: HIGH-1 and all four MEDIUMs are genuinely closed, each with a regression test that would fail against the pre-fix code (I checked the pre-fix control flow for each rather than trusting the test name). Six of eight LOWs are closed, one is properly deferred into the existing register row, one is unaccounted for (C2-3).

## Cycle-2 findings (delta)

No CRITICAL and no HIGH. All four below are cheap, and none blocks merge.

| ID | Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- | --- |
| C2-1 | MEDIUM | maintainability | **Eight code comments cite bare `AR#1`/`AR#2`/`AR#3` into a triage report that `/cross-review` rewrites in place every cycle.** `docs/reports/cross-review-triage-overlay-scaffold-v2-p4.md` is a single per-slug file whose header reads `Cycle: 1/2` and whose ACTION_REQUIRED table is renumbered from scratch on each pass. The cycle-2 cross-review that runs immediately after this review will overwrite it, at which point every one of these eight pointers resolves to whatever cycle 2's finding #1/#2/#3 turns out to be — or to nothing. The immediately preceding phase already hit this and settled on a cycle-qualified convention in the same package: `internal/cli/upgrade_v2.go` writes "AR#2 (cycle-2 cross-review, docs/reports/cross-review-triage-overlay-scaffold-v2-p3.md)" and "(AR#2, cycle 2)". Phase 4's new comments dropped the qualifier. | `internal/cli/migrate.go:997`, `:1083`, `:1528`, `:1580`; `internal/cli/migrate_test.go:2279`, `:2303`, `:2362`, `:2451` — all bare. Contrast `internal/cli/upgrade_v2.go:148`, `:213`, `:247`, `:466`, `:790`, `:834`, all cycle-qualified. | Insert the cycle into all eight citations (`AR#1, cycle 1, docs/reports/…`) — eight one-token edits, matching the convention the sibling file already uses. Do it before `/cross-review` runs this cycle; afterwards the pointers are already wrong and someone has to reconstruct which cycle each meant. |
| C2-2 | LOW | maintainability | **The LOW-1 fix skips the settings.json rewrite only when there are zero *candidates*, not when there are zero *removals*.** `executeMigrationEntries` guards on `len(e.PrunedHookCommands) == 0`, which is the substring-match candidate list; when a candidate is found but no entry matches exactly (precisely the near-miss case the MEDIUM-4 fix introduced), `pruneLegacySettingsHooks` still re-marshals the whole document and `writeMigrationFile` writes it — losing the user's key order and HTML-escaping `<`/`>`/`&` for a prune that removed nothing. The delta's own end-to-end near-miss test is exactly this shape. | `internal/cli/migrate.go:1162-1170` (candidate guard), `:1182-1184` (unconditional `writeMigrationFile` after the call), `:1286-1376` (the re-marshal runs whenever a `hooks` object exists, matched or not); fixture at `internal/cli/migrate_test.go:2238-2245`. | One line: move the skip to after the call — `if len(removed) == 0 { … }` — or write only when `removed` is non-empty. Nothing was pruned, so leaving the file byte-identical is strictly the safer outcome, and the chained 3-way merge still handles the rest. |
| C2-3 | LOW | maintainability | **Cycle-1 LOW-6 (`buildDesiredStateV2` runs twice per migration, so every pack warning prints twice on stderr) is unfixed, undocumented, and untracked — while the cycle-1 verify report states it was documented.** That report says LOW-7 and LOW-10 "both now carry an explanatory doc comment citing the self-review finding"; LOW-7's comment does exist (`buildMigratedManifest`), but there is no comment of any kind at the duplicate `buildDesiredStateV2` call. On the final cycle a "recommended but not taken" LOW is a deferral, and the plan is archived at `/pr`, so nothing carries it forward. | `internal/cli/migrate.go:834` (no comment) and `internal/cli/upgrade_v2.go:80`; warning emitted at `internal/cli/upgrade_v2.go:372-378`; the claim at `docs/reports/verify-2026-08-18-overlay-scaffold-v2-p4.md` line 24. | Either add the one-line comment the verify report already claims exists, or open one batched tech-debt row for the residue this PR is knowingly leaving (this item plus whichever of C2-1/C2-2/C2-4 are not taken). Also flag the stale sentence to `/verify` so the cycle-2 verify report does not repeat it. |
| C2-4 | LOW | readability | **Three doc comments were left behind by the new op kind, all in code the fix commits touched.** (a) `MigrationEntry.ForkedFromVersion` still says it is set "(OpForkRelocate, OpForkInPlace)" — `OpDeleteOldPathAdoptFork` is now a third producer. (b) `MigrationEntry.NewPath` enumerates which kinds set it and omits the adopt-fork destination, even though for that kind `NewPath` is load-bearing twice (the preflight adoption target and the `forkByPath` key). (c) `classifyLegacyEntryState`'s doc asserts that `ClassifyMigration` handles the already-relocated case "before this function is ever consulted for that path" — the new `classifyRerunRelocatedDestination` consults it for exactly that path, deliberately relying on the "disk file absent → LegacyModified" branch the sentence says is unreachable there. | `internal/cli/migrate.go:219-222`, `:205-211`, `:122-126` vs `:407-424`. Lower-priority sibling: `validateMigrationOp`'s new doc enumerates the kinds the hoisted chain check covers and omits that it now also runs for the three no-write kinds (`OpKeepInPlace`, `OpForkInPlace`, `OpUntouched`) — harmless (fail-closed) but the enumeration reads as exhaustive. | One line each: add `OpDeleteOldPathAdoptFork` to (a) and (b); reword (c) to say the pre-check calls this function with `hasDisk=false` on purpose rather than that it bypasses it. |

Not raised as findings, recorded so the judgment is visible:

- `TestRunMigrateLegacy_CollisionMatrixA_HalfMigrated_RerunDeletesOldOnly` (`internal/cli/migrate_test.go:1848`) keeps a name asserting "deletes old only" while its body now asserts the destination is adopted as a fork. A rename would be correct, but the name is cited by the cycle-1 verify report and would churn a pointer for a cosmetic gain on the final cycle.
- `relocationOutcome`'s collision return is `return 0, "…"`, and `0` is `OpDeleteOldPath` — a caller that ignored `collisionReason` would delete. The single caller checks the reason, and this line is unchanged by the delta (it was reviewable in cycle 1 and not raised), so it stays as-is; worth a named sentinel value the next time that function is touched.
- The verify and test reports in this delta describe HEAD `b1babe7` and a Go test inventory that `c51497e` has since grown by three tests. Both are re-run as part of this cycle, so the staleness resolves rather than merging.
- The cycle-1 triage report's `internal/cli/migrate.go:995-996` / `:1479` / `:1521-1522` pointers no longer resolve after `c51497e` shifted those lines. `/cross-review` rewrites that file this cycle; C2-1 is the durable half of the problem.

## Positive notes (cycle 2)

- The HIGH-1 fix went to the type system rather than to the two symptom sites: a distinct `OpDeleteOldPathAdoptFork` kind, so "delete only" and "delete and adopt the fork" can never be confused by a future reader, and the exhaustive-looking `switch` sites all had to be revisited. Its doc comment states explicitly that `OpDeleteOldPath` never carries `Owner`/`ForkedFromVersion`.
- The MEDIUM-2 fix is placed at the dispatch point rather than at the two functions the finding named, which closes the class instead of the instances.
- `validateMigrationForkAdoptionTarget` correctly uses `mustExist=true` — the opposite of the write-target helper it is modelled on — and its doc says why (the content is read and trusted, never written). Its regression test writes the fixture *through* the symlink and then asserts the external directory still has exactly one entry, the legacy manifest is byte-unchanged, `OldPath` survives with its original content, and the symlink itself is untouched. That is an absence-of-side-effects assertion, not a return-value check.
- The AR#2 seed-advisory fix omits the path from the v3 manifest instead of inventing a hash for it, which is the only shape that lets the chained engine's `classifyUntracked` see the untracked seed collision at all; its test asserts the advisory reaches the chained upgrade's own report and that the following run is a genuine no-op.
- No secrets, credentials, debug prints, commented-out code, or `TODO`/`FIXME` markers anywhere in the delta (grepped `+` lines only; the sole hit is a row label in the test report's script table).
- The new tests follow the file's existing global-fixture convention (`scaffold.EmbeddedFS` / `Version`) exactly rather than inventing a second one, and `isolateGitConfig(t)` is used consistently.
- `docs/tech-debt/README.md` was edited in the honest direction: the LOW-5 deferral updates an existing row's count, file list, and references rather than opening a competing row.

## Tech debt identified (cycle 2)

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| Whichever of C2-1 through C2-4 the fix pass does not take, plus cycle-1 LOW-6 (duplicate pack warning on stderr, unfixed and currently untracked anywhere). | Comment/report pointers that stop resolving (C2-1), cosmetic settings.json churn on a no-op prune (C2-2), doc comments that misdescribe the new op kind (C2-4), a warning printed twice per migration (LOW-6). None is a correctness defect. | Final pipeline cycle (2/2) under `RALPH_STANDARD_MAX_PIPELINE_CYCLES`; each item is individually small. | Next touch to `internal/cli/migrate.go`'s migration classification or settings prune, or the next `/cross-review` pass that renumbers a triage report. | This report (Cycle 2, C2-1..C2-4), `docs/reports/cross-review-triage-overlay-scaffold-v2-p4.md` |

This row should only be opened for the items actually left unfixed. If the fix pass takes all four (they total roughly a dozen single-line edits), no row is needed.

## Recommendation (cycle 2)

- Merge: **Yes.** The cycle-1 HIGH and all four MEDIUMs are closed with regression tests that fail against the pre-fix code, and the cross-review AR#1–AR#3 fixes are each narrower and better-guarded than the finding asked for. Nothing in the delta is a correctness or security defect.
- Suggested order for the remaining time: C2-1 first (its cost rises the moment `/cross-review` rewrites the triage report this cycle), then C2-2 and C2-4 (one line each), then either fix C2-3 or open the batched register row above.
