# Verify report: org-implementer-seat-envelope

- Date: 2026-09-16
- Plan: `docs/plans/active/2026-09-16-org-implementer-seat-envelope.md`
- Verifier: `verifier` subagent (Claude Code, sonnet)
- Scope: `git diff main...HEAD` (merge-base `8c9f6f1`, HEAD `649901a`, 10 commits including the `649901a` fix-self-review-findings commit). Spec compliance (AC-1..AC-9 incl. AC-7b) + static analysis (changed-language scope: Go + shell) + documentation drift. No behavioral tests run (`go test`, `run-test.sh` are `/test`'s scope).
- Evidence: `docs/evidence/verify-2026-09-16-110907.log` (`run-static-verify.sh` raw output)

## Spec compliance

| Acceptance criterion | Status | Evidence |
| --- | --- | --- |
| AC-1: `ralph org spawn --role implementer` renders the embedded template and starts | Met | `internal/org/prompts_test.go:44-60` (`TestRenderRolePrompt_Implementer_AllKnownVarsSubstituted`, asserts `ok=true`, all known vars present, no leftover `{{...}}`). Live check: `ralph org spawn --org-id verify-demo --id implementer-1 --role implementer --driver claude --model fable --dry-run` → `spawned seat "implementer-1" (... dry_run=true)`, exit 0. `internal/org/spawn.go` untouched by this diff (`git diff main...HEAD -- internal/org/spawn.go` empty) and calls `RenderRolePrompt(p.Role, ...)` generically (`spawn.go:614`), so the new template activates without a code change, matching the plan's Affected-areas note. |
| AC-2: `implementer.md`/`reviewer.md`/`qa.md` carry a "座席内 fan-out" section (children never message lead, star topology unchanged); `lead.md` delegates to implementer/reviewer/qa | Met | `## 座席内 fan-out` present in all three (`implementer.md:69-80`, `reviewer.md:54-65`, `qa.md:52-63`); normative bullets are byte-identical across the three (verified by direct read — only the trailing `例:` illustrations differ). `internal/org/prompts_test.go:140-163` (`TestRolePrompts_SeatTemplatesContainFanOutSection`) asserts the section header, `max_seats`, and the lead-prohibition phrase for all three roles. `lead.md:10-11`: "実装は原則として implementer 座席へ委譲し、レビューは reviewer 座席、検証は qa 座席へ委譲する" — confirmed by `TestRenderRolePrompt_Lead_DelegatesToImplementer` (`prompts_test.go:183-198`). |
| AC-3: `config.Default().Org.ModelPool` = D3's 9-entry order, lockstep across `ralph.toml` / `ralph-config.sh`, `DefaultModelForDriver(claude)`="fable", `(codex)`="gpt-6-astra" | Met | `internal/config/config.go:141-151`: `fable, opus, sonnet, haiku` (claude) then `gpt-6-astra, gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna, gpt-5.5` (codex), exact D3 order. `templates/base/ralph.toml:24-34` and `scripts/ralph-config.sh:44` match byte-for-byte (`RALPH_ORG_MODEL_POOL` value manually diffed against the Go slice). `scripts/ralph-config.sh` and `templates/base/scripts/ralph-config.sh` are `diff`-identical. `internal/config/defaults_sync_test.go:147-148` cross-checks all three faces via `formatOrgModelPool`. `internal/org/envelope_summary_test.go:81-96` (`TestDefaultModelForDriver_DefaultPoolHeads`) pins `fable`/`gpt-6-astra` against `config.Default()`. |
| AC-4: `ralph doctor` codex-slug check with pass/warn/info fixture coverage; `--probe-models` unchanged | Met | `internal/cli/doctor_codex_models.go` new file, 5 test cases in `doctor_org_test.go`: `TestCheckCodexModelSlugs_AllPresent_Pass` (:399), `_SomeMissing_WarnNamesEach` (:422), `_CacheMissing_InfoNamesPath` (:446), `_NoCodexEntries_Info` (:467), `_UnparsableCache_InfoNotWarn` (:485) — covers the plan's 3 named paths plus 2 extra edge cases. No process spawn (`os.ReadFile` only, confirmed by reading `doctor_codex_models.go`). `--probe-models`/`checkOrgModelProbes` (`doctor.go:763-`) is a separate, untouched code path (diff shows no changes to that function). Live check: `ralph doctor` → `✓ Org codex model slugs: pass — 5 codex model_pool slug(s) present in .../models_cache.json`. |
| AC-5: `spawn`/`start` `--model` omission falls back with a warning instead of erroring; `/org` skill states the explicit-`--model` rule | Met | `internal/cli/org.go:234-238`: `spawn`'s flag list (`id`, `role`, `driver`, `cwd`) no longer includes `model` in the required set (confirmed no `--model` required-loop entry remains). `resolveModelOrWarn` (`org.go:145-167`, post-fix) is shared by `spawn` (`org.go:220`, threads `role`) and `start` (`org.go:312`, threads `org.LeadIdentity`). Tests: `TestOrgSpawn_ModelFlagOmitted_DefaultsToFirstMatchingPoolEntry` (:642), `TestOrgSpawn_ModelFlagOmitted_RespectsRoleAllowlist` (:717, post-fix), `TestOrgSpawn_ModelFlagOmitted_NoPermittedModel_NonZeroExit` (:762, post-fix), `TestOrgStart_ModelFlagOmitted_DefaultsToFirstMatchingPoolEntry` (:1518). `.claude/skills/org/SKILL.md:38-40`: "**`--model` は `spawn` / `start` で必ず明示する**". Live check confirms the warning text and exit 0 on omission. |
| AC-6: `[org.budget]`/`OrgBudgetConfig`/env vars removed from all 3 faces; residual `budget` hits are only the unrelated byte-cap concepts; a legacy `[org.budget]` `ralph.toml` loads without error | Met | `grep -rn -i budget internal/ scripts/ templates/base/ralph.toml templates/base/scripts .claude/skills/org docs/quality` → only `identifier.go:37`'s 32-char budget comment, `watch.go`'s `scopeChangeBodyBudget`/`truncateScopeOutput`, `doctor_org_test.go`'s unrelated `ContextBudget` probe-timeout test, and deliberate test/comment references documenting the *removal* of the budget concept (`config_test.go:354-374` `TestLoad_IgnoresRetiredOrgBudgetTable`, `prompts_test.go:179-180`, `watch_test.go`'s `TestWatch_PrunesRetiredBudgetEntriesFromStatus_NoEscalation` and its fixture literals). `grep -rn 'SEAT_BUDGET\|TOTAL_BUDGET\|MAX_FIX_ROUNDS\|OrgBudgetConfig\|\[org\.budget\]'` across `.go`/`.sh`/`.toml` (excluding `_test.go`) → zero hits. `TestLoad_IgnoresRetiredOrgBudgetTable` exercises the "old ralph.toml with `[org.budget]` loads without error" requirement directly. |
| AC-7: `ralph org watch` still detects stall/liveness/scope-change/deadman without budget; zero `Cutoff`/`OrgStartTS` references | Met | `grep -n "Cutoff\|OrgStartTS" internal/org/watch.go internal/org/watch_test.go` → zero hits. `evaluateTotalBudget`/`evaluateSeatBudget`/`condSeatBudget`/`condTotalBudget` absent from `watch.go` (confirmed via the AC-6 grep sweep above). Non-budget watch tests were not re-run here (behavioral — `/test`'s scope); static removal is complete. |
| AC-7b: legacy `watch-status` fixture with budget `Conditions`/`PendingAlerts`/`Escalated` entries is pruned on one cycle, no deadman escalation fires | Met (code + test present; not executed here) | `internal/org/watch.go:548` `retiredConditionNames = map[string]bool{"seat_budget": true, "total_budget": true}`, `pruneRetiredConditions`/`retiredConditionTypeFromAlertID` implement the prune, placed before `checkDeadman` per the self-review's confirmed ordering. `internal/org/watch_test.go:1689` `TestWatch_PrunesRetiredBudgetEntriesFromStatus_NoEscalation` hand-writes a legacy fixture (including the retired `"cutoff"` JSON field, per self-review's positive note) and asserts pruning + no escalation. Execution is `/test`'s responsibility. |
| AC-8: spec/quality-gates/tech-debt/`/org` skill/model-routing/README in sync; `check-sync.sh`/`check-skill-sync.sh`/`check-pipeline-sync.sh`/`test-no-loop-references.sh` green | Met | All four scripts pass (see Static analysis below). `docs/quality/quality-gates.md` and `templates/base/docs/quality/quality-gates.md`: `grep -i budget` → zero hits (both faces). `docs/tech-debt/README.md:95-96`: the `Cutoff` ratchet row is struck through with a `RESOLVED 2026-09-16 in feat/org-implementer-seat-envelope: obsolete` note. `.claude/rules/ralph/model-routing.md:62` and its template mirror both list `fable` in the stable-alias set. `README.md:229-232` org-runtime paragraph now names `implementer`, `reviewer`, `qa` seats and has no `budget` mention. The 4 `/org` SKILL.md mirrors (`​.claude/skills/org`, `.agents/skills/org`, both `templates/base/` copies) are byte-identical (`diff` x3, all empty). The org-runtime spec's 2026-09-16 revision note (`docs/specs/2026-08-01-org-runtime.md:9`) now enumerates the Desired-state budget bullet and FR-10's "budget 作法" alongside FR-2/FR-7/FR-8/NFR, resolving self-review MEDIUM-4's core complaint (missing enumeration items). See Documentation drift below for one residual inconsistency in this same area. |
| AC-9: `go build ./...` / `go vet ./...` green (static portion); `go test ./...` / `./scripts/run-verify.sh` deferred to `/test` | Partially verified here | `go build ./...` → exit 0. `go vet ./...` → exit 0 (also covered inside `run-static-verify.sh`'s golang verifier: `gofmt: ok`, `0 issues.`). `go test ./...` and the full `./scripts/run-verify.sh` (which chains static + test) were **not** run in this pass — `/test`'s scope per the pipeline split (`.claude/rules/ralph/post-implementation-pipeline.md`). |

## Static analysis

| Command | Result | Notes |
| --- | --- | --- |
| `RALPH_VERIFY_SCOPE=changed ./scripts/run-static-verify.sh` | Pass (exit 0) | shellcheck + `sh -n` over all hook/dispatch scripts (root + template mirrors) OK; `jq -e .` on both `settings.json` faces OK; Codex hook single-source guard / inline-hook detector / PR-provenance guard OK; `check-sync.sh` PASS (157 identical, 0 drifted, 11 template-only `.gitkeep`/scaffold-only, 5 known diffs — all pre-existing); `check-pipeline-sync.sh` OK (6/6 docs reference all pipeline steps); `check-skill-sync.sh` OK (13 skills in lock-step); `check-template-purity.sh` PASS; golang verifier: `gofmt: ok`, `0 issues.` (golangci-lint). |
| `go build ./...` | Pass (exit 0) | Run directly (also implied by static verifier's golang pack). |
| `go vet ./...` | Pass (exit 0) | Run directly. |
| `sh tests/test-no-loop-references.sh` | Pass (exit 0) | "PASS: no live references to the retired Ralph Loop execution system outside historical documents." |
| `ralph doctor` (live) | Pass, codex slug check green | `✓ Org codex model slugs: pass — 5 codex model_pool slug(s) present in ~/.codex/models_cache.json`. (Overall `ralph doctor` reports some unrelated failures in this dev environment — e.g. agmsg/herdr tooling not installed locally — irrelevant to this plan's scope.) |
| `ralph org spawn --role implementer --dry-run` (live, with and without `--model`) | Pass | With `--model fable`: `spawned seat "implementer-1" (... dry_run=true)`, exit 0. Without `--model`: warns `falling back to first [org].model_pool entry permitted for role implementer on claude: fable`, then spawns, exit 0. |

## Documentation drift

| Doc / contract | In sync? | Notes |
| --- | --- | --- |
| `.claude/skills/org/SKILL.md` (+ 3 mirrors) | Yes | Byte-identical across all 4 copies; `--model` explicit rule, model_pool table, implementer role, and `permission 作法` (no `budget 作法`) all present. |
| `templates/base/ralph.toml` `[org]` comments | Yes | `model_pool` comment explains claude-alias vs codex-slug; `[org.roles]` example comment includes `implementer = ["sonnet"]`. |
| `docs/quality/quality-gates.md` (root + template) | Yes | No `budget` residue in either face. |
| `docs/tech-debt/README.md` | Yes | Cutoff-ratchet row properly closed with a dated `RESOLVED` annotation; the MEDIUM-3 self-review finding (`StopParams.Reason` dead plumbing) is recorded as a new, open tech-debt row (`docs/tech-debt/README.md:129`). |
| `.claude/rules/ralph/model-routing.md` (root + template) | Yes | `fable` added to the stable-alias bullet in both faces; also gained a new sentence on codex having no aliases. |
| `README.md` org runtime section | Yes | Names implementer/reviewer/qa seats, no budget mention. |
| `docs/specs/2026-08-01-org-runtime.md` 2026-09-16 revision note | Minor residual inconsistency | The fix commit (`649901a`) added the two enumeration items self-review MEDIUM-4 flagged as missing (Desired-state budget bullet, FR-10's "budget 作法") and added inline `**(2026-09-16 改訂で撤去)**` markers to both. However, self-review's second ask — "apply the inline marker uniformly to every statement the note names (or drop the inline markers entirely)" — is still only partially done: FR-2 (`:37`), FR-7 (`:42`), FR-8 (`:43`), and the NFR (`:51`) are all named in the note's enumeration but remain unmarked inline, while the Desired-state bullet (`:29`), FR-10 (`:45`), one AC (`:60`), and one Alternatives cell (`:137`) now carry the marker. This is a cosmetic, non-blocking inconsistency in a historical-document annotation convention — new, not present before the fix commit widened marker usage — and does not affect the substance of any FR/AC/NFR text. |
| `internal/org/envelope_summary.go`'s `DefaultModelForDriver` doc comment (new drift, introduced by fix commit `649901a`) | Drifted (new, LOW) | The doc comment (`envelope_summary.go:39-49`) still describes `DefaultModelForDriver` as "`ralph org start`'s --model default ... newOrgStartCmd" — true before the fix commit, but the fix commit rewired `newOrgStartCmd` to call `resolveModelOrWarn`, which now calls `DefaultModelForDriverAndRole` exclusively (`org.go:312`). `grep -rn "DefaultModelForDriver("` shows `DefaultModelForDriver` now has **zero** production callers — only `envelope_summary_test.go` and its own doc calls it. A second stale reference to the same claim exists at `internal/cli/org.go:97-98`'s `newOrgRuntime` doc comment ("newOrgStartCmd's --model default resolution via `org.DefaultModelForDriver`"). Not self-review MEDIUM-3/tech-debt (different finding) and not flagged in the self-review report, since the report predates commit `649901a`; genuinely new doc drift from the fix commit itself. Functionally harmless (an anchoring-test pattern per project convention — `DefaultModelForDriver` stays as a documented, independently-tested building block that `DefaultModelForDriverAndRole` conceptually generalizes, even though it no longer reuses its loop body), but the two comments should be corrected to name `DefaultModelForDriverAndRole` and the two current callers (`spawn`, `start`) the next time either file is touched. |
| Self-review LOW findings (batch, e.g. `RolePromptVars.PlanPath` doc, `config.go` "later slice" wording, `lead.md` 文体 mix, redundant `Escalated` delete, stale envelope-summary fixture literal) | Not fixed, as expected | Self-review's own recommendation was to batch these as an optional follow-up ("all are single-line edits ... this PR already touches"), not a merge blocker. The fix commit addressed only the three MEDIUMs plus MEDIUM-3's tech-debt recording, consistent with the self-review's "Merge: Yes, with fixes" framing. Not scored as drift; carried forward as known gaps below. |

## Observational checks

- `ralph org spawn --role implementer --driver claude --model fable --dry-run` and the `--model`-omitted variant both produced the expected manifest `spawned` event and stderr warning (see Static analysis table).
- `ralph doctor`'s "Org codex model slugs" check ran live against this machine's real `~/.codex/models_cache.json` and returned `pass` with a count of 5, matching the plan's Assumptions section (5 `visibility: list` slugs on codex-cli 0.149.1 as of 2026-09-16).

## Coverage gaps

- `go test ./...` was not run (this is `/test`'s job). AC-7's "watch_test.go all non-budget tests pass" and AC-7b's fixture-based prune/no-escalation assertion are therefore **code-verified, not execution-verified** here.
- `./scripts/run-verify.sh` (the combined static+test gate `run-static-verify.sh` alone does not cover) was not run end-to-end; only its static half ran.
- The self-review's own "Known gaps" (behavior not executed, spec compliance not assessed, doc drift beyond the diff not swept) are addressed by this report for the spec-compliance and in-scope doc-drift portions; broader doc-drift sweep beyond the plan's named files remains `/sync-docs`'s scope.
- Live multi-seat / real (non-dry-run) `ralph org spawn` with an actual herdr/agmsg driver was not exercised (this dev environment lacks agmsg/herdr tooling, consistent with `ralph doctor`'s unrelated failures noted above); `--dry-run` evidence is the practical ceiling for `/verify`.

## Verdict

**PASS.** All 9 acceptance criteria (AC-1 through AC-9, including AC-7b) are met by code, test, and/or live `--dry-run`/`ralph doctor` evidence. Static analysis (`run-static-verify.sh`, `check-sync.sh`, `check-skill-sync.sh`, `check-pipeline-sync.sh`, `check-template-purity.sh`, `test-no-loop-references.sh`, `go build`, `go vet`) is entirely green. The `649901a` fix commit correctly closes self-review MEDIUM-1 (commit-guidance citation, and the new wording's claim about `pre_bash_guard.sh` rejecting the `-m "$(` form was independently confirmed against the hook source), MEDIUM-2 (role-aware `--model` fallback, with a well-targeted new function + 5 new tests), and the enumeration half of MEDIUM-4 (spec revision note). MEDIUM-3 is recorded as an open, well-justified tech-debt row rather than fixed, matching the self-review's own recommendation.

- Verified: AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7 (static), AC-8, AC-9 (build/vet portion); all listed static analysis and doc-drift checks.
- Partially verified: AC-7b (implementation + test present, not executed); AC-9 (`go test ./...` / `run-verify.sh` deferred to `/test`).
- Not verified: behavioral test execution (all of it — explicitly out of `/verify`'s scope), live non-dry-run org spawn with a real driver.
- New findings for follow-up (non-blocking): (1) `docs/specs/2026-08-01-org-runtime.md`'s inline `(2026-09-16 改訂で撤去)` marker convention remains unevenly applied (FR-2/FR-7/FR-8/NFR unmarked despite being named in the note) — cosmetic; (2) `DefaultModelForDriver`'s doc comment (`internal/org/envelope_summary.go:39-49`) and a second reference in `internal/cli/org.go:97-98` both still claim it as `newOrgStartCmd`'s sole caller, which the `649901a` fix commit made false by rewiring `start` through `DefaultModelForDriverAndRole` — `DefaultModelForDriver` now has zero production callers, kept alive only by its own unit tests (a legitimate anchoring-test pattern, but the doc text needs a follow-up correction).
