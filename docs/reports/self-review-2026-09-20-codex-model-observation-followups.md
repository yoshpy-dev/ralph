# Review report: codex-model-observation-followups (self-review, cycle 1)

- Date: 2026-09-20
- Plan: `docs/plans/active/2026-09-20-codex-model-observation-followups.md` (issue #173)
- Reviewer: `reviewer` subagent (Claude Code), pipeline cycle 1/2
- Scope: diff quality only for `git diff main...HEAD` at `e086fda` (branch `fix/codex-model-observation-followups`).
  Not in scope: spec compliance (`/verify`), a test verdict (`/test`), documentation drift (`/sync-docs`).

## Evidence reviewed

- `git log --format='%h parent=%p %s' main..HEAD` — linear: `099e028` → `e64725d` → `1205775` (Slice A) → `b133022` (Slice B) → `6b63b69` (inline `lastErr` fix) → `e086fda` (plan). Report order matches DAG order; no commit in this range predates a report that claims to cover it.
- `git diff main...HEAD` — 7 files, +1057/-147. Production: `internal/org/codex_session.go`, `internal/org/spawn.go`, `internal/org/verbs.go`.
- Full read of `ObserveCodexEffectiveModel`, `codexRolloutCandidates`, `scanRolloutRecord`, `isCtxDoneErr`, `codexSpawnCorrelation`, `observeStopModelReceipt`, `Stop`, `observeCodexSpawnReceipt` at HEAD.
- **Mutation runs** (throwaway `git archive` copy of HEAD under the session scratchpad; deleted afterwards, worktree `git status --porcelain` empty):
  | mutation | `go test ./internal/org/` |
  |---|---|
  | baseline | ok |
  | drop per-date-directory `ctx.Err()` (`codex_session.go:274-276`) | **ok** |
  | drop per-entry `ctx.Err()` (`codex_session.go:283-285`) | **ok** |
  | drop before-open `ctx.Err()` (`codex_session.go:425-427`) | **ok** |
  | drop all three of the above at once | **ok** |
  | drop per-line `ctx.Err()` (`codex_session.go:450-452`) | FAIL — `..._CtxDoneMidFileRead_...` |
  | `CodexModelObserveTimeout = time.Nanosecond` → `2 * time.Second` in both new budget tests | FAIL (both) — fixtures are genuinely findable, so those two tests discriminate |
- Determinism runs in the worktree: `go test ./internal/org/ -run '...ObservationBudgetExhaustedMidPass|...ParentCtxCancelledFirst|...ObservationCutShort' -count=60` → ok; `-race -count=25` over the same set plus the five new ctx tests → ok.
- `$(go env GOROOT)/src/context/context.go:644-648` (go1.26.0): `WithDeadline` cancels **synchronously** when `dur <= 0`, so a 1 ns budget has no timer race.
- `grep -rn 'AR-[0-9]\|AR#[0-9]' --include='*.go' internal/` for citation-format comparison; `docs/tech-debt/README.md` diffed against the delta (no row added, none invalidated — row 134 about the parser's codex-format dependency is untouched and still accurate).

## Findings

| ID | Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- | --- |
| M1 | MEDIUM | `internal/org/spawn.go` | The rewritten reason contract contradicts its own code and its own sibling comment. Bullet 1 says a last pass **"itself cut short mid-pass by that same budget"** yields `codexNotFoundReason`; the code returns `codexReadErrorReason` in that situation whenever any *earlier completed* pass had a genuine read error, because `lastErr` is deliberately preserved across a cut-short pass. Bullet 2's condition ("the last poll attempt COMPLETED with a genuine error") is likewise not the condition the code tests — the code tests "the last *completed* pass", which need not be the last attempt. Separately, the `lastErr` block comment cites "this function's own doc comment, **third** bullet"; the third bullet is the parent-ctx/`codexCutShortReason` case, while the rule it describes is what decides between bullets 1 and 2, and bullet **2** is the one carrying the `isCtxDoneErr` clause. Bullet 3's "was done **first**" also over-specifies: the code tests `ctx.Err() != nil` (done at all), not ordering. | `spawn.go:1079-1086` (bullets 1-2), `spawn.go:1087-1090` (bullet 3), `spawn.go:1102-1106` (the `lastErr` block comment, incl. "third bullet"), `spawn.go:1121-1125` (`if !isCtxDoneErr(err) { lastErr = err }`), `spawn.go:1137-1139` (the read of `lastErr`). Reachable input: pass 1 hits a permission-denied candidate → `lastErr` set; pass 2 is cut short by `obsCtx` → `lastErr` retained; `ctx.Err() == nil` → `codexReadErrorReason`. | Reword bullet 1 as "…and **no completed pass** reported a genuine read error", bullet 2 as "…the last **completed** pass reported a genuine read error (a later pass cut short by the budget never clears it)", bullet 3 as "was done **at all** when the wait ended", and repoint `spawn.go:1106` at the second bullet. Code is correct; only the prose needs to move. |
| M2 | MEDIUM | `internal/org/codex_session_test.go` | `TestObserveCodexEffectiveModel_CtxDoneDuringCandidateCollection_NotFound`'s own comment claims `n=3` "lands inside the 20-entry loop … proving the cut lands mid-collection, not merely at the very start or end". The assertions (`not-found` + `DeadlineExceeded`) hold for *any* single `ctx.Err()` check anywhere in the pass, so the test proves nothing about placement. Its arithmetic is also wrong in one half: `codexSessionDateDirs` returns day-1, day, day+1 **ascending**, and the budget is consumed inside the day directory's entry loop, so day+1 is never reached and never "costs one failed-ReadDir check". | Comment: `codex_session_test.go:268-272`. Ordering: `codex_session.go:368-378`. Disproof: deleting the per-entry check (`codex_session.go:283-285`) leaves the whole `internal/org` suite green; deleting the per-date-directory and before-open checks as well (three of AR-4's four checkpoints, all three documented in `ObserveCodexEffectiveModel`'s doc comment at `codex_session.go:149-167`) still leaves it green. Only the per-line check is pinned by a test. | Make the test assert the discriminator directly — `countingCtx` already records `calls`, so a full-collection calibration run can assert `collectCtx.calls >= 1 + numEntries` (i.e. ctx is consulted per entry, not only per directory). At minimum, delete the "proving…" claim and fix the day+1 sentence. Hand the "3 of 4 checkpoints unpinned" half to `/test` as a coverage item, not to this report. |
| L1 | LOW | `internal/org/verbs.go` | Four lines below a new comment insisting the roster "must never decide", `Stop` still builds the `stopped` event's `Role`/`Driver`/`Model` from the roster. In the PR's own AC-2 forward-case scenario the single appended event therefore reads `driver=claude`, the rejected retry's model, **and** `model_observed=true` side by side. The plan makes the roster-overwrite behaviour a non-goal, so this is intentional — but nothing at the append site says so, and the comment above it invites the opposite expectation. | Comment `verbs.go:657-666`; append `verbs.go:685-689` (`Driver: seat.Driver, Model: seat.Model`). The scenario is asserted as a setup invariant by `verbs_test.go` (`TestOrgStop_Codex_ObservesWhenRejectedRetryUsedADifferentDriver` fatals unless `seat.Driver == "claude"`), and the same test then asserts `model_observed=true` on that event. | One clause at the append site: the state event keeps recording the roster's view (unchanged, plan non-goal) while `Details`' `model_observed=` token is correlated-spawn-derived. |
| L2 | LOW | `internal/org/verbs.go` | "Is this a codex seat" now depends on `observeStopModelReceipt`'s own second `o.Manifest.Read()`, with two side effects the diff does not mention: (a) **every** non-dry-run stop, claude seats included, now pays that extra full manifest read, where the old code short-circuited on `seat.Driver == "codex"` before reading; (b) a failed second read now emits **no** `model_observed=` token at all, where the old code emitted `model_observed=none`. It also keeps the two-read window the plan consciously left open. | `verbs.go:896-903` (read + correlate + driver gate) vs the pre-fix `if !p.DryRun && seat.Driver == "codex"` in `git show main:internal/org/verbs.go`. `findSeat` (`verbs.go:56-67`) has already read the same file moments earlier. | Optional and safely deferrable. If touched: have `Stop` hand `findSeat`'s events to `observeStopModelReceipt` — one read, no disagreement window, no extra cost on the claude path, and the manifest-read-failure case cannot change the emitted token. Otherwise add one sentence at `verbs.go:897` recording that the second read is accepted. |
| L3 | LOW | `internal/org/verbs.go` | Three of the six new AR citations are bare (`AR-3`, `AR-3`, `AR-4`) while the other three carry the triage path — and the same file already uses the fully-qualified form for a different PR's finding. This PR's own `/cross-review` will write `docs/reports/cross-review-triage-codex-model-observation-followups.md` numbering its findings `AR-1…`, at which point a bare "AR-3" inside `internal/org/` points at two different files. | Bare: `verbs.go:664`, `verbs.go:716`, `verbs.go:925`. Qualified: `verbs.go:701`, `verbs.go:879`, `spawn.go:1064`. Pre-existing correct form in the same file: `verbs.go:94` ("See cross-review AR-1 (docs/reports/cross-review-triage-org-send-enter-timing.md)"). Dominant repo form: `internal/cli/upgrade_v2.go:148`, `internal/cli/migrate.go:261` ("AR#n, cycle N, <path>"). | Qualify the three bare sites. Cheapest before this PR's `/cross-review` runs — afterwards the ambiguity is already shipped. |
| L4 | LOW | `internal/org/{spawn,codex_session,verbs}.go` | Four new comments describe the pre-PR state, which post-merge has no referent: "the manual deadline/remaining-budget arithmetic **the previous version of this function** needed is no longer necessary" (the `deadline`/`remaining` locals grep to nothing at HEAD), "exactly **as before** ctx was threaded through this file", "(those are skipped silently, **as before**)", "StartedAt/StartedTS/PromptPath **as before**, plus Model/Driver/Role". | `spawn.go:1071-1073`, `codex_session.go:233`, `codex_session.go:265`, `verbs.go:695`. | State the invariant instead of the delta ("a genuine read error is recorded in `firstErr` and scanning continues", "a directory-read error is skipped silently", "obsCtx's Done() carries the budget"). In-branch history belongs in the triage report. The two equivalents in the test files (`verbs_test.go:1628`, `:1675`) are fine — recording that a test discriminates against the pre-fix code is useful there. |
| L5 | LOW | `internal/org/verbs.go` | `StopResult.ModelReceipt`'s "the zero Receipt on **every other path**" enumeration is now one-and-a-half items short: it does not list "no commanded model on the correlated `spawn_started`" (the new `corr.Model == ""` return) or "no correlated `spawn_started` at all", and its "a claude seat" entry now means "a seat whose *correlated spawn* was claude", which is the whole point of the AR-3 fix. | Doc `verbs.go:597-606`; the unlisted returns at `verbs.go:902` and `verbs.go:905`. The `corr.Model == ""` path has its own dedicated test (`TestOrgStop_Codex_EmptyModelOnCorrelatedSpawn_NothingObserved`), so it is a real, exercised path. | Add the two entries and reword "a claude seat" to "a seat whose correlated spawn was a claude spawn". |

Counts: CRITICAL 0, HIGH 0, MEDIUM 2, LOW 5.

## Positive notes

- **AR-3 is fixed at the deciding site, not beside it.** `codexSpawnCorrelation` reads only `EventSpawnStarted` / `EventSpawnStep` and skips `DryRun`, so a `rejected` state event cannot displace the launching spawn under any input; `Stop`'s codex-ness gate, the receipt's `CommandedModel`/`Driver`/`Role`, and the comparison value all come from that one event now. The AC-1 test asserts the roster invariant (`seat.Model == "gpt-9-nonexistent"`) *before* asserting the fix, so it cannot pass for the wrong reason.
- **The "a completed pass keeps its result even if ctx expired at the very end" rule is implemented exactly.** There is no `ctx.Err()` check after the candidate loop (`codex_session.go:214-216`), and inside `scanRolloutRecord` both terminal branches — `decided()` and `readErr != nil` — return/break *before* the next top-of-loop check (`codex_session.go:477-490`), so a match completed on the last line of the last candidate is never thrown away. Pinned by `..._CtxExhaustedExactlyAsPassCompletes_FoundStillReturned`, which calibrates the exact call count and then asserts the budget really is exhausted.
- **No ctx error can reach a receipt reason or a manifest event.** `observeCodexSpawnReceipt` filters with `isCtxDoneErr` before assigning `lastErr`, and all three reasons are package constants; `observeStopModelReceipt` discards the observer's error outright (`verbs.go:932-934`). `isCtxDoneErr` is also the right predicate: `ctx.Err()` is `Canceled`/`DeadlineExceeded` even under `WithCancelCause`, `errors.Is` handles wrapping, and no local-filesystem read error wraps a context error.
- **`6b63b69` is a genuine improvement, not churn.** Before it, `lastErr` could only ever be set, so an early unreadable candidate made the final reason `codexReadErrorReason` forever, even after a later clean pass. The one-line widening makes the declaration's own first clause ("the LAST COMPLETED pass's own error (nil or genuine)") true. (M1 is about the *other* comment not having caught up, not about this change.)
- **Both nanosecond-budget tests discriminate**, confirmed by mutation: raising the budget to 2 s makes both fail, i.e. the fixtures really are findable and the tests are not passing on an empty directory. The `TimeoutMS: 1` test cannot take the wrong branch even if the parent had not yet expired when the observation started, because that test writes no fixture and `ctx.Err()` is consulted before `lastErr`.
- **No spin, and the poll's shape is unchanged.** `waitOrCtxDone(obsCtx, interval)` always waits the full interval unless `obsCtx` is done, so dropping the manual `remaining` clamp neither shortens nor lengthens the poll — `obsCtx.Done()` carries the same bound. With a tiny `CodexModelObserveInterval` the loop runs many passes, as it did before this PR; each pass is three `ReadDir`s plus the ModTime pre-filter.
- **The correlation test's new rows were checked for vacuity.** The added dry-run `spawn_started` is discriminating (the dry-run filter is what excludes it); the added `rejected` row is documentation rather than a discriminator under the current code (state events can never be selected by either loop), but it guards a plausible future widening of the first loop's filter. Worth keeping, worth knowing which is which.
- **`docs/tech-debt/README.md` is consistent in both directions**: the delta adds no row and invalidates none, and the pre-existing row about the codex session-record parser (line 134) is untouched and still true after the ctx threading.
- No secrets, no debug statements, no commented-out code, no `docs/plans/active/...` paths in shipped comments, and no credential-shaped strings introduced. `-race` is clean over all new tests.

## Answers to the requested judgment points

1. **Three-value return / token parity.** The `(receipt, observed, isCodexSpawn)` shape is acceptable — the two bools are independent (`isCodexSpawn` gates the *token*, `observed` gates the *append*) and the call site reads cleanly as nested `if`s. A struct would not add much at one call site; if it is ever reused, prefer splitting correlation (pure, testable) from observation (I/O) over a struct. Token parity: `model_observed=` is now emitted iff `!DryRun && isCodexSpawn`. Never for a dry-run (dry-run short-circuits, and `codexSpawnCorrelation` skips dry-run events anyway), never for a claude-launched seat, never for a seat with no real `spawn_started`. Two deliberate divergences from the old `seat.Driver == "codex"` gate: a seat whose only events are `rejected` no longer gets `model_observed=none` (documented as intended at `verbs.go:874-878`), and a failed second manifest read now suppresses the token (**not** documented — see L2).
2. **Double manifest read.** No harmful disagreement that I can construct: `findSeat`'s roster supplies `PaneID`/`AgmsgTeam`/`Role`/`Driver`/`Model` for the event, and the second read supplies only correlation. A concurrent `spawn` landing between the two would make Stop correlate the *new* spawn after having C-c'd the old pane — narrow, and narrow before this PR too. It is not worth removing for correctness; it *is* worth removing to delete the new claude-path cost and the token-suppression case (L2). A comment is the acceptable minimum.
3. **Roster vs correlated spawn elsewhere.** `grep`ed `seat.Model` / `seat.Driver` / `seat.Role` across `internal/org/`: the only remaining roster-derived codex-adjacent site is the `stopped` event itself (`verbs.go:687`) — L1. `spawn.go`'s other uses are `p.*` (the request being served) or `existing.*` in `compensateStale`, both correct. Nothing in PR #174's other code takes a commanded model from the roster. The `stopped` event is *pre-existing* behaviour and an explicit plan non-goal, so intended — but now inconsistent with the token on the same line, hence LOW rather than silence.
4. **ctx plumbing.** Every unbounded loop is covered: the date-directory loop, the entry loop, the candidate loop (via `scanRolloutRecord`'s before-open check) and the line loop. `os.ReadDir` returning a whole directory at once and `sort.Slice` are both uninterruptible, which the doc comment states honestly ("can overrun ctx's deadline by … one syscall (ReadDir/Lstat/Open)"). The sort is bounded by the entries of at most three directories and is not separately checkable; acceptable, though `codexRolloutCandidates`' comment listing "sorting" among the work the checks address reads as broader than what the checks do. `isCtxDoneErr` is the right test (see Positive notes). No ctx error leaks into a reason. The "completed pass wins" rule is implemented exactly, with no check after the last candidate and no check after the last line.
5. **Spawn poll.** `ctx.Err() != nil` is checked at the right moment and in the right priority: the parent is the outer bound, and `codexCutShortReason` is the less specific, safer claim when both are done. A parent cancelled a microsecond after the budget expired reporting "cut short" is acceptable; the only cost is that bullet 3's word "first" over-promises (folded into M1). No path spins: `waitOrCtxDone` always consumes the interval unless `obsCtx` is done. With a tiny interval and no record, the loop runs `budget / interval` passes, each three `ReadDir`s plus `Lstat`s — same cost as before this PR, and now genuinely bounded *within* a pass, which is the whole point of AR-4.
6. **Stop's `context.Background()` + the 8 s budget.** Sound: Stop has no ctx of its own (every other driver call in `Stop` uses `context.Background()` too), and reusing `codexModelObserveTimeout` avoids a second knob, as the plan decided. The operator-visible gap is real — `model-routing.md` says stop "looks once more" and nothing anywhere says that one look can take up to 8 s. That is documentation drift, so I am *not* filing it as a diff-quality finding; flagging it for `/sync-docs` instead.
7. **Tests.** `countingCtx` is a good seam and no test depends on an exact count in a way that a harmless refactor would break *for the wrong reason*: the three calibrated tests re-derive their budget from the real code, and the one hardcoded test (`n=3`) is robust to placement changes — too robust, which is exactly M2. Calibrating by calling the internals first is the right design; it is what makes `..._CtxDoneBeforeSecondCandidate_...` and `..._CtxExhaustedExactlyAsPassCompletes_...` self-maintaining. The 1 ns / `TimeoutMS: 1` Spawn tests are deterministic: go1.26's `WithDeadline` cancels synchronously for a non-positive duration, and the `TimeoutMS: 1` test has no fixture and no competing branch; 60 plain runs and 25 `-race` runs were clean. One nit worth knowing but not filing: `countingCtx` is not goroutine-safe (`calls++` and the `close(done)` are unguarded), which technically breaks `context.Context`'s "safe for simultaneous use" contract — harmless because all five users call it on the test goroutine, and `-race` is clean.
8. **History-narrating comments and finding ids.** Four new "previous version / as before" comments (L4) and three bare AR ids (L3). No `docs/plans/active/...` path reaches shipped code. The two "before the fix" mentions in `verbs_test.go` are appropriate where they are.

## Coverage gaps

- Three of AR-4's four `ctx.Err()` checkpoints can each be deleted with the entire `internal/org` suite green (evidence table above). This is a `/test` item, recorded here because the mutation runs were part of grading M2.
- No test covers a seat whose only manifest event is `rejected` being stopped — the case where the new gate drops `model_observed=none` entirely.
- The M1 scenario (an early pass with a genuine read error followed by a pass cut short by the budget) has no test; it was established by reading, not by running.

## Recommendation

- **Merge: yes.** No CRITICAL, no HIGH. Both fixes land at the site that actually decides, the AC-1/AC-2 tests would fail against the pre-fix code, and no finding affects a shipped behaviour — M1, L1, L3, L4, L5 are comment accuracy and M2/L2 are a test claim and an avoidable read.
- Suggested in-cycle: M1 and M2 (both are small and both are "the prose does not match the code next to it"), plus L3 while it is still cheap — before this PR's `/cross-review` assigns its own `AR-n`.
- Follow-ups: L1, L2, L4, L5 as one batched comment-hygiene pass, or deferred with a single tech-debt row if the cycle cap is reached.
