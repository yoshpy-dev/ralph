# Verify report: org-stop-reason-removal

- Date: 2026-09-18
- Plan: `docs/plans/active/2026-09-17-org-stop-reason-removal.md` (issue #153)
- Verifier: `verifier` subagent (Claude Code, standard flow)
- Diff scope: `git diff c6b8127...HEAD` on `refactor/org-stop-reason-removal` (HEAD `e9914cb`), 7 files, +446/-47. Substantive commits: `bce892c` (Go), `cb70f13` (tech-debt row), `72b89d5` and `ec9a226` (self-review fixes).
- Scope note: spec compliance (AC-1..AC-7 grep/doc gates) and static analysis only. No behavioral test suite was run — that is `/test`'s job. `git status` confirmed a clean working tree (`nothing to commit, working tree clean`) before this pass; nothing here reflects uncommitted changes.

## Overall verdict: Pass

All 7 acceptance criteria confirmed with direct evidence. Static analysis is clean, including `go vet` on `internal/org`. The `watch.go` diff against `main` is comment-only, confirming runtime behavior is unchanged. One deliberate, plan-recorded doc drift (Scope item 2's wording superseded by self-review M1) is flagged below as informational, not a fail.

## Static analysis

`RALPH_VERIFY_SCOPE=changed ./scripts/run-static-verify.sh` (changed-language scope = golang):

- `gofmt -l internal/org/verbs.go internal/org/watch.go internal/org/watch_test.go` → empty (also confirmed via the verifier's own `gofmt: ok`)
- `go vet ./internal/org/...` → exit 0 (silent, pass)
- `golangci-lint run` (inside the verifier) → `0 issues.`
- staticcheck is installed (`command -v staticcheck` resolves) and ran silently (no output = pass, per this repo's convention)
- `scripts/check-sync.sh` → `PASS: all files in sync.` (`DRIFTED: 0`)
- `scripts/check-skill-sync.sh` → `[ok] check-skill-sync: 13 skill(s) in lock-step`
- `scripts/check-pipeline-sync.sh`, `scripts/check-template-purity.sh` → both OK
- Overall: `==> All verifiers passed.`
- Evidence: `docs/evidence/verify-2026-09-18-031103.log`

## Acceptance criteria

| AC | Verdict | Evidence |
| --- | --- | --- |
| AC-1 | Pass | `grep -n 'Reason' internal/org/verbs.go` empty (`StopParams` at `verbs.go:229-233` has only `OrgID`, `Seat`, `DryRun`; `Stop`'s Details is built as `paneNote + " " + leaveNote` at `:284`, no reason concat). `grep -n 'Reason:' internal/org/watch_test.go` empty. `grep -rn 'p.Reason' internal/` empty. The one legitimate exception the plan names — `watch.go`'s escalation-record `Reason` field — is confirmed distinct: `grep -n 'Reason' internal/org/verbs.go internal/org/watch.go` (the issue's literal, unnarrowed gate) returns exactly three lines, all unrelated to `StopParams`: `watch.go:272` (`Reason string \`json:"reason"\`` on the escalation struct), `:794` (a comment mentioning the removed field by name, for context), and `:1029` (`Reason: reason` assigning that same escalation-record field). No `StopParams.Reason` residue anywhere. |
| AC-2 | Pass | `leadActivityEventCount`'s code (`watch.go:817-833`) is byte-identical in control flow to `main` — `git diff c6b8127 -- internal/org/watch.go \| grep -vE '^[+-]//' \| grep -E '^[+-]'` returns nothing, confirming the whole file's diff is comment-only. `grep -c 'reason=watchdog_' internal/org/watch.go` → 2 (one in the doc comment, one in the live `strings.Contains` check at `:829`). `grep -n 'dormant\|future watchdog enforcement' internal/org/watch.go` empty. The rewritten comment (`:790-793`) states "No code path has produced such an event since PR #152 removed the org budget concept (2026-09-17)" and calls the guard a legacy-manifest compatibility mechanism, citing PR #152 by number. `go vet ./internal/org/...` is clean (see Static analysis). |
| AC-3 | Pass | `grep -n 'WatchdogsOwnStopEvent\|_WatchdogStopDoesNot' internal/org/watch_test.go` empty — confirmed via a `func Test` set diff against `main`: the only changes are the rename `WatchdogsOwnStopEvent` → `LegacyWatchdogStopEvent` (`watch_test.go:864`), the rename `..._WatchdogStopDoesNot` → `..._LegacyWatchdogStopDoesNot` (`:1152`), and one net addition (`PersistedAlertBaseline...`, `:952`) — nothing deleted. Both renamed tests append the legacy event via `o.Manifest.Append(ManifestEvent{..., Event: EventStopped, Details: "pane=ok leave=ok reason=watchdog_cutoff ..."})` (`:896`, `:1183`), not via `o.Stop(`. `TestWatch_Deadman_ManualStopOfOtherSeat_ClearsPendingAlert` (`:1300-1337`) calls `o.Stop(StopParams{OrgID: "org-a", Seat: "seat-2"})` with no `Reason:` field (compiles and reads cleanly since the field no longer exists). |
| AC-4 | Pass | `TestWatch_Deadman_PersistedAlertBaseline_SurvivesLegacyWatchdogStop` exists (`watch_test.go:952-1076`) with two subtests: (i) `"no new events: alert survives and escalates once at deadline"` (`:1035-1055`) asserts `f.escalateCalls != 1` fails the test and that the alert is removed from `PendingAlerts` and recorded in `Escalated`; (ii) `"genuine lead sent event clears the alert without escalation"` (`:1057-1075`) appends a `sent` event and asserts zero escalations and an empty `PendingAlerts`. The shared fixture (`:965-1033`) computes `baseline := leadActivityEventCount(rr.Events, "org-a")` at `:986` **before** appending the legacy cutoff at `:990`, matching the plan's Deviation-notes-recorded ordering rationale (the Discrimination note at `:935-951` explains why this order is what makes subtest (i) discriminate). |
| AC-5 | Not evaluated here | Out of scope for `/verify` per this task's instructions and the repo's pipeline contract (`go test ./internal/org/... ./internal/cli/... -count=1` and `./scripts/run-verify.sh` belong to `/test`). The static-analysis half of this AC (gofmt/go vet/golangci-lint/staticcheck/sync gates) is covered above and is green. |
| AC-6 | Pass | `docs/tech-debt/README.md:129`: the row is struck (`~~...~~`), and the debt-item cell carries `(RESOLVED 2026-09-18 in refactor/org-stop-reason-removal)` immediately after the closing `~~`, matching the convention used by the other struck rows. The Trigger cell's "Closed 2026-09-18 in `refactor/org-stop-reason-removal` (issue #153): …" text names "chose deletion, narrowed to the producer side after the Codex plan advisory's HIGH-1" and explains the `ManifestLen` baseline/recount rationale. The row still has 6 pipes (`grep -o '\|' … \| wc -l` → 6), `git diff c6b8127 --stat -- docs/tech-debt/README.md` shows exactly 1 line changed (1 insertion, 1 deletion — no other row touched), and the struck-row count moved 44 (at `main`) → 45 (at HEAD). The Related cell appends the archive path and this task's self-review report path. |
| AC-7 | Pass | `grep -rn 'StopParams.Reason' docs/specs docs/recipes README.md .claude templates/base` empty. The only remaining repo-wide hits are in `docs/reports/`, `docs/plans/`, and `docs/tech-debt/README.md` — none of which the AC's grep scope includes, and all of which are the expected historical/register record of the removal. |

## Documentation drift

- **No unresolved drift found in the six invariants this task called out for extra scrutiny.** (1) `watch.go`'s diff against `main` is comment-only and the `reason=watchdog_` exclusion is unchanged (confirmed above). (2) `StopParams` has no `Reason` field and `Stop`'s Details output for Reason-less callers is unchanged (`paneNote + " " + leaveNote`, identical to `main`'s pre-suffix behavior). (3) Both renamed deadman tests append the legacy event via `Manifest.Append`, never via `Stop`. (4) `TestWatch_Deadman_PersistedAlertBaseline_SurvivesLegacyWatchdogStop` exists with the two described subtests, baseline computed before the cutoff append. (5) The tech-debt row is correctly struck, 6 pipes, other rows untouched. (6) The issue's literal AC-1 grep is confirmed unsatisfiable as written, and the plan's narrowing (AC-1 limited to `StopParams`-derived references) is documented in the plan's Assumptions section; the remaining `Reason` hits in `watch.go` are exactly the escalation-record field's definition, an explanatory comment, and its assignment — nothing StopParams-related.
- **Informational, not a fail — Scope item 2's plan text was deliberately superseded.** The plan's Scope table (item 2, `docs/plans/active/2026-09-17-org-stop-reason-removal.md:20`) prescribes the exact "(i) 旧 cutoff を lead activity に数えない / (ii) ベースラインの一致" comment wording. The shipped comment (`watch.go:794-809`) instead uses the wording that replaced it after self-review M1 (baseline/recount cases (a) mixed pre-#152 persisted alert, (b) mixed-version window), because M1 found reason (i) as originally worded was not independently load-bearing. This divergence is explicitly recorded in the plan's Deviation notes (last entry, "2026-09-18 plan drift") as "出荷するコメントは plan の原文ではなく M1 後の文言が正" — the plan itself names the shipped comment as authoritative over its own earlier prescription. No action needed; this is the self-review's recommended follow-up #3, already actioned as a plan note rather than a second-pass fix.
- **`/org` SKILL.md and the org runtime spec are unaffected, as the plan's Non-goals claim.** `git diff c6b8127 --stat -- .claude/skills/org/SKILL.md docs/specs/ .agents/skills/org/SKILL.md` is empty. `grep -n 'StopParams\|reason=watchdog\|leadActivityEventCount' docs/specs/2026-08-01-org-runtime.md` is empty — the spec never named this seam, so there is nothing to update.
- **The self-review's own follow-up items (N1-N4) are confirmed landed at HEAD**, cross-checked directly against the shipped comment text: the guard paragraph now names two call sites (`sendAlert`/`checkDeadman`) rather than one, the umbrella sentence covers case (b) ("a cutoff missing from the baseline but present in the recount"), and the test-file changes the self-review's orchestrator note describes are present in `watch_test.go` (not independently re-diffed line-by-line here, since the self-review report's own fix-and-revalidate table already did that against `72b89d5`/`ec9a226`).

## Known gaps / out of scope for this pass

- Behavioral tests (`go test ./internal/org/... ./internal/cli/... -count=1`) were not run — `/test` owns AC-5's test clause.
- `./scripts/run-verify.sh` (the unscoped, test-inclusive wrapper AC-5 names) was not run for the same reason; the static-only path (`run-static-verify.sh`) was used instead, per this task's instructions and the repo's verifier/tester split.
- The self-review's fix commits (`72b89d5`, `ec9a226`) were re-read directly against the current file contents in this pass rather than re-diffed hunk-by-hunk against the self-review's own revalidation table; no discrepancy was found between the two.
