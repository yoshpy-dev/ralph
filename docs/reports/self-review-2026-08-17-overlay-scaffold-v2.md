# Self-review report: overlay-scaffold-v2 (Phase 1 — エンジン基盤)

- Date: 2026-08-17
- Plan: `docs/plans/active/2026-08-17-overlay-scaffold-v2.md`
- Reviewer: `reviewer` subagent (`/self-review`)
- Scope: `git diff main...HEAD` on `docs/spec-overlay-scaffold-v2` (base `main` @ 1025d13) — 18 files, +3685/-20. Diff quality only; spec compliance, static analysis, test coverage, and doc drift are out of scope by design (`/verify`, `/test`, `/sync-docs`).

## Evidence reviewed

- Commits `7d3faca..2e58116` (spec, 5 implementation slices, 1 inline refactor, 1 out-of-scope test fix, plan progress update).
- Full read of every new non-test source file: `internal/scaffold/paths.go`, `internal/scaffold/manifest.go` (diff), `internal/scaffold/baseline.go` (diff), `internal/upgrade/block.go`, `settingsmerge.go`, `replaceplan.go`, `advisory.go`, `report.go`.
- Test files skimmed for diff quality (debug code, copy-paste errors, environment coupling): `manifest_test.go`, `paths_test.go`, `block_test.go`, `settingsmerge_test.go`, `replaceplan_test.go`, `advisory_test.go`, `report_test.go`.
- Cross-checks run: repo-wide grep for every new exported symbol (non-test callers), grep for `TODO`/`FIXME`/`fmt.Print`/`println` in added lines, U+FFFD scan, trailing-whitespace scan, `docs/tech-debt/README.md` scanned in both directions against the diff.
- Secrets scan: no credentials, tokens, or hostnames in the diff.

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | maintainability | `SetFileOwned` accepts `OwnerFork` and produces a fork entry that contradicts `SetFileFork`: `Managed=false` but `State: FileStateManaged`, and no `ForkedFromVersion`. Two constructors can therefore record the same ownership with different on-disk manifest shapes; the one that skips the fork record is exactly the state `PlanCoreReplace` cannot distinguish from a suppressed-but-unattributed fork. | `internal/scaffold/manifest.go:176-191` (`SetFileOwned` hardcodes `State: FileStateManaged` for every owner) vs `internal/scaffold/manifest.go:196-206` (`SetFileFork` sets `FileStateUnmanaged` + `ForkedFromVersion`); `managedForOwner` at `internal/scaffold/manifest.go:208-217` deliberately returns `false` for `OwnerFork`, so this path is reachable, not theoretical. | Make `managedForOwner`/`SetFileOwned` reject `OwnerFork` with an error pointing at `SetFileFork`, so there is exactly one way to write a fork entry. Cheap now; a latent wrong-manifest trap once Phase 3 wires this. |
| MEDIUM | maintainability | `OwnedSettingsPaths` is exported, documented as the owned-path contract, and referenced by `MergeOwnedSettings`'s doc comment — but has zero references anywhere in the repo, tests included. The actual owned paths are string literals inside `MergeOwnedSettings`. The declared contract and the implemented contract can silently diverge. | `internal/upgrade/settingsmerge.go:19-24` (declaration) vs `settingsmerge.go:72-76` (`mergeEnv` / `mergeOwnedArray(..., []string{"permissions","allow"})` / `{"permissions","deny"}` / `mergeHooks` hardcoded). Repo-wide grep for `OwnedSettingsPaths` returns only those two doc/declaration sites. | Either drive the merge from the list (or a test that asserts the list matches the implemented paths), or drop the exported var and keep the paths documented in the function comment only. As written it is a second source of truth with nothing keeping it honest. |
| MEDIUM | readability | `AdvisoryEntry`'s doc says it records a path "where disk content differs from the new template", but `classifyFork` appends an advisory unconditionally — including when the fork content is byte-identical to the new template. Consequence: every fork path shows up in the report's Advisories count and renders a `_No differences._` section on every run, including an otherwise no-op upgrade. (The *behavior* matches spec FR-2, which asks for a fork advisory every time; the doc comment is what is wrong, and `classifySeed` right below it really does gate on a hash change, so the comment reads as accurate for one caller and false for the other.) | `internal/upgrade/replaceplan.go:77-86` (doc) vs `replaceplan.go:262-280` (`classifyFork`, no comparison before append); contrast `classifySeed` at `replaceplan.go:304-309`, which only appends when `templateHash != recordedTemplateHash`. Rendering side: `internal/upgrade/report.go:146-148`. | Fix the `AdvisoryEntry` doc to state the per-owner rule (fork: always; seed: only when the template changed), and decide explicitly whether identical-content fork advisories should be suppressed before Phase 3 wires the report. |
| LOW | maintainability | `classifyFork` reports a stale `DiskHash` when the fork file is absent from disk: it falls back to the manifest's recorded hash, so the advisory claims a disk hash for a file that is not there. Only programmatic consumers see it today (`report.go` does not render `AdvisoryDiff` hashes), and `RenderAdvisoryDiffs` correctly renders a full-addition diff for the missing file. | `internal/upgrade/replaceplan.go:275-278`; renderer at `internal/upgrade/advisory.go:49-52`. | Leave `DiskHash` empty when `!hasDisk`, or document the fallback on the field. |
| LOW | typo | The spec's security section names `cleanTemplateRelPath`, which this same PR deleted (renamed and promoted to `scaffold.CleanLocalRelPath`). A reader following the spec's security requirement lands on a symbol that does not exist at HEAD. | `docs/specs/2026-08-17-overlay-scaffold-v2.md:172`; removal at `internal/scaffold/baseline.go` (function deleted) and new home `internal/scaffold/paths.go:12`. The plan's mention at `docs/plans/active/2026-08-17-overlay-scaffold-v2.md:36` is describing the pre-PR state and is fine as-is. | Update the spec line to `scaffold.CleanLocalRelPath`. |
| LOW | security | `UpgradeReportRelPath` sanitizes `version` (with a doc comment explaining that path separators must not escape the reports directory) but applies no sanitization to `date`, which is interpolated into the same filename. Containment currently rests entirely on `WriteUpgradeReport`'s later `cleanPathKey` check, so this is defense-in-depth, not a live escape. | `internal/upgrade/report.go:186-196` (`versionSanitizeRe` applied to `version` only) and `report.go:201-206` (the validation that actually contains it). Test coverage exists for both the version sanitization and the parent-escape rejection (`report_test.go:122`, `report_test.go:151`). | Run `date` through the same regexp, or note in the doc comment that `date` is trusted caller input and containment is enforced downstream. |
| LOW | readability | `ReplacePlan.LegacySkipped`'s doc enumerates "owner=block or is a legacy (unattributed) entry", but the classifier also funnels unknown/future owner values into the same slice. A Phase 3 reader trusting the field doc would not expect a forward-compat value there. | Doc at `internal/upgrade/replaceplan.go:96-99`; the third producer is the `default` branch at `replaceplan.go:212-216`. | Add "or an unrecognized owner value" to the field doc. |
| LOW | maintainability | `WriteUpgradeReport` hardcodes `0644`/`0755` while every other write site in the repo goes through `scaffold.FilePerm`. Correct for a `.md` file today, but it adds a fourth permission-decision site to a codebase that already carries an open tech-debt row about duplicated permission logic. | `internal/upgrade/report.go:208-213`; existing call sites `internal/upgrade/replaceplan.go:364`, `internal/cli/upgrade.go:465`, `internal/cli/language_pack.go:157`, `internal/scaffold/baseline.go:34`; related debt row `docs/tech-debt/README.md:23`. | Use `scaffold.FilePerm(clean)` for consistency, or leave and accept it as a report-only path. |
| LOW | readability | Sibling helpers take the same argument in two different shapes: `getObjPath(root, keys ...string)` is variadic, `getArrayPath(root, keys []string)` takes a slice. | `internal/upgrade/settingsmerge.go:415` and `settingsmerge.go:433`. | Pick one shape. |
| LOW | maintainability | `TestPlanCoreReplace_ReplanAfterPartialFailureIsStable` forces the write failure with `chmod 0444`, which does not deny writes to root. The test would fail (not flake — fail at `t.Fatal("expected ApplyOps to fail...")`) in a container CI that runs as root. The sibling partial-failure test avoids this by using a directory-in-the-way, which is uid-independent. | `internal/upgrade/replaceplan_test.go:608-617` (chmod approach) vs `replaceplan_test.go:540-556` (directory approach). | Consider using the directory-collision technique here too, or skip when `os.Geteuid() == 0`. |
| LOW | readability | `TestExistingConstructorsWriteNoV3Fields` builds its last entry by reading a map key that was never set (`m.Files["with-hash.md"]` returns the zero value, which is then given a template hash). It exercises `WithTemplateHash` as intended, but reads like an oversight rather than a deliberate zero-value start. | `internal/scaffold/manifest_test.go:234`. | Seed the entry first, or add a one-line comment saying the zero value is the point. |

## Positive notes

- Path validation is genuinely applied at every boundary rather than declared and forgotten: `PlanCoreReplace` validates manifest keys *and* template-walk results, `RenderAdvisoryDiffs` validates before reading, `WriteUpgradeReport` validates before writing, and each has a negative test (`replaceplan_test.go:420`, `:432`, `advisory_test.go:125`, `report_test.go:151`). `cleanPathKey` additionally normalizes to forward slashes, which is what keeps the `io/fs` reads correct on a non-slash platform.
- The commit barrier is not just asserted in prose — `ApplyOps` structurally cannot touch the manifest, and `ManifestRefreshEntry` exists specifically so that re-planning over a half-applied tree reclassifies settled files instead of calling them drift. `TestPlanCoreReplace_ReplanAfterPartialFailureIsStable` pins exactly that.
- `block.go` earns its `rawLine` split: the round-trip (text + term) makes "every byte outside the markers is preserved" a property of the representation rather than a promise, and CRLF / no-trailing-newline / already-blank-line-at-EOF are each covered.
- `marshalJSONString` disabling HTML escaping is the right call for `Bash(a && b)` permission patterns, and the reason is written down at the call site.
- No debug statements, no commented-out code, no TODO/FIXME markers, no secrets, no U+FFFD, no trailing whitespace in the diff. The single `panic` (`settingsmerge.go:592`) is guarded by an invariant that is stated in the code.
- Errors are handled explicitly throughout; the only swallowed errors are `fs.ErrNotExist` / `os.ErrNotExist`, each converted into an explicit `has*` boolean that the caller must branch on.
- The out-of-scope `tests/test-gc-artifacts.sh` fix is correctly minimal, carries a comment explaining why the date must be relative, and is recorded as a deviation in the plan rather than slipped in silently.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| — | — | — | — | — |

No new rows. `docs/tech-debt/README.md` was checked in both directions: this PR adds no deferred work of its own (all findings above are small, fixable-now items, not accepted shortcuts), and it invalidates no open row — the unwired Phase 1 primitives change no behavior, so the existing `ralph upgrade` rows (`README.md:20`, `:25`, `:26`) remain accurate until Phase 3 lands. Row `:23` (duplicated permission logic) is adjacent to the LOW finding about hardcoded `0644` but is not resolved by this PR.

## Recommendation

- Merge: **Yes, with follow-ups.** No CRITICAL and no HIGH findings; the diff is unusually disciplined for its size (3685 added lines across five primitives), the plan's own non-goal of "CLI 未配線" is respected (grep confirms no non-test caller of any new exported symbol outside `internal/upgrade`/`internal/scaffold`), and no existing behavior is touched.
- Follow-ups, in priority order:
  1. **Fix in this PR (one-liners, and both otherwise reach Phase 3 as wrong contracts):** reject `OwnerFork` in `SetFileOwned` (MEDIUM), and correct the `AdvisoryEntry` doc comment to state the per-owner advisory rule (MEDIUM).
  2. **Decide in this PR:** what to do with `OwnedSettingsPaths` (MEDIUM) — drive the merge from it, pin it with a test, or delete it. Leaving an unreferenced exported contract is the option that ages worst.
  3. **Optional now, cheap:** spec line 172's stale `cleanTemplateRelPath` reference, `date` sanitization symmetry in `UpgradeReportRelPath`, `LegacySkipped` doc, fork `DiskHash` when the file is absent, and the root-runnable chmod test.
- Known gaps in this review: static analysis, spec compliance against FR-1/FR-4/FR-5/NFR-2, test execution, and documentation drift (notably the `AGENTS.md` Repo map line for `internal/upgrade/`) were deliberately not evaluated here — they belong to `/verify`, `/test`, and `/sync-docs`.

---

# Cycle 2 (re-run after cross-review fixes)

- Date: 2026-08-17
- Pipeline cycle: 2/2 (`RALPH_STANDARD_MAX_PIPELINE_CYCLES` default cap reached — no third cycle will run automatically)
- Scope of this section: the delta `2e58116..HEAD`, i.e. commits `5a90118` (self-review MEDIUM fixes), `d4a0e88` (spec doc fix), `df5c3b4` / `6564230` / `ba2384f` (verify, test, cross-review-triage report artifacts), `4c38cf1` (AGENTS.md repo-map sync + `check-sync.sh` `KNOWN_DIFFS`), `1ef5be7` (cross-review fixes: `ManifestRemove` signal, `ApplyOps` validate-all-upfront, report-path hardening). Cycle-1 findings above are not repeated; each is re-classified in the status table below.

## Cycle-1 finding status

| # | Cycle-1 finding | Status | Evidence |
| --- | --- | --- | --- |
| MEDIUM-1 | `SetFileOwned` accepts `OwnerFork` | **Fixed** | `managedForOwner` → `validateManagedOwner`; `OwnerFork` now returns an error naming `SetFileFork` (`internal/scaffold/manifest.go:207-214`). `TestSetFileOwned_RejectsFork` asserts both the error *and* that no entry was recorded. |
| MEDIUM-2 | `OwnedSettingsPaths` unreferenced, contract can diverge | **Half-fixed** | See C2-2 below. |
| MEDIUM-3 | `AdvisoryEntry` doc contradicts `classifyFork` | **Half-fixed** | See C2-1 below. |
| LOW-1 | fork `DiskHash` falls back to the recorded hash when the file is absent | **Not fixed, untracked** | `internal/upgrade/replaceplan.go:291-294` unchanged. |
| LOW-2 | spec names deleted `cleanTemplateRelPath` | **Fixed** | `d4a0e88` → `scaffold.CleanLocalRelPath` at `docs/specs/2026-08-17-overlay-scaffold-v2.md:172`. |
| LOW-3 | `UpgradeReportRelPath` does not sanitize `date` | **Fixed** | `1ef5be7`; `safeDate` + a prefix check in `WriteUpgradeReport`, with two new tests. |
| LOW-4 | `LegacySkipped` doc omits the unknown-owner producer | **Not fixed, untracked** | Doc at `internal/upgrade/replaceplan.go:98-101` still says "owner=block or is a legacy (unattributed) entry"; the `default` branch at `replaceplan.go:224-227` still funnels unknown owners there. |
| LOW-5 | `WriteUpgradeReport` hardcodes `0644`/`0755` instead of `scaffold.FilePerm` | **Not fixed, untracked** | `internal/upgrade/report.go:218,221`. |
| LOW-6 | `getObjPath` variadic vs `getArrayPath` slice | **Not fixed, untracked** | `internal/upgrade/settingsmerge.go:415,433`. |
| LOW-7 | `chmod 0444` failure injection is defeated by root | **Not fixed, untracked** | `internal/upgrade/replaceplan_test.go:641-644`. |
| LOW-8 | `TestExistingConstructorsWriteNoV3Fields` reads an unset map key | **Not fixed, untracked** | `internal/scaffold/manifest_test.go:245`, no explanatory comment added. |

## Findings (new in cycle 2)

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | readability | **C2-1.** The `AdvisoryEntry` doc fix replaced one false statement with a different false statement. It now says a fork advisory's "diff against the new template may be empty, in which case **rendering skips it**" — rendering does not skip it. `renderAdvisoriesSection` emits a `### <path> (owner: fork)` heading followed by `_No differences._` for exactly that case. Worse, "skip" is already a *named, different* concept in this package: `AdvisoryDiff.Skipped` means "the template no longer has this path", and even that case still renders a heading plus `_Skipped: …._`. A Phase 3 reader wiring the report from this doc would expect empty-diff fork paths to disappear from the output; they will not. The cycle-1 finding's second half ("decide explicitly whether identical-content fork advisories should be suppressed") was also not decided and is not recorded anywhere. | Doc at `internal/upgrade/replaceplan.go:77-82`; the renderer that does not skip at `internal/upgrade/report.go:140-148`; the colliding field name at `internal/upgrade/advisory.go:18-20`. `classifyFork` still appends unconditionally (`replaceplan.go:278-296`). | Say what the code does: every fork path produces an advisory entry, and an identical-content fork renders as a `_No differences._` section. If suppression is actually wanted, do it in `renderAdvisoriesSection` (skip `!Skipped && Diff == ""`) and say so — but do not use the word "skip" for two different behaviors in one package. |
| MEDIUM | maintainability | **C2-2.** `TestOwnedSettingsPaths_AnchorsMergeBehavior` does not anchor the list to the merge implementation, which is what cycle-1 MEDIUM-2 asked for. Its first assertion compares `OwnedSettingsPaths[:]` against a hardcoded literal copy of the same four strings — a change-detector against a duplicate, not against `MergeOwnedSettings`. Concretely: deleting the `mergeOwnedArray(..., []string{"permissions","deny"})` call, or dropping `mergeHooks`, leaves this test green while `OwnedSettingsPaths` still advertises those paths. Only the reverse direction (adding a handler without updating the var) is caught, and only because the literal must then be edited. The test's own doc comment overclaims: "keeps the exported `OwnedSettingsPaths` contract honest **against the merge implementation**". | Test at `internal/upgrade/settingsmerge_test.go:310-339` (the `slices.Equal(got, want)` assertion at `:315-318`); the four handler call sites it is supposed to anchor at `internal/upgrade/settingsmerge.go:72-77`. | Either assert per-entry behavior (for each entry in `OwnedSettingsPaths`, a template value at that path must reach the merged output — the test already has the machinery for `env`), or reword the doc comment to state honestly that it is a deliberate change-detector on the declared list plus a separate un-owned-key regression. The second half of the test (`model` preserved, `outputStyle` not introduced) is genuinely valuable and should stay either way. |
| MEDIUM | maintainability | **C2-3.** `AGENTS.md` was added to `check-sync.sh`'s `KNOWN_DIFFS`, which is a **whole-file** suppression (`is_known_diff` matches the path, not a hunk — `scripts/check-sync.sh:117-123`, consumed at `:165-167`). To accommodate a two-line repo-map extension, this permanently stops `check-sync.sh` from reporting *any* future divergence between root `AGENTS.md` and `templates/base/AGENTS.md`, on what is the most structurally important synced file in the repo. The expiry condition ("remove once Phase 2/3 reconciles") is recorded only in the code comment and in `docs/reports/sync-docs-2026-08-17-overlay-scaffold-v2.md`, a point-in-time artifact nobody re-reads when Phase 2 starts. It is not in `docs/tech-debt/README.md` (that file has zero diff in this PR) and not in the plan's Deviations section, which does record the two *other* out-of-band fixes (`1e38046`, `7dca14e`) but not this script change. | `scripts/check-sync.sh:95-102` (the new entry) vs the whole-file matcher at `:117-125`; plan Deviations at `docs/plans/active/2026-08-17-overlay-scaffold-v2.md:119-122`; `git diff main...HEAD -- docs/tech-debt/README.md` is empty. | Add one `docs/tech-debt/README.md` row with the concrete trigger ("remove the `AGENTS.md` `KNOWN_DIFFS` entry when Phase 2 lands the template-facing rollout"), and add the `check-sync.sh` change to the plan's Deviations list so the two out-of-scope script edits in this PR are recorded the same way. |
| MEDIUM | maintainability | **C2-4.** Six of the eight cycle-1 LOWs are unfixed and appear in no register (see the status table above: LOW-1, 4, 5, 6, 7, 8). Cycle 1's "Tech debt identified" table says "No new rows … all findings above are small, fixable-now items", which was true as a recommendation and is false at merge. This is cycle **2/2** — the cap. The plan is archived by `/pr`, so nothing carries these forward, and one of them (LOW-7, `chmod 0444`) is a test that will hard-fail, not flake, in a root CI container. | Cycle-1 report rows above; the six cited locations re-verified at HEAD and listed in the status table. `docs/tech-debt/README.md` unchanged. | One batched `docs/tech-debt/README.md` row covering all six, with "Phase 3 wires these primitives into `internal/cli`" as the trigger. Consider fixing LOW-7 outright (swap `chmod 0444` for the uid-independent directory-collision technique already used at `replaceplan_test.go:570-576`) — it is the only one with a concrete failure mode rather than a readability cost. |
| LOW | readability | **C2-5.** `ManifestRemove`'s doc tells the reader the commit barrier is described in `ApplyOps`' doc comment, but `ApplyOps`' doc enumerates the manifest state a caller may advance as "recorded hashes for creates, updates, and `ReplacePlan.ManifestRefresh` entries" — `ManifestRemove` is absent from the enumeration the same commit pointed at it. | Field doc at `internal/upgrade/replaceplan.go:102-111` ("see ApplyOps' doc comment"); the enumeration at `replaceplan.go:368-371`. | Add `ManifestRemove` to the `ApplyOps` enumeration; it is the one entry in that list that *removes* rather than advances a hash, so it is the one most worth naming there. |
| LOW | readability | **C2-6.** `versionSanitizeRe` is now applied to `date` as well as `version`. Its doc comment was updated ("version or date component") but the identifier was not, so a grep for the date-sanitization logic lands on a name that says version. | `internal/upgrade/report.go:184-186` (declaration) and `:194-195` (both call sites). | Rename to something component-neutral, e.g. `reportNameSanitizeRe`. |
| LOW | security | **C2-7.** The new `WriteUpgradeReport` prefix guard carries an unexplained exception that admits the one input capable of breaking the reports directory: `clean != strings.TrimSuffix(upgradeReportDir, "/")` lets `relPath == "docs/reports"` through, after which `os.WriteFile` creates a **file** named `docs/reports`, permanently blocking every later report write in that tree. No caller can produce that value (`UpgradeReportRelPath` always appends `upgrade-…​.md`), so the exception has no legitimate input and no test. Separately, the new `upgradeReportDir` const claims to match `UpgradeReportRelPath`'s output, but that function still builds its path from the literals `filepath.Join("docs", "reports", …)` — two sources of truth for one directory, introduced in the same commit. | Guard at `internal/upgrade/report.go:213-215`; const and its claim at `:199-201`; the unconverted literal at `:196`. | Drop the `clean != …` clause so the guard is a plain `strings.HasPrefix(clean, upgradeReportDir)`, and build `UpgradeReportRelPath`'s path from `upgradeReportDir` so the const is load-bearing rather than decorative. |
| LOW | maintainability | **C2-8.** `docs/insights/events/2026-08-17-overlay-scaffold-v2.jsonl` records three phases — `self_review`, `test`, `cross_review` — but the pipeline also ran `/verify` (report committed in `df5c3b4`) and `/sync-docs` (report committed in `4c38cf1`). `ralph insights` will under-report this task's pipeline by two phases. | The three-line events file vs the five report files under `docs/reports/`. | Append the missing `verify` and `sync_docs` events with `scripts/insights-append.sh` before `/pr`. |

## Positive notes (cycle 2)

- The `ManifestRemove` fix is complete rather than symptomatic: it covers **both** template-removed-core cases (disk present and unmodified → `OpDelete` + `ManifestRemove`; disk already absent → `ManifestRemove` with no op), and it correctly stays *out* of the drifted case, with `TestPlanCoreReplace_CoreTemplateRemovalOfModifiedFileIsDriftNotDelete` extended to assert `len(plan.ManifestRemove) == 0` there. That negative assertion is the one that keeps the non-destructive default honest.
- The doc's "in sorted order" claim for `ManifestRemove` is actually true and not merely asserted: `PlanCoreReplace` iterates `sortedPaths` (`replaceplan.go:152-156`) and `classifyCore` only appends, so sortedness is a property of the iteration rather than a promise.
- The fix also closes a real gap in the partial-failure replan path that cycle 1 did not catch: before `1ef5be7`, the `!hasDisk` branch of `classifyCore` returned silently, so a run interrupted after a successful delete left a manifest entry no subsequent plan would ever mention.
- `ApplyOps`' validate-all-upfront is genuinely all-upfront, and the test proves it by the right thing: `TestApplyOps_RejectsInvalidOpPathBeforeWritingAnything` asserts that the *valid* op preceding the invalid one was not written, so it tests the absence of a side effect rather than just the error string. Ordering the validation loop before the execution loop (rather than checking inside it) is what makes that property hold.
- `TestSetFileOwned_RejectsFork` likewise asserts absence of a side effect (`m.Files["x.md"]` must not exist), not just a non-nil error.
- `OwnedSettingsPaths` being `[...]string` rather than `[]string` is the right shape for an exported constant-like contract — it cannot be appended to by a caller.
- Renames in the fix commits left nothing behind: `managedForOwner` has zero remaining references outside historical report artifacts, and `cleanTemplateRelPath` survives only in the plan line that deliberately describes pre-PR state.
- Delta re-scans clean: no `TODO`/`FIXME`, no `fmt.Print`/`println`, no commented-out code, no secrets, no U+FFFD, no trailing whitespace in `2e58116..HEAD`.

## Tech debt identified (cycle 2)

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| Six unfixed cycle-1 LOWs (fork `DiskHash` fallback, `LegacySkipped` doc, `WriteUpgradeReport` perm literals, `getObjPath`/`getArrayPath` shape, root-defeated `chmod 0444` test, unset-map-key test setup) | Readability/consistency on API surfaces Phase 3 inherits; the `chmod` one is a hard test failure under a root CI container | Pipeline cap 2/2 reached; each is individually small | Phase 3 wires these primitives into `internal/cli` | This report (C2-4) |
| `AGENTS.md` whole-file entry in `check-sync.sh` `KNOWN_DIFFS` | Root ↔ `templates/base/` drift in `AGENTS.md` goes unreported for reasons unrelated to this feature | Phase 1 must not touch `templates/`, per the plan's scope | Phase 2 lands the template-facing overlay rollout | This report (C2-3); `docs/reports/sync-docs-2026-08-17-overlay-scaffold-v2.md` |

Both rows should be written into `docs/tech-debt/README.md` before `/pr`; this report is not a substitute for the register.

## Recommendation (cycle 2)

- Merge: **Yes, with follow-ups.** No CRITICAL and no HIGH findings, so there is no stop condition. The three cross-review fixes in `1ef5be7` are the strongest work in the PR — each pairs a real behavioral gap with a test that asserts the absence of a side effect, and the `ManifestRemove` signal turns out to close a partial-failure hole neither review had named.
- Before `/pr`, in priority order:
  1. **C2-1** — correct the `AdvisoryEntry` doc a second time. This is the same contract cycle 1 flagged; shipping a third reader-facing description that the renderer does not implement is the finding most likely to cost Phase 3 real time.
  2. **C2-3 + C2-4** — add the two `docs/tech-debt/README.md` rows above, and add the `check-sync.sh` edit to the plan's Deviations list. At cycle 2/2 this is the only mechanism that survives plan archival.
  3. **C2-2** — at minimum reword the test's doc comment so it does not claim an anchoring it does not perform.
  4. **C2-5 / C2-6 / C2-7 / C2-8** — one-line each; C2-8 (`insights-append.sh` for the missing `verify` and `sync_docs` events) is the cheapest and is pure artifact hygiene.
- Known gaps in this review: unchanged from cycle 1 — static analysis, spec compliance, test execution, and documentation drift belong to `/verify`, `/test`, and `/sync-docs`. This section additionally did not re-review the cycle-1 code that `2e58116..HEAD` left untouched.
