# Self-review report: org-runtime-mechanism

- Date: 2026-08-01
- Plan: `docs/plans/active/2026-08-01-org-runtime-mechanism.md`
- Reviewer: `reviewer` subagent (Claude Code, `/self-review`)
- Scope: diff quality only for `git diff 9abaaed...HEAD` on `docs/spec-org-runtime`. Authored artifacts (`docs/specs/`, `docs/plans/`) excluded. Spec compliance, test coverage, and documentation drift are explicitly out of scope (`/verify`, `/test`, `/sync-docs`).

## Evidence reviewed

- `git diff --stat 9abaaed...HEAD` — 33 files, +5233/-5. 12 commits (`a237ed0`..`ceb16c4`).
- Full read: `internal/org/{spawn,verbs,manifest,envelope,receipts,seat}.go`, `internal/org/driver/{driver,herdr,agmsg,probe}.go`, `internal/cli/org.go`.
- Diff read: `internal/cli/doctor.go`, `internal/cli/root.go`, `internal/config/config.go`, `internal/config/defaults_sync_test.go`, `scripts/ralph-config.sh`, `templates/base/ralph.toml`, `AGENTS.md`, `templates/base/AGENTS.md`.
- Targeted greps: dead-symbol usage (`NewManifestStore`, `ManifestRelPath`, `AgentGet`, `PaneRun`, `History`, `TeamMembers`, `Mode`), `RALPH_ORG_*` readers, `TODO|FIXME|XXX|HACK|panic(`, secret-shaped literals, U+FFFD.
- Mirror parity: `cmp scripts/ralph-config.sh templates/base/scripts/ralph-config.sh` → identical; `cmp AGENTS.md templates/base/AGENTS.md` → identical.
- Working tree clean; no generated artifacts (`coverage.out`, `*.prof`, `bin/`, `.harness/state/`) in the diff.

No tests, linters, formatters, or static analysis were run (skill scope).

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | The herdr **agent name** is the raw `seat_id`, not namespaced by `org_id`, while the agmsg team name *is* (`ralph-<org_id>`). Two concurrent orgs that both spawn a seat named `reviewer` register two herdr agents under the same name; `org send` / `org wait` then target whichever one herdr resolves. This defeats the diff's own "org_id 名前空間必須化" design decision at the external-resource boundary — manifest accounting stays isolated, live processes do not. `org wait` makes this visible: it *requires* `--org-id` and then discards it. | `internal/org/spawn.go:189` `AgentStart(ctx, p.SeatID, …)` vs `spawn.go:199,235-237` `agmsgTeam(orgID) = "ralph-"+orgID`; `internal/org/verbs.go:83` `AgentWait(ctx, p.To, …)`; `internal/org/verbs.go:113-117` `WaitParams` has no `OrgID`; `internal/cli/org.go:229-239` requires `--org-id` then builds `WaitParams{Seat: seat, …}` without it | Namespace the herdr agent name the same way the agmsg team is (e.g. `<org_id>/<seat_id>` or `ralph-<org_id>-<seat_id>`), persist it on the manifest event, and have `send`/`wait`/`read`/`stop` resolve the agent name from the seat record instead of re-deriving it from the bare seat id. Until then, `wait` should at least error rather than silently accept an ignored `--org-id`. |
| MEDIUM | maintainability | `ralph org status --all` does not merely *add* dry-run rows — dry-run events can **override real seat state**. With `IncludeDryRun: true`, a `stop --dry-run` event becomes the seat's latest state event (showing a live seat as `stopped`), and a `disband --dry-run` event enters `disbandedIdx` and marks every real seat in the org inactive. This contradicts the plan's "dry-run の監査分離" decision. (`ActiveSeatCount` is unaffected — it always passes `RosterOptions{}` — so `max_seats` accounting is safe; the damage is display-only but misleading.) | `internal/org/manifest.go:132-141` (dry-run filter runs before the disband/state branches), `manifest.go:150-158`; `internal/org/verbs.go:208-212` (`Stop` writes `DryRun: p.DryRun` on a state event), `verbs.go:280-283` (`Disband` likewise) | Track dry-run seats on a separate axis: derive the roster from real events only, then append dry-run entries as a distinct, clearly-labelled set when `--all` is passed. A dry-run event must never become the "latest state" of a real seat, nor mark real seats disbanded. |
| MEDIUM | exception-handling | `org stop --seat <typo>` on a seat that was never spawned appends a `stopped` **state** event and exits 0. Because `Roster` derives seat status from any state event, this materialises a phantom seat in `ralph org status` with role/driver/model all empty. A typo silently pollutes the audit trail and reports success. | `internal/org/verbs.go:192-212` (`if ok { paneID = seat.PaneID }` — the `!ok` case falls through to the append), `internal/cli/org.go:314-319` (prints `stopped seat %q`, returns nil); `internal/org/manifest.go:147-151` | Return a distinct error when the seat is not found in `org_id` (or, if a no-op stop must stay tolerated, record it as a non-state event so it cannot create a roster entry). |
| MEDIUM | readability | `--timeout-ms` is documented as "per-step herdr timeout" but the same value is *also* used as the deadline for a single context that wraps the **entire** saga (workspace create → tab create → agent start → agmsg announce). With the 60000 default a four-step spawn gets 60s total, not 60s per step, and the last step can be cancelled by a deadline the operator believed applied to each step individually. | `internal/org/spawn.go:145-146` (one `context.WithTimeout(…, p.TimeoutMS)` reused at lines 169, 174, 189, 201) vs `internal/cli/org.go:158` `"per-step herdr timeout in milliseconds"`. Same shape in `Send`: `verbs.go:80-83` uses `timeoutMS` for both the ctx and herdr's own `--timeout` | Either create a fresh per-step context from `p.TimeoutMS` and add a separate overall budget, or reword the flag help to "overall spawn timeout" and derive the per-call herdr `--timeout` from the remaining budget. |
| MEDIUM | typo | Six doc comments still describe this PR as config-only / defer the verbs to "later slices", but the same diff ships all seven verbs and the driver adapters. These are the first thing a reader hits in each file and they are now actively wrong. | `internal/config/config.go:94-96` "This PR is config-only: no verbs, no seats"; `templates/base/ralph.toml:62-63` "config-only in PR①: no seats run yet"; `scripts/ralph-config.sh:67-68` "Config-only in PR①"; `internal/org/seat.go:4-6` "no cobra wiring … land in later slices (… the `ralph org` verbs …)" (verbs.go is in this package); `internal/org/envelope.go:11-12` "herdr/agmsg calls are Slice 3/4 concerns; this package validates and records only"; `internal/org/receipts.go:12-13` "callers in later slices" | Sweep all six for the end-state of the PR. The `ralph-config.sh` note is the one partial truth worth keeping ("no `ralph org` verb reads these shell vars — the Go config is the runtime source"); make that explicit rather than implying the whole PR is config-only. |
| MEDIUM | exception-handling | Three manifest/receipt writes on the error paths are discarded with `_ =`. Those exact records are the deliverable of AC-1 (rejection recorded) and AC-10 (`spawn_failed` + compensation traceable) — if the write fails (disk full, read-only `.harness/`, permission), the operator sees only the original validation/step error and the audit trail is silently absent. `.claude/rules/golang.md`: "Keep error handling explicit; do not silently swallow errors." | `internal/org/spawn.go:243` and `:248` (`reject`), `spawn.go:343` (`failStep`), `spawn.go:359` (`compensateStale`) | Join the append error into the returned error (e.g. `errors.Join(cause, appendErr)`) or at minimum surface it on `SpawnResult` so the CLI can print "rejection was NOT recorded to the manifest". |
| MEDIUM | unnecessary-change | `ManifestRelPath`, `ReceiptsRelPath`, `NewManifestStore`, and `NewReceiptStore` have zero callers — not in production, not in tests. The CLI instead hardcodes the same knowledge in two other places: the `--state-dir` default string and two bare filename literals. The path is now expressed three times, and the one place named as authoritative is the one nothing reads. | grep for all four symbols returns only their own definitions (`internal/org/manifest.go:36,45`, `internal/org/receipts.go:37,46`); `internal/cli/org.go:37` `".harness/state/org"`, `org.go:75-76` `filepath.Join(stateDir, "manifest.jsonl")` / `"model-receipts.jsonl"` | Export `ManifestFileName` / `ReceiptsFileName` constants and have `internal/cli/org.go` join against those; delete `NewManifestStore`/`NewReceiptStore`/the Rel-path constants if the root-relative constructor is not the shape the CLI wants. |
| LOW | maintainability | `truncateForDetails` slices by byte (`text[:200]`), which can cut a multi-byte UTF-8 rune in half. `json.Marshal` then substitutes U+FFFD, writing a replacement character into `manifest.jsonl`. Harmless functionally, but this repo runs a dedicated U+FFFD guard (`.claude/hooks/check_mojibake.sh`) precisely because stray U+FFFD is hard to distinguish from real corruption. | `internal/org/verbs.go:104-109`; `sendDetailsMaxLen = 200` at `verbs.go:17` | Truncate on a rune boundary (`[]rune(text)[:n]`, or back off to the last `utf8.RuneStart` byte). |
| LOW | naming | `DisbandResult.StoppedSeats` includes seats whose `Stop` returned an error — the append happens unconditionally after the error is recorded. `ralph org disband` then prints `stopped seat "x"` for a seat it failed to stop, alongside a non-zero exit. | `internal/org/verbs.go:272-275` (append is outside the `if res.Err != nil` guard); `internal/cli/org.go:439-445` | Only append on success, or rename to `AttemptedSeats` and print the per-seat outcome. |
| LOW | unnecessary-change | Two lint-modernisation edits in `internal/cli/doctor.go` are unrelated to org runtime and enlarge the review surface of an otherwise purely additive PR: `strings.Split` → `strings.SplitSeq` in `probeBinary`, and dropping the redundant `err != nil` in `checkCodexEffectiveConfig`. Both are behaviour-preserving (the dropped `err != nil` is provably always true in that `else if`), so this is scope, not correctness. | `internal/cli/doctor.go:173` (commit `55bba4b`), `doctor.go:274` (commit `e1514ff`/`766c1f3` sweep) | Acceptable to keep, but prefer landing incidental lint sweeps as their own commit/PR so a feature diff stays reviewable as one thing. |
| LOW | null-safety | `spawn --dry-run` records a full validated saga trail, but `send --dry-run` skips the seat lookup and pane check entirely and appends a `sent` event for any seat id, existent or not. The plan describes `--dry-run` as "検証・記録のみ" (validate and record); `send`'s dry-run only records. | `internal/org/verbs.go:56-63` (early return before `findSeat`) vs `internal/org/spawn.go:128-134` (`ValidateSpawn` runs before the `DryRun` branch) | Run `findSeat` (and the `pane_id` check) in the dry-run path too, skipping only the driver calls, so a dry run catches a bad `--to` the way a spawn dry run catches a bad `--model`. |
| LOW | maintainability | `max_seats` is enforced across a read-then-append window with no lock: two concurrent `ralph org spawn` processes both read `activeSeats = N`, both pass `ValidateSpawn`, and both append. The plan's edge-case list covers append *line integrity* (`O_APPEND` single write) but not this count race. | `internal/org/spawn.go:120-130` (`Manifest.Read()` → `ActiveSeatCount` → `ValidateSpawn` → later `appendEvent`); `internal/org/manifest.go:222-241` (no advisory lock) | Acceptable for PR① (spawns are operator-driven today), but take a lock file around read+validate+append before a Lead spawns seats programmatically. Logged as tech debt below. |
| LOW | maintainability | Speculative API surface with no production caller: `Herdr.AgentGet`, `Herdr.PaneRun`, `Agmsg.History`, `Agmsg.TeamMembers`, `Agmsg.Mode`, and `HonoredTrue` are exercised only by their own unit tests. Each encodes an unverified assumption about a young external CLI (see the `agmsg.go` ASSUMPTION note) that nothing yet forces to be correct. Relatedly, `doctor --probe-models` issues a real, billed `claude -p ping` per pool entry, unlike every other doctor check (`--version` only). | grep: only `*_test.go` references for all six symbols; `internal/org/driver/probe.go:20`; `internal/cli/doctor.go` Check 13 | Fine to keep given the adapters are the documented isolation seam, but state in `probe.go` / the `--probe-models` flag help that each probe is a billable model invocation, so `doctor --probe-models` is not free. |
| LOW | naming | `EventOrgWorkspaceCreated` lives in `spawn.go` while every sibling event constant is in `seat.go`, and it is deliberately absent from `stateEvents`. A reader grepping `seat.go`'s event block will conclude the list is exhaustive. | `internal/org/spawn.go:21` vs `internal/org/seat.go:30-38,46-54` | Move the constant next to the others and note inline why it is org-level and excluded from `stateEvents`. |

## Positive notes

- **No injection surface.** Every external call goes through `exec.CommandContext(ctx, name, args...)` with an argv slice — no shell, no string interpolation. The `--prompt` value is placed after a literal `--` separator (`herdr.go:39-42`) so a leading-dash prompt cannot be parsed as a flag. No secrets, credentials, `TODO`/`FIXME`, debug prints, `panic()`, or commented-out code anywhere in the new code.
- **Interfaces at the consumption site.** `HerdrClient`/`AgmsgClient` are declared in `internal/org` (`spawn.go:29-44`) rather than in `internal/org/driver`, keeping `internal/org` free of `exec` and matching `.claude/rules/golang.md`. The rationale is written down in the doc comment.
- **Manifest reads are failure-tolerant on purpose and it is documented:** missing file reads as empty, corrupt lines are counted and skipped rather than aborting, and the count is surfaced to the operator (`manifest.go:249-275`, `cli/org.go:419-421`) — `status` genuinely cannot be taken down by one bad line (AC-4's stated intent).
- **Roster ordering avoids a real trap:** "latest" is file/append order, not RFC3339 `TS` parsing, with the second-resolution tie ambiguity called out in the comment (`manifest.go:115-123`).
- **Compensation uses a fresh `context.Background()`** so best-effort cleanup is not cut short by the saga's already-expired deadline (`spawn.go:366-371`) — an easy detail to get wrong.
- **Config validation is strict and each rejection is a distinct, grep-able message** (`config.go` `[org]` block, `envelope.go:29-43`), and the "why absent keys never reach these checks" reasoning is written into the diff rather than left implicit.
- **Three-surface lock-step was actually wired,** not just asserted: `defaults_sync_test.go` gained seven `check(...)` calls with an explicit canonicalisation for the shell's array-less encoding. Both `templates/base/` mirrors are byte-identical (`cmp` exit 0).

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| herdr agent names are not `org_id`-namespaced (agmsg team names are) | Concurrent orgs sharing a seat id collide in herdr's global agent namespace; `org send`/`wait` may act on another org's live seat | PR① is a mechanism layer with operator-driven, typically single-org usage; the namespacing decision belongs with the seat model in PR② | Before PR③ (`ralph org start` / Lead autonomy) can run two orgs concurrently | `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md` |
| `max_seats` read-then-append race in the spawn saga | Two concurrent `ralph org spawn` invocations can both pass the `max_seats` gate and exceed the envelope | Spawns are operator-driven in PR①; a lock file is only needed once a Lead spawns seats programmatically | PR③, when Lead autonomy issues concurrent spawns | `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md` |
| Unverified agmsg CLI flag shape (`--team` / `--as`) centralised in `agmsgArgs` | First live `ralph org spawn` may fail at the `agmsg_announce` step | The real agmsg CLI was not available during PR①; assumption is isolated to one function by design | First live use against a real agmsg install (plan Open questions) | `docs/plans/active/2026-08-01-org-runtime-mechanism.md` (Open questions) |

## Recommendation

- Merge: **Yes, with follow-ups.** No CRITICAL findings; no secrets, no injection surface, no debug code, no generated artifacts, mirrors in sync, working tree clean. The diff is genuinely additive (`internal/cli/root.go` +1 line is the only wiring into an existing path) and the design decisions from the plan — saga trail, `org_id` namespacing, dry-run separation, tri-state receipts — are all visible in the code with their rationale written down.
- Blocking before merge (recommend fixing in this PR):
  - **MEDIUM** `status --all` letting dry-run `stopped`/`disbanded` events override real seat state — it directly defeats a stated design guarantee and is cheap to fix in `Roster`.
  - **MEDIUM** stale "PR① is config-only / later slices" comments in six files — they ship as documentation of the merged state and are wrong on arrival.
- Follow-ups (this PR or next):
  - **HIGH** herdr agent-name namespacing (tech debt logged; must land before two orgs can run concurrently). At minimum, stop accepting a silently-ignored `--org-id` on `org wait` now.
  - **MEDIUM** `org stop` on an unknown seat creating a phantom roster entry with exit 0.
  - **MEDIUM** `--timeout-ms` per-step vs whole-saga semantics — pick one and make flag help and code agree.
  - **MEDIUM** swallowed `appendEvent`/`Receipts.Append` errors on the `reject`/`failStep`/`compensateStale` paths.
  - **MEDIUM** dead `ManifestRelPath`/`ReceiptsRelPath`/`NewManifestStore`/`NewReceiptStore` vs. path literals duplicated in `internal/cli/org.go`.
  - LOW items: rune-safe truncation, `StoppedSeats` accuracy, `send --dry-run` validation parity, `EventOrgWorkspaceCreated` placement, `--probe-models` billing note.
- Known gaps (not evaluated here, by skill scope): spec compliance against `docs/specs/2026-08-01-org-runtime.md` and AC-1..AC-10 → `/verify`; test adequacy for the saga/failure-injection paths → `/test`; documentation drift → `/sync-docs`.
