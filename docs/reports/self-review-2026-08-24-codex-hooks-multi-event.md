# Self-review — codex-hooks-multi-event

- Date: 2026-08-24
- Plan: `docs/plans/active/2026-08-24-codex-hooks-multi-event.md`
- Cycle: 1
- Reviewer: `reviewer` subagent (Claude Code)
- Scope: `git diff main...HEAD` — implementation commit `c8dfdf2` plus plan/evidence bookkeeping (`38612ef`, `7f86d8d`, `ed14f4c`, `0408609`, `8eb6cd6`, `ec12fd4`). 12 files, +618/−58.
- Dimension: diff quality only (naming, readability, unnecessary changes, comment/doc accuracy, error-message quality, test quality, security, maintainability). No test execution, no static analysis, no spec-compliance audit, no doc-drift audit — those belong to `/verify`, `/test`, `/sync-docs`.
- Verdict: **pass** (no CRITICAL, no HIGH). Merge recommendation: **merge after fixing M1 and M3** (both are shipped-text defects that reach downstream projects and are one-paragraph edits); M2 and M4 are acceptable as tracked follow-ups.

## Evidence reviewed

- `.codex/hooks.json` + `templates/base/.codex/hooks.json` (verified byte-identical with `diff`; PostToolUse command string untouched in the diff → trust hash preserved, as the plan's hard constraint requires)
- `internal/cli/doctor.go:356-472` (`codexShippedHookEvents`, `validateCodexHooksJSON`)
- `internal/cli/doctor_hooks_test.go:105-166`, `internal/cli/cli_test.go:380-427`
- `tests/test-hook-wiring.sh:98-247`
- `.codex/README.md` + `templates/base/.codex/README.md` (verified identical)
- `docs/evidence/codex-hooks-multi-event-slice1-2026-08-24.md`, `docs/evidence/codex-hooks-multi-event-fixture-2026-08-24.md`
- `docs/tech-debt/README.md:125`
- Consumers named by the diff but not changed by it: `.claude/hooks/session_start_context.sh`, `.claude/hooks/prompt_gate.sh`, `.claude/hooks/pre_bash_guard.sh`, `.claude/hooks/post_tool_failure_feedback.sh`, `.codex/hooks/README.md`

## Findings

| ID | Severity | Area | Finding | Evidence | Recommendation |
|----|----------|------|---------|----------|----------------|
| M1 | MEDIUM | typo / maintainability | The shipped README states a safety property that the script it names contradicts. The new bullet says `SessionStart` and `UserPromptSubmit` "both drive additionalContext-only hooks (`session_start_context.sh`, `prompt_gate.sh`) **with no write side effects**". `prompt_gate.sh` is indeed pure stdout, but `session_start_context.sh` writes on every run: `mkdir -p .harness/state .harness/logs docs/plans/active docs/plans/archive docs/reports`, then truncates `.harness/state/tool_failures.count` to `0`. That counter is the Claude-side escalation counter read by `post_tool_failure_feedback.sh` — and Codex has no `PostToolUseFailure`, so under Codex the file is only ever *reset*, never incremented. Concretely: every `codex exec` invocation (including the `codex exec review` that `/cross-review` runs from inside a Claude Code session) now clears a Claude session's failure counter in the same repo. The claim is also the plan's own safety rationale for wiring the event without an AC ("session_start_context.sh は additionalContext 出力のみで書き込み副作用なし(安全)"), so the wrong sentence is load-bearing, not decorative. | `templates/base/.codex/README.md:118-120` (and identical `.codex/README.md:118-120`); `.claude/hooks/session_start_context.sh:4` (mkdir), `:5` (counter reset); consumer at `.claude/hooks/post_tool_failure_feedback.sh:5`; plan rationale at `docs/plans/active/2026-08-24-codex-hooks-multi-event.md:28`. | Correct the sentence in both twins: `prompt_gate.sh` is output-only; `session_start_context.sh` creates the `.harness/` and `docs/` scaffold directories and resets `.harness/state/tool_failures.count`, i.e. no git-history side effects (unlike `SessionEnd`/`PreCompact`) but not side-effect-free. If the cross-CLI counter reset is unwanted, that is a separate follow-up — do not silence it by leaving the doc wrong. |
| M2 | MEDIUM | maintainability | The shipped event set now exists as four independent hardcoded copies and no assertion binds the authoritative one to the file it describes. `codexShippedHookEvents` (Go, what `ralph doctor` enforces in every downstream project), `tests/test-hook-wiring.sh`'s `for event in PreToolUse SessionStart UserPromptSubmit`, the `validHooksJSON` fixture, and `doctor_hooks_test.go`'s own literal list. The Go list is pinned only against the *fixture copy* of hooks.json, never against the tracked/embedded `templates/base/.codex/hooks.json`. Failure mode is silent in the direction that matters: add an event to `hooks.json` and the shell test (updated by the same author) goes green while doctor quietly stops covering it; or add one to `codexShippedHookEvents` alone and every scaffolded project starts warning. The doc comment's "Keep in sync with the shipped hooks.json event set" is currently an honour-system instruction. The repo already has the precedent for mechanising exactly this (`internal/config/defaults_sync_test.go` pins a cross-file default). | `internal/cli/doctor.go:358-365`; `tests/test-hook-wiring.sh:180`; `internal/cli/cli_test.go:384-427`; `internal/cli/doctor_hooks_test.go:133`; precedent `internal/config/defaults_sync_test.go`. | Add one small Go test that parses `templates/base/.codex/hooks.json` (or the embedded copy) and asserts its top-level event key set equals `codexShippedHookEvents`. That makes the Go list the single source and turns all remaining copies into consumers. |
| M3 | MEDIUM | unnecessary-change / maintainability | The template README ships meta-repo rollout narrative to downstream readers. Trust-UX item 4 now reads "**This includes adding a brand-new event entry** (e.g. this rollout's `PreToolUse` / `SessionStart` / `UserPromptSubmit` additions) … so **upgrading to this `hooks.json`** requires one more interactive `codex` session … the existing `PostToolUse` entry's command string is unchanged, **so its prior approval still holds**". For a fresh `ralph init` reader none of that is true: there is no prior approval, no upgrade, and all four commands are unapproved. The same wording appears in the `SessionEnd`/`PreCompact` bullet ("deliberately not wired **in this rollout**"). This is the class `check-template-purity.sh` exists to prevent ("template content must not cite meta-repo build history") — it evades the guard only because it is phrased as prose rather than as a plan slug or a dated report path, and the previous PR in this family had a cross-review AR for the same class in the codex config comment. | `templates/base/.codex/README.md:154-161` (trust UX), `:124-125` ("in this rollout"), `:114` ("live-fire confirmed", milder); guard rationale at `scripts/check-template-purity.sh:56-57`. | Rewrite in timeless, reader-relative terms: "each `command` string carries its own approval hash, so adding or editing any event entry needs a fresh interactive approval; approvals for unchanged commands survive." Keep the maintainer-facing upgrade note in the meta-repo copy only if it is genuinely wanted, or drop it — the two files must stay byte-identical, so the phrasing has to work for the downstream reader. |
| M4 | MEDIUM | security / maintainability | Wiring `PreToolUse` ships more than the `deny` path that was live-fired. `pre_bash_guard.sh` emits `ask` for four routine cases (`.git` writes, `.env` writes, `rm -rf `, `gh pr create`), and the README's new bullet only claims that a `deny` "actually blocks the command". Codex's handling of an `ask` permission decision is untested in both evidence docs. The two plausible outcomes are opposite and both matter: fail-open (`ask` ignored → the confirmations silently disappear under Codex, the "illusion of protection" the plan's risk register set out to avoid) or fail-closed (`ask` treated as a block → non-interactive `codex exec`, including CI, hard-fails on `rm -rf` and `gh pr create`). Not a blocker — nothing regresses relative to today's unwired state — but it is an unverified behavior change now shipping to every scaffolded project. | `.claude/hooks/pre_bash_guard.sh:37,40,43,46` (`ask` decisions); README claim at `templates/base/.codex/README.md:114-117`; evidence covers `deny` only — `docs/evidence/codex-hooks-multi-event-slice1-2026-08-24.md:43-59`. | Either live-fire one `ask` case and record the observed semantics next to the `deny` result, or state the gap explicitly (README bullet + a tech-debt row alongside the `SessionEnd`/`PreCompact` one). Prefer the former; it is one probe on the harness that already exists. |
| L1 | LOW | maintainability | The sibling doc in the directory the change is about was not swept. `.codex/hooks/README.md` (and its byte-identical template twin) still describes the wiring as single-event: "`hooks.json` routes `PostToolUse` through …" and "See also: `.codex/hooks.json` — **the wired `PostToolUse` group**". After this diff there are four groups. Filed LOW rather than MEDIUM because nothing in it is *dangerous*, and `/sync-docs` is the designated owner — but it is the same "the migration swept every `.codex` doc except the one about the hook directory itself" shape this file was already caught by in the previous PR. | `.codex/hooks/README.md:6`, `:32`; `templates/base/.codex/hooks/README.md` identical (`diff` clean); neither file appears in `git diff main...HEAD --stat`. | Hand to `/sync-docs`: pluralise both sentences and point the "See also" at the four wired groups. |
| L2 | LOW | readability | The new `PreToolUse` matcher assertion produces a malformed failure message in exactly the cases it is meant to report. `record_fail "$label: hooks.json PreToolUse matcher is $(printf '%s' "$pre_tool_use_matchers" \| tr '\n' ',') , want exactly \"Bash\""` yields `matcher is  , want exactly "Bash"` when the key is absent (empty value, stray space before the comma) and a trailing comma when several groups exist. | `tests/test-hook-wiring.sh:233`. | Quote the value and drop the stray space, e.g. `… matcher is "${pre_tool_use_matchers//$'\n'/,}", want exactly "Bash"`, or special-case the empty rendering as `(none)`. |
| L3 | LOW | readability / unnecessary duplication | The new per-event loop is a near-verbatim copy of the PostToolUse block 45 lines above (same three assertions, same `grep -qF` probes, same heredoc-fed `while` loop), with only the routing `case` pattern tightened to `*"ralph-dispatch.sh $event"*`. PostToolUse could join the loop with its matcher handled separately, which would also remove the asymmetry the copy introduced: the new events record a pass for "has at least one command entry" while PostToolUse records nothing on that dimension, so the pass counts differ per event for no stated reason. | `tests/test-hook-wiring.sh:128-172` (PostToolUse) vs `:174-223` (loop); asymmetric pass at `:188`. | Optional. If left duplicated, note in the header comment why PostToolUse stays separate (its matcher is asserted elsewhere / its command form is the trust-hash-frozen one), so the next contributor does not "fix" it by folding the two and changing the frozen string. |
| L4 | LOW | maintainability | Four new permanent citations point at `docs/plans/active/2026-08-24-codex-hooks-multi-event.md`, which `/pr` moves to `docs/plans/archive/` as part of this very task. The repo's own better form is one file away: `internal/config/config.go:31` writes "See docs/plans/active/… (or its archived copy)". The dominant convention is the bare active path, so this is convention-consistent, not a rule break — but the pointers are known-stale on merge day. | `internal/cli/doctor.go:362`; `internal/cli/cli_test.go:383`; `internal/cli/doctor_hooks_test.go:108`; `tests/test-hook-wiring.sh:174`. | Add "(or its archived copy)" to the four new citations, matching `config.go`. Alternatively cite the durable artifact — `docs/evidence/codex-hooks-multi-event-slice1-2026-08-24.md` — which the shell test's other comments already do. |

Counts: **CRITICAL 0, HIGH 0, MEDIUM 4, LOW 4.**

## Targeted checks (no finding filed)

- **Trust-hash constraint.** The existing `PostToolUse` entry is untouched in the diff: only the `description` string and the three appended event keys changed in both `hooks.json` copies. Constraint honored.
- **Twin identity.** `diff .codex/hooks.json templates/base/.codex/hooks.json` and `diff .codex/README.md templates/base/.codex/README.md` are both clean; `check_codex_hooks_json_byte_identical` still runs (`tests/test-hook-wiring.sh:443`) and `check_codex_hooks_json` is invoked for both surfaces (`:434`, `:439`), so the new per-event assertions apply to root and template alike.
- **Shipped `.d` layers exist.** `PreToolUse.d/10-pre-bash-guard.sh`, `SessionStart.d/10-session-start-context.sh`, `UserPromptSubmit.d/10-prompt-gate.sh` are present under both `.claude/hooks/` and `templates/base/.claude/hooks/`, so no shipped event routes into an empty layer set.
- **Validator rewrite is behavior-preserving for the old case.** The PostToolUse finding string is unchanged (`hooks.json PostToolUse has no handler routed through ralph-dispatch.sh`), findings are emitted in `codexShippedHookEvents` order rather than Go's randomized map order, and the shipped-event membership test (`if _, shipped := dispatcherRoutedByEvent[eventName]; shipped && …`) correctly leaves non-shipped events (e.g. a downstream `Stop` entry) unflagged while still schema-checking them. Slightly indirect to read — the discarded value *is* the flag being set — but not wrong.
- **New Go tests fail on a revert.** Deleting any of the three events from `codexShippedHookEvents` makes `TestValidateCodexHooksJSON_PostToolUseOnly_FlagsMissingShippedEvents` fail; deleting an event from the shipped hooks.json makes the shell test fail. The negative test also guards against the inverse mistake (PostToolUse falsely flagged as missing). The remaining gap is M2's Go-list ↔ shipped-file binding.
- **Non-goal guard.** The `SessionEnd`/`PreCompact` absence assertion (`tests/test-hook-wiring.sh:240-246`) turns a plan non-goal into a mechanical gate rather than a comment. Good pattern; it survives plan archival.
- **Evidence hygiene.** Both evidence docs redact machine-absolute paths (`<tmp>`), contain no credentials or tokens, and state cleanup explicitly. `docs/evidence/codex-hooks-multi-event-slice1-2026-08-24.md:47-57` proves the AC-4 claim with filesystem state (marker file absent), not just log lines — the heading matches the data.
- **Secrets / debug code.** No hardcoded credentials, no leftover probe scripts, no commented-out code in the diff. The temporary probes described in the evidence (`99-probe.sh`, `50-deny-probe.sh`) are not present in the tree (`.claude/hooks/*.d/` contain only the `10-*` shims).
- **Tech-debt row.** The new row (`docs/tech-debt/README.md:125`) has all five cells populated, a concrete trigger, and a live plan pointer; it matches the deferral the plan's Non-goals declare. English-language row is consistent with the header and with row 116.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| No assertion binds `codexShippedHookEvents` to the shipped `templates/base/.codex/hooks.json`; the event set exists as four hardcoded copies (M2) | Drift is silent in the direction that matters — doctor stops covering a shipped event, or every scaffolded project warns about an event ralph does not ship | Only if the operator judges the sync test out of scope for this PR; it is a ~20-line test with in-repo precedent (`internal/config/defaults_sync_test.go`) | Next touch to `codexShippedHookEvents` or to `.codex/hooks.json`'s event set — whichever comes first | `docs/reports/self-review-2026-08-24-codex-hooks-multi-event.md` (M2) |
| Codex semantics for `pre_bash_guard.sh`'s `ask` permission decisions are unverified; only `deny` was live-fired (M4) | Either the four `ask` confirmations silently vanish under Codex (illusion of protection) or they hard-block non-interactive `codex exec`/CI on `rm -rf` and `gh pr create` | Live-fire probe is cheap but was outside the AC-4 hard condition, which was scoped to `deny` | Next touch to `pre_bash_guard.sh` or to the Codex `PreToolUse` wiring; or the first downstream report of an unexpected block/pass under `codex exec` | `docs/reports/self-review-2026-08-24-codex-hooks-multi-event.md` (M4), `docs/evidence/codex-hooks-multi-event-slice1-2026-08-24.md` |

_(If these rows are accepted as deferrals rather than in-cycle fixes, append them to `docs/tech-debt/README.md` before `/pr` — the plan is archived at `/pr` and nothing else carries them.)_

## Recommendation

- Merge: **yes, after M1 and M3** — both are shipped-text corrections in the byte-identical README twins, each a single paragraph, and both are wrong in a way a downstream reader cannot detect. No CRITICAL or HIGH findings; the pipeline is not blocked.
- Follow-ups: M2 (sync test) and M4 (`ask` live-fire) as in-cycle fixes if cheap, otherwise as the two tech-debt rows above. L1 hands to `/sync-docs`. L2–L4 optional.
- Known gaps: this review did not run tests, static analysis, spec-compliance checks, or a doc-drift audit; AC coverage and green-suite claims belong to `/verify` and `/test`.

---

# Cycle 2

- Date: 2026-08-24
- Cycle: 2 (fix-and-revalidate after cross-review ACTION_REQUIRED #1)
- Reviewer: `reviewer` subagent (Claude Code)
- Scope: `c86eafb..HEAD` (`c86eafb` cycle-1 fixes, `7a98297`/`fbdcac5`/`ffc67de` reports, `5a2b80c` sync-docs, `8c2901f` triage, **`381b938` AR#1 fix**), re-confirmed against the full `git diff main...HEAD` (23 files, +1108/−75).
- Dimension: diff quality only. No test execution, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL, no HIGH).
- Merge recommendation: **merge**. C2-1 and C2-2 are each a few lines and are worth taking in-cycle if the operator raises the cap; otherwise they must land as one batched tech-debt row (see "Cycle 2 tech debt / deferrals").

## Cycle-1 findings, re-checked at HEAD

| Cycle-1 ID | Status at HEAD | Evidence |
|---|---|---|
| M1 (README claims `session_start_context.sh` has no write side effects) | **fixed** — `c86eafb` replaced the sentence with the concrete housekeeping list (mkdir + `tool_failures.count` reset) and named the cross-CLI reset explicitly, in both twins | `.codex/README.md:118-128` = `templates/base/.codex/README.md:118-128` (`diff` clean) |
| M2 (no assertion binds `codexShippedHookEvents` to the shipped hooks.json) | **fixed** — `TestCodexShippedHookEventsMatchesShippedHooksJSON` parses the tracked `templates/base/.codex/hooks.json` and compares the sorted key set | `internal/cli/doctor_hooks_test.go:270-322` |
| M3 (template README ships meta-repo rollout narrative) | **fixed** — trust-UX item 4 rewritten in timeless reader-relative terms; "in this rollout" dropped from the SessionEnd/PreCompact bullet. Only `:114` ("live-fire confirmed the real Codex tool name") survives, which cycle 1 rated milder and which reads as a property of Codex, not of this PR | `templates/base/.codex/README.md:157-165`, `:124`; `grep -n "rollout\|this PR\|cycle"` over the template README returns only `:114` |
| M4 (`ask` semantics under Codex unverified) | **deferred and tracked** — register row added by `c86eafb` with all five cells populated and a concrete trigger | `docs/tech-debt/README.md` (last row) |
| L1 (`.codex/hooks/README.md` still single-event) | **fixed** — both twins pluralised, "See also" now names the four groups | `.codex/hooks/README.md:3-11`, `:33-34`; twin `diff` clean |
| L2 (malformed `record_fail` message for the PreToolUse matcher) | **open, untracked** | `tests/test-hook-wiring.sh:233` still `… matcher is $(… \| tr '\n' ',') , want exactly "Bash"` |
| L3 (per-event loop duplicates the PostToolUse block) | **open, untracked** — and it is now load-bearing, see C2-1 | `tests/test-hook-wiring.sh:136-172` vs `:180-223` |
| L4 (four citations point at the plan `/pr` will archive) | **open, untracked** | `internal/cli/doctor.go:362`, `internal/cli/cli_test.go:383`, `internal/cli/doctor_hooks_test.go:111`, `tests/test-hook-wiring.sh:174` |

## Cycle-2 findings

| ID | Severity | Area | Finding | Evidence | Recommendation |
|----|----------|------|---------|----------|----------------|
| C2-1 | MEDIUM | partial fix / gate coverage | **The AR#1 fix landed in `doctor.go` but not in the sibling shell gate, and the triage recorded the opposite as its scoping reason.** The triage's ACTION_REQUIRED rationale reads "tests/test-hook-wiring.sh は出荷ファイル自体には event 引数照合済みのため、修正対象は doctor 側のみ". That is true of the new per-event loop (`case "$event_cmd" in *"ralph-dispatch.sh $event"*)`), which this PR added, but false of the **PostToolUse** arm 45 lines above it, which still matches the bare basename: `case "$cmd" in *ralph-dispatch.sh*) routed=1 ;;`. So the exact mis-wiring class AR#1 named — a `command` that references the dispatcher with another event's argument — is caught by `ralph doctor` for all four events but by the shell gate for only three, and the one it misses is the highest-traffic event and the trust-hash-frozen one. Secondary: the shell loop's glob has no boundary check, so `ralph-dispatch.sh PreToolUseExtra` satisfies `*"ralph-dispatch.sh PreToolUse"*` — precisely the case the Go fix added `(?:[^A-Za-z0-9_]\|$)` and a dedicated test for. Cycle-1 L3 flagged the duplication as cosmetic; AR#1 turned it into a coverage gap. | `tests/test-hook-wiring.sh:139-141` (PostToolUse, bare) vs `:193-195` (loop, event-matched, no boundary); `internal/cli/doctor.go:376-382`, `:468`; triage rationale in `docs/reports/cross-review-triage-codex-hooks-multi-event.md` (ACTION_REQUIRED row 1, "Affected file(s)" column names only doctor). | Tighten the PostToolUse arm to `*"ralph-dispatch.sh PostToolUse"*` (one line), or fold PostToolUse into the loop as cycle-1 L3 proposed and keep its matcher/cd-prefix assertions separate. Either way also correct the triage row's rationale sentence, which is a merged artifact asserting coverage that does not exist. |
| C2-2 | MEDIUM | error message / maintainability | **The finding string was not updated for the failure mode the fix introduced.** `validateCodexHooksJSON` still emits `hooks.json <event> has no handler routed through ralph-dispatch.sh` — now also for a command that *visibly does* invoke `ralph-dispatch.sh`, just with the wrong event argument. An operator reading that warn will open `hooks.json`, see `ralph-dispatch.sh` in the command, and conclude `ralph doctor` is broken; the actual defect (wrong argument) is never named. The fix's own test comment has to spell the discrepancy out in prose because the string cannot ("the dispatcher call does not count for the event it's wired under"), which is the tell. The `.codex/README.md` summary of what doctor checks ("present, valid JSON, schema-conformant, routed through the dispatcher") undercounts the same way. | `internal/cli/doctor.go:483-487` (unchanged by `381b938`); `internal/cli/doctor_hooks_test.go:226-236` (the mis-wired fixture asserts exactly this string); `templates/base/.codex/README.md:167-168`. | Reword to name the real condition, e.g. `hooks.json %s has no handler invoking ralph-dispatch.sh with the %s event argument`, and update the two test `want` strings plus `internal/cli/doctor_hooks_test.go:137`'s constructed expectation. Optionally extend the README sentence to "routed through the dispatcher **with the matching event argument**". |
| C2-3 | LOW | null safety / false positive | **The new regexp rejects a quoted event argument.** `dispatchEventArgRe` requires the event name to follow whitespace immediately, so `./.claude/hooks/ralph-dispatch.sh "PostToolUse"` — legal shell, semantically identical, arguably better hygiene — is reported as unrouted. Probed directly against the compiled expression: unquoted `true`, double-quoted `false`, single-quoted `false`, `PostToolUseExtra` `false` (correct), `… PostToolUse --flag` `true`, `bash … PostToolUse 2>&1` `true`. Pre-fix the quoted form passed. Bounded impact — this check is warn-only and never `--strict`-eligible (doctor.go:392-395) — but it is a permanent false warn in a downstream project's `ralph doctor`, in the one check whose purpose is detecting downstream wiring drift. | `internal/cli/doctor.go:379`; probe run against `regexp.MustCompile("ralph-dispatch\\.sh\\s+" + QuoteMeta(ev) + "(?:[^A-Za-z0-9_]\|$)")`. | Accept an optional surrounding quote: `ralph-dispatch\.sh\s+["']?` + `QuoteMeta(ev)` + `(?:["']\|[^A-Za-z0-9_]\|$)` (the trailing quote already matches the existing class), and add the quoted form as a third case in `TestValidateCodexHooksJSON_DispatchEventArgRe_PrefixEventNameDoesNotMatch`. |
| C2-4 | LOW | dead code / naming | **Residue in the fixed function.** (a) `dispatcherRoutedByEvent` is still pre-populated with `false` for every shipped event at `:412-415`, but nothing reads membership any more — `381b938` moved the `shipped` test to `dispatchEventArgRe`, and the final loop at `:483` reads the map with Go's zero-value semantics. The loop is now a no-op that misleads a reader into thinking membership is load-bearing (cycle 1 explicitly called that idiom out as "the discarded value *is* the flag being set"). (b) `dispatchEventArgRe` is a `map[string]*regexp.Regexp`, but the singular `…Re` suffix matches `hookScriptBasenameRe` one declaration above, which is a single `*regexp.Regexp`. | `internal/cli/doctor.go:412-415`, `:468`, `:483-487`; sibling `:356`. | Drop the pre-population loop (keep the sized `make`) or restore membership as the map's job; rename to `dispatchEventArgReByEvent` (or `…Res`) to match the type. |
| C2-5 | LOW | stale symbol name | **A rename from the implementation commit left one mention behind.** `c8dfdf2` renamed `dispatcherRouted` → `dispatcherRoutedByEvent`; `internal/cli/cli_test.go:732` still says "(so dispatcherRouted is satisfied)". `grep -rn "dispatcherRouted\b" --include="*.go" .` returns exactly that one line, and it now grep-matches nothing in non-test code — the failure mode `.claude/rules/ralph/architecture.md` ("prefer grep-able names") is written against. | `git show main:internal/cli/doctor.go` lines 384/438/452 (old name) vs `internal/cli/doctor.go:412` (new name); leftover at `internal/cli/cli_test.go:732`. | One-word edit to `dispatcherRoutedByEvent`. |
| C2-6 | LOW | deferral tracking | **Three cycle-1 LOWs are open at HEAD and appear in neither a fix nor the register.** L2 (`tests/test-hook-wiring.sh:233` still emits `matcher is  , want exactly "Bash"` when the key is absent), L3 (duplicated block — now upgraded to C2-1), L4 (four `docs/plans/active/…` citations that `/pr` archives in this same task). Cycle 1 rated them "optional", which was true as a recommendation; at cap 2/2 with the plan archived at `/pr`, "optional and not done" is a deferral and nothing else carries it. | `tests/test-hook-wiring.sh:233`; `internal/cli/doctor.go:362`, `internal/cli/cli_test.go:383`, `internal/cli/doctor_hooks_test.go:111`, `tests/test-hook-wiring.sh:174`; `docs/tech-debt/README.md` (no matching row). | Fix L2 and L4 in-cycle (a quoted variable and four `(or its archived copy)` suffixes, matching `internal/config/config.go:31`), or add one batched register row covering L2/L3/L4 together with C2-1..C2-5, as the "Cycle 2 tech debt" table below proposes. |
| C2-7 | LOW | bookkeeping | **This cycle leaves almost no trace in the plan, and one pipeline phase never emitted its insight event.** (a) `docs/insights/events/2026-08-24-codex-hooks-multi-event.jsonl` has four lines — `self_review`, `verify`, `test`, `cross_review` — but no `sync_docs` line, although `5a2b80c` wrote `docs/reports/sync-docs-2026-08-24-codex-hooks-multi-event.md`; `ralph insights` therefore under-reports the pipeline. (b) The plan's Progress checklist still has `[ ] Post-implementation pipeline` after two full cycles, and the plan has no Deviations/decision-log entry for cycle 2 — the AR#1 strengthening (doctor now requires the matching event argument) is not reflected in AC-5's wording, and `/pr` archives the plan as the durable record. | `docs/insights/events/2026-08-24-codex-hooks-multi-event.jsonl` (4 lines, phases as listed); `docs/plans/active/2026-08-24-codex-hooks-multi-event.md` (Progress checklist; Acceptance criteria AC-5; no Deviations section). | Append the missing `sync_docs` event with `./scripts/insights-append.sh` (and this cycle's `self_review`/`verify`/`test` events with `--cycle 2`), tick the pipeline checkbox at `/pr` time, and add a one-line cycle-2 note to the plan recording the event-argument strengthening. |

Counts: **CRITICAL 0, HIGH 0, MEDIUM 2, LOW 5.**

## Targeted checks (no finding filed)

- **AR#1's tests fail on a revert.** `TestValidateCodexHooksJSON_PreToolUseCallsWrongEvent_FlagsPreToolUseAsUnrouted` goes red under the pre-fix `strings.Contains(cmdVal, "ralph-dispatch.sh")` condition (the fixture's PreToolUse command contains that substring, so pre-fix the event is marked routed and the expected finding is absent). `TestValidateCodexHooksJSON_DispatchEventArgRe_PrefixEventNameDoesNotMatch` cannot compile against the pre-fix tree at all. Neither test is satisfied by a sibling guard, and the fixture's PostToolUse negative assertion pins the inverse mistake.
- **The fix does not widen the check's blast radius.** The `shipped` membership source moved from `dispatcherRoutedByEvent` to `dispatchEventArgRe`, but both maps are built from `codexShippedHookEvents`, so a downstream non-shipped event (e.g. `Stop`) is still schema-checked and never flagged as unrouted. The shipped `hooks.json` still matches (probe: the real command string returns `true`).
- **Twins still byte-identical after two fix commits.** `diff` clean for `.codex/hooks.json`, `.codex/README.md`, `.codex/hooks/README.md` against their `templates/base/` counterparts.
- **M2's fix mirrors the precedent it cites.** `TestCodexShippedHookEventsMatchesShippedHooksJSON` uses `t.Skipf` for the missing-file case, which is exactly what `internal/config/defaults_sync_test.go:97,104,181` does for the same "vendored repo" scenario, and `t.Fatalf` for a present-but-invalid file. Not a fail-open divergence.
- **No new register row was invalidated by a later commit in this PR.** Both rows `c86eafb` added (SessionEnd/PreCompact; `ask` semantics) are still accurate at HEAD; `381b938` touches neither surface. The pre-existing `checkCodexEffectiveConfig` row's parenthetical ("dispatcher ルーティング…は検出する") remains true after the fix.
- **`/sync-docs` introduced no churn.** `5a2b80c` changes exactly one line each in `AGENTS.md`, `README.md`, `docs/architecture/repo-map.md` — the event enumeration — with no whitespace or alignment drift.
- **Secrets / debug code / security.** Nothing added in `c86eafb..HEAD` contains credentials, tokens, machine-absolute paths, leftover probes, or commented-out code. The new regexp is built with `regexp.QuoteMeta` over a package-level constant list, so no untrusted input reaches pattern compilation, and `MustCompile` at init over a fixed list cannot panic at runtime on user data.
- **ID-token hygiene.** `internal/cli/doctor_hooks_test.go:174` cites "the cross-review AR#1 fix" without a cycle or path, while `tests/test-hook-wiring.sh:105,143,163` already use bare `AR#1` for a *different* AR (the previous PR's cd-prefix fix) and the repo's better convention writes `AR#n (cycle N, docs/reports/cross-review-triage-<slug>.md)` (e.g. `internal/cli/upgrade_v2.go:148`). Not filed as a finding because the two files are unrelated surfaces and the triage file for this slug is unambiguous today — but if C2-1/C2-2 are fixed, adding `(cycle 1, docs/reports/cross-review-triage-codex-hooks-multi-event.md)` to that one comment is free.

## Cycle 2 tech debt / deferrals

If the operator ends the pipeline here (cap 2/2) rather than raising it, everything below is a deferral and needs one register row before `/pr`:

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| `tests/test-hook-wiring.sh`'s PostToolUse routing assertion matches the bare `ralph-dispatch.sh` basename while the per-event loop and `ralph doctor` both require the matching event argument (C2-1); doctor's unrouted finding string does not name the wrong-argument failure mode (C2-2); the event-argument regexp rejects a quoted argument (C2-3); post-fix residue in `validateCodexHooksJSON` — dead pre-population loop and a singular `…Re` name on a map (C2-4); one stale `dispatcherRouted` mention (C2-5); cycle-1 L2/L3/L4 (C2-6) | A wrong-event `command` in a shipped `hooks.json` passes the shell CI gate for PostToolUse; downstream operators get a warn that names the wrong defect, or a false warn if they quote the argument. All warn-level — no `--strict` gate is affected and no shipped wiring is wrong today | Reached the default pipeline cap (2/2) after the AR#1 fix; every item is a one-to-five-line edit, so the batch is cheap whenever the file is next touched | Next touch to `validateCodexHooksJSON` / `dispatchEventArgRe` / `tests/test-hook-wiring.sh`'s `check_codex_hooks_json`, or the next `.codex/hooks.json` event-set change | `docs/reports/self-review-2026-08-24-codex-hooks-multi-event.md` (Cycle 2, C2-1..C2-6), `docs/reports/cross-review-triage-codex-hooks-multi-event.md` (ACTION_REQUIRED #1) |

## Recommendation (cycle 2)

- Merge: **yes.** No CRITICAL, no HIGH. The AR#1 fix is correct at the site it was aimed at, is pinned by two tests that genuinely fail on a revert, and preserves the twin/trust-hash constraints.
- Take in-cycle if the cap is raised: C2-1 (one-line glob tighten + one sentence in the triage row) and C2-2 (message reword + three test expectations). C2-3..C2-5 are one-liners worth folding into the same commit.
- Otherwise: add the single batched register row above before `/pr`, and append the missing `sync_docs` insight event (C2-7).
- Known gaps: this review ran no tests, static analysis, spec-compliance checks, or doc-drift audit; AC coverage and green-suite claims belong to `/verify` and `/test`. C2-1's severity assumes `tests/test-hook-wiring.sh` is meta-repo CI only (no `tests/` tree ships downstream) — the downstream-facing gate is the Go check, which is correct.

---

# Cycle 3

- Date: 2026-08-24
- Cycle: 3 (cap raised from 2 by the operator; fix-and-revalidate after cycle-2 cross-review ACTION_REQUIRED #1)
- Reviewer: `reviewer` subagent (Claude Code)
- Scope: `cb854f0..HEAD` — `89251ec` (cycle-2 sync-docs: `.codex/README.md` twin sentence + report section), `5b56773` (cycle-2 `sync_docs` insight event), `6c41189` (AR#1 fix: one-field data correction in the insight jsonl + triage rewrite + cycle-2 `cross_review` event). Re-confirmed against the full `git diff main...HEAD` (23 files, +1368/−122), including `ed2a2d0`, whose contents no self-review pass had yet examined (it is the parent of the commit that carried the cycle-2 report).
- Dimension: diff quality only. No test execution, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL, no HIGH).
- Merge recommendation: **merge.** C3-1 wants one tech-debt row before `/pr`; C3-2..C3-5 are one-line edits or `/pr`-time bookkeeping.

## AR#1 fix verification (the cycle-3 delta's reason for existing)

- **The jsonl edit changed exactly one field on exactly one line.** `git show 6c41189 -- docs/insights/events/2026-08-24-codex-hooks-multi-event.jsonl` is a single `-`/`+` pair whose only textual difference is `"cycle":1` → `"cycle":2` on the `ts:2026-08-24T03:38:24Z` `sync_docs` record, plus one appended `cross_review` line. No other record was touched.
- **The file is still valid JSONL with uniform formatting.** All 10 lines parse as JSON objects; key order is byte-identical across every line (`schema, ts, slug, flow, phase, cycle, verdict, findings, triage, source`); `schema` is `1` everywhere; `slug`/`flow`/`source` are uniform; `ts` values are in ascending order; the file ends with a newline.
- **Phase × cycle coverage is now complete and internally consistent.** Cycle 1: `self_review`, `verify`, `test`, `sync_docs` (backfilled), `cross_review`. Cycle 2: `self_review`, `verify`, `test`, `sync_docs`, `cross_review`. Both `cross_review` records have identical shape (`verdict:"action_required"`, zero `findings`, `triage.action_required:1`), so the cycle-2 row does not diverge from the cycle-1 precedent.
- **The `(cycle 1, docs/reports/cross-review-triage-…md)` pointer added by `ed2a2d0` still resolves after the in-place rewrite.** `6c41189` replaced the whole triage body, so `AR#1` in that file now names the jsonl data fix — but the rewrite kept a "前 cycle からの解消済み所見" entry that names cycle-1 AR#1 explicitly and attributes it to `381b938`/`ed2a2d0`. The cycle-2 report's "ID-token hygiene" note is therefore closed, not merely deferred.

## Cycle-2 findings, re-checked at HEAD

| Cycle-2 ID | Status at HEAD | Evidence |
|---|---|---|
| C2-1 (shell gate's PostToolUse arm matched the bare basename) | **fixed** — the PostToolUse block was folded into the four-event loop with a boundary-checked glob. Probed under `/bin/sh` (not zsh): the shipped command → routed; `…PostToolUse --flag` → routed; `…PostToolUseExtra`, `…PostToolUse_extra` → not routed. The negation uses POSIX `[!A-Za-z0-9_]`, not the bash-only `[^…]` form | `tests/test-hook-wiring.sh:155-157`, `:133-140` (header comment explains the fold and the boundary) |
| C2-2 (finding string did not name the wrong-argument failure mode) | **fixed** — now `hooks.json %s has no handler routed through ralph-dispatch.sh with the matching event argument` | `internal/cli/doctor.go:490` |
| C2-3 (regexp rejected a quoted event argument) | **fixed** — probed the compiled expression at HEAD over 12 variants: unquoted `true`, double-quoted `true`, single-quoted `true`, `…PostToolUseExtra` in all three quotings `false`, `…PostToolUse --flag` `true`, `bash … PostToolUse 2>&1` `true`, extra whitespace `true`, `$EVENT` `false`, `…PostToolUse_extra` `false`, and `… "PreToolUse" # PostToolUse` `false` (no false positive from a trailing comment) | `internal/cli/doctor.go:384`; two new assertions at `internal/cli/doctor_hooks_test.go:274`, `:277` name C2-3 |
| C2-4 (dead pre-population loop; singular `…Re` name on a map) | **fixed** — the loop is gone and replaced by a comment stating why zero-value reads suffice; renamed to `dispatchEventArgRes` at all 5 sites plus 4 test assertions | `internal/cli/doctor.go:417-420`, `:381`, `:473` |
| C2-5 (stale `dispatcherRouted` mention) | **fixed** — `grep -rn "dispatcherRouted\b" --include="*.go" .` returns nothing | — |
| C2-6 (cycle-1 L2/L3/L4 open and untracked) | **fixed, all three** — L2: the matcher failure message now renders `(none)` for the empty case and quotes the value (`tests/test-hook-wiring.sh:198-204`); L3: the duplicated block is folded into the loop; L4: no `docs/plans/active/2026-08-24-…` citation remains in any `*.go`/`*.sh`, all four now read "the codex-hooks-multi-event plan, docs/plans/" | `grep -rn "docs/plans/active/2026-08-24-codex-hooks-multi-event" --include="*.go" --include="*.sh" .` → empty |
| C2-7 (a) missing `sync_docs` insight event | **fixed** — both cycles' `sync_docs` events are present and correctly stamped | jsonl lines 6 and 9 |
| C2-7 (b) plan bookkeeping | **open, untracked** — see C3-4 | `docs/plans/active/2026-08-24-codex-hooks-multi-event.md:66`, `:129`; no Deviations section |

No cycle-2 finding regressed, and the batched tech-debt row the cycle-2 report proposed as a fallback is correctly *absent* from `docs/tech-debt/README.md` — every item it covered was fixed in `ed2a2d0`.

## Cycle-3 findings

| ID | Severity | Area | Finding | Evidence | Recommendation |
|----|----------|------|---------|----------|----------------|
| C3-1 | MEDIUM | maintainability / recurrence | **The AR#1 fix corrects the one bad value and leaves the producer that wrote it unchanged — for the third time in this repo.** The defect is "a post-implementation skill appends an insight event without `--cycle`, and `scripts/insights-append.sh` silently defaults it to `1`". `docs/tech-debt/README.md:109` records the *same class* from `overlay-scaffold-v2-p2` ("two entries … are stamped `cycle:1` although they were appended by cycle-2 fix commits … and cycle 2 has no `self_review` event at all"), struck through as RESOLVED 2026-08-18 by editing those rows — producer untouched. This PR then hit it twice: cycle-1's `sync_docs` event was missing entirely (C2-7a) and cycle-2's was mis-stamped (AR#1). The producer-side reason is visible: `.claude/skills/sync-docs/SKILL.md` (and its `.agents/` mirror) contains **no** `insights-append.sh` step at all — `grep -i insight` returns nothing — while `self-review`, `verify`, `test`, and `cross-review` all carry one; and none of those four snippets includes `--cycle`, so the copy-paste path always produces `cycle:1`. `scripts/insights-append.sh:173-176` justifies the default with "source:skill events written by post-implementation skills also use cycle 1 for the standard flow where `--cycle` is typically omitted" — a comment this very file now contradicts (5 of its 10 `source:"skill"` records are cycle ≥ 2). The operator having to spell out `--cycle 3` in this review's own dispatch instruction is the same compensation. | `docs/tech-debt/README.md:109`; `.claude/skills/sync-docs/SKILL.md` (no insight section) vs `.claude/skills/{self-review,verify,test,cross-review}/SKILL.md`; `scripts/insights-append.sh:54`, `:173-176`; `docs/insights/events/2026-08-24-codex-hooks-multi-event.jsonl` lines 5-10. | Out of this PR's scope as a code change — add **one** tech-debt row before `/pr` naming the producer, not the row: add an `insights-append.sh` step to `sync-docs/SKILL.md` + its `.agents/` mirror, and put `--cycle <N>` in all five snippets (or make the script require `--cycle` for `--source skill`). Reference `docs/tech-debt/README.md:109` so the third occurrence is visible from the first. |
| C3-2 | LOW | stale evidence | **A cycle-2 report claim was invalidated by its own descendant commit in this PR.** `docs/reports/sync-docs-…md` (written by `89251ec`) states, of the triage row's stale rationale sentence: "Checked the current file: it was already corrected in commit `ef03408` … The verify report's own note is now itself a stale snapshot of a since-fixed doc, not a live gap — no action needed here." Two commits later `6c41189` rewrote that triage file wholesale for cycle 2, so neither the sentence `ef03408` fixed nor the row it lived in exists at HEAD. The *conclusion* (no action needed) still holds; the *evidence trail* points at a file state that no longer exists, and `/cross-review` will rewrite the same file again in cycle 3. | `docs/reports/sync-docs-2026-08-24-codex-hooks-multi-event.md` (Cycle 2, "What was checked and found already in sync", triage bullet); `git show ef03408 -- docs/reports/cross-review-triage-codex-hooks-multi-event.md` (one-row rationale edit) vs `git show 6c41189 -- …` (full rewrite). | One sentence: qualify it as "verified at `ef03408`; that file is rewritten in place each cycle, so re-read it at HEAD rather than trusting this note". Same class as the pointer-decay findings already recorded for `overlay-scaffold-v2-p2`/`-p5`. |
| C3-3 | LOW | bookkeeping | **The triage report's header asserts a pipeline state the operator has already changed.** At HEAD it reads `Cycle: 2/2 (cap reached)`, but the cap was raised to 3 and cycle 3 is running. `/cross-review` rewrites this file each cycle so it will self-correct *if* cycle 3's `/cross-review` runs to completion — if the operator instead proceeds straight to `/pr`, the merged artifact ships a false "cap reached" claim. This is the fourth PR in which a `cap N/N` justification was falsified by a later cap raise. | `docs/reports/cross-review-triage-codex-hooks-multi-event.md` (header, `Cycle:` line); `docs/tech-debt/README.md:109` and the `checkCodexEffectiveConfig` row both carry `cap 3/3` narratives from earlier PRs for the same reason. | Confirm at `/pr` that the header reads `3/3` (or drop the `(cap reached)` annotation and let the counter speak). Better producer-side fix: `/cross-review` should write the cap it observed at write time, not a "reached" verdict. |
| C3-4 | LOW | plan hygiene / deferral tracking | **C2-7(b) is unaddressed and reaches neither a fix nor the register, on what may be the final cycle.** The plan is the durable record after `/pr` archives it, and at HEAD: (a) AC-5 still reads "doctor が配布対象イベント集合の各イベントについて dispatcher ルーティングの**存在**を検査し" — the check now requires the matching event argument, which is the entire substance of cycle-1 AR#1; (b) there is no Deviations / decision-log entry for cycle 2 or cycle 3, so neither the AR#1 strengthening nor the cap raise leaves a trace; (c) the Progress checklist still shows `[ ] Post-implementation pipeline` and `[ ] /pr` after three full cycles. | `docs/plans/active/2026-08-24-codex-hooks-multi-event.md:66` (AC-5), `:129-130` (checklist); `grep -n "Deviation" <plan>` → empty. | Three small edits at `/pr` time: extend AC-5's wording to "…dispatcher ルーティング(イベント引数一致)の存在を検査し", add a two-line Deviations entry naming `381b938`/`ed2a2d0`/`6c41189` and the cap raise, tick the checklist. If any is skipped, it belongs in the C3-1 register row. |
| C3-5 | LOW | gate divergence | **`ed2a2d0` unified the two gates on the event-argument dimension and left them divergent on quoting — in opposite directions.** The C2-3 fix made `ralph doctor` deliberately *accept* `ralph-dispatch.sh "PostToolUse"` (a new test asserts it), while the shell gate's `case` glob in the same commit accepts only the bare form. Probed under `/bin/sh`: the quoted command returns `no`. So a contributor who takes doctor's blessing and quotes the event argument in the shipped `hooks.json` gets a clean `ralph doctor` and a red CI (`hooks.json PostToolUse does not route through ralph-dispatch.sh PostToolUse`) — the same "two gates, one file, different answers" shape C2-1 was filed for. Defensible as intent (the shell gate pins ralph's own shipped form exactly, as it also does for the `cd` prefix), but nothing says so. | `internal/cli/doctor.go:384` (`["']?` … `(?:["']\|…)`) vs `tests/test-hook-wiring.sh:155-157`; `internal/cli/doctor_hooks_test.go:274`, `:277`. | One comment line in the shell loop's header: the glob intentionally pins the exact shipped form and is stricter than `ralph doctor`, which tolerates a quoted argument in downstream files. Alternatively accept `"$event"`/`'$event'` in the glob to match. |

Counts: **CRITICAL 0, HIGH 0, MEDIUM 1, LOW 4.**

## Targeted checks (no finding filed)

- **The cycle-3 delta is data and prose only.** `git diff cb854f0..HEAD` touches 4 files: two byte-identical `.codex/README.md` twins (one sentence), one insight jsonl, one triage report, one sync-docs report. No Go, no shell, no JSON wiring — the trust-hash constraint on the `PostToolUse` command string cannot have moved.
- **Twins still byte-identical after the delta.** `diff .codex/README.md templates/base/.codex/README.md` and `diff .codex/hooks.json templates/base/.codex/hooks.json` are both clean.
- **The new README sentence matches the code it describes.** "each shipped event routed through the dispatcher with that event's own name as the dispatcher argument — not just referenced by basename" is exactly `validateCodexHooksJSON`'s post-`ed2a2d0` behaviour over `codexShippedHookEvents` (4 events). It also closes C2-2's secondary observation that the README summary undercounted the check. No meta-repo build history, plan slug, dated report path, or `tests/`-tree reference enters the shipped template copy.
- **Hygiene of the delta.** No trailing whitespace on any added line, no U+FFFD, no absolute `/Users/` paths, no secrets, no debug or commented-out code.
- **The in-place rewrite is now self-declaring.** The triage header records `Date: 2026-08-24(cycle 2 実行で全面更新)`, which is an improvement over the silent in-place rewrites that produced pointer decay in the two preceding PRs — it tells a reader arriving from an older pointer that the file was replaced.
- **No register row was invalidated by this delta.** The two rows this PR added (`SessionEnd`/`PreCompact`; `ask` semantics) are untouched by `89251ec`/`5b56773`/`6c41189` and remain accurate at HEAD.

## Cycle 3 tech debt / deferrals

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| Insight-event cycle stamping is producer-broken, not row-broken (C3-1): `.claude/skills/sync-docs/SKILL.md` and its `.agents/` mirror carry no `insights-append.sh` step, none of the other four post-implementation skill snippets passes `--cycle`, and `scripts/insights-append.sh` silently defaults it to `1`. The same class was closed instance-only for `overlay-scaffold-v2-p2` (register row 109) and recurred twice in this PR | `ralph insights` mis-attributes fix-and-revalidate cycles for every multi-cycle task in this repo and in every scaffolded project; each occurrence costs a cross-review round trip to correct one character | The producer fix touches the skills mirror pair and `insights-append.sh`, all outside this PR's Affected areas; the data at HEAD is correct | Next touch to `scripts/insights-append.sh` or to any post-implementation SKILL.md pair — add the missing `sync-docs` step, put `--cycle <N>` in all five snippets, and correct the `:173-176` comment | `docs/reports/self-review-2026-08-24-codex-hooks-multi-event.md` (Cycle 3, C3-1), `docs/tech-debt/README.md:109`, `scripts/insights-append.sh`, `.claude/skills/sync-docs/SKILL.md` |

C3-2..C3-5 do not need register rows if they are taken at `/pr` time as described; if `/pr` skips C3-4, fold it into the row above.

## Recommendation (cycle 3)

- Merge: **yes.** No CRITICAL, no HIGH. The AR#1 fix does exactly what it claimed, changes exactly one field on one line, leaves the file valid and uniformly formatted, and touches no executable surface. `ed2a2d0` — reviewed here for the first time — is correct at both gates and is pinned by assertions that were probe-verified against the compiled regexp and the POSIX `case` glob, not reasoned about.
- Before `/pr`: add the single C3-1 register row; apply the C3-4 plan edits; confirm C3-3's header reads `3/3`.
- Optional: C3-2 (one qualifying sentence), C3-5 (one comment line).
- Known gaps: this review ran no tests, static analysis, spec-compliance checks, or doc-drift audit; AC coverage and green-suite claims belong to `/verify` and `/test`. C3-5's severity assumes `tests/test-hook-wiring.sh` is meta-repo CI only — the downstream-facing gate is the Go check, which accepts both forms.
