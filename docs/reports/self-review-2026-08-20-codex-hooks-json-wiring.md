# Self-review — codex-hooks-json-wiring

- Date: 2026-08-20
- Plan: `docs/plans/active/2026-08-20-codex-hooks-json-wiring.md`
- Cycle: 1
- Scope: `git diff main...HEAD` (implementation commits `6717ff6`, `2bda3a1` + plan/evidence bookkeeping `cdd8e2e`, `3b9e3f9`, `95d1140`, `808df48`, `f795732`)
- Dimension: diff quality only (naming, readability, unnecessary changes, comment accuracy, error-message quality, test quality, security, maintainability). No test execution, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL). Merge recommendation: **hold for H1 + M1 + M2** — all three are shipped-text defects that reach downstream projects, and this branch gates v5.0.0.

## Findings

| ID | Severity | Finding | Evidence | Recommendation |
|----|----------|---------|----------|----------------|
| H1 | HIGH | The migration swept every `.codex` doc except the one that documents the hook directory itself. `.codex/hooks/README.md` (and its byte-identical template copy) still states the default-on Codex hooks "are wired in `.codex/config.toml` inline `[[hooks.*]]` entries", ships a TOML sample telling the reader to add `[[hooks.PostToolUse]]` with a direct `check_mojibake.sh` call, and closes with "**Do not add `.codex/hooks.json`** next to the existing inline hook entries". A reader who follows it produces exactly the two states the same commit taught `ralph doctor` and `tests/test-hook-wiring.sh` to flag: a surviving `[hooks]` table (warn / test fail) and a direct hook-script call (test fail). Both copies ship, so downstream projects receive the contradicting instruction. | `.codex/hooks/README.md:3-6` (wiring claim), `:8-12` (TOML sample), `:14-18` ("reference it from `.codex/config.toml`"), `:23-26` (the inverted "do not add hooks.json" rule), `:29-30` (See-also pointing at the `[[hooks.*]]` blocks); identical at `templates/base/.codex/hooks/README.md`. Neither file appears in `git diff main...HEAD --stat`. | Rewrite both copies for the hooks.json wiring in this commit: replace the TOML sample with the shipped hooks.json group, invert the last paragraph ("hooks.json is the source of truth; do not add a `[hooks]` table to config.toml"), and repoint the See-also. This is the same class of gap the shipped `.codex/README.md` rewrite already closed. |
| M1 | MEDIUM | Three shipped surfaces still promise a doctor warning that the new doctor deliberately does not emit. `checkCodexEffectiveConfig` now warns only on an **explicit** `[features] hooks = false` and is intentionally lenient on an absent key ("the official hooks doc does not specify a default … doctor does not assume either value"), pinned by `TestCheckCodexEffectiveConfig_HooksFeatureAbsent_Lenient`. But `.codex/README.md` says doctor "checks … that `[features] hooks = true` is set", `docs/recipes/codex-setup.md` says doctor "warns if `[features] hooks = true` is missing", and the shipped `config.toml` comment asserts the default outright: "Without it, hooks are silently ignored." The config.toml claim and the doctor comment directly contradict each other about whether an absent key disables hooks. | `internal/cli/doctor.go:288-297` (lenient branch + rationale); `internal/cli/cli_test.go:436-449`; `.codex/README.md:29-30` (+ `templates/base/` copy); `docs/recipes/codex-setup.md:24-25` (+ `templates/base/` copy); `.codex/config.toml:32-33` (+ `templates/base/` copy). All five texts were rewritten by this PR. | Pick one story and state it once. Either the docs adopt the leniency ("doctor flags an explicit `hooks = false`; an omitted key is left to Codex's own default") and `config.toml`'s comment drops its unsourced "without it, hooks are silently ignored", or — if that assertion is believed — doctor warns on absent too. Today a reader gets both answers from files in the same commit. |
| M2 | MEDIUM | The shipped README states a matcher causality the repo's own evidence refutes. `.codex/README.md`: "Codex reports its file-edit tool as `apply_patch`, so the literal tool name **has to be in the matcher for the hook to fire**." The live-fire record says the opposite — a `matcher = "Edit\|Write\|MultiEdit"` group (no `apply_patch`) *did* fire on a file edit, and the primary-source note says `apply_patch`/`Edit`/`Write` are all accepted. The sibling `config.toml` comment written in the same commit gets it right ("accepted alongside it for readability and parity"), so the two shipped surfaces disagree. Overstating this converts a conservative belt-and-braces choice into a false requirement a future maintainer will "simplify" against. | `.codex/README.md:98-100` (+ `templates/base/` copy); contradicted by `docs/evidence/codex-hooks-livefire-slice1-2026-08-20.md:16-19` and `docs/plans/active/2026-08-20-codex-hooks-json-wiring.md:20`; contrast `.codex/config.toml:67-70`. | Reword to match config.toml: the payload reports `tool_name=apply_patch`, so the literal name is included as the conservative default; `Edit`/`Write`/`MultiEdit` are kept for readability and Claude-side parity and are also accepted. |
| M3 | MEDIUM | The new "no `[hooks]` table in config.toml" guard can be evaded by a whitespace variant, and the repo already contains the detector that handles it. `grep -qE '^[[:space:]]*\[\[?hooks(\.\|])'` requires `hooks` to abut the bracket, so a reintroduced `[[ hooks.PostToolUse ]]` — valid TOML, and the exact form `scripts/verify.local.sh`'s own fixture uses — is invisible. Probed: a file containing `[[ hooks.PostToolUse ]]` plus a direct `check_mojibake.sh` command is reported clean by this guard. The sibling detector strips whitespace before matching precisely to avoid this. This is a fail-closed guard, so a silent miss is the whole failure mode. | `tests/test-hook-wiring.sh:165` (regex) and its header at `:154-158`; contrast `scripts/verify.local.sh:46-55` (`gsub(/[[:space:]]/, "", line)` then `/^\[\[?hooks(\.\|\]\]?)/`); fixture precedent at `scripts/verify.local.sh:85`. | Normalize whitespace before matching (awk like the sibling, or `grep -qE '^[[:space:]]*\[\[?[[:space:]]*hooks([.[:space:]]\|])'`), and add a spaced-form fixture so the guard's own regression coverage pins it. |
| M4 | MEDIUM | A live gate still tells the operator to delete the new source of truth. `check_codex_hook_single_source` in `scripts/verify.local.sh` fires when config.toml has inline hooks **and** hooks.json exists, and its remediation reads: "Remove one representation; this repo uses `.codex/config.toml` as the source of truth." Post-migration that instruction points at `hooks.json` — the file that actually fires. The gate does not fail today (config.toml has no hooks table), so nothing forced the text to be swept, which is exactly why it went stale. | `scripts/verify.local.sh:58-73`, message at `:69`; source-of-truth inversion at `.codex/config.toml:56-66` and `internal/cli/doctor.go:299-303`. | Invert the message ("hooks.json is the source of truth — remove the config.toml `[hooks]` entries"), matching the wording doctor and `tests/test-hook-wiring.sh` already use. |
| M5 | MEDIUM | The call-site comment for the inverted check still describes the old contract, item for item. `doctor.go:105` reads "Check 3: Codex effective config (project trust + hooks feature + at least one hook)" — the check has never probed project trust, no longer requires the hooks feature (M1), and no longer counts hook entries; it validates hooks.json's schema and dispatcher routing. The function's own doc block 150 lines down was rewritten; this one-liner, which is what a reader scanning `runDoctorFull` sees first, was not. | `internal/cli/doctor.go:105` vs the rewritten doc block at `:251-263` and the body at `:286-324`. | Update to what it checks now: "Codex hook wiring (hooks.json schema + dispatcher routing; stale config.toml `[hooks]` table)". |
| L1 | LOW | The new doc block overstates the check's severity floor. "This is an environment check (warn-level findings) … it never fails under `doctor --strict`" and `validateCodexHooksJSON`'s "callers treat a non-empty result as a warn, never a fail" read as "this check cannot produce a fail". It can: the preserved invalid-TOML branch returns `Status: "fail"`, and `runDoctorFull` flips `allPass` on any `fail` regardless of `strict` — `--strict` only changes `checkScaffoldIntegrity`'s severity. The intended meaning (strict never *escalates* these findings) is right; the phrasing is not. | `internal/cli/doctor.go:262-263` and `:332-335` vs `:280-284` (fail branch, still covered by `TestCheckCodexEffectiveConfig_InvalidTOML_Fails`) and `:145-160` (`allPass` / `--strict` scope at `:83-86`). | Say it precisely: "`--strict` never escalates these findings; the only `fail` this check produces is an unparseable `config.toml`." |
| L2 | LOW | Permanent code comments cite plan-local coordinates that stop resolving once `/pr` archives the plan. `doctor.go` cites "Slice 1 live-fire evidence", "(AC-3b)", and "(Slice 1 open-questions resolution)"; five new test doc comments cite "AC-3b schema-defect case 1…4" and "the Slice 1 open-questions resolution". None names the artifact, and the plan moves to `docs/plans/archive/`. The durable pointer — `docs/evidence/codex-hooks-livefire-slice1-2026-08-20.md` — exists and is committed. | `internal/cli/doctor.go:253`, `:256`, `:291`; `internal/cli/cli_test.go:434`, `:573`, `:591`, `:610`, `:626`. | Replace the bare `Slice 1` / `AC-3b` tokens with the evidence file path (the shell test already cites `docs/evidence/` this way). |
| L3 | LOW | Shipped template content names a gate that does not ship. The rewritten Hooks section tells the reader that "`ralph doctor` / `tests/test-hook-wiring.sh` both flag the stale dual representation", but `templates/base/` has no `tests/` tree, so a scaffolded project has no such file. `check-template-purity.sh` bans `check-sync.sh` from template content for exactly this reason ("any reference to it in template content is a broken instruction for downstream users") but does not cover `tests/`. Downgraded to LOW because precedent already exists — the shipped cross-review skill cites `tests/test-xreview-helpers.sh` the same way — so this is a pre-existing convention, not a new rule break. | `templates/base/.codex/README.md:103-105`; `templates/base/` has no `tests/` directory; precedent at `templates/base/.claude/skills/cross-review/SKILL.md:44`; guard rationale at `scripts/check-template-purity.sh:52`. | Either drop the test-file name from the shipped sentence (keep `ralph doctor`, which does ship) or, if these references are considered acceptable, note the convention once rather than growing it silently. |
| L4 | LOW | The replacement dropped an assertion without saying so. The old `check_codex_config_toml` resolved every Codex hook command to a real file and asserted it exists and is executable; `check_codex_hooks_json` only substring-matches `ralph-dispatch.sh` in the command string. A deleted or non-executable `ralph-dispatch.sh` no longer fails on the Codex side. Impact is small because `check_settings_json` still resolves the same script at both surfaces, and the git-root `$(…)` prefix makes naive path resolution wrong — but the header still advertises the existence check as this script's durable purpose. | `tests/test-hook-wiring.sh:98-136` (new `check_codex_hooks_json`) vs the removed `count`/`abs_path` block in the diff; header at `:4-11`; residual coverage at `:52-96` (`check_settings_json`). | Note in the function header that dispatcher existence is asserted by `check_settings_json` (shell-evaluated commands are not path-resolvable here), so a future reader does not assume the Codex path still checks it. |
| L5 | LOW | `check_no_direct_hook_scripts_in_config_toml` is currently vacuous and silent about it: config.toml has no `command =` assignment lines any more, so `basenames` is empty and the function returns without recording a pass or a fail. In the results block, "guard ran and found nothing" is indistinguishable from "guard never ran" — the same silent-`continue` shape that hides residue. | `tests/test-hook-wiring.sh:216-232` (extraction at `:223`, silent early return at `:225`); `.codex/config.toml` has zero `command =` lines (`grep -n '^[[:space:]]*command' .codex/config.toml` → no match). | Record a pass on the empty path (`"$label: config.toml has no hook command assignments"`), matching how `check_codex_config_toml_no_hooks_tables` reports its clean state. |

Counts: CRITICAL 0, HIGH 1, MEDIUM 5, LOW 5.

## Targeted verification (checks requested in the handoff)

**(a) Schema validator vs the official shape** — matches the shape the evidence records (`hooks` → event keys → matcher-group arrays → `{type:"command", command:<string>}`), and each level is asserted with its own finding: non-array event value, non-object group, missing/non-array `hooks`, non-object handler, missing-or-wrong `type`, missing `command`, non-string `command` (with `array` spelled out rather than `[]interface {}`), empty `command`. Finding strings carry the exact JSON path (`PostToolUse[0].hooks[0]`), which is the actionable part. All four negative cases the plan requires have tests. No finding filed.

**(b) Inverted contract, fail-open/fail-noisy symmetry** — no fail-open found. Every I/O error path warns rather than skipping: a non-`NotExist` read error on hooks.json becomes a finding instead of being swallowed, matching config.toml's own handling. Findings accumulate rather than short-circuiting on the first defect, so one run shows the operator every problem (`[features] hooks = false` + dual representation + schema defects together). The deliberate asymmetries are: absent `config.toml` early-returns before hooks.json is ever inspected (defensible — the whole `.codex/` tree is missing), and dual representation dropped from `fail` to `warn` (stated in the plan's risk register as a migration-noise decision). The one contract gap is documentation, not code: M1. Note that only `PostToolUse` counts toward `dispatcherRouted`, so a project wiring some other event through the dispatcher still warns — correct for today's single-event distribution, worth revisiting when a second event ships.

**(c) Shipped command string vs the live-fire evidence** — byte-identical. `.codex/hooks.json` decodes to `"$(git rev-parse --show-toplevel)/.claude/hooks/ralph-dispatch.sh" PostToolUse`, character-for-character the form in `docs/evidence/codex-hooks-livefire-slice1-2026-08-20.md:8`; matcher `Edit|Write|MultiEdit|apply_patch` matches `:7`. Root and `templates/base/` copies are identical (`diff` clean), and the PR adds `check_codex_hooks_json_byte_identical` so a future one-sided edit fails closed. The Go fixture `validHooksJSON` reproduces the same command string, so a change to the shipped form surfaces in the unit tests too.

**(d) Can the new shell checks false-pass or false-fail?** — command extraction is real jq parsing (`.hooks.PostToolUse // [] | .[] | (.hooks // [])[] | select(.type == "command") | .command`), not a grep approximation, so the `description` field cannot be mistaken for a command; the earlier false positive is properly fixed by scoping to jq-extracted strings on the JSON side and to `command =` assignment lines on the TOML side (comment lines start with `#`, so the prose mentions of `ralph-dispatch.sh` and the secret-guard scripts cannot match). `jq` is required up front (`:27-28`), so its absence fails loudly rather than as a bogus "not valid JSON". Two residues filed: M3 (spaced-table evasion) and L5 (vacuous silent guard); L4 records the assertion that was dropped in the swap.

**(e/f) Comment/doc accuracy and shipped-content purity** — H1, M1, M2, M4, M5 are this dimension. On purity specifically: no leak found in shipped content — `templates/base/.codex/**` and `templates/base/docs/recipes/codex-setup.md` contain no plan slug, no `docs/evidence`/`docs/plans` path, no dated report filename, no `codex-cli 0.147.0` version, and no `2026-08-*` date. The config.toml comment deliberately says "on the codex-cli release this was verified against" instead of pinning the version, which is the right instinct for shipped text. The one near-miss is L3 (`tests/test-hook-wiring.sh` named in shipped content). `.codex/AGENTS.override.md:56` still describes project-level hook requirements as "`[features] hooks = true` + project trust" without the new hook-approval step — untouched by this PR and adjacent to M1; flagging it here rather than as its own row since it belongs to the same one-story fix.

## Positive notes

- The atomic Slice-2 commit is the right call and holds up: wiring, doctor, shell test, and init/manifest owner flip in one commit, so there is no revision where the shipped form and the gates that check it disagree. The rollback note in the plan is accurate — `git revert 6717ff6` restores a consistent state.
- Error-message quality is a step up. `hooks.json PostToolUse[0].hooks[0] "command" must be a string (got array)` tells the operator the file, the path, the expectation, and what was found; the special-case that prints `array` instead of Go's `[]interface {}` is a small, deliberate readability choice.
- The dual-representation check moved from `fail` to `warn` *and* the surviving-table detection moved from "does hooks.json also exist" to "does config.toml still have a table" — the inversion is complete on both axes, not half-applied.
- Test naming carries intent (`_HooksFeatureAbsent_Lenient`, `_DispatcherRoutingMissing_Warns`, `_DeprecatedFeatureFlagKey_TreatedAsAbsent`), and the last of those pins the *changed* expectation for a pre-existing test rather than deleting it — the deprecated-flag case now asserts pass-with-reason instead of quietly disappearing.
- `TestCheckCodexEffectiveConfig_DispatcherRoutingMissing_Warns` is a genuine second axis, not a restatement of the schema tests: a schema-valid file that routes elsewhere still warns.
- The tech-debt register is handled in both directions: the wiring-gap row is struck through with a RESOLVED note that describes what actually landed (including the honest bypass caveat), the row is preserved for traceability, and the stale "only trust approval remains" sentence in the 2026-08-19 row was updated rather than left to rot.
- The evidence file states its own limits plainly — that the shipped git-root command string's proof used `--dangerously-bypass-hook-trust` because its hash differs from the approved absolute-path form. That caveat survives into the tech-debt note instead of being smoothed away.
- No secrets, no debug leftovers, no commented-out code, no unrelated churn in the diff.

## Known gaps

- Not assessed here (other pipeline phases): spec compliance against `docs/specs/2026-08-17-overlay-scaffold-v2.md`, `ralph upgrade`'s handling of a downstream project that already holds an untracked `.codex/hooks.json`, doc-drift beyond the comment/claim accuracy above, and any test execution.
- The live-fire question this PR cannot close by construction: a non-bypass fire of the exact shipped command string requires one interactive re-approval, which the evidence documents rather than claims.

## Merge recommendation

**Hold for H1, M1, M2** — all three are text-only fixes in files this PR already touches (or, for H1, should have touched), all three ship to downstream projects, and this branch gates the v5.0.0 release, so the shipped instructions should not contradict the shipped wiring at the tag. M3 is the one behavioral fix worth doing in the same pass: a fail-closed guard that a valid TOML variant walks past is worth ten lines now.

M4, M5, and the LOW rows can be batched into the same fix commit at low cost; if any are deferred, record them as a single tech-debt row rather than leaving them unstated.

---

# Cycle 2

- Date: 2026-08-20
- Cycle: 2/2 (cap `RALPH_STANDARD_MAX_PIPELINE_CYCLES` = 2 — this is the final cycle unless the operator raises it)
- Scope: the cycle-2 delta only, `git diff d6a04fb..HEAD` — `c72e644` (cycle-1 fix pass, H1/M1-M5/L1-L5), `7af720a` (AGENTS.override.md hooks row), `10432f6`/`4cff65f`/`ae5ece5`/`0217f08` (verify/test/sync-docs artifacts + residual drift), `8aaa27b`/`cb54856` (pre_bash_guard tech-debt row + line-break fix), `cdb0aad` (cross-review triage), `d1df46f` (cross-review AR#1 fix). HEAD at review time: `d1df46f`.
- Dimension: diff quality only. No test execution, no static analysis, no spec-compliance audit, no doc-drift audit.
- Verdict: **pass** (no CRITICAL). Merge recommendation: **fix C2-M1 and C2-M2 before merge** — both are shipped-text defects reaching downstream projects on a branch that gates v5.0.0. The five LOWs are batchable or deferrable as one tech-debt row.

## Re-classification of the cycle-1 findings at HEAD

| Cycle-1 ID | Status at HEAD | Evidence |
|-----|-----|-----|
| H1 (HIGH, `.codex/hooks/README.md` contradicts the new wiring) | **partially fixed** | Both copies rewritten for hooks.json: the TOML sample is gone, the inverted "do not add hooks.json" paragraph now reads "do not add a `[hooks]`/`[[hooks.*]]` table to `.codex/config.toml`", and the See-also repoints at `.codex/hooks.json`. Copies still byte-identical. The one instruction carried forward unchanged is the wrapper escape hatch — see C2-M1. |
| M1 (three surfaces promise a warning doctor does not emit) | **fixed, then re-opened by the AR#1 fix** | `.codex/README.md:29-32`, `docs/recipes/codex-setup.md:24-26`, and `.codex/config.toml:32-36` were all rewritten to the leniency story and now agree with each other and with `checkCodexEffectiveConfig`'s comment. `d1df46f` then added a third branch none of them mentions — see C2-M2. |
| M2 (README asserts `apply_patch` "has to be in the matcher") | **fixed** | `.codex/README.md:100-103` now reads "the payload reports `tool_name=apply_patch`, so the literal name is included as the conservative default; `Edit`/`Write`/`MultiEdit` are kept for readability and Claude-side parity and are also accepted" — matching `.codex/config.toml:72-74` and the live-fire evidence rather than contradicting them. |
| M3 (whitespace variant walks past the fail-closed guard) | **fixed, with a real regression fixture** | `codex_config_toml_has_hooks_table` (`tests/test-hook-wiring.sh:162-177`) replaces the anchored `grep -qE` with the same strip-then-match awk as `codex_config_has_inline_hooks` in `scripts/verify.local.sh:46-56` (mirror claim checked: that function exists and matches). `test_codex_config_toml_hooks_table_detector` (`:180-257`) pins the tight form, the spaced form, and a clean file. Probed the detector directly against `.codex/config.toml` at HEAD: clean, so the reference-comment block does not false-positive. |
| M4 (live gate tells the operator to delete the new source of truth) | **fixed** | `scripts/verify.local.sh:69` now reads "this repo uses .codex/hooks.json as the source of truth — delete the config.toml [hooks]/[[hooks.*]] entries", matching doctor and `tests/test-hook-wiring.sh`. |
| M5 (Check 3 call-site comment describes the old contract) | **fixed, but the new enumeration lost an item** | `internal/cli/doctor.go:105` rewritten as recommended. The rewrite dropped "hooks feature" from the list, and `d1df46f` then strengthened exactly that branch — see C2-L1. |
| L1 (doc block overstates the severity floor) | **fixed** | `doctor.go:264-266` now says "`--strict` never escalates these findings; the only `fail` this check produces is an unparseable `config.toml`". The second site cited in L1 (`validateCodexHooksJSON`'s "callers treat a non-empty result as a warn, never a fail", now `:346-350`) is scoped to that function's own return value and is accurate as written, so no residue. |
| L2 (permanent comments cite plan-local coordinates) | **fixed in the Go/test comments; re-introduced elsewhere** | All eight cited sites now carry `docs/evidence/codex-hooks-livefire-slice1-2026-08-20.md`; `grep -rn "AC-3b\|Slice 1" internal/ tests/ scripts/ .codex/ templates/base/` returns only unrelated `internal/org/` hits (a different AC-3b, another package, untouched by this PR). The sync-docs commit then wrote a fresh plan-path citation into a permanent spec file — see C2-L2. |
| L3 (shipped template content names a gate that does not ship) | **half-fixed** | `.codex/README.md` dropped `tests/test-hook-wiring.sh` ("`ralph doctor` flags the stale dual representation"). The parallel promise in `.codex/config.toml` was not swept — see C2-L3. |
| L4 (replacement dropped the dispatcher-existence assertion silently) | **fixed** | `check_codex_hooks_json`'s header (`tests/test-hook-wiring.sh:99-107`) now states why the Codex side does not path-resolve and where the assertion actually lives. |
| L5 (`check_no_direct_hook_scripts_in_config_toml` vacuous and silent) | **fixed** | `tests/test-hook-wiring.sh:311-314` records `"$label: config.toml has no hook command assignments"` on the empty path instead of returning silently. |

Nothing from cycle 1 was deferred into `docs/tech-debt/README.md` — because nothing needed to be: all eleven were attempted in `c72e644`. Two attempts are incomplete (H1, L3) and two were undone by later commits in the same PR (M1, M5); those four are re-filed below rather than left as "fixed".

## Cycle-2 findings

| ID | Severity | Finding | Evidence | Recommendation |
|----|----------|---------|----------|----------------|
| C2-M1 | MEDIUM | The H1 fix repointed the wrapper escape hatch at a file where the PR's own guard forbids it. `.codex/hooks/README.md` still tells the reader that when a Codex-shaped wrapper is required, "drop the wrapper here and reference it from `.codex/hooks.json`" — but `check_no_direct_hook_scripts_in_hooks_json`, added by this PR, `record_fail`s on any `*.sh` basename in a hooks.json command that is not `ralph-dispatch.sh`, and it runs against both surfaces. The dispatcher only fans out to `.claude/hooks/<event>.d/`, `.ralph/local/hooks/<event>.d/`, and `.claude/hooks/local/<event>.d/`, so a script sitting in `.codex/hooks/` has no legal way to fire. Pre-fix the sentence pointed at `config.toml`, which the sibling guard bans identically — so the directory's entire stated purpose is now unreachable, and following the instruction breaks CI. This is the same defect class H1 named (shipped instruction produces a state the shipped gates flag), surviving the fix. | `.codex/hooks/README.md:11-15` (+ byte-identical `templates/base/` copy); guard at `tests/test-hook-wiring.sh:259-297`, invoked for both surfaces at `:333` and `:338`; dispatcher layer list in `AGENTS.md` repo map. | Say what the escape hatch actually costs: route the wrapper through a `.d` directory if at all possible, and note that a genuine direct `hooks.json` entry requires `check_no_direct_hook_scripts_in_hooks_json`'s allowlist to be widened in the same change. One sentence in both copies. |
| C2-M2 | MEDIUM | The M1 fix authored a closed "only when" claim on six shipped surfaces, and the AR#1 fix four commits later added a case outside it. Every rewritten surface says doctor warns *only* on an explicit `false`: `.codex/README.md` ("that `[features] hooks` is not explicitly disabled (`hooks = false`; an absent key is left to Codex's own undocumented default)"), `docs/recipes/codex-setup.md` ("warns if `[features] hooks = false` is set explicitly"), `.codex/config.toml` ("warns only when this is explicitly set to false") — each with a `templates/base/` twin. At HEAD `checkCodexEffectiveConfig` also warns on a *present but non-boolean* value, so a reader who hits `hooks = "false"` gets a warning three shipped docs say cannot happen. `doctor.go`'s own leniency comment above the branch has the same problem: it still frames the decision as explicit-false vs absent, while the code beneath it now has three arms (the AR#1 rationale lives only inside the `default` case). | `.codex/README.md:29-32`, `docs/recipes/codex-setup.md:24-26`, `.codex/config.toml:32-36` (+ three `templates/base/` twins, all confirmed byte-identical); code at `internal/cli/doctor.go:296-313`; block comment at `:291-295`. | Add the third case once per surface ("…and warns if the key is present with a non-boolean value") and extend the block comment at `:291-295` to cover all three arms. Prefer an open phrasing over a fresh closed list — this is the second time in this PR an "only these" enumeration has been falsified by a later commit in the same PR. |
| C2-L1 | LOW | The M5 fix's replacement enumeration dropped the `[features] hooks` item, and AR#1 then made that branch bigger. `doctor.go:105` now reads "Check 3: Codex hook wiring (hooks.json schema + dispatcher routing; stale config.toml [hooks] table)" — three items, none of them the features check the function still performs and `d1df46f` just expanded from one arm to three. The pre-fix comment did name "hooks feature"; the fix removed it. | `internal/cli/doctor.go:105` vs the body at `:296-313`; pre-fix text visible in `git show c72e644 -- internal/cli/doctor.go`. | Restore the item: "…(`[features] hooks`; hooks.json schema + dispatcher routing; stale config.toml `[hooks]` table)". |
| C2-L2 | LOW | The sync-docs commit wrote a `docs/plans/active/…` pointer into a permanent spec file — the exact citation style cycle-1 L2 removed from the Go comments, and a style already demonstrably broken twice in that same file. The AC-10 bracket cites `docs/plans/active/2026-08-20-codex-hooks-json-wiring.md`, which `/pr` moves to `docs/plans/archive/`. Two pre-existing pointers in the same spec (`docs/plans/active/2026-08-18-overlay-scaffold-v2-p4.md` at `:66`, `…-p3.md` at `:185`) already resolve to nothing — both files are in `docs/plans/archive/` today. Impact is limited because the bracket also cites the durable `docs/evidence/…` path. Separately, the retained clause immediately before the bracket still sends the reader to "docs/tech-debt/README.md の該当行" for an open gap, and that row is now struck through as RESOLVED. | `docs/specs/2026-08-17-overlay-scaffold-v2.md:92`; dead precedents at `:66` and `:185`; `ls docs/plans/active/` contains only the current plan and `.gitkeep`. | Drop the plan path from the bracket (the evidence file already carries the proof), or cite `docs/plans/archive/2026-08-20-codex-hooks-json-wiring.md` since that is where it lands. Worth fixing the two dead pointers in the same touch. |
| C2-L3 | LOW | The L3 sweep removed the non-shipping gate from one surface and left it on the other. `.codex/config.toml:68-69` — byte-identical to `templates/base/.codex/config.toml`, i.e. shipped into every scaffolded project — still reads "`ralph doctor` and the shell hook-wiring test both flag that as a stale duplicate representation", while `templates/base/` has no `tests/` tree. Milder than L3's original instance because it describes the gate rather than naming the file, but it is the same promise to the same reader, and the fix commit demonstrably knew to remove it (it did so in `.codex/README.md` in the same commit). | `.codex/config.toml:68-69` (+ template twin); `templates/base/` has no `tests/` directory; the swept sibling at `.codex/README.md:105-107`. | Drop "and the shell hook-wiring test" from both copies, keeping `ralph doctor`, which does ship. |
| C2-L4 | LOW | The AR#1 finding string prints a raw Go type where the sibling code in the same function deliberately does not. `fmt.Sprintf("[features] hooks has a non-boolean value (%T) …", hv)` renders `hooks = ["x"]` as `[]interface {}` and an inline table as `map[string]interface {}` — the exact Go-internals leak that `validateCodexHooksJSON` special-cases away 100 lines down (`kind := fmt.Sprintf("%T", cmdRaw); if …{ kind = "array" }`), a choice the cycle-1 report called out as a deliberate readability decision. Readability nit in the same hunk: `switch v := hv.(type)` binds `v`, uses it in `case bool`, and then the `default` arm reaches back for `hv` — legal Go, but the reader has to check why. The new test only asserts the substring `"non-boolean"`, so neither shape is pinned. | `internal/cli/doctor.go:310` vs `:409-414`; test at `internal/cli/cli_test.go:473-496`. | Reuse the same TOML-friendly type naming (`array` / `table` / `string`) rather than `%T`, and print `v` in the `default` arm for consistency. |
| C2-L5 | LOW | The new tech-debt row's parenthetical claims guard behavior the named guard cannot produce. The row says `.claude/hooks/pre_bash_guard.sh` "rejects even this bug's own description text (recording this very row required going via python)". That script's only message-related decision is gated on `case "$command" in *"git commit"*"-m "*)`; writing a table row into `docs/tech-debt/README.md` never reaches it. The row's substantive claim is accurate and worth keeping — the guard does deny `git commit -m "$(cat <<'EOF' … EOF)"`, which is precisely the form `git-commit-strategy.md` prescribes, because the deny pattern matches `$(` anywhere inside a double-quoted `-m` argument regardless of whether the payload is shell-expanded. Only the parenthetical overreaches, and it is the sentence that will send the next reader to the wrong script. | `docs/tech-debt/README.md` (last row, added `8aaa27b`, line break fixed in `cb54856`); guard at `.claude/hooks/pre_bash_guard.sh:52-60`; recommended form at `.claude/rules/ralph/git-commit-strategy.md`. Row renders correctly: 7 awk `\|`-fields, identical to the four rows above it. | Drop the parenthetical, or name the mechanism that actually blocked the write (a different hook, or the commit-message attempt rather than the file write). |

Counts: CRITICAL 0, HIGH 0, MEDIUM 2, LOW 5.

## Targeted verification (checks requested in the handoff)

**(a) Is the type-switch warn branch actionable and consistent with its siblings?** Actionable, partly inconsistent. The message names the file section, the defect, and the remedy (`use \`hooks = true\``), which is the right shape and matches the schema validator's house style. The inconsistency is the `%T` rendering — filed as C2-L4. The branch is also correctly placed: it fires only when the key is `present`, so the lenient absent-key contract that `TestCheckCodexEffectiveConfig_HooksFeatureAbsent_Lenient` pins is untouched, and the pre-existing `hooks = false` message is unchanged rather than merged into the new string.

**(b) Does the new test genuinely discriminate?** Yes — checked against the pre-fix code rather than by reading the assertion. Pre-fix the branch was `if v, ok := features["hooks"].(bool); ok && !v`; a `hooks = "false"` string fails the type assertion, so `ok` is false and no finding is appended. The fixture supplies `validHooksJSON` and a config with no `[hooks]` table, so every other finding source is silent and the pre-fix result is `pass` — the test's `Status != "warn"` assertion fails on the old code. The `strings.Contains(r.Detail, "non-boolean")` assertion additionally distinguishes the new branch from the pre-existing `hooks = false` message, so the test cannot be satisfied by the sibling arm. This is a genuine discriminator, not a change-detector.

**(c) Does the pre_bash_guard tech-debt row render correctly, and is its wording accurate?** Renders correctly: `cb54856` split the row that `8aaa27b` had glued onto the end of the preceding line, and the row now parses as 7 `\|`-delimited awk fields, matching every other row in the table (`awk -F'|' '/^\|/ {print NF}'` → 7 for lines 116-119 and 122-123). Wording is accurate on the substance and overreaches in one parenthetical — C2-L5. The row's remediation trigger, owner file list, and the `tests/test-pre-bash-guard.sh` fixture requirement all point at files that exist.

**(d) Are the citations cycle-qualified?** Yes, at both new sites, and this is the right call. `internal/cli/doctor.go:305-309` and `internal/cli/cli_test.go:473-478` both write "cross-review AR#1 (cycle 1, `docs/reports/cross-review-triage-codex-hooks-json-wiring.md`)" — the per-slug triage file is rewritten in place on each cycle, so a bare `AR#1` would go ambiguous the moment this cycle's `/cross-review` runs. `grep '^+.*AR#'` over the whole cycle-2 delta returns exactly these two sites; there are no bare citations.

**(e) Was the orchestrator's inline exception for the AR#1 fix justified?** Defensible in spirit, outside the letter of the rule, and the residue shows the cost. `subagent-policy.md` scopes the exception to "trivial single-file edits where the handoff cost exceeds the change cost"; `d1df46f` touches two files and adds ~37 lines, converting a single type assertion into a three-arm type switch on an operator-facing check. The argument for inline is real — the orchestrator held the cross-review triage context, and the change is one localized branch plus its test. But three of this cycle's seven findings (C2-M2, C2-L1, C2-L4) are sibling-site sweeps that an implementer handoff's "files in scope" list would have surfaced: the block comment directly above the edited branch, the enumeration at the call site 200 lines up, and the type-naming convention 100 lines down. Recommendation for the next AR fix of this size: dispatch it, or run a one-command sibling grep before committing.

## Positive notes

- The M3 fix is the strongest work in this delta. It replaces the regex with the sibling's proven awk, names the mirrored function in the comment (checked — `codex_config_has_inline_hooks` exists at `scripts/verify.local.sh:46-56` and matches), and adds a three-case self-test covering the tight form, the spaced form, *and* a clean file, so it pins both the miss and the false positive. The `trap 'rm -rf "$tmp_dir"' RETURN` cleanup was probed under `bash`: it fires on the helper's own return and does not leak to subsequent function calls, so it does not clobber a caller-level `EXIT` trap — the correct choice for a helper inside a `set -eu` script.
- Mirror discipline holds after the fix pass. `.codex/README.md`, `.codex/config.toml`, `.codex/hooks/README.md`, `.codex/AGENTS.override.md`, and `docs/recipes/codex-setup.md` are all `diff`-clean against their `templates/base/` twins at HEAD, including the files touched by three separate commits.
- The insight-event ledger is complete and correctly labelled — five events for cycle 1 (`self_review`, `verify`, `test`, `sync_docs`, `cross_review`), every one stamped `"cycle":1`. Both of the usual gaps (a `verify` or `sync_docs` commit forgetting `insights-append.sh`; a `cross_review` event mislabelling its cycle) are absent.
- The L2 sweep was complete rather than partial: all eight cited sites now carry the durable evidence path, and re-grepping the old tokens across `internal/`, `tests/`, `scripts/`, `.codex/`, and `templates/base/` finds no residue.
- The spec AC-10 edit takes the right shape — it appends a dated bracket rather than rewriting a completed spec's historical AC text, which keeps the record of what was known when. The pointer style inside the bracket is the only problem (C2-L2).
- The plan's new `Deviations` section is honest about scope creep: it names the out-of-plan `.codex/AGENTS.override.md` touch as out-of-plan rather than quietly folding it in, and records that AC-2(b)'s evidence came out *stronger* than planned instead of only recording shortfalls.
- No secrets, no debug leftovers, no commented-out code, no whitespace churn. `git diff d6a04fb..HEAD` contains no unrelated file.

## Known gaps

- Not assessed here (other pipeline phases): spec compliance, test execution, static analysis, and doc-drift auditing beyond the comment/claim accuracy above.
- `.codex/AGENTS.override.md`'s rewritten row now lists "project trust + one-time interactive hook approval" among the requirements, while the sentence below it still reads "If `ralph doctor` reports any of these as missing, follow the remediation it prints" — doctor cannot probe either, as `.codex/README.md` states explicitly. The imprecision predates this PR (project trust was already in the row); `7af720a` widened it by two items rather than introducing it, so it is recorded here rather than filed.

## Merge recommendation

**Fix C2-M1 and C2-M2, then merge.** Both are text edits in files this PR already owns, both ship into every scaffolded project, and both are instructions that contradict code shipped in the same commit — the same failure mode as cycle-1's H1 and M1, which is why they are worth closing before the v5.0.0 tag rather than after. Neither requires a code change.

The five LOWs are individually cheap and can ride in the same fix commit. If any are deferred, record them as **one batched tech-debt row** rather than leaving them unstated: this is cycle 2 of 2, the plan is archived at `/pr`, and nothing else carries an unfixed LOW forward.
