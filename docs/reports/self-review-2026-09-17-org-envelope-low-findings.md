# Self-review report: org-envelope-low-findings

- Date: 2026-09-17
- Plan: `docs/plans/active/2026-09-17-org-envelope-low-findings.md` (issue #154)
- Reviewer: `reviewer` subagent (Claude Code, standard flow, cycle 1)
- Scope: diff quality only for `git diff 4905c34...HEAD` on `chore/org-envelope-low-findings`, first pass at HEAD `0bdbb4c`, then revalidated through `ff30ee2` (see Fix-and-revalidate below). Tests, static analysis, spec-compliance and doc-drift checks were not run — those belong to `/verify` and `/test`.

## Evidence reviewed

- `git diff 4905c34...HEAD` (15 files, +288/-46) and each commit's stat: `ea29ab1` (plan), `314b89f` (Slice A), `746c70d` (Slice B), `f9d6dce` (plan bookkeeping), `3a9362c` (Slice C), `0bdbb4c` (AC ticks).
- `git show 4905c34:internal/cli/doctor_codex_models.go` to compare the pre-fix resolver against the new one and against the added tests.
- Full reads of `internal/cli/doctor_codex_models.go`, `internal/org/watch.go:562-600` (`pruneRetiredConditions` + `retiredConditionTypeFromAlertID`), `internal/org/prompts.go`, `internal/org/prompts/*.md` (all four templates), `internal/org/prompts_test.go:1-200`, `internal/cli/org_test.go:625-700`, `internal/config/config.go:290-300`, `internal/config/config_test.go:720-835`, `.claude/skills/org/SKILL.md:60-110`.
- Sibling-site sweeps: `grep -rn 'PLAN_PATH' internal/`, `grep -rn 'reviewer\.md' internal/ .claude/skills/org docs/specs`, `grep -rn '{{ENVELOPE}}|{{TASK}}' internal/org/prompts/`, `grep -rn 'later slice|this slice' --include='*.go' internal/`, `grep -rn 'codexModelsCachePath' --include='*.go' .`, `grep -rn 'TestLoad_DriverPoolOnlyOverride_Codex\b'`.
- Register comparison: 44 struck rows in `docs/tech-debt/README.md` scanned for the closure-marker convention; `git diff --word-diff` over the edited row to confirm no collateral cell edits.
- Hygiene: `git status --porcelain` (clean), diff scanned for U+FFFD, trailing whitespace, TODO/debug statements, and credentials (none found).

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| LOW | maintainability | **The same stale-template-list defect survives in a sibling comment.** Item 3 of the batch fixed `RolePromptVars.PlanPath`'s doc to enumerate all four templates, but `Spawn`'s own comment still describes the known-role template set as "(reviewer.md / qa.md / lead.md)" — omitting `implementer.md`, the template PR #152 added and the exact omission this item exists to fix. The sweep followed the register row's `file:line` citation rather than the defect class, and AC-3's grep gate (`PLAN_PATH`, `PR③`) cannot see this site. | `internal/org/spawn.go:607-608` ("a known --role expands the embedded template (reviewer.md / qa.md / lead.md)") vs the corrected `internal/org/prompts.go:36` ("the four templates (lead.md, implementer.md, reviewer.md, qa.md)"). The adjacent sentence at `:610-612` ("Task and Envelope are only referenced by prompts/lead.md today") is still accurate — `grep -rn '{{ENVELOPE}}\|{{TASK}}' internal/org/prompts/` matches only `lead.md:5,34`. | Insert `implementer.md` into the `spawn.go:607` list in this cycle. It is a comment-only one-line edit in the same package, and the alternative is a new register row for a one-word fix the moment the row that tracked this class is struck. |
| LOW | maintainability | **The fix adds a sixth terminal outcome to a function whose header comment says "Five".** `checkCodexModelSlugs`'s doc enumerates five deterministic outcomes; the new early return (home unresolvable → `info`) is a sixth, and it appears in none of the five bullets. The count and the enumeration are both wrong at the definition site now. | `internal/cli/doctor_codex_models.go:60` (`// Five deterministic outcomes:`) and bullets `:61-71`, vs the new branch at `:89-94`. | Change to "Six" and add a bullet ("CODEX_HOME empty and the home directory unresolvable -> info, names the resolution error"), or drop the count and keep the bullets open-ended. |
| LOW | maintainability | **The register closure skips the convention every other closed row follows.** The row is struck and carries a detailed closure note, but the note lives in the *Trigger* cell and the debt-item cell has no closure marker. It is the only one of 44 struck rows without one: 43 carry `(RESOLVED <date> in <branch>)`, `(RESOLVED-by-removal …)`, or `(VERIFIED …)` in the debt-item cell, and 36 rows also carry a preceding `<!-- RESOLVED … Row preserved for traceability. -->` comment. Consequence: `grep -n 'RESOLVED' docs/tech-debt/README.md` — the natural inventory query — does not list this closure. Two smaller defects in the same note: "(3)–(10) fixed as listed (commits 314b89f, 746c70d, and this slice's commit)" leaves the third commit unresolvable to a later reader (it is `3a9362c`), and the *Related plan/report* cell was not extended with the closing plan, unlike the `probeBinary` row which records its resolving commit there. | `docs/tech-debt/README.md:130` (new row state) vs `:116` and the nine `<!-- RESOLVED … -->` rows above it; cell shape verified intact (6 pipes before and after, "since every file is already open." preserved per `git diff --word-diff`). | Add `(RESOLVED 2026-09-17 in chore/org-envelope-low-findings — issue #154)` to the debt-item cell after the `~~`, replace "this slice's commit" with `3a9362c`, and append this plan/report to the last cell. Optionally add the `<!-- RESOLVED … -->` comment for symmetry with the 36 rows that have one. |
| LOW | readability | **The new "used literally" test passes against the pre-fix code, so its comment overstates what it pins.** The base resolver trimmed only for the *emptiness test* and joined the raw value (`home := os.Getenv("CODEX_HOME")` … `return filepath.Join(home, "models_cache.json")`), so `CODEX_HOME="<tmp>/codex "` already resolved to the literal directory before this PR. The trimmed-sibling fixture therefore does not exercise the behavior the PR changed; the only input whose result actually moved is a whitespace-only `CODEX_HOME` (previously treated as unset → `$HOME/.codex`, now joined literally). The plan names that case under Test plan / Edge cases but adds no assertion for it, and the register closure note leans on this test ("doctor keeps the literal join and `…TrailingSpace` pins it"). | `internal/cli/doctor_org_test.go:506-515` (doc comment) and `:516-542` (body) vs `git show 4905c34:internal/cli/doctor_codex_models.go` lines 22-31; `filepath.Join`/`Clean` do not strip spaces, so both implementations return `<tmp>/codex /models_cache.json`. | Reword the comment (and the register note) to say the test guards the literal join *going forward* rather than pinning this change; or add the input that does discriminate — `CODEX_HOME="   "`, asserting the `info` Detail names the space-prefixed path. |
| LOW | readability | **Operator-facing stutter in the new info Detail.** The resolver wraps its error with "resolve home directory for codex models cache", and the caller wraps it again with "could not resolve codex models cache path", so `ralph doctor` prints `could not resolve codex models cache path: resolve home directory for codex models cache: $HOME is not defined — skipping`. | `internal/cli/doctor_codex_models.go:36` (`fmt.Errorf`) and `:92` (`r.Detail`). The new test asserts only the outer clause (`doctor_org_test.go:558`), so either half can be shortened without touching it. | Return a shorter wrapped error from the resolver (`fmt.Errorf("resolve home directory: %w", err)`) or drop the caller's prefix; one clause is enough. |
| LOW | readability | **The new cross-reference generalizes `lead.md`'s stance past the row directly above it.** The Leaded row now reads "Lead 自身は実装しない(`lead.md` の implementer 優先委譲と同じ立場)", but the Solo row one line up has that same "Lead(現セッション)" implementing directly. `lead.md`'s "火消しに限定します" governs a *spawned* lead seat, whereas the skill's "Lead" is the interactive session; the parenthetical blurs the two, so a reader applying "同じ立場" uniformly reads Solo as prohibited. Secondary: `lead.md` is not a path a scaffolded project can open — `templates/base/` ships the skill but not `internal/org/prompts/` — though `:74` already sets that precedent. | `.claude/skills/org/SKILL.md:82` (Solo) vs `:83` (Leaded, new text), and the three mirrors (all four files md5-identical: `92a37e5cda1fe1ef4c315ca925da1f79`); `internal/org/prompts/lead.md:9-12`. | Narrow the parenthetical (e.g. "座席を立てた場合は `lead.md` と同じ立場") or drop it — the plain "Lead 自身は実装しない" already resolves the contradiction the item was filed for. Apply to all four mirrors via `scripts/sync-skills.sh` plus the two `templates/base/` copies. |
| LOW | maintainability | **A doc comment pins identity against a moving reference.** Both the resolver's doc and the new test's doc assert codex's `find_codex_home` is "identical at rust-v0.149.1, rust-v0.154.0, and main". The two tags are checkable; `main` is not, and the claim silently becomes false the first time codex edits that file. Nothing in this repo can detect the drift. | `internal/cli/doctor_codex_models.go:18-22`; `internal/cli/doctor_org_test.go:507-511`. | Keep the two pinned tags and either drop "and main" or qualify it ("and main as of 2026-09-17"). |
| LOW | readability | **`markdownSection`'s contract is looser than its doc comment.** The helper says it returns "the body of the level-2 markdown section that starts with header", but `strings.Index(text, header)` matches the header substring anywhere, unanchored to line start. A future template that demotes the heading to `### 座席内 fan-out` still satisfies `found` and still yields the same body, so the tightened test would not notice the demotion — a small hole in a helper introduced precisely to make these assertions exact. | `internal/org/prompts_test.go:17-31` (doc plus `start := strings.Index(text, header)` at `:24`), consumed at `:176`. | Search for `"\n"+header+"\n"` (or verify the byte before `start` is `'\n'`, or that `start == 0`) so a heading-level change fails the test. |

## Positive notes

- **The `Escalated` delete removal is provably behavior-preserving.** `pruneRetiredConditions`'s third loop (`internal/org/watch.go:573-577`) iterates every `Escalated` key under the identical predicate `retiredConditionNames[retiredConditionTypeFromAlertID(alertID)]`, and that predicate is a pure function of the key, so the deleted line could only ever remove entries the next loop also removes. No ordering or aliasing subtlety: the two maps are distinct fields of `watchStatusFile`.
- **The error-path fix removes a real footgun, not just a discarded error.** The base code, on `os.UserHomeDir` failure, fell through to `filepath.Join("", ".codex")` and returned the *relative* path `.codex/models_cache.json`, so doctor would silently read (or miss) a file under the operator's cwd. The new code returns an error before any path is built (`internal/cli/doctor_codex_models.go:34-38`).
- **The tightened assertions are genuinely tighter.** `strings.Count(out, wantWarn) != 1` (`internal/cli/org_test.go:673-675`) now actually checks the "exactly one fallback warning line" claim its own doc comment at `:639` makes. The fan-out assertions are section-scoped against a real boundary — all three seat templates place `max_seats`, `lead`, and `送ることは絶対に` between `## 座席内 fan-out` and `## レポート契約`.
- **The config error assertion matches the producer byte-for-byte.** `want` at `internal/config/config_test.go:831` reproduces `internal/config/config.go:299`'s format string exactly, including the quotes `%q` adds around `gpt-5.5`.
- **The fixture de-staling introduces no import cycle and no vacuous assertion.** `internal/org/envelope_summary.go:19` already imports `internal/config` (which imports neither `internal/org` nor `internal/cli`), and the rendered text is still asserted to contain the derived envelope (`internal/org/prompts_test.go:127-131`), so an empty `EnvelopeSummary` could not pass silently.
- **Mirror and register mechanics were handled carefully.** All four `/org` SKILL.md copies are byte-identical, and the word-diff of the tech-debt row shows only the `~~` markers plus the appended note — the clause the plan's Deviation notes record as accidentally deleted and restored ("since every file is already open.") is intact, and the row still has exactly five cells.
- **Hygiene clean.** No secrets, debug statements, TODO markers, commented-out code, trailing whitespace, or U+FFFD in the diff; no unrelated files in any of the five commits; worktree clean at review time. The plan's evidence timestamp (`verify-2026-09-17-092738.log`) is consistent with the Slice C commit once `run-verify.sh`'s UTC naming (`date -u`, `scripts/run-verify.sh:28`) is applied: 09:27:38Z = 18:27:38 JST, between `3a9362c` (18:18) and `0bdbb4c` (18:29).

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |

_None. All eight cycle-1 findings were one-to-three-line edits recommended for in-cycle fix, and all eight landed in `ff30ee2` — nothing from the first pass is deferred. The two findings the fix itself introduced (N1, N2) are also recommended in-cycle; see Fix-and-revalidate._

Note for the orchestrator: this PR exists because the previous PR's LOW batch was deferred at the cycle cap and needed a register row. That trap is now avoided for L1–L8. If N1 or N2 is still unfixed when this pipeline reaches its cap, that is a deferral — record the remainder as **one** batched row in `docs/tech-debt/README.md` (using the closure-marker convention restored by the L3 fix) rather than letting a second generation of the same class go untracked.

## Fix-and-revalidate (cycle 1)

All eight findings were fixed in-cycle in `ff30ee2` ("chore: address self-review L1-L8 for org-envelope-low-findings", 9 files, +79/-42), with the plan's Slice D deviation note in `46c46d5`. Revalidated against `git show ff30ee2` and the files at that commit.

| # | Verdict | Evidence at `ff30ee2` |
| --- | --- | --- |
| L1 | **Resolved** | `internal/org/spawn.go:607-609` now reads "the embedded template (lead.md / implementer.md / reviewer.md / qa.md)", matching `internal/org/prompts.go:36`'s order. Re-swept the class: `grep -rn 'reviewer\.md' --include='*.go' internal/` leaves only these two comments plus an unrelated `spawn_test.go:1789` fixture note, and the adjacent "Task and Envelope are only referenced by prompts/lead.md" sentence remains true. |
| L2 | **Resolved** | `internal/cli/doctor_codex_models.go:60` now says "Six deterministic outcomes:", and the new bullet ("CODEX_HOME empty and the home directory unresolvable -> info, names the resolution error") is inserted second — the position matching the branch's actual evaluation order at `:89-94`. Bullet count is six; `grep -rn 'Five deterministic' internal/` is empty. |
| L3 | **Resolved** | `docs/tech-debt/README.md:130` debt-item cell now carries `(RESOLVED 2026-09-17 in chore/org-envelope-low-findings)` after the `~~`; the `awk -F' \| ' '/^\| ~~/ {if ($1 !~ /RESOLVED\|Closed\|VERIFIED/) print NR}'` convention check now returns nothing for all 44 struck rows. "this slice's commit" is replaced by `3a9362c`, and the Related plan/report cell gained this report plus the closing plan. Row shape intact (6 pipes). |
| L4 | **Resolved, over-delivered** | `internal/cli/doctor_org_test.go:506-520` no longer claims the trailing-space test pins the change — it now states outright that "the pre-fix resolver also joined CODEX_HOME's raw value, so a trailing-space CODEX_HOME alone does not distinguish old from new behavior" and reframes the test as a forward regression guard. The discriminating input also gained a test: `TestCheckCodexModelSlugs_WhitespaceOnlyCodexHome_UsedLiterally` (`:549-571`) sets `CODEX_HOME=" "` and asserts `info` plus `not found at  /models_cache.json`, which is the one input whose outcome this PR actually moved. The register's closure note was corrected in the same commit. |
| L5 | **Resolved** | `internal/cli/doctor_codex_models.go:36` is now `fmt.Errorf("resolve home directory: %w", err)`, so the operator line reads `could not resolve codex models cache path: resolve home directory: $HOME is not defined — skipping`. `TestCheckCodexModelSlugs_HomeUnresolvable_Info` asserts only the outer clause (`doctor_org_test.go:583`), so it still holds — the shortening needed no test edit, as predicted. |
| L6 | **Resolved** | The `lead.md` parenthetical is gone; `.claude/skills/org/SKILL.md:83` now ends "Lead 自身は実装しない。" with no cross-reference to compare against the Solo row. All four mirrors re-synced and byte-identical (`md5` = `9ce9b00a952b1ff1120fa270e6082204`). |
| L7 | **Resolved** | "and main" is gone from all three copies of the citation — `internal/cli/doctor_codex_models.go:19-20`, `internal/cli/doctor_org_test.go:508-509`, and the register's closure note — each now reading "identical at rust-v0.149.1 and rust-v0.154.0, checked 2026-09-17". `grep -rn 'and main' internal/ docs/tech-debt/README.md` is empty. The plan's Assumptions line was swept too, and states the policy ("タグのみを根拠にし、動く `main` は引かない") while the Deviation note keeps the historical record of what was consulted — a deliberate split, not a residue. |
| L8 | **Resolved for the reported case; see N1** | `internal/org/prompts_test.go:22-33` now anchors the lookup to a whole line (`padded := "\n" + text`, `needle := "\n" + header + "\n"`), so a heading demoted to `### 座席内 fan-out` returns `found == false`. Confirmed by replicating both versions' exact string logic on a demoted fixture: old returned the body, new returns not-found. The anchoring introduced a boundary regression — N1 below. |

### New findings introduced by the fix

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| LOW | maintainability | **N1 — the anchoring fix drops the header line's newline from `body`, so an *empty* section now returns the following section.** `body = padded[start+len(needle):]` starts the body *after* the header's trailing `\n`, so when the target section has no content the body begins with `## <next header>` rather than `\n## <next header>`, and the `strings.Index(body, "\n## ")` terminator no longer matches at position 0. The helper then returns the next section's text — exactly the cross-section leakage it exists to prevent — and contradicts its own doc comment ("up to, but not including, the next line that starts with `## `"). | `internal/org/prompts_test.go:24-32` (anchoring + `body = padded[start+len(needle):]`) vs the doc at `:17-21`. Verified by replicating both versions' string logic outside the repo on a fixture shaped `## B\n## C\ngamma has max_seats\n`: the pre-fix helper returned `""`, the post-fix helper returns `"## C\ngamma has max_seats\n"`. Not currently reachable — all four templates have non-empty fan-out sections — so no assertion is wrong today. | One character: `body = padded[start+len(needle)-1:]`, keeping the header line's trailing newline in `body`. Verified against the same four fixtures: empty section → `""`, normal → `"\nbeta body\n"`, demoted → not-found (L8 still fixed), section-at-EOF → full body. |
| LOW | consistency | **N2 — the new test is the only one of the three in its block without a Windows guard, and its assertion depends on platform error mapping.** It expects `os.ReadFile(" /models_cache.json")` to fail with `fs.ErrNotExist` so the Detail takes the "not found at" branch rather than "could not read … at". On Windows a `" "` path component is invalid rather than merely absent, and whether that reaches `errors.Is(err, fs.ErrNotExist)` is a platform detail the test does not control. Its two siblings at `:522` and `:579` both carry explicit `runtime.GOOS == "windows"` skips. | `internal/cli/doctor_org_test.go:549-571` (no skip) vs `:522` and `:579`; the branch it depends on is `internal/cli/doctor_codex_models.go:97-102`. CI is Linux-only (`.github/workflows/verify.yml:6`, `runs-on: ubuntu-latest`), so nothing runs this on Windows today. | Add the same `runtime.GOOS == "windows"` skip with a one-line reason, or relax the assertion to the path substring alone (`strings.Contains(r.Detail, wantPath)`), which holds on both branches. |

### N1/N2 revalidation (`b677a95`)

Both resolved in `b677a95` ("test: keep markdownSection empty-section body empty and skip whitespace-only CODEX_HOME test on windows", 2 files, +28/-1). One new LOW (N3).

- **N1 — resolved.** `internal/org/prompts_test.go:36` is now `body = padded[start+len(needle)-1:]`, with the rationale recorded at the definition site (`:33-35`, "Keep the header line's trailing newline at the head of body so that an empty section … yields `""` rather than leaking the following section's text"). The new table test `TestMarkdownSection_AnchorsHeaderAndBoundsBody` (`:42-65`) pins all four behaviors plus a fifth case. I replicated the implementation's exact string logic against its own fixture (`"intro\n## A\nbody a\n### A sub\nsub text\n## B\n## C\nbody c"`): all five expectations match, and two of them are real regression guards — "empty section returns an empty body" goes red on a revert of the `-1`, and "a level-3 header does not satisfy a level-2 lookup" goes red on a revert of the anchoring (the unanchored `strings.Index` finds `## A sub` inside `### A sub`). The normal-section case also usefully documents that level-3 subsections stay inside the body.
- **N2 — resolved.** `internal/cli/doctor_org_test.go:558-560` adds the guard, and the skip reason names the actual mechanism rather than restating the platform ("relies on a whitespace-only path component resolving to fs.ErrNotExist, which Windows does not guarantee"). `runtime` was already imported for the two sibling skips. All three tests in the block now guard symmetrically.

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| LOW | maintainability | **N3 — the table's fifth case cannot fail.** "a mid-line mention does not satisfy the lookup" expects `markdownSection(doc, "## text")` to return not-found, but the fixture contains no mid-line mention of that header — the substring `## text` is absent from `doc` entirely (`sub text` is the near-miss the case name presumably had in mind). The case therefore passes under the anchored implementation, under the unanchored one it was written to exclude, and under any implementation at all. It is the same shape as item 5 of the batch this PR pays down: an assertion that checks less than its name claims. | `internal/org/prompts_test.go:43` (fixture) and `:49` (the case). Verified by running both implementations over the fixture: for `"## text"` the unanchored version also returns not-found, so the case discriminates nothing, whereas the `"## A sub"` case at `:48` does. | Give the fixture a real mid-line mention and look that up — e.g. append `"see ## D for details\n"` to `doc` and expect `("", false)` for `"## D"`. The unanchored implementation finds that mid-line occurrence, so the case would then guard the anchoring it names. |

### N3 revalidation (`02ad186`)

- **N3 — resolved.** The fixture at `internal/org/prompts_test.go:44` now carries a real mid-line mention (`body a, see ## D for details`) and the fifth case looks up `## D` (`:53`), so the case can fail: replaying both implementations over the new fixture, the unanchored lookup returns found, the anchored one returns not-found. All five expectations still hold against the implementation, four of the five now discriminate against at least one implementation this helper replaced, and the mention also lands inside section A's body — which incidentally documents that a mid-line `## ` does not terminate a body, with case 1's expected body updated to match. No further finding in this change.

### Residue noted, not raised as a finding

- The Related plan/report cell the L3 fix edited still cites `docs/plans/active/2026-09-16-org-implementer-seat-envelope.md`, which no longer exists — that plan was archived by PR #152 and now lives at `docs/plans/archive/2026-09-16-org-implementer-seat-envelope.md`. Pre-existing, not introduced by the fix, and the fix's own newly added citation avoids the trap by pointing at the archive path with an explicit "(closing plan; archived by `/pr`)" label, so the forward-looking path is self-describing rather than silently dangling. Worth one more character of sweep if that cell is touched again.
- `ralph doctor` still performs a cwd-relative lookup for a whitespace-only `CODEX_HOME` (` /models_cache.json`). That is the deliberate contract — codex's own resolver would use `" "` as its home directory too — and it terminates in `info`, never a false pass or warn. The new test documents it as intended rather than leaving it implicit.
- The re-wrapped doc comment block (`internal/cli/doctor_codex_models.go:16-27`, now ≤79 columns) is consistent with the rest of the file, which already had 78- and 79-column comment lines. No width drift.

## Recommendation

- Merge: **Yes** — final. No CRITICAL, HIGH, or MEDIUM finding was raised in any round, and no finding is left open: the eight cycle-1 LOWs are resolved in `ff30ee2`, the two that round introduced (N1, N2) in `b677a95`, and the one that round introduced (N3) in `02ad186`. Across all three fix rounds not one line of production logic changed — the edits were comments, one error string, one test helper, four added tests, one skip guard, the four skill mirrors, and the register row. The PR's only runtime surface remains the single best-effort `info` path in `ralph doctor`.
- Follow-ups: none outstanding. Every finding this review raised was fixed in-cycle, so nothing is being deferred to `docs/tech-debt/README.md` — which is the point of the PR, since it exists to pay down a batch that *was* deferred at the cap.
- Known gaps in this review: no tests, linters, or formatters were executed, and spec compliance, AC evidence, and documentation drift were not assessed — `/verify` and `/test` own those. Claims about `markdownSection` and the two implementations it replaced rest on replicating their exact indexing logic outside the repo, not on running the suite; the same method established N1's original unreachability and N3's vacuity. Claims about codex's `find_codex_home` were not independently verified against codex's source (not present in this repo); L7 concerned only the unverifiable half of that citation, which the fix removed.
