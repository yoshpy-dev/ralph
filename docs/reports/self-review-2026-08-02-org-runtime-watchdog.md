# Self-review report: org-runtime-watchdog

- Date: 2026-08-03
- Plan: `docs/plans/active/2026-08-02-org-runtime-watchdog.md`
- Reviewer: `reviewer` subagent (self-review, diff quality only)
- Scope: `git diff e7a32b9...HEAD` on `feat/org-runtime-watchdog` (HEAD `4c44f56`), 33 files / +3535 −93. Diff quality only — no tests run, no static analysis, no spec-compliance or doc-drift judgment (those belong to `/verify` and `/test`).

## Evidence reviewed

- New code: `internal/org/watch.go` (749), `internal/org/watcher.go` (340), `internal/org/statedir.go` (83)
- Changed code: `internal/cli/org.go`, `internal/config/config.go`, `internal/org/permissions.go`, `internal/org/protocol/protocol.go`, `internal/org/spawn.go`, `internal/org/verbs.go`
- Supporting reads (not in diff, needed to judge the diff): `internal/org/manifest.go` (`Roster`), `internal/org/seat.go` (`stateEvents`), `internal/org/receipts.go` (`Honored` tri-state contract), `internal/org/protocol/protocol.go` (`ValidateText`)
- Config/doc/mirror surfaces: `scripts/ralph-config.sh`, `templates/base/ralph.toml`, `templates/base/scripts/ralph-config.sh`, `.claude/rules/agent-messaging.md`, `.claude/skills/org/SKILL.md`, `AGENTS.md`, `docs/quality/definition-of-done.md`, `docs/tech-debt/README.md`
- Evidence artifact: `docs/evidence/org-watchdog-smoke-2026-08-02.txt`
- Mirror parity verified with `cmp`: root vs `templates/base/` for `.claude/skills/org/SKILL.md`, `.claude/rules/agent-messaging.md`, and `.agents/skills/org/SKILL.md` — all byte-identical (exit 0).

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | `watch-status.json` has a fixed, org-agnostic file name but org-scoped contents. Two orgs watched from the same repository share one state dir (`ResolveOrgStateDir` takes no org id) and therefore one status file: `OrgID`, `OrgStartTS`, `Cycles`, `LastCycleTS`, `WatchdogJoined`, and `SeatSnapshots` (keyed by bare `SeatID`) all clobber each other. `Conditions`/`PendingAlerts`/`Escalated` *are* namespaced via `conditionKey(orgID, …)` — that asymmetry inside one struct is the tell. | `watch.go:40` `WatchStatusRelName = "watch-status.json"`; `watch.go:344` `filepath.Join(p.StatusDir, WatchStatusRelName)`; `watch.go:416` `status.OrgID = orgID` overwrites unconditionally; `watch.go:189` `SeatSnapshots map[string]*watchSeatSnapshot`; `statedir.go:47` signature has no org id. Concrete effect: `ensureWatchdogJoined` (`watch.go:615`) sees org A's `WatchdogJoined: true` and never joins org B's agmsg team, so org B's ALERTs are sent from an unjoined identity. | Either key the file per org (`watch-status-<org_id>.json`) or namespace every org-scoped field the way `conditionKey` already does (at minimum `SeatSnapshots` → `conditionKey`-style key, and `WatchdogJoined` → `map[string]bool` by org). |
| HIGH | exception-handling | The budget cutoff — the feature's only enforcement action — swallows `Stop`'s error *and* sets the permanent `Cutoff: true` ratchet regardless of whether the stop actually landed. `Stop` returns a non-nil error when `findSeat` fails or `appendEvent` fails (lock timeout, write error); in that case no `stopped` event exists, the seat keeps running, the key is marked cut-off "ever", and nothing is written to stderr — while `sendAlert` 3 lines later *does* log its failure. | `watch.go:474` `_ = w.org.Stop(...)` then `watch.go:476` sets `Cutoff: true`; identically `watch.go:559`/`watch.go:560`. Contrast `watch.go:637-639`, which logs `Send` failures to `w.stderr`. `verbs.go:298-303` shows `Stop` returning `appendEvent`'s error. | Only set `Cutoff: true` when `Stop` returned nil; log a non-nil `Stop` error to `w.stderr` in the same shape `sendAlert` already uses so a failed cutoff is visible and retried next cycle. |
| MEDIUM | readability | `RunWatcher`'s doc comment still describes the timeout design that the live-smoke fix (`7101351`) replaced, and names a symbol that does not exist. It claims the timeout is `watcherTimeout(cfg), always strictly below one pulse interval` — the code uses a fixed 60s `watcherInvokeTimeout` that is explicitly *not* interval-bound, as the same file's package note and const doc both say. `grep -rn watcherTimeout internal/` returns exactly one hit: this comment. | `watcher.go:263-264` vs `watcher.go:26-33` (package note) and `watcher.go:67-74` (`watcherInvokeTimeout` doc). `grep -rn "watcherTimeout" internal/` → `internal/org/watcher.go:263` only. | Replace the parenthetical with `watcherInvokeTimeout, a fixed 60s bound` and drop the "strictly below one pulse interval" clause. |
| MEDIUM | readability | The LOW-batch refactor that lifted `checkCapacityAndStart` out of `Spawn` inserted it *between* `Spawn`'s 40-line doc comment and `func (o *Org) Spawn`. The two comment blocks fused: the whole spawn-saga ordering doc (including the "Cycle-2 M-1 fix" dry-run-parity rationale) now documents `checkCapacityAndStart`, and `Spawn` — the package's most contract-heavy exported method — has no doc comment at all. | `spawn.go:220-259` (Spawn's saga doc) runs straight into `spawn.go:260-270` (`checkCapacityAndStart`'s doc) with no blank line; `spawn.go:271` is `func checkCapacityAndStart`; `spawn.go:287` is `func (o *Org) Spawn` with nothing above it. | Move `checkCapacityAndStart` (and its own doc) above the `Spawn` doc block, or below `Spawn`, so the saga doc reattaches to `Spawn`. |
| MEDIUM | maintainability | `RunWatcher` records `honored = HonoredTrue` purely because the claude envelope carried *some* `model` string, without comparing it to `cfg.WatcherModel`. `receipts.go` states `"true" is reserved for cases where a driver-observed confirmation of the effective model actually exists` and the field means "whether the command was verifiably honored" — so the one case the tri-state exists to catch (driver reported a *different* model) is recorded as honored. `HonoredFalse` is never used anywhere in `watcher.go`. The `claudeEnvelope.Model` doc also forward-references a "Honored-tri-state decision at the bottom of RunWatcher" that was never written — the bottom of `RunWatcher` is a bare 3-line `if`. | `watcher.go:310-314`; contract at `receipts.go:8-13, 20-22`; dangling forward reference at `watcher.go:118-120`. | `if envelope.Model == "" { HonoredUnknown } else if envelope.Model == cfg.WatcherModel { HonoredTrue } else { HonoredFalse }`, and either write the referenced rationale comment or delete the forward reference. |
| MEDIUM | maintainability | The deadman's "lead activity" information source #1 is *state-dir-wide* manifest growth, not lead-specific activity — and it counts the watchdog's own writes. Sequence: cycle N cuts off seat X and registers a pending alert with `ManifestLen = N`; cycle N+1 cuts off seat Y, appending a `stopped` event; the end-of-cycle re-read then satisfies `len(rr.Events) > pending.ManifestLen` for X, so X's pending alert is deleted as "lead responded" and never escalates. Any unrelated seat's (or, per the HIGH above, any *other org's*) event does the same. | `watch.go:697` `activity := len(rr.Events) > pending.ManifestLen`; `watch.go:434-438` re-reads the full manifest after the cutoff loop; `watch.go:641` snapshots `len(rr.Events)` from the same unfiltered read. The doc comment at `watch.go:683-689` describes this as "lead activity". | Filter the count to events attributable to lead (e.g. `ev.OrgID == status.OrgID && (ev.SeatID == LeadIdentity \|\| ev.SeatID == "")`), or drop source #1 and rely on the herdr/agmsg sources, which are already lead-scoped. |
| MEDIUM | maintainability | `--once` is documented as the cron/smoke mode, but nothing waits for the semantic-trigger goroutine. `RunWatch` returns immediately after cycle 1; `RunE` returns; the process exits and kills an in-flight `RunWatcher` — so `--once` can never produce a watcher receipt or a watcher-verdict ALERT. `newWatchdogHooks` holds only an `atomic` single-flight flag, no `sync.WaitGroup`. | `internal/cli/org.go:668-674` (`Cycles: 1` → `RunWatch` returns), `internal/cli/org.go:687-716` (`var busy int32` + bare `go func()`, no join); `watch.go:367-369` returns as soon as `n >= p.Cycles`. Smoke evidence confirms the working path was the *looping* mode (`"cycles": 8`, `docs/evidence/org-watchdog-smoke-2026-08-02.txt:29`), not `--once`. | Track in-flight watcher goroutines with a `sync.WaitGroup` returned from `newWatchdogHooks` and `Wait()` (bounded by `watcherInvokeTimeout`) before `RunE` returns, or document in the `--once` flag help that semantic judgment is skipped in that mode. |
| MEDIUM | maintainability | The stall condition's time term is inert. `Roster` only advances `SeatStatus.TS` on *state* events (`stateEvents` excludes `sent`), so for a healthy active seat `s.TS` is frozen at its `spawned` timestamp — `watch.go`'s own `evaluateSeatBudget` comment says exactly this. `isStallByTime(s.TS, now, StallMinutes)` is therefore permanently true for any seat older than `stall_minutes`, and the only live discriminator left is a *single-interval* (default 30s) comparison of herdr's raw text. That contradicts `config.go`'s `StallMinutes` doc ("how long a seat's last manifest event time and herdr state_change_seq may **both** stay unchanged") and `watch.go`'s own inline comment. | `manifest.go:180-183` (non-state events skipped) + `seat.go:47-55` (`stateEvents` has no `sent`); `watch.go:534-538` (comment asserting `s.TS` == spawned TS for an active seat); `watch.go:510` composes the two terms; `config.go:185-187` (StallMinutes doc). | Either track the herdr probe value with its own first-seen timestamp so the `StallMinutes` window applies to it too, or correct both comments to state that the herdr term is a one-interval comparison and `StallMinutes` only gates seat age. |
| LOW | unnecessary-change | `newOrgWatchCmd` resolves the state dir twice: once inside `newOrgRuntime` and again in its own `RunE`. Each call shells out to `git rev-parse --show-toplevel`, and the value is re-derived rather than read back from the object that already holds it. | `internal/cli/org.go:102` and `internal/cli/org.go:663` — the same `org.ResolveOrgStateDir(stateDir, cmd.Flags().Changed("state-dir"))` expression; `statedir.go:64-70` execs git each time. | Expose the resolved dir off `*org.Org` (or have `newOrgRuntime` return it) and read it in `newOrgWatchCmd` instead of re-resolving. |
| LOW | maintainability | `ResolveOrgStateDir`'s `source` return exists specifically for diagnostics (its own doc: "a grep-able tag naming which precedence tier produced it, useful for diagnostics"), but every production call site discards it with `_`; only `statedir_test.go` asserts on it. `ralph org watch`'s startup banner prints the resolved path but not how it was resolved — the one place the tag would earn its keep. | `grep -rn ResolveOrgStateDir internal/` → production hits only at `internal/cli/org.go:102` and `:663`, both `resolvedStateDir, _ :=`. Banner at `internal/cli/org.go:665-666`. | Print `source` in the `org watch` banner and/or in `ralph org status`/`doctor`, or drop the second return value. |
| LOW | maintainability | The `org watch` startup banner reports the raw flag value, so the default run prints `interval-seconds=0` while the effective interval is 30s (resolved later inside `RunWatch`). An operator reading the banner cannot tell what cadence is actually running. | `internal/cli/org.go:665-666` prints `intervalSeconds` (flag default `0`, `internal/cli/org.go:678`); `watch.go:323-329` resolves 0 → `Watchdog.IntervalSeconds` → 30s. | Resolve the interval in `RunE` before printing (or have `RunWatch` report the effective interval back). |
| LOW | typo | `%w` is used with an error that can be `nil` on the malformed-envelope path: the condition is `err != nil \|\| strings.TrimSpace(envelope.Result) == ""`, so a well-formed envelope with an empty `result` produces `org: watcher: malformed claude envelope: %!w(<nil>)`. | `watcher.go:298-301`. | Split the two cases, or use a non-wrapping message when `err == nil` (e.g. `... : empty result field`). |
| LOW | maintainability | `checkDeadman`'s `if status.Escalated[alertID] { delete(...); continue }` guard is unreachable: `escalateAlert` already deletes the key from `PendingAlerts` in the same statement it sets `Escalated`, and new alert ids embed `now.UnixNano()` so they never collide with an escalated one. Relatedly, `status.Escalated` is persisted and never pruned, so a long-running `watch` grows `watch-status.json` without bound. | `watch.go:692-695` (guard) vs `watch.go:747-748` (`Escalated[alertID] = true; delete(status.PendingAlerts, alertID)`); `watch.go:642` (`alertID` includes `UnixNano`). | Drop the dead guard (or comment it as forward-compat for older status files) and cap/expire `Escalated` entries. |
| LOW | exception-handling | An oversized scope-change ALERT silently degrades to a content-free message. `git status --porcelain` output is interpolated whole into the ALERT body; a seat with many changed files exceeds `DefaultMaxBodyChars` (2000), `ValidateText` fails, and `sendAlert` replaces the entire message with a fallback that has no `SEAT` header and no scope information — lead is told "message failed protocol validation" and nothing else. | `watch.go:525-527` (full `out` interpolated); `watch.go:632-634` (whole-message replacement, fallback header set has no `SEAT`); `protocol.go:174-180` + `DefaultMaxBodyChars`. | Truncate `out` to a bounded head (e.g. first 20 lines + "… N more") before building the message, and include `SEAT` in the validation-failure fallback. |
| LOW | exception-handling | In `permissionArgsForDriver`'s codex branch, an *unknown* mode string now falls through the `switch` to the fail-closed error, so it is reported as `permission mode %q not yet live-verified; only guarded is allowed` rather than as an unknown mode. The claude branch has a dedicated `unknown permission mode` default for exactly this. | `permissions.go:107-120` — the `switch` has no `default:`, so an unrecognized mode reaches the same `return` as a recognized-but-unverified one; contrast `permissions.go:104-105`. | Add a `default:` to the codex switch returning the same `unknown permission mode %q for driver %q` error the claude branch uses. |

## Positive notes

- Mirror discipline held: `cmp` confirms root vs `templates/base/` byte-identity for `.claude/skills/org/SKILL.md`, `.claude/rules/agent-messaging.md`, and the `.agents/` mirror. No new `internal/`-path references were introduced into the shipped `agent-messaging.md` copy (the pre-existing ones carry their "not part of a scaffolded project" disclaimer).
- The three tech-debt rows this PR claims to close are struck through *and* annotated with an HTML comment naming the commit, the mechanism, and — for item (b) — an explicit "kept as-is, final" rationale rather than a silent drop. That is the register discipline the repo asks for.
- Test-only seams are honest about being seams: `watcherInvokeTimeout` is a `var` with a doc comment saying why it is not a `const`, and `watchSeatSnapshot`'s `*Seen` booleans have a comment explaining the "empty string vs no baseline" distinction they exist to preserve.
- No secrets, credentials, tokens, or absolute home paths in the diff. `docs/evidence/org-watchdog-smoke-2026-08-02.txt` carries an explicit redaction header and contains no `$HOME`-derived path.
- No debug prints, commented-out code, or stray `TODO`/`FIXME` markers anywhere in the new Go files.
- `realEscalate` passes the message through `%q` and `exec.CommandContext("osascript", "-e", …)` with no shell, so operator-controlled `--org-id` text cannot break out of the AppleScript string literal.
- `checkDeadman` deletes from the map it is ranging over, which is well-defined in Go — reviewed and confirmed not a bug.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| — | — | — | — | — |

No new deferred work is proposed by this review. Every finding above is a fix within this PR's own diff, not accumulated complexity to hand forward; the two HIGH findings in particular should not be converted into register rows.

## Recommendation

- Merge: **no-merge until the two HIGH findings are addressed.** Neither is a CRITICAL (no secret leak, no injection, no data destruction), so this does not hard-stop the pipeline under `.claude/rules/post-implementation-pipeline.md`'s CRITICAL rule — but H-1 (shared `watch-status.json`) silently corrupts watchdog state the moment a second org is watched in the same repo, and H-2 (cutoff ratchet set on a failed `Stop`) can permanently disable the feature's only enforcement action without any log line. Both are small, localized fixes.
- Fix in this cycle: H-1, H-2, M-1 (stale `watcherTimeout` comment), M-2 (orphaned `Spawn` doc comment).
- Fix or explicitly accept with a comment: M-3 (`Honored` semantics), M-4 (deadman activity source), M-5 (`--once` goroutine), M-6 (inert stall time term).
- Follow-ups (batchable, no urgency): all seven LOW findings; L-1/L-2 pair naturally (resolve once, surface `source` in the banner).

---

## Cycle 2 (fix-and-revalidate)

- Date: 2026-08-03
- Reviewer: `reviewer` subagent (self-review, diff quality only)
- Scope: delta `git diff e601fdf...HEAD` on `feat/org-runtime-watchdog` (HEAD `19c7630`), code files only — `internal/cli/org.go`, `internal/cli/org_test.go`, `internal/org/{permissions,spawn,watch,watcher}.go` + their tests, plus `docs/tech-debt/README.md`. Delta commits: `79d9f40` (cycle-1 finding fixes), `0ac03b6` (switch/WaitGroup modernization), `19c7630` (cross-review AR-1/AR-2). Diff quality only — no tests run, no static analysis, no spec-compliance or doc-drift judgment.

### Cycle-1 regression status

Every cycle-1 finding was re-checked at the paths the original finding cited.

| Cycle-1 finding | Status | Evidence in current tree |
| --- | --- | --- |
| H-1 shared `watch-status.json` | Fixed, no regression | `WatchStatusFileName(orgID)` (`internal/org/watch.go:54-56`) → `watch-status-<org_id>.json`; `RunWatch` uses it at `watch.go:394`. Per-file namespacing subsumes the `SeatSnapshots`/`WatchdogJoined` asymmetry the finding named. |
| H-2 `Cutoff` ratchet set on a failed `Stop` | Fixed at both sites, no regression | `watch.go:550-560` (`allStopped`) and `watch.go:677-684` (`stopErr == nil`); both now log to `w.stderr`; `conditionFirstTS` (`watch.go:574`) preserves `FirstTS` across retries. |
| M-1 stale `watcherTimeout` doc | Fixed | `internal/org/watcher.go:267-268` now names `watcherInvokeTimeout, a fixed 60s bound`; `grep -rn watcherTimeout internal/` → no hits. |
| M-2 orphaned `Spawn` doc comment | Fixed | `checkCapacityAndStart` moved below `Spawn` (`internal/org/spawn.go:776`); the saga doc at `spawn.go:250-259` again terminates at `func (o *Org) Spawn` (`spawn.go:260`). |
| M-3 `Honored` optimistic | Fixed | `internal/org/watcher.go:318-330` compares `envelope.Model` against `cfg.WatcherModel`; `HonoredFalse` is now reachable and pinned by `watcher_test.go`. |
| M-4 deadman activity source | **Half-fixed** | Watchdog-event filter + `orgID` filter added (`watch.go:791-802`); the seat-attribution half of the original recommendation is still open — see M2-1 below. |
| M-5 `--once` goroutine not joined | Fixed, but see M2-2 | `newWatchdogHooks` returns a `*sync.WaitGroup` (`internal/cli/org.go:751`); `RunE` calls `watcherWG.Wait()` (`org.go:705`). |
| M-6 inert stall time term | Fixed | `latestSeatEventTS` (`watch.go:294-305`) replaces `s.TS` in `isStallByTime`; regression-pinned at `watch_test.go:1096`. |
| L-1 double state-dir resolve | Fixed | `newOrgRuntimeAt` (`internal/cli/org.go:114`); one `ResolveOrgStateDir` call in `newOrgWatchCmd`. |
| L-2 `source` return discarded | Deferred, tracked | `docs/tech-debt/README.md:71` item (1). |
| L-3 banner prints raw flag | Fixed | `ResolveWatchInterval` (`watch.go:352`), banner at `org.go:689-690`. |
| L-4 `%w` with a nil error | Fixed | `internal/org/watcher.go:302-309` splits unmarshal failure from empty `result`. |
| L-5 unreachable `Escalated` guard + unbounded growth | Deferred, tracked | `docs/tech-debt/README.md:71` item (3). |
| L-6 oversized scope-change ALERT | Deferred, tracked | `docs/tech-debt/README.md:71` item (2). |
| L-7 codex unknown-mode default | Fixed | `internal/org/permissions.go:121-122` `default:` case; pinned by `permissions_test.go:101`. |

No cycle-1 fix regressed, and no cycle-1 deferral is untracked — with the single exception recorded as M2-1.

### Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | maintainability | The `leadActivityEventCount` tech-debt row was struck through as RESOLVED, but only half of the defect its own text describes was fixed. The row says the function "is not scoped to `org_id` **or to lead-attributable seats** (`ev.SeatID == LeadIdentity`)" and that "**any unrelated seat's own event** — or, in a shared state dir, a different org's event — still counts as 'lead activity'". AR-1 closed the org half; the seat half is unchanged. The closure note nevertheless claims "cross-org filtering **fully closes this row**", so the still-live behavior (a busy seat's own `sent` event in the same org clearing a *different* seat's pending deadman alert, or the org-level total-budget alert's) is now tracked in no register row at all. The row's own impact sentence — "A genuinely unresponsive Lead ... could have its deadman escalation silently cleared by unrelated seat ... manifest growth" — still reads true against the shipped code. | `docs/tech-debt/README.md:70` (strikethrough + "fully closes this row"); `internal/org/watch.go:791-802` filters on `ev.OrgID != orgID` and `reason=watchdog_` only, never on `ev.SeatID`; `checkDeadman` at `watch.go:851`. The cycle-1 recommendation was `ev.OrgID == status.OrgID && (ev.SeatID == LeadIdentity \|\| ev.SeatID == "")`. | Either un-strike the row and narrow its text to the remaining seat-attribution gap (keeping a concrete trigger), or add the `SeatID` filter so the closure claim becomes true. Do not leave a struck-through row describing live behavior — the register's value is that a strikethrough means "nothing here is still true". |
| MEDIUM | maintainability | The M-5 `--once` fix makes `ralph org watch`'s **long-running** mode hang for up to ~60s after SIGINT. `RunWatch` returns as soon as `ctx` is done, then `watcherWG.Wait()` blocks — but the tracked goroutine runs `RunWatcher(context.Background(), ...)`, so the command's own cancelled context does not reach it; it runs out the full `watcherInvokeTimeout` (60s) plus `watcherWaitDelay`. Before this fix, Ctrl-C exited immediately. The fix's own comment asserts the opposite: "Bounded by RunWatcher's own watcherInvokeTimeout, so this **can never hang the command**". | `internal/cli/org.go:698-705` (comment + `watcherWG.Wait()`); `internal/cli/org.go:766` passes `context.Background()`; `internal/org/watcher.go:74` `watcherInvokeTimeout = 60 * time.Second`; `internal/org/watcher.go:277` `context.WithTimeout(ctx, watcherInvokeTimeout)`. The `--once` help text ("useful for cron/smoke") is the mode the fix targeted; the loop mode is the one that regressed. | Pass `cmd.Context()` (not `context.Background()`) into `RunWatcher` so SIGINT cancels the in-flight judgment, or bound the join (`Wait` on a channel with a short timeout) and print a one-line "waiting for in-flight watcher" notice. Either way, correct the "can never hang the command" clause. |
| MEDIUM | readability | The AR-2 guard's comment states a mechanism the code does not have. It claims that without the guard an over-budget org with zero active seats "would still send a spurious total-budget ALERT and register an AC-5 pending-alert deadman record **every cycle**". It would not: with zero seats the H-2 `allStopped` loop is vacuously true, so the record is written `Cutoff: true` and the next cycle returns at the `rec.Cutoff` gate — exactly one spurious ALERT, not one per cycle. The consequence the guard actually prevents — the vacuous ratchet permanently disabling total-budget enforcement for any seat spawned into that org later — is what the accompanying test's third phase asserts, and is nowhere in the comment. | `internal/org/watch.go:524-533` (the guard + comment); `watch.go:550-557` (`allStopped := true` over an empty slice); `watch.go:544-546` (`rec.Cutoff` early return); `internal/org/watch_test.go:432-501` — the test's cycle-3 phase ("must not silently disable enforcement once a seat becomes active again") is the real rationale. | Rewrite the comment around the ratchet consequence the test pins, and drop "every cycle". |
| LOW | readability | `watchStatusFile`'s doc comment names `WatchStatusRelName`, a constant the H-1 fix deleted. `grep -rn WatchStatusRelName` finds this comment and nothing else, which is precisely the grep-ability failure `.claude/rules/architecture.md` ("prefer grep-able names", "make public contracts obvious") is about. | `internal/org/watch.go:195`; the replacing symbol is `WatchStatusFileName(orgID)` at `watch.go:54`. | `// watchStatusFile is the JSON shape persisted to WatchStatusFileName(org_id).` |
| LOW | readability | Two more doc sites still describe the pre-H-1 fixed file name: the package note ("persists its own heartbeat/dedupe state to `watch-status.json`") and `WatchParams.StatusDir` ("the directory `watch-status.json` and `escalations.jsonl` live in"). A reader looking for `watch-status.json` on disk will not find it. | `internal/org/watch.go:25` and `internal/org/watch.go:132`. | Say `watch-status-<org_id>.json` at both sites, matching `verify-2026-08-02-org-runtime-watchdog.md`'s own wording. |
| LOW | naming | `leadActivityEventCount` counts *every* non-watchdog event in the org, not lead-attributable activity — its own doc comment admits this ("genuine lead/seat activity"). The name asserts a scope the body does not implement, which is what made the half-closure in M2-1 easy to miss. | `internal/org/watch.go:779-802`; call sites `watch.go:773` and `watch.go:851`. | If the seat filter is not added, rename to `orgActivityEventCount` (and update `watchPendingAlert.ManifestLen`'s "3 lead-activity information sources" doc at `watch.go:160-166`) so the name and the semantics agree. |
| LOW | readability | `watchConditionRecord`'s doc still says a budget cutoff "is **attempted** and ALERTed at most once per key, ever" — the H-2 fix deliberately made the *attempt* retriable across cycles (only the ALERT stayed once-per-key). The struct comment now contradicts `evaluateTotalBudget`/`evaluateSeatBudget`'s own new doc blocks two hundred lines below. | `internal/org/watch.go:151-153` vs `watch.go:504-519` and `watch.go:650-656`. | "…so a budget cutoff is ALERTed at most once per key, ever, and is retried until one `Stop` pass succeeds (see `evaluateTotalBudget`)." |
| LOW | typo | `ResolveWatchInterval`'s doc has a garbled em-dash clause: "the banner used to print the raw `--interval-seconds` flag value, which is 0 by default -- the effective interval -- rather than the interval that is actually running". The middle clause appears to be a half-deleted edit; as written the sentence says the flag default *is* the effective interval. | `internal/org/watch.go:344-351` (doc block above `func ResolveWatchInterval` at `watch.go:352`). | Drop `-- the effective interval --`. |
| LOW | maintainability | AR-2 plus the never-cleared `Active` flag means a later-spawned seat can be cut off **silently**. Sequence: cycle N alerts + cuts (record `Active: true`); all seats end (guard now returns early, record untouched); a new seat spawns; the next cycle cuts it, but `alreadyAlerted` is true so no ALERT is sent. The new test does not cover this ordering (it starts from a cycle that never alerted). | `internal/org/watch.go:524-533` (early return leaves the record as-is), `watch.go:559-565` (`alreadyAlerted` gate). | Either clear `Active` when the guard fires (so a resumed org re-alerts), or state in the guard's comment that a resumed cutoff is intentionally silent because the org-level condition never "recovers". |

### Positive notes (cycle 2)

- The H-2 fix is complete at both cutoff sites, not one — `evaluateTotalBudget` and `evaluateSeatBudget` each got the conditional ratchet, a stderr line, and `conditionFirstTS`. The multi-site half-fix shape that usually shows up in cycle 2 did not occur here.
- Both AR fixes shipped with regression tests whose doc comments state the *invariant*, not the mechanics (`TestWatch_Deadman_CrossOrgActivity_DoesNotClearPendingAlert`, `TestWatch_TotalBudgetCutoff_NoActiveSeats_NoAlertNoEscalation_ThenCutsNewSeat`), and the latter asserts a positive control (enforcement resumes) rather than only the absence of an alert.
- `TestNewWatchdogHooks_*` were rewritten to join on the real `sync.WaitGroup` instead of polling a side effect, and the now-unused `waitForCondition` helper was deleted in the same commit — the producer went with the consumer.
- The three deferred cycle-1 LOWs reached `docs/tech-debt/README.md:71` as one row with a concrete per-item fix shape and trigger.
- Finding-ID cross-references in code comments were renumbered consistently in `0ac03b6`/`19c7630` (M-3 = Honored, M-4 = deadman, M-5 = `--once`, M-6 = stall) and now match this report's Recommendation section at all 12 sites.
- `wg.Go` (Go 1.25) is valid against `go.mod`'s `go 1.25.8`; the `switch envelope.Model { case "": … case cfg.WatcherModel: … }` rewrite is behavior-identical to the boolean switch it replaced, including when `cfg.WatcherModel` is empty (first case wins → `HonoredUnknown`).
- No secrets, debug prints, commented-out code, or `TODO`/`FIXME` markers in the delta. No mirror-managed files (`.claude/`, `templates/base/`, `.agents/`) were touched, so mirror parity is unchanged from cycle 1.

### Tech debt identified (cycle 2)

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| — | — | — | — | — |

No new deferred work. M2-1 is a *correction* to an existing register row (`docs/tech-debt/README.md:70`), not a new row: that row should be reopened and narrowed rather than duplicated.

### Recommendation (cycle 2)

- Merge: **merge with follow-ups** — 0 CRITICAL, 0 HIGH. The two HIGH findings that blocked cycle 1 are fixed and regression-tested, and both cross-review ACTION_REQUIRED items landed.
- Fix before PR (cheap, and both are correctness-of-record issues): M2-1 (un-strike / narrow the tech-debt row) and M2-2 (SIGINT hang + the comment that denies it).
- Fix or explicitly accept: M2-3 (AR-2 comment rationale).
- Follow-ups (batchable): L2-1 … L2-6; L2-1/L2-2/L2-4/L2-5 are all one-line comment corrections in `internal/org/watch.go` and pair naturally into a single pass.

---

## Cycle 3 (third-cycle delta review)

- Date: 2026-08-03
- Scope: `git diff 6836efe...HEAD` (HEAD `d6e557a`) — delta since the cycle-2 review baseline only. Code files: `internal/cli/org.go`, `internal/cli/org_test.go`, `internal/org/watch.go`, `internal/org/watch_test.go`. Diff quality only.
- Delta commits: `ccf506e` (cycle-2 M2-1/M2-2 fixes), `764b01f` (cycle-3 cross-review P1/P2: lead-only history filter, same-cycle cut-seat skip), `d6e557a` (`strings.Cut`/`SplitSeq` lint), plus report/plan/tech-debt doc commits.
- Cap note: pipeline cycle cap was raised to 3 per `docs/reports/cross-review-triage-org-runtime-watchdog.md`.

### Regression check against cycle-1 + cycle-2 fixes

| Prior finding | Status in delta | Evidence |
| --- | --- | --- |
| H-1 per-org status file | Not regressed | `WatchStatusFileName(orgID)` still the only path builder; the delta only corrected three stale `watch-status.json` doc mentions (`watch.go:25`, `:132`, `:200`) — this closes cycle-2 L2-1/L2-2. |
| H-2 cutoff ratchet only on successful `Stop` | Not regressed | `watch.go:732` still `Cutoff: stopErr == nil`; `watch.go:590` still `Cutoff: allStopped`. The new `return stopErr == nil` (`watch.go:738`) is added *after* the ratchet write and does not alter it. |
| AR-1 `orgID` filter on deadman activity | Not regressed | `watch.go:849-851` `if ev.OrgID != orgID { continue }` is unchanged and still the first gate. |
| AR-2 zero-active-seats guard | Not regressed | `watch.go:548-559` still returns before the vacuous `allStopped` loop; only the `return` value changed to `cutSeats`. |
| M-5 `--once` goroutine join | Not regressed | `watcherWG.Wait()` still at `internal/cli/org.go:710`; `--once` still joins because `cmd.Context()` is live on a normal `RunE` return. |
| M2-2 SIGINT hang in loop mode | Fixed | `newWatchdogHooks(ctx, …)` (`internal/cli/org.go:764`) threads `cmd.Context()` into both `RunWatcher` (`org.go:780`) and `SendWatchdogAlert` (`org.go:793`); pinned by `TestNewWatchdogHooks_CtxCancelled_RunWatcherReturnsQuickly` (`internal/cli/org_test.go:1662-1691`), which joins on the real `WaitGroup` rather than polling. The "can never hang the command" clause the cycle-2 review called out was rewritten, not just deleted. |
| M2-3 AR-2 comment rationale | Fixed | `watch.go:548-559` now names the vacuous-ratchet consequence and points at the test's third phase; "every cycle" is gone. |
| L2-4 `ResolveWatchInterval` garbled em-dash | Fixed | `watch.go:351-356`. |
| L2-5 `watchConditionRecord` "attempted … once per key" | Fixed | `watch.go:151-158` now says "ALERTed at most once per key, ever, and is retried until one Stop pass succeeds". |
| L2-6 AR-2 + never-cleared `Active` → silent later cutoff | Still open, still untracked | `watch.go:548-559` early return still leaves the record; the `alreadyAlerted` gates are unchanged at `watch.go:589-592` and `watch.go:731-734`. Not a regression, but it survived a third cycle without reaching `docs/tech-debt/README.md`. |

No cycle-1 or cycle-2 fix regressed. `L2-3` (`leadActivityEventCount` naming) was addressed by narrowing the body rather than renaming — see H3-1 below for why the narrowing is wrong.

### Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | The M2-1 seat-attribution fix is **inverted for the org's dominant message flow**. `leadActivityEventCount`'s new `ev.SeatID == LeadIdentity` branch is justified by a doc comment claiming lead's "own spawn event **or any message it sends** carries this". `Send` records the *recipient*, not the author: `SeatID: p.To`. So (i) every `ralph org send --to lead` a seat makes — the star topology's mandated seat→lead direction, i.e. exactly the "unrelated seat's own traffic" this fix exists to exclude — lands as `SeatID == "lead"` and **still clears a pending deadman alert**, and (ii) every message lead sends *to* a seat lands as `SeatID == <seat>` and is **not** counted, so genuine lead responses are invisible to this source. The counterpart fix in the same commit pair (`filterLeadHistoryLines`) gets the direction right by parsing a real `from` field — that asymmetry inside one PR is the tell. | `internal/org/watch.go:833-836` (the "any message it sends carries this" claim) vs `internal/org/verbs.go:97` and `verbs.go:136` (`SeatID: p.To, Event: EventSent`, dry-run and real paths). Star topology contract: `.claude/rules/agent-messaging.md` — "A seat only ever addresses `TO: lead`". Filter body at `watch.go:846-861`. The history-source counterpart parses an author field at `watch.go:911-929`. The new regression test appends `SeatID: "seat-2", Event: EventSent` and labels it "seat-2's own traffic" (`internal/org/watch_test.go:1129`) — under `Send`'s semantics that record is a message sent *to* seat-2, i.e. by lead, so the test passes while encoding the inverted model. `ManifestEvent` has no author field (`manifest.go:20-23`). | Either (a) add an author field to the `sent` event (`from_seat`, set to the sender) and filter on it — the only fix that makes the source actually lead-attributable — or (b) drop `sent` from this source entirely (count only `EventStopped`/`EventDisbanded` non-watchdog writes plus lead's own state events) and say so in the doc comment. Do not leave the current branch with a comment that asserts the opposite of what `Send` writes. Whichever is chosen, correct `watch_test.go:1129`'s label. |
| MEDIUM | maintainability | The fix's own stated rule — "a manual (non-watchdog) stop or disband of another seat is a lead-driven action lead had to take, so it is still evidence lead is alive" — is applied to `stopped`/`disbanded` but not to `spawned`. A lead that responds to a stall ALERT by spawning a replacement seat produces `EventSpawned` with `SeatID == <new seat>`, which now counts as nothing, so the deadman escalates against a demonstrably responsive lead. `spawn_started`/`spawn_step`/`spawn_failed`/`rejected` are in the same bucket. | `internal/org/watch.go:856` enumerates only `EventStopped`/`EventDisbanded`; the full state-event set is `internal/org/seat.go:47-55`. Rationale sentence at `watch.go:838-843`. `Spawn` writes the new seat's id, not lead's (`internal/org/spawn.go`, `SpawnParams.SeatID`). | Either extend the (b) branch to the full non-watchdog state-event set (`isStateEvent(ev.Event) && !strings.Contains(ev.Details, "reason=watchdog_")`), or state in the doc comment that spawn-side lead actions are deliberately excluded and why. Right now the omission reads as an oversight because the comment's own reasoning covers it. |
| MEDIUM | maintainability | `filterLeadHistoryLines` narrows the snapshot but the window it narrows is still the unfiltered last-20 messages, so non-lead chatter can *change* the filtered result and produce the false "activity" the filter was added to prevent. `historySnapshot` calls `History(ctx, team, "", 20)`; with an empty `agentID` the driver drops the limit entirely (its own doc: "there is no way to pass limit without agentID"), so the script's default `LIMIT=20` applies to *all* team traffic. As seats chat, lead's lines are evicted from that window, the filtered string shrinks, `cur != pending.History` becomes true while `cur != ""` still holds, and the pending alert is cleared — caused purely by other seats' messages. The `20` at the call site is also silently dead. | `internal/org/watch.go:895` `probe.History(ctx, agmsgTeam(orgID), "", 20)`; driver contract at `internal/org/driver/agmsg.go:57-69`; the underlying `LIMIT` default and `ORDER BY created_at DESC LIMIT` are in the agmsg skill's `history.sh` (user-global install, see L3-1). Change detection at `watch.go:961-964` (`cur != "" && cur != pending.History`). | Pass `LeadIdentity` as `agentID` so the SQL window is scoped to lead-related traffic before the Go-side `from == lead` narrowing (this also makes the `20` effective), or compare a lead-line *count* rather than the joined string so eviction-driven shrinkage cannot read as activity. |
| LOW | readability | `leadHistoryFromField`'s doc cites `.agents/skills/agmsg/scripts/history.sh` as if it were repo-relative. This repo *has* a `.agents/skills/` tree (16 skills, no `agmsg`), so the path resolves to nothing here and a reader greps in vain; the real file is the user-global agmsg skill install (`~/.agents/skills/agmsg/scripts/history.sh`). Same grep-ability class as the cycle-2 `WatchStatusRelName` finding. | `internal/org/watch.go:903-906`; `ls .agents/skills/` shows `anti-bottleneck … work`, no `agmsg`. | Qualify it: "the agmsg skill's `scripts/history.sh` (user-global install under `~/.agents/skills/agmsg/`, not vendored in this repo)". |
| LOW | readability | The parse in `leadHistoryFromField` is anchored on the first `"] "` in the line, but the format it targets puts `status` *before* the timestamp (`"  $status [$ts] $from → $to: $body"`) and the body is newline-escaped to a literal `\n` by the producer — so a body containing `"] "` before any `" → "` is impossible to reach, but the doc comment does not record *why* the first-occurrence assumption is safe. Given the parse deliberately excludes-on-failure, the safety argument is the load-bearing part. | `internal/org/watch.go:911-929`; producer format and `replace(replace(body, char(10), '\n'), …)` escaping are in the agmsg skill's `history.sh`. | One sentence in the doc: the producer escapes newlines, so each history record is exactly one line and the first `"] "` is always the timestamp's. |
| LOW | maintainability | `docs/tech-debt/README.md:70` is struck through as fully closed for the **second** consecutive cycle, and for the second time the closure claim outruns the code. Its new text says the seat half is closed because the function "now also counts an event only when `ev.SeatID == LeadIdentity`, or it is a `stopped`/`disbanded` event that is not the watchdog's own cutoff write". Per H3-1, that predicate does not exclude another seat's own traffic (seat→lead `sent`), which is precisely the behavior the row's own impact sentence describes. | `docs/tech-debt/README.md:70` ("Both halves of the original M-4 recommendation are now implemented; this row is fully closed") vs `internal/org/watch.go:846-861`. Cycle-2 finding M2-1 flagged the identical overclaim on the same row. | Do not re-strike this row until the author-attribution problem in H3-1 is resolved. If H3-1 is deferred, un-strike and narrow the row to "the `sent` event records the recipient, so seat→lead traffic still counts as lead activity", with a concrete trigger. |

### Positive notes (cycle 3)

- Every cycle-2 MEDIUM and four of six cycle-2 LOWs were closed, and each fix carries a regression test or a corrected comment rather than a silent edit.
- The M2-2 ctx fix threads the command context into *both* async calls (`RunWatcher` and `SendWatchdogAlert`), not just the one the finding named — the "partial fix leaves the executing site unpatched" shape did not recur here.
- `TestNewWatchdogHooks_CtxCancelled_RunWatcherReturnsQuickly` uses a pre-cancelled context and a real 3s stub, so it fails if the code regresses to `context.Background()` — it exercises the gating mechanism, not a side effect.
- The cut-seat skip (`cutSeats` in `evaluateTotalBudget`, `bool` return from `evaluateSeatBudget`) is applied symmetrically at both cutoff sites, with the failed-`Stop` case explicitly falling through so a still-running seat keeps being probed (`watch.go:617-624`, `watch.go:699-703`).
- `strings.Cut`/`strings.SplitSeq` are valid against `go.mod`'s `go 1.25.8`; the `d6e557a` rewrite is behavior-identical to the `strings.Index`/`strings.Split` form it replaced.
- No secrets, debug prints, commented-out code, or `TODO`/`FIXME` markers in the delta. No mirror-managed surfaces (`.claude/`, `.agents/`, `templates/base/`) were touched, so mirror parity is unchanged.

### Tech debt identified (cycle 3)

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| — | — | — | — | — |

No new row proposed. L3-3 is a *correction* to the existing `docs/tech-debt/README.md:70` row (re-open rather than duplicate); L2-6 from cycle 2 remains the one genuinely untracked deferral and should be folded into the existing three-LOW row at `docs/tech-debt/README.md:71` if it is deferred again.

### Recommendation (cycle 3)

- Merge: **no-merge until H3-1 is resolved or explicitly accepted in writing** — 0 CRITICAL, 1 HIGH, 2 MEDIUM, 3 LOW. The HIGH is not a new regression (pre-fix behavior also cleared on seat traffic), but the shipped code now carries a doc comment and a struck-through tech-debt row that both assert the opposite of what it does, which is worse than the untouched state for anyone maintaining this next.
- Cheapest acceptable close: option (b) in H3-1 — drop the `sent`-reachable `SeatID == LeadIdentity` branch, keep only non-watchdog state events, correct the comment and `watch_test.go:1129`'s label, and un-strike `docs/tech-debt/README.md:70`. That is a ~10-line change plus one test-label fix and needs no manifest schema change.
- Fix or explicitly accept: M3-1 (spawn-side lead actions), M3-2 (history window vs. filter).
- Follow-ups (batchable, comment-only): L3-1, L3-2.
