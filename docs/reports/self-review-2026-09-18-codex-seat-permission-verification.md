# Self-review report: codex-seat-permission-verification

- Date: 2026-09-18
- Plan: `docs/plans/active/2026-09-18-codex-seat-permission-verification.md` (issue #155)
- Reviewer: `reviewer` subagent (Claude Code, standard flow, cycle 1)
- Scope: diff quality only for `git diff e23c1f4...HEAD` on `docs/codex-seat-permission-verification` at HEAD `529b360`. Tests, static analysis, spec-compliance and doc-drift checks were not run — those belong to `/verify` and `/test`.

## Evidence reviewed

- `git diff e23c1f4...HEAD` (12 files, +457/-25) and the commit list: `91d3d07` (plan), `5e1699a` (Codex advisory fold-in), `e38dc27` (in-progress), `7f299cc` (evidence), `0771073` (recipe + repositioning), `8525a40` (plan bookkeeping), `529b360` (template-purity placeholder).
- Full reads of `docs/evidence/codex-seat-permissions-2026-09-18.md`, `docs/recipes/codex-seat-permissions.md`, the plan (Objective / Scope / Non-goals / Design decisions / Deviation notes), and the changed hunks in `templates/base/ralph.toml`, `internal/org/permissions.go`, `docs/recipes/codex-setup.md`, `.claude/skills/org/SKILL.md`.
- Code reads to check the docs' factual claims: `internal/org/permissions.go:54-132`, `internal/config/config.go:68-95`, `internal/org/spawn.go:218-235, 405-426, 827-837`, `internal/cli/org.go:40-42, 234-244, 341-381, 499-532`, `internal/cli/doctor.go:238-330, 698-761`, `internal/org/permissions_test.go:80-117`, `scripts/ralph-config.sh:55-64`, `scripts/check-sync.sh:30-90`.
- Sibling-site sweeps: `grep -rn 'codex_verified\|CodexVerified'` (repo-wide, 25 files), `grep -rn 'tech-debt' --include='*.go' --include='*.sh' --include='*.toml'`, `grep -rn '暫定制約\|実機検証未了\|実機未検証'` (0 hits outside plans/reports), `grep -c 'codex_verified' docs/tech-debt/README.md` (0), agmsg team-name convention (`internal/org/driver/agmsg_test.go:73-91`, `ralph-<org-id>`).
- Redaction: `grep -c '/Users/'` = 0 and `grep -Ec '[0-9a-f]{8}-[0-9a-f]{4}-…'` = 0 on every new/changed file; no `hiroki` / `yoshioka` / `dena` matches; home rendered as `~`, scratchpad as `<scratch>`.
- Mirror identity: `cmp` passes for the recipe pair, the `codex-setup.md` pair, and all four `/org` SKILL.md copies.
- Hygiene: `git status --porcelain` clean; `git diff --check` clean; 0 U+FFFD in the new Japanese files; `git diff e23c1f4...HEAD -- internal/org/permissions.go` filtered to non-comment lines is empty (comment-only, as the plan claims); no secret-shaped tokens in the diff; the two 56K `docs/evidence/verify-2026-09-18-*.log` files are gitignored and untracked.

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | **The "no longer unverified" reframing reached three of five surfaces; the other two still assert the opposite and keep the dangling `docs/tech-debt/README.md` pointer this PR exists to remove.** The plan's Non-goals say the register has no `codex_verified` row (`grep -c 'codex_verified' docs/tech-debt/README.md` = 0, confirmed) and Scope 5 is "fix the wrong pointer". `templates/base/ralph.toml`, the four skill copies, and one block of `permissions.go` were updated. Two sites were not: `internal/config/config.go` — the file that *defines* the flag — still points at the nonexistent row twice and still states the flags have not been live-verified; and `internal/org/permissions.go`, the file this PR edited, still carries the pre-verification framing 27 lines above the new block that contradicts it. AC-4's gate is `grep -c 'tech-debt' templates/base/ralph.toml`, which cannot see either file. | `internal/config/config.go:73-75` ("codex seats only accept guarded until codex's own interactive permission flags are live-verified (fail-closed -- see docs/tech-debt/README.md)") and `:87-93` ("codex's interactive sandbox/approval flags have not been live-verified against a real codex seat as of this field's introduction -- only Slice 5's live smoke does that … see docs/tech-debt/README.md for the verification follow-up this flag exists to close out"). `internal/org/permissions.go:59-64` ("These flag shapes are the plan's own stated assumption … Slice 5's live smoke is what actually confirms them against a real codex seat") vs the same file's new `:91-99` ("The mapping was live-verified on 2026-09-18 against codex-cli 0.154.0"). Both stale blocks also cite `docs/plans/active/2026-08-02-org-runtime-watchdog.md`, which now lives under `docs/plans/archive/`. | Sweep in this cycle — all comment-only edits in the same package pair. In `config.go`, drop both `docs/tech-debt/README.md` pointers and replace "have not been live-verified …" with the per-machine opt-in framing plus the evidence/recipe paths. In `permissions.go:59-64`, replace "Slice 5's live smoke is what actually confirms them" with a pointer to the new `:91-99` block. Repoint both `docs/plans/active/2026-08-02-…` citations at `docs/plans/archive/`. `scripts/ralph-config.sh:55-63` and its template mirror ("until an operator has live-verified their installed codex CLI's flags") stay true under the new framing, but they are the third member of the lock-step trio pinned by `internal/config/defaults_sync_test.go:158` — worth one sentence for symmetry. |
| MEDIUM | maintainability | **A new code comment claims a tighter sandbox than the PR's own evidence recorded.** `permissions.go` now says autonomous "runs with no approval prompt and the sandbox rejects writes outside cwd". The evidence's problem P4 records the opposite measurement: a write outside cwd *succeeded* because `/tmp` is a writable root, which is why the whole negative test had to be redone against a real-home path. The recipe states the correct rule. Left as-is, a maintainer reads the strictest of three shipped answers at the call site. | `internal/org/permissions.go:92-93` vs `docs/evidence/codex-seat-permissions-2026-09-18.md:48-50` ("`/tmp` が writable root だから") and `docs/recipes/codex-seat-permissions.md:10` ("writable root は cwd と `/tmp` 系だけ"). | Change to "rejects writes outside the sandbox's writable roots (cwd plus `/tmp`)". One clause, same commit. |
| MEDIUM | maintainability | **Recipe step 2 never tells the reader to spawn the edits seat, and reusing step 1's identifiers verifies nothing.** Step 2 opens directly with observations ("子プロセス引数が `--sandbox workspace-write` のみであること") with no spawn command, no second config file name, and no second `--org-id` / `--state-dir`. Step 1 pins `--org-id perm-auto --id reviewer --state-dir <scratch>/state-auto`, and cleanup is step 3, i.e. after step 2. A reader who repeats step 1's command with the edits config hits `Spawn`'s idempotent early return: the existing autonomous seat is returned with no new manifest events and no driver call, so `ps` still shows `--ask-for-approval never`. The evidence itself used a separate org per run. | `docs/recipes/codex-seat-permissions.md:56-62` (no spawn line) vs `:44-47` (step 1's command); `internal/org/spawn.go:416-426` ("idempotent respawn of an already-spawned seat returns the existing seat … no new manifest events, no driver calls"); evidence `:57` (`codex-perm-auto-0918f`) vs `:84` (`codex-perm-edits1-0918`). | Add the step 2 spawn command with a distinct `--org-id`, `--state-dir`, and the edits `ralph.toml`, or move the cleanup step between the two modes and say the edits run reuses the identifiers after disband. |
| MEDIUM | maintainability | **The shell-alias hazard is scoped to verification time, but it breaks every codex seat spawn.** The recipe says "検証中は alias を外すか、alias を読まない HOME / rc で herdr を起動する". Nothing about this is verification-specific: herdr sends the command to the pane's interactive shell on every spawn, so an operator who removes the alias only for the verification run, flips `codex_verified = true`, and then runs real seats gets `spawn_failed` behind an `agent_start` timeout — the same three failures the evidence records. The evidence also notes claude seats should hit it (marked 未検証, correctly). The `/org` skill bullet names the agmsg writable root as the environment dependency but not the alias. | `docs/recipes/codex-seat-permissions.md:9` vs `docs/evidence/codex-seat-permissions-2026-09-18.md:34-40` (P1: `error: the argument '--model <MODEL>' cannot be used multiple times`, `spawn_failed` on retries 1–3, `ZDOTDIR` override defeated by `/etc/zshenv`); `.claude/skills/org/SKILL.md:193-198`. | Reword to cover normal operation ("検証中も運用中も"), and either add a clause to the skill bullet or record it as tech debt behind the planned `ralph doctor` alias check (plan Deviation notes, derived issue (a)). |
| LOW | readability | **A 184-column line in a file that wraps at 79.** The `## See also` bullet appended to `codex-setup.md` (and its byte-identical template copy) is one unwrapped 184-column line; every other line in that file is ≤ 79. | `docs/recipes/codex-setup.md:111` and `templates/base/docs/recipes/codex-setup.md:111`; file max column before this PR was 79. | Wrap at ~79 in both copies. |
| LOW | maintainability | **The evidence cites a plan path that does not exist yet.** The header points at `docs/plans/archive/2026-09-18-codex-seat-permission-verification.md`; the plan is under `docs/plans/active/` until `/pr` archives it. Correct after merge, broken for anyone reading the branch or reviewing the PR. Repo precedent splits by authoring time: pipeline reports cite `active/`, the `/pr`-time walkthrough cites `archive/`. | `docs/evidence/codex-seat-permissions-2026-09-18.md:5`; compare `docs/reports/verify-2026-09-16-…md:4` (active) and `docs/reports/walkthrough-2026-09-17-…md:4` (archive). | Cite `docs/plans/active/…` and let `/pr` rewrite it, or add "(merge 後は archive/)". |
| LOW | maintainability | **The evidence's `writable_roots` snippet contradicts the recipe's own rule about `~`.** The evidence shows the working value as `["~/.agents/skills/agmsg/db"]`; the recipe says to write an absolute path because `~` is not expanded. The `~` in the evidence is almost certainly the file-wide home redaction, but the snippet sits in a code span with no marker, so copying it reproduces exactly the readonly-database failure P3 describes. Separately, the recipe's "`~` は展開されない" is not backed by anything recorded in the evidence — nothing there tried the tilde form. | `docs/evidence/codex-seat-permissions-2026-09-18.md:46` vs `docs/recipes/codex-seat-permissions.md:17`. | Mark the evidence value as redacted (e.g. `["<home>/.agents/skills/agmsg/db"]`), and either cite where the tilde behavior was observed or soften to "絶対パスで書く(展開されない可能性がある)". |
| LOW | maintainability | **Both raw-evidence pointers are unresolvable after the session.** The evidence's only raw-log pointer is the session scratchpad (`<scratch>/codex-perm/logs/…`), which is session-scoped and redacted, and the plan's Deviation notes cite `docs/evidence/verify-2026-09-18-072736.log`, which is gitignored (confirmed untracked). The EVIDENCE-as-pointers principle assumes the pointer resolves for a later reader. | `docs/evidence/codex-seat-permissions-2026-09-18.md:6`; plan Deviation notes, last bullet. | One clause saying the raw logs were not retained, so this file is the record of record. |
| LOW | readability | **The new recipe is the only Japanese file in a directory of four English ones, and the pointer this PR adds to it is English.** `adding-a-language-pack.md`, `agent-teams.md`, `codex-setup.md`, `worktrees.md` contain zero CJK lines; the new recipe has 33, and both copies ship downstream via `templates/base/docs/recipes/`. Long unwrapped lines are house style for Japanese docs here (specs reach 2714 columns), so only the language choice diverges. | `docs/recipes/*.md` CJK line counts (0/0/0/0 vs 33); `docs/recipes/codex-setup.md:111` (English) links to the Japanese file. | Defensible either way — skills and specs in this repo are Japanese. If it stays Japanese, consider making the See-also line Japanese too, or note the convention somewhere so the next recipe does not have to guess. |
| LOW | typo | **A TOML fragment in prose is not valid TOML if copied.** "edits 用は上に `[org.permissions.roles] reviewer = \"edits\"` を足す" puts a table header and a key on one line. The skill bullet uses the correct two-part phrasing, and the rest of the recipe's config is in fenced blocks. | `docs/recipes/codex-seat-permissions.md:36`; same shorthand in `docs/evidence/…:18`; contrast `.claude/skills/org/SKILL.md:191-192`. | Show it as a two-line fenced block, or phrase it as "`[org.permissions.roles]` に `reviewer = \"edits\"` を足す". |
| LOW | maintainability | **The surface an operator actually hits still uses the old wording and does not name the new recipe.** The fail-closed error reads "codex seat permission mode %q not yet live-verified … set [org.permissions].codex_verified=true after live-verifying your codex CLI's flags". After this PR the authoritative how-to is `docs/recipes/codex-seat-permissions.md`, and the error is the one place a blocked operator is guaranteed to read. Deliberately out of scope (the plan restricts `permissions.go` to comments) and the string is asserted by substring in tests, so this is a follow-up, not a cycle-1 fix. | `internal/org/permissions.go:121` and `:126`; assertion at `internal/org/permissions_test.go:94` (`strings.Contains(err.Error(), "not yet live-verified")`). | Follow-up issue: append "see docs/recipes/codex-seat-permissions.md" to both error strings, keeping the "not yet live-verified" substring the tests pin. |

## Positive notes

- Redaction is genuinely clean, not just AC-deep: zero `/Users/` paths, zero UUIDs, zero name/employer strings across the evidence, both recipe copies, the skill copies, and the config/Go comments. The `529b360` follow-up that replaced the developer path with `<your home directory>` also removed the only template-purity trap in the diff.
- Every mirror pair is byte-identical (`cmp`): recipe root/template, `codex-setup.md` root/template, and all four `/org` SKILL.md copies. `docs/evidence/` is a `check-sync.sh` ROOT_ONLY prefix, so the single-copy evidence file is correct.
- The Go change really is comment-only, verified mechanically rather than by eyeballing the hunk, and the new comment block matches the file's 76-column comment style.
- The docs' checkable factual claims hold up: `spawned` Details carry only `scope=…`/`permission_mode=…` and never the argv (`internal/org/spawn.go:827-837`); the agmsg team is `ralph-<org-id>`, so the recipe's `history.sh ralph-perm-auto lead 20` is right; every flag in the recipe's spawn command exists on `ralph org spawn`; and `ralph doctor` really has no `codex_verified` check (`checkOrgEnvelope` is an info line, `checkCodexEffectiveConfig` reads the project `.codex/config.toml`, not `~/.codex/config.toml`).
- The evidence is internally consistent where it is easy to slip: JST 15:36–16:19 maps to the stated UTC 06:36–07:19 and both RESULT timestamps (07:13:02Z, 07:14:29Z) fall inside it; org ids are distinct per run and match the run each judgement is attached to; the P1–P5 problems are each referenced from the run that hit them.
- The three-way judgement is reported honestly rather than rounded up: E1 is labelled partial, the conclusion table marks "edits が cwd 外を問い合わせる" as 条件付き, and the recipe repeats the `approval_policy = "never"` caveat at the point of action instead of only in the summary. The claude-seat alias collision is explicitly marked 未検証.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| The codex fail-closed runtime error (`internal/org/permissions.go:121,126`) still says "not yet live-verified" and names no recipe, so a blocked operator gets no pointer to `docs/recipes/codex-seat-permissions.md` | An operator hitting the gate has to find the recipe on their own; the error's framing also predates this PR's "per-machine opt-in by design" repositioning | The plan's Non-goals restrict `internal/org/permissions.go` to comment changes, and the string is pinned by substring in `internal/org/permissions_test.go:94` | Next change to `permissionArgsForDriver`, or the first operator report of "I hit the fail-closed error and did not know what to do" | `docs/plans/active/2026-09-18-codex-seat-permission-verification.md` (Non-goals), this report (LOW, last row) |

_(This reviewer did not edit `docs/tech-debt/README.md` — the orchestrator owns that append. Add the row only if the follow-up is not taken in this cycle.)_

## Recommendation

- Merge: **not as-is.** No CRITICAL findings, but one HIGH is open and the repo's approval criteria block on HIGH. Fix HIGH-1 (comment-only, two files) and the two one-clause MEDIUMs (`permissions.go:92-93`, recipe step 2's missing spawn command, recipe line 9's scoping), then merge. The MEDIUMs are cheap enough that deferring them costs more in register churn than fixing them.
- Follow-ups: (1) the fail-closed error string pointer, recorded as tech debt above; (2) the derived issues the plan already lists — doctor-side alias detection, `ralph org send` Enter timing, auto-injecting the agmsg writable root, retired-model migration in `honored` receipts; (3) decide whether `docs/recipes/` is English-only and write that down somewhere the next recipe author will see.

## Known gaps

- No tests, static analysis, `check-sync.sh`, `check-skill-sync.sh`, or `check-template-purity.sh` were run — `/verify` and `/test` own those. Mirror equality here was checked with `cmp` only.
- The codex behavioral claims (sandbox rejection, approval-prompt conditions, model retirement migration) were not independently reproduced; this review compared the prose against the committed evidence and the repo's own code, not against a live codex run.
- Whether codex expands `~` inside `sandbox_workspace_write.writable_roots` is unresolved — the LOW above reports the disagreement between two shipped surfaces, not a verdict on codex's behavior.
- Spec compliance against the plan's ACs, and documentation drift against `docs/specs/2026-08-01-org-runtime.md`, were deliberately not assessed.
