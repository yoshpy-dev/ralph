# Self-review report: org-codex-slug-update-procedure

- Date: 2026-09-18
- Plan: `docs/plans/active/2026-09-18-org-codex-slug-update-procedure.md` (issue #156)
- Reviewer: `reviewer` subagent (Claude Code, standard flow, cycle 1)
- Scope: diff quality only for `git diff 93cf3bb...HEAD` on `docs/org-codex-slug-update-procedure` at HEAD `081635d` (6 files, +165/-0, docs only). Tests, static analysis, spec-compliance and doc-drift checks were not run — those belong to `/verify` and `/test`. For a prose-only diff, "diff quality" is read as: is every technical claim true of the code as it stands, is the shipped copy valid for a downstream reader, and does the new text agree with its siblings.

## Evidence reviewed

- `git diff 93cf3bb...HEAD` plus per-commit stat: `5242f0c` / `321096e` / `217c7aa` / `081635d` (plan bookkeeping), `4fa920e` (Slice A — one paragraph in `.claude/skills/org/SKILL.md` + 3 mirrors), `b66ec5c` (Slice B — one `###` section in `docs/specs/2026-08-01-org-runtime.md`).
- Mirror check: `diff` of `.claude/skills/org/SKILL.md` against `.agents/skills/org/SKILL.md`, `templates/base/.claude/skills/org/SKILL.md`, `templates/base/.agents/skills/org/SKILL.md` → all three identical (0 lines out). New paragraph at `:61-70` in all four.
- Shipped-copy validity: the new paragraph names only `ralph.toml`, `~/.codex/models_cache.json`, `brew`, `ralph version`, `ralph upgrade` — no `internal/`, `cmd/`, `packs/` path and no meta-repo deictic ("this rollout", "this phase"). The spec section does cite `internal/` and `cmd/`, and `docs/specs/` is a `ROOT_ONLY_EXCLUSIONS` entry (`scripts/check-sync.sh:56`) with `templates/base/docs/specs/` holding only `.gitkeep`, so that text stays maintainer-only.
- Claim-by-claim verification against the code: `checkCodexModelSlugs` and its "Org codex model slugs" name (`internal/cli/doctor_codex_models.go:72`), the cache-path resolution `$CODEX_HOME/models_cache.json` else `~/.codex/models_cache.json` (`:28-38`), the single `os.ReadFile` with no refresh and no `ModTime` call (`:98`), the warn branch (`:129`); `scaffold.EmbeddedFS = ralph.TemplatesFS` (`cmd/ralph/main.go:18`) and the absence of any network/self-update path in `internal/cli/upgrade.go`; seed-once ownership of `ralph.toml` and core ownership of `.claude/skills/` + `scripts/` (`docs/specs/2026-08-17-overlay-scaffold-v2.md:45`, `:49`); `classifySeed` emitting a seed `AdvisoryEntry` instead of an op when the template changed (`internal/upgrade/replaceplan.go:307-317`, `:416-419`; `internal/upgrade/replaceplan_test.go:336` "seed is advisory-only, never auto-applied").
- Lock-step surfaces: `internal/config/config.go:142-150` (`Default()`), `templates/base/ralph.toml:29-39`, `scripts/ralph-config.sh:44` + `templates/base/scripts/ralph-config.sh:44`, and what `internal/config/defaults_sync_test.go` actually reads (`:95` root `scripts/ralph-config.sh`, `:102` `templates/base/ralph.toml` — no third file).
- Named test files really do hard-code the default slug: `internal/config/config_test.go:127,743`, `internal/org/envelope_summary_test.go:84,95-96,153,155`, `internal/cli/org_test.go:775,777`.
- Full-repo slug sweep: `git grep -n gpt-6-astra` (17 hits outside `docs/plans/`, `docs/reports/`), used to test the (a) enumeration for completeness.
- Verification-chain claim (b): `scripts/run-verify.sh` → `scripts/verify.local.sh:185-198` (`check-sync.sh`, `check-skill-sync.sh`) and `packs/languages/golang/verify.sh:109` (`go test ./...`, which is what runs `defaults_sync_test`). Claim confirmed.
- Env-precedence check for the skill's "follow the new default" sequence: `git grep RALPH_ORG_` over `internal/**/*.go` + `cmd/**/*.go` → only `RALPH_ORG_STATE_DIR` and `RALPH_ORG_AGMSG_HOME` are read by Go; nothing reads `RALPH_ORG_MODEL_POOL`, and the only files mentioning `ralph-config.sh` are itself, its mirror, and `tests/test-ralph-config.sh`.
- Style: display-width measured per line (east-asian width aware). The new skill paragraph runs 65-77 columns; 42 of 126 prose lines in that file already exceed 72, max 84 outside frontmatter, so the new wrap sits inside the file's existing distribution. The new spec section uses long lines, matching the rest of that spec. The new `###` nests under `## Summary` alongside the existing `### 2026-09-16 改訂`.
- Hygiene: no `git status` residue, no U+FFFD, no TODO/FIXME, no credentials, no commented-out leftovers, no unrelated files in the diff.

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | **H1: the shipped operator paragraph tells the reader to act on a doctor warn, while the same PR's maintainer section establishes that the warn can be stale — and the operator's action mutates their own config.** Spec (d) states the case plainly: `checkCodexModelSlugs` "はローカルの `models_cache.json` を読むだけで、更新も鮮度確認もしない", so "観測は毎回 **cache を更新してから** 行う", and it records a slug that vanished for hours on 2026-09-17 and came back. The operator paragraph shipped to every scaffolded project carries neither half: "Check が warn したら … 自分の `ralph.toml` の `[org].model_pool` と、そのスラッグを参照する `[org.roles]` を書き換える". An operator whose cache was last written days ago, or who hit the transient window the PR itself documents, edits a seed-once file to remove a slug that still exists — and seed-once means nothing upstream ever corrects it back. This is the adopted Codex advisory MEDIUM-2 landing on the maintainer surface but not on the operator surface written in the same commit. | `.claude/skills/org/SKILL.md:61-63` (and the three identical mirrors) vs `docs/specs/2026-08-01-org-runtime.md:22`; the mechanism at `internal/cli/doctor_codex_models.go:98` (`os.ReadFile` only — no `ModTime`, no refresh, no exec) and the warn text at `:129-131`, which names the path but says nothing about freshness. The 2026-09-17 transient is recorded at `docs/specs/2026-08-01-org-runtime.md:17`. | Add one clause to the shipped paragraph before the "書き換える" instruction: refresh the cache first (launch codex once, or `ralph doctor --probe-models`, which per `internal/cli/doctor.go:36-37` launches a minimal invocation per pool entry), and do not remove a slug on a single warn because a slug can disappear for a few hours and return. Keep it to one sentence — the operator does not need the 2〜4 週間 maintainer criterion, only "confirm before you edit". |
| MEDIUM | maintainability | **M1: the (a) "同時に変える面" list is a closed enumeration and misses two surfaces that hard-code the same slug, neither of which any gate catches.** `git grep -n gpt-6-astra` finds two live sites outside the list: (i) `.claude/skills/org/SKILL.md:39` (×4 mirrors) — "省略するとプール先頭(claude は `fable`、codex は `gpt-6-astra`)へ … フォールバックする", which is a claim about the *head* of the pool, so replacing the head slug falsifies it in four files while the checklist updates only the 「既定の model_pool」表 15 lines below; and (ii) `templates/base/ralph.toml:55` — the commented `[org.roles]` example `# reviewer = ["opus", "gpt-6-astra"]`, outside the `model_pool` block the list names. `defaults_sync_test` reads neither (it compares parsed values, not prose or comments), `check-sync.sh` / `check-skill-sync.sh` only compare copies against each other, and `ralph doctor` validates the pool against the cache — so a maintainer who follows (a) and passes (b) ships a document that contradicts itself in the same file. | The enumeration at `docs/specs/2026-08-01-org-runtime.md:19` ("`.claude/skills/org/SKILL.md` の「既定の model_pool」表", "`templates/base/ralph.toml` の `model_pool`"); the missed sites at `.claude/skills/org/SKILL.md:39` / `.agents/skills/org/SKILL.md:39` / `templates/base/.claude/skills/org/SKILL.md:39` / `templates/base/.agents/skills/org/SKILL.md:39` and `templates/base/ralph.toml:55`. Head-of-pool semantics confirmed at `internal/org/envelope_summary_test.go:84-96` (`DefaultModelForDriver(codex) == "gpt-6-astra"`). | Prefer the mechanism over a longer list: close (a) with "最後に `git grep -n '<旧スラッグ>'` で残存を掃く(`docs/insights/events/` の履歴は除く)". Naming the two sites as well is cheap, but the grep is what survives the next surface nobody anticipated. |
| MEDIUM | maintainability | **M2: "追従には … → `ralph upgrade` の順が要る" states a requirement the mechanism does not have.** For the `model_pool`-omitted case the sentence describes, the effective pool comes from `config.Default()` inside the binary, so the new default takes effect the moment the binary is replaced; `ralph upgrade` replaces core scaffold files and never changes the effective pool in either configuration (a declared `model_pool` lives in seed-once `ralph.toml`, which upgrade does not write; `scripts/ralph-config.sh`'s `RALPH_ORG_MODEL_POOL` has no Go reader at all). The trailing sentence — "`ralph upgrade` 単体は … 新しい既定は届かない" — is exactly right and is the load-bearing warning; the "順が要る" clause in front of it promotes a good hygiene step into a precondition. Spec (c) gets this right by assigning upgrade to "core(skill・`ralph-config.sh`)を置換する", so the two surfaces in this PR describe the same sequence with different force. | `.claude/skills/org/SKILL.md:66-70` vs `docs/specs/2026-08-01-org-runtime.md:21`; `internal/config/config.go:142-150` (the inherited default) and `cmd/ralph/main.go:18`; `git grep RALPH_ORG_ -- 'internal/**/*.go' 'cmd/**/*.go'` returns only `RALPH_ORG_STATE_DIR` / `RALPH_ORG_AGMSG_HOME`. Also note the driver filter the sentence omits: omitting `model_pool` inherits the default *narrowed to declared drivers* (`docs/specs/2026-08-01-org-runtime.md:11` item (e), and the comment shipped at `templates/base/ralph.toml:17-21`). | Rephrase to separate effect from hygiene: binary update is what moves the inherited default; `ralph upgrade` is what brings the core skill docs and `ralph-config.sh` along, and running it before the binary update applies the old templates. One clause on the driver filter would also stop the sentence from over-promising for a narrowed `driver_pool`. |
| LOW | typo | **L1: (a) says "3 面ロックステップ" and then lists four paths, attributing all of them to `defaults_sync_test.go`.** That test reads exactly two files — root `scripts/ralph-config.sh` (`:95`) and `templates/base/ralph.toml` (`:102`) — and compares both against `config.Default()`. The template copy `templates/base/scripts/ralph-config.sh` is gated by `scripts/check-sync.sh`, not by the test. As written, a maintainer could change the template copy alone and expect `go test` to catch it. | `docs/specs/2026-08-01-org-runtime.md:19` vs `internal/config/defaults_sync_test.go:91-130`; the mirror gate is `scripts/verify.local.sh:185-186` → `scripts/check-sync.sh`. | "3 面(`Default()` / `templates/base/ralph.toml` / `ralph-config.sh`)を `defaults_sync_test.go` が、`ralph-config.sh` の root↔template 一致を `check-sync.sh` が検査する" — same length, correct attribution. |
| LOW | maintainability | **L2: both new passages say `ralph.toml` is never updated, and neither mentions the one signal that tells a downstream operator the default changed.** `ralph upgrade` emits a seed `AdvisoryEntry` for `ralph.toml` whenever the embedded template's hash differs from the recorded one, and that advisory is rendered as a diff in `docs/reports/upgrade-*.md` (it even keeps the run from counting as fully converged). That is precisely where a new default pool becomes visible after a binary update, and it is the natural entry point for the operator procedure the skill paragraph describes. | `internal/upgrade/replaceplan.go:307-317` and `:416-419`; `internal/cli/upgrade_v2.go:259-261`; `internal/upgrade/replaceplan_test.go:336`, `:481-504`; the ownership contract at `docs/specs/2026-08-17-overlay-scaffold-v2.md:49` ("advisory diff の表示・レポート出力のみ"). Neither `.claude/skills/org/SKILL.md:64-66` nor `docs/specs/2026-08-01-org-runtime.md:21` mentions it. | One clause in the skill paragraph: upgrade leaves `ralph.toml` alone but reports the template's new content as an advisory diff in the upgrade report, so compare there instead of guessing. |
| LOW | readability | **L3: "(`$CODEX_HOME` 配下)" restates, less precisely, a gloss that appears four lines above.** The preceding paragraph already says "`~/.codex/models_cache.json`(既定。`$CODEX_HOME` で上書き可)", which matches the resolver: `CODEX_HOME` replaces the whole directory rather than containing `~/.codex`. The new parenthetical reads as if the default path sat under `$CODEX_HOME`. | `.claude/skills/org/SKILL.md:62` vs `:57-58`; resolver at `internal/cli/doctor_codex_models.go:28-38`. | Drop the parenthetical and refer to the same file ("同じ `models_cache.json`"), or repeat the earlier wording verbatim. |
| LOW | readability | **L4: the "判定基準" heading promises a criterion and the text gives a 2-week-wide range.** "更新済み cache で 2〜4 週間続けて消えたままの場合だけ既定から外す" leaves the actual decision undefined at the moment it is needed, which is the moment someone reaches for this section. | `docs/specs/2026-08-01-org-runtime.md:22`. | Pick the floor and let judgment extend it: "2 週間連続で消えたままなら外す判断に入る(迷う場合は 4 週間まで観測を延長)". |

## Positive notes

- The four skill mirrors are byte-identical, and the new paragraph is free of `internal/` / `cmd/` references and of meta-repo deictics — both failure modes this repo has shipped before. The maintainer-only text with code pointers was correctly routed to `docs/specs/`, which `scripts/check-sync.sh:56` excludes from the template tree.
- Every mechanism claim I could check against code is accurate: the doctor check's name, its cache-path precedence, its read-only/no-refresh behavior, `scaffold.EmbeddedFS` as the reason upgrade applies the installed binary's templates, `ralph.toml` as seed-once and `.claude/skills/` + `scripts/` as core, and the (b) chain (`run-verify.sh` really does reach `check-sync.sh`, `check-skill-sync.sh`, and `go test ./...`).
- The three test files named in (a) do hard-code `gpt-6-astra`, including the non-obvious one (`internal/org/envelope_summary_test.go` asserts it as the codex head-of-pool), so that part of the enumeration was verified rather than guessed.
- Telling operators to fix `[org.roles]` alongside `model_pool` is not decoration: `internal/config/config.go:299` hard-fails with "`[org.roles].%s references model %q not present in [org].model_pool`", so skipping it would break `ralph org` outright.
- The (d) observation protocol is falsifiable by construction — the mtime gate means a stale-cache observation is recorded as inconclusive instead of silently counting toward the removal decision. That discipline is what makes H1's omission on the operator side fixable with one sentence rather than a redesign.
- Plan bookkeeping is honest: the Deviation notes record the actual mtime measurement (12:31 → 12:40:07) behind the "codex 起動で cache が書き直される" claim, and AC-3/AC-5 are left unchecked because they belong to `/pr`.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |

_No rows. All six findings are one-clause edits inside files this PR already touches; none is deferred work._

## Recommendation

- Merge: **conditional**. No CRITICAL, so the pipeline continues, but H1 and M1 should be fixed before `/pr` — H1 because the shipped copy asks a downstream operator to edit a seed-once file on evidence this same PR documents as possibly stale, and M1 because an incomplete "同時に変える面" checklist reproduces the exact drift the section exists to prevent, with no gate behind it. M2 is a one-word softening. L1-L4 are cheap enough to fold into the same commit.
- Follow-ups:
  - The warn text at `internal/cli/doctor_codex_models.go:129-131` names the cache path but not its age. Emitting the cache's mtime in the detail string would make the freshness caveat self-evident at the point of use instead of only in prose. Out of scope here (docs-only PR); worth a separate issue.
  - `scripts/ralph-config.sh`'s `RALPH_ORG_MODEL_POOL` has no reader in Go or in any shell script other than its own test, yet it is maintained in lock-step. Pre-existing, unrelated to this diff, and only noted because (a) now documents the maintenance cost.
- Known gaps: I did not execute `codex exec --sandbox read-only 'echo ok' </dev/null` (the command (d) recommends), so its sandbox interaction with the cache write is unverified by me; the plan's recorded measurement came from the plan-advisory `codex exec` run, not from that exact form. The section's own mtime gate contains the risk — a form that fails to refresh shows up as an inconclusive observation rather than a wrong one. I also did not run tests, linters, or sync gates, per the `/self-review` scope.
