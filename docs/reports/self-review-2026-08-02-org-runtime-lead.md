# Self-review report: org-runtime-lead

- Date: 2026-08-02
- Plan: `docs/plans/active/2026-08-02-org-runtime-lead.md`
- Reviewer: `reviewer` subagent (`/self-review`, Cycle 1)
- Scope: diff quality only — `git diff 8ec4a48...HEAD` on `feat/org-runtime-lead` (36 files, +3949/−172). No tests, static analysis, spec-compliance, or documentation-drift checks were run (those belong to `/verify` and `/test`).

## Evidence reviewed

- `git diff 8ec4a48...HEAD` in full for: `internal/org/{spawn,verbs,manifest,seat,prompts,permissions,lockfile,report,envelope_summary}.go`, `internal/cli/{org,doctor}.go`, `internal/config/{config,defaults_sync_test}.go`, `scripts/ralph-config.sh` (+ template mirror), `templates/base/ralph.toml`, `docs/tech-debt/README.md`, `CLAUDE.md`.
- Full reads of the new files: `internal/org/permissions.go`, `internal/org/lockfile.go`, `internal/org/report.go`, `internal/org/envelope_summary.go`, `internal/org/prompts/lead.md`, `.claude/skills/org/SKILL.md`.
- Mirror check: `diff -q .claude/skills/org/SKILL.md` vs `.agents/skills/org/SKILL.md`, `templates/base/.claude/skills/org/SKILL.md`, `templates/base/.agents/skills/org/SKILL.md` — all three byte-identical.
- Commit list: `9e3f8b5` (plan) → `c04186e` (tech-debt batch) → `9fe2af1` (permission modes) → `4b7a2e6` (start/report) → `7be4993` (FieldsSeq refactor) → `039b18e` (/org skill) → `d35f157` (herdr `done` fix) → `428177b` (evidence).
- Grep sweeps: debug markers (`TODO|FIXME|fmt.Print|log.Print|console.log`) — none added; secrets/credentials — none; permission-mode string literals; `internal/`-path references inside the shipped `templates/base/` skill copy.
- `docs/evidence/org-lead-smoke-2026-08-02.txt` (live herdr envelopes, incl. `"agent_status":"done"` for an interactive claude seat at line 44).
- Plan sections honoured as intentional and therefore not re-litigated: "Implementation notes (deviations)" (3 live-smoke findings), "Design decisions" (codex fail-closed, autonomous default + AC-2b gate).

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | The live-smoke discovery that herdr reports an interactive agent resting at its input prompt as `done` (not `idle`) was fixed at **one** call site only. `Send` now waits on `{"idle","done"}`, but the `wait` verb still defaults to `--until idle` **and** `--timeout-ms 0`, which `Wait` maps to "no context timeout" and the herdr adapter maps to "omit `--timeout`" — i.e. an unbounded block. Both new docs added by this same PR instruct the lead to use it that way. A headless lead following its own prompt hangs forever against a perfectly receptive seat. | `internal/org/verbs.go:118-126` (Send fix + its own comment: "waiting on `idle` alone times out against a perfectly receptive seat"); `internal/cli/org.go:367` `--until` default `"idle"`, `:368` `--timeout-ms` default `0`; `internal/org/verbs.go:185-193` (`if p.TimeoutMS > 0` → no timeout otherwise); `internal/org/driver/herdr.go:161,168` ("timeoutMS <= 0 omits --timeout"); `.claude/skills/org/SKILL.md:43` (`--until idle` example); `internal/org/prompts/lead.md:68` ("`ralph org wait` で idle になるまでブロック"); `docs/evidence/org-lead-smoke-2026-08-02.txt:44` (`"agent_status":"done"`). | Change `wait`'s `--until` default to `idle,done` (mirroring the Send fix), give `--timeout-ms` a bounded non-zero default, and update the `SKILL.md` example + `lead.md` line 68 to match. |
| MEDIUM | maintainability | The canonical `ralph org start` example shipped in the lead's operating manual omits `--scope`, but the AC-2b gate added in this same PR rejects exactly that command under the shipped default (`[org.permissions].default = "autonomous"`). The repo's own new test pins the rejection, so the code and the manual disagree by construction. | `.claude/skills/org/SKILL.md:49` and `:86` (`ralph org start --org-id X --cwd . "<task>"`, no `--scope`, no `--allow-unscoped`); `internal/org/spawn.go:281` (gate); `templates/base/ralph.toml:167` (`default = "autonomous"`); `internal/cli/org_test.go` `TestOrgStart_MissingScope_AutonomousDefault_RejectedUnlessAllowUnscoped`. | Add `--scope "<担当範囲>"` to both `org start` examples in `SKILL.md` (the `spawn` example on line 41 already does this correctly). |
| MEDIUM | maintainability | `compensateStale` — which performs a herdr `pane send-keys C-c` round trip bounded by the caller's `--timeout-ms` (default 60 000 ms) — now runs **inside** `withManifestLock`. This contradicts both new comments that claim the lock excludes driver round trips, and because `manifestLockTimeout` is only 5 s, a concurrent spawn racing a stale-seat compensation fails with `org: manifest lock: ... timed out after 5s` instead of queueing behind it. | `internal/org/spawn.go` locked closure calls `o.compensateStale(ctx, p, *existing)`; `internal/org/spawn.go` comment "The lock is scoped to this section only (not the workspace/agent/agmsg side effects that follow)"; `internal/org/lockfile.go:27` (`manifestLockTimeout = 5 * time.Second`), `:49` ("not the subsequent herdr/agmsg round trip"); `internal/org/spawn.go:1097-1105` (`compensateStale` → `compensatePaneCtx` → `PaneSendKeys`). | Either move `compensateStale`'s driver call outside the lock (compensate first, then re-enter the lock for the fresh read+validate+append), or drop the "no driver round trip inside the lock" claim from both comments and raise `manifestLockTimeout` above the spawn timeout so the contention path degrades to waiting rather than failing. |
| MEDIUM | naming | The permission-mode enum has no grep-able constants: the three values are re-spelled as bare literals in 4 independent places across 2 packages, plus twice more inside hand-written error strings. Adding or renaming a mode requires 4 coordinated edits with zero compiler help. This is the same asymmetry this PR fixed for the lead identity (`LeadIdentity` was extracted precisely so no call site spells `"lead"`), left unfixed for its sibling convention. | `internal/config/config.go:217,227-229,409,413`; `internal/org/permissions.go:16,62,64,66,72`; `internal/org/spawn.go:281` (`resolvedPermMode == "autonomous"`); vs `internal/org/spawn.go:19` (`const LeadIdentity = "lead"` with a doc comment mandating its use at every call site). | Export `config.PermissionModeAutonomous/Edits/Guarded` (or an `OrgPermissionMode` string type), build `orgPermissionModeAllowed` and the two error messages from that set, and use the constants at `permissions.go`'s switch and `spawn.go`'s gate. |
| MEDIUM | maintainability | The `templates/base/` copy of the new skill points at `internal/org/prompts/lead.md` and `internal/org/prompts/` — paths that exist only in the ralph CLI's own repo, never in a scaffolded project. The embedded `lead.md` prompt has the same problem (`internal/org/protocol`). This is the recurring "byte-identical mirror, downstream-invalid content" class: `.claude/rules/model-routing.md` is in `check-sync.sh`'s `KNOWN_DIFFS` for exactly this reason, and `agent-messaging.md` shipped 7 `internal/org/protocol` references downstream on 2026-08-02 for the same reason. | `templates/base/.claude/skills/org/SKILL.md:7,68,87`; `internal/org/prompts/lead.md:49` (`internal/org/protocol` が正としてバリデーションを行う); precedent in `.claude/rules/agent-messaging.md` ("that source tree is not part of what `ralph init` scaffolds"). | Rephrase the three references as "the role prompt templates embedded in the `ralph` binary" (downstream-valid, no `KNOWN_DIFFS` entry needed), or add the skill to `KNOWN_DIFFS` and ship a trimmed copy. `lead.md` should use the same "the `ralph` CLI validates this" wording `agent-messaging.md` already settled on. |
| LOW | exception-handling | The AC-2b fail-closed rejection returns a bare `SpawnResult` without going through `reject()`, so — unlike every other envelope rejection — it appends no `rejected` manifest event and no receipt. An autonomous lead that retry-loops unscoped spawns leaves zero trace in `ralph org report`'s timeline. The choice is documented and consistent with the identifier-shape checks, so this is a note, not a defect. | `internal/org/spawn.go:275-285` ("This is a plain rejection -- no `rejected` manifest event"); contrast `internal/org/spawn.go:871-882` (`reject()` writes both event and receipt). | Consider routing the gate through `reject()` so the org report's timeline shows refused spawn attempts; if kept as-is, say so in `SKILL.md`'s permission section so an operator debugging a silent lead knows where not to look. |
| LOW | maintainability | `newOrgStartCmd` loads and parses the org config twice per invocation: once directly via `resolveOrgConfig(*configPath)` for the `--model` default, then again inside `newOrgRuntime`, which calls `resolveOrgConfig` itself. Two `ralph.toml` reads, and two chances to diverge if config resolution ever stops being deterministic. | `internal/cli/org.go` `newOrgStartCmd` (`orgCfg, err := resolveOrgConfig(*configPath)` then `rt, err := newOrgRuntime(*stateDir, *configPath)`); `internal/cli/org.go:86-99` (`newOrgRuntime` → `resolveOrgConfig`). | Have `newOrgRuntime` return the resolved `config.OrgConfig` alongside the runtime (or read it off `rt.Config`) and drop the standalone call. |
| LOW | secrets | The new evidence file adds 3 more absolute `/Users/hiroki.yoshioka/...` paths and a herdr/claude session UUID — the exact unredacted-artifact class this very diff re-affirmed as open tech debt when it split item (d) into its own row. Not credential leakage, but the debt row's own trigger ("apply it retroactively to ... any other pre-convention evidence files") now has one more file to cover. | `docs/evidence/org-lead-smoke-2026-08-02.txt:5,44,134`; `docs/tech-debt/README.md` (new standalone evidence-redaction row). | No action required this cycle; the debt is correctly tracked. Optionally `sed` the home path to `~` before merge, since the file is new. |
| LOW | naming | `/org` is the only skill whose frontmatter `description:` is written in Japanese; all 12 existing skills use English. `description` is the auto-invocation matching surface and ships downstream via `templates/base/`. The body being Japanese is consistent with the repo's other Japanese docs — the frontmatter is the one cross-skill uniform field. | `.claude/skills/org/SKILL.md:3` vs `.claude/skills/{plan,work,pr,verify,test,loop,spec,sync-docs,self-review,cross-review,release,audit-harness}/SKILL.md`. | Translate the `description:` line to English; leave the body as-is. |
| LOW | unnecessary-change | The plan file added by this diff still reads `Status: Draft` with every acceptance-criterion checkbox and most of the Progress checklist unchecked, though all 5 slices and both live smokes are complete and recorded in the same file's "Implementation notes (deviations)". `.claude/rules/planning.md` requires progress checklists be kept current while the task is in flight. | `docs/plans/active/2026-08-02-org-runtime-lead.md:3` (`Status: Draft`), `:68-81` (AC-1..AC-12 all `[ ]`), `:132-140` (Progress checklist), vs `:127-130` (deviations documenting completed smokes A/B). | Flip `Status` to `In review`, tick AC-1..AC-11b and the "Implementation started" / "Review artifact created" boxes. |

## Positive notes

- `RALPH_ORG_PERMISSION_DEFAULT` is deliberately **not** added to `ralph-config.sh`'s export block, with a comment explaining why. This is exactly right: exporting it would have inverted the precedence for the Go env-first resolver, the trap that bit a previous PR.
- All four copies of the new skill (`.claude/`, `.agents/`, and both `templates/base/` mirrors) are byte-identical — verified with `diff -q`, all three comparisons clean.
- Both new fail-closed gates are genuinely closed, not advisory: `permissionArgsForDriver` rejects non-`guarded` codex seats with a loud, self-explaining error rather than emitting a stub argv, and the AC-2b `--scope` gate runs before the `DryRun` branch so dry runs are gated identically to real ones.
- The `herdr_agent_name` persistence is correctly `omitempty`-additive with a documented derive-fallback (`resolvedHerdrAgentName`), and `Wait`'s deliberate non-adoption is documented at the type, not buried in a test.
- `dryRunSpawn`'s two swallowed errors are now both propagated through the same `failStep` the real path uses, and the `absPath` test seam mirrors the existing `lookPath` precedent rather than inventing a new injection style.
- `withManifestLock` correctly separates the acquire-timeout error from `fn`'s own error, and `acquireFlock` polls `LOCK_EX|LOCK_NB` so the wait is bounded strictly by the context rather than by a blocking `flock(2)`.
- No secrets, credentials, debug statements, commented-out code, or `TODO`/`FIXME` markers anywhere in the +3949 lines.
- 61 new test functions; the tech-debt register rows are struck through with resolution provenance comments rather than deleted, preserving traceability.
- `syscall.Flock` is Linux/macOS-only, but the repo ships no Windows target (`.github/workflows/release.yml`), so this is not a live portability gap.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| _(none)_ | | | | |

No new tech-debt rows are proposed. Every finding above is either recommended for fix in this cycle (HIGH-1, MEDIUM-2..5) or is a note on an already-tracked item (LOW evidence redaction). If the fix cycle chooses to defer MEDIUM-4 (permission-mode enum constants), that is the one finding that warrants a `docs/tech-debt/README.md` row rather than an in-cycle fix.

## Recommendation

- Merge: **No-merge as-is** — no CRITICAL findings, so the pipeline is not blocked, but HIGH-1 should be fixed before `/pr`. A headless lead following the manual and prompt this PR ships will block indefinitely on `ralph org wait`, which undercuts the PR's headline capability. MEDIUM-2 is a one-line doc fix in the same file and should ride along.
- Follow-ups:
  1. Fix HIGH-1 at all three sites (`--until` default, `SKILL.md:43`, `lead.md:68`) plus a bounded `--timeout-ms` default — this is the "partial fix leaves a gate site unpatched" pattern; grep for `idle` across `internal/`, `.claude/skills/`, and all mirrors before declaring it done.
  2. Fix MEDIUM-2 (`--scope` in both `org start` examples) and MEDIUM-5 (downstream-invalid `internal/` paths in the shipped skill + `lead.md`) — both docs-only, both in files this PR authored.
  3. Decide MEDIUM-3 (lock scope vs. its own comments) — fix the scope or fix the comments; leaving them contradictory is the worst option.
  4. MEDIUM-4 (permission-mode constants) and the LOW items can be batched or deferred.

---

# Cycle 2 (fix-and-revalidate)

- Date: 2026-08-02
- Reviewer: `reviewer` subagent (`/self-review`, Cycle 2)
- Scope: diff quality only — the code delta since the Cycle-1 review baseline, `git diff 0066b90...HEAD` (branch `feat/org-runtime-lead`, merge-base `8ec4a48`, HEAD `de4de50`). No tests, static analysis, spec-compliance, or documentation-drift checks were run.
- Delta commits reviewed: `9a22942` (self-review fixes: wait done-state, lock scope, mode constants) and `de4de50` (cross-review fix: idempotent-before-scope-gate reorder). Doc-only commits in the range (`0bb2b75`, `979b11d`, `f3dfd19`, `94f72f0`, `f11ed4b`, `4bd6d68`) are pipeline artifacts and were read for cross-reference, not reviewed as code.

## Evidence reviewed

- Full read of `git diff 0066b90...HEAD -- internal/` (801 lines) covering `internal/cli/org.go`, `internal/cli/org_test.go`, `internal/config/config.go`, `internal/org/permissions.go`, `internal/org/prompts/lead.md`, `internal/org/spawn.go`, `internal/org/spawn_test.go`.
- Targeted re-reads of the rewritten region: `internal/org/spawn.go:218-546` (Spawn's doc comment, the DryRun branch, the Phase 1 / Phase 2 lock split, `checkCapacityAndStart`), `:736-753` (`autonomousScopeGateErr`), `:990-1001` (`reject`), `:132-144` (`Org` struct), `internal/org/envelope.go:38-51` (`ValidateSpawn` = envelope + capacity), `internal/cli/org.go:95-112` (`newOrgRuntime`).
- Cycle-1 finding re-verification, one row at a time (see the regression table below), including grep sweeps for `--until` / `--timeout-ms` across `internal/`, `.claude/skills/`, `.agents/skills/`, `templates/base/` and both `org start` example sites.
- Mirror check re-run after the fix commits: `cmp .claude/skills/org/SKILL.md` vs `.agents/skills/org/SKILL.md`, `templates/base/.claude/skills/org/SKILL.md`, `templates/base/.agents/skills/org/SKILL.md` — all three byte-identical (exit 0).
- Debug/secret sweep over the delta (`TODO|FIXME|XXX|fmt.Print|log.Print|panic(`): none added.

## Cycle-1 finding regression status

| Cycle-1 finding | Status | Evidence |
| --- | --- | --- |
| HIGH-1 (`wait` unbounded, waits on `idle` only) | **Fixed at every site** | `internal/cli/org.go:376-377` (`--until` default `"idle,done"`, `--timeout-ms` default `60000`), `:345-352` (new `Long` explaining the live-probed `done` rest state and the `--timeout-ms 0` opt-out); `internal/org/prompts/lead.md:67-69` rewritten; `.claude/skills/org/SKILL.md:44` + all 3 mirrors. Grep for `--until` across `internal/`/`.claude/`/`.agents/`/`templates/` shows no remaining `--until idle`-only default or doc example. |
| MEDIUM-2 (`org start` examples omit `--scope`) | **Fixed at both sites** | `.claude/skills/org/SKILL.md:50` and `:87` now carry `--scope`; mirrors byte-identical. |
| MEDIUM-3 (`compensateStale` driver round trip inside the 5s manifest lock) | **Fixed** (see Cycle-2 M-2 for the window it opens) | `internal/org/spawn.go:521-546`: Phase 1's lock now stops at stale *detection* and releases; `o.compensateStale` runs lock-free; Phase 2 re-acquires for the capacity check + append. The contradicted comment is rewritten at `:341-360`. |
| MEDIUM-4 (permission-mode enum has no grep-able constants) | **Partially fixed** (see Cycle-2 L-3) | `internal/org/permissions.go:9-25` adds `PermissionModeAutonomous/Edits/Guarded`; all 5 `internal/org` literal sites converted. `internal/config/config.go:262-265` still spells the three literals, with a hand-sync comment. |
| MEDIUM-5 (`internal/` paths in the downstream-shipped skill + `lead.md`) | **Fixed** | `internal/org/prompts/lead.md:49` now reads "`ralph` CLI がランタイムでこれを正としてバリデーションを行う"; the three `templates/base/.claude/skills/org/SKILL.md` pointers are gone. Remaining `internal/` strings in the shipped copy (`:42`, `:118`, `:137`) are illustrative `--scope` example values, not repo-structure pointers — acceptable. |
| LOW (AC-2b gate bypasses `reject()`, leaves no audit trail) | **Fixed** | `internal/org/spawn.go:494-498` and `:308-310` both route through `o.reject(p, err)`; `reject` writes a `rejected` event + `honored=false` receipt (`:990-1001`) and carries `DryRun: p.DryRun`. |
| LOW (`newOrgStartCmd` reads `ralph.toml` twice) | **Fixed** (see Cycle-2 L-4 on the shape chosen) | `internal/cli/org.go:246-252`: the standalone `resolveOrgConfig` call is gone; `newOrgRuntime` now returns the resolved config. |
| LOW (evidence-file redaction) | **Correctly deferred and tracked** | `docs/tech-debt/README.md` evidence-redaction row now names `org-lead-smoke-2026-08-02.txt` explicitly in both the description and the pay-down trigger. |
| LOW (`/org` frontmatter `description:` is the only Japanese one) | **Not fixed, not tracked** (see Cycle-2 L-5) | `.claude/skills/org/SKILL.md:3` unchanged; no `docs/tech-debt/README.md` row mentions it. |
| LOW (plan still `Status: Draft`, checkboxes unticked) | **Half-fixed** (see Cycle-2 L-6) | AC-1..AC-12 and 3 Progress boxes are now `[x]` with evidence pointers, but `docs/plans/active/2026-08-02-org-runtime-lead.md:3` still reads `- Status: Draft`. |

**No regressions** were introduced against any Cycle-1 fix: each fixed row was re-checked at every file the original finding cited, the four skill copies are still byte-identical after the fix commits, and the delta adds no debug statements, commented-out code, `TODO`/`FIXME` markers, secrets, or credentials.

## Findings (Cycle 2 delta)

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | maintainability | The reorder made dry-run and the real spawn path disagree on **gate-vs-envelope ordering**, and `Spawn`'s own doc comment — rewritten in the same commit — asserts the opposite of what the dry-run code now does. Real path: envelope → permission → AC-2b gate. Dry-run: AC-2b gate → envelope → permission. For a request that is both envelope-invalid (driver/model outside the pool) *and* unscoped-autonomous, `--dry-run` now reports the `--scope` error while the real spawn reports the envelope error — and since both paths write `reject()`'s `Details: cause.Error()`, the dry-run's `rejected` event records a different cause than the real run would. Dry-run's stated purpose is to predict the real path's rejection. The same sentence also claims "The DryRun path is unchanged", which is false twice over: the gate moved *and* it now writes a manifest event + receipt. | `internal/org/spawn.go:248-251` ("The DryRun path is unchanged: validate the full envelope (ValidateSpawn) up front, then the AC-2b gate") vs the actual code at `:308-323` (`autonomousScopeGateErr` first, `ValidateSpawn` second, `permissionArgsForDriver` third); `internal/org/envelope.go:38-43` (`ValidateSpawn` = `ValidateSpawnEnvelope` + `ValidateSpawnCapacity`); the contradicting inline comment at `:299-300` ("gate-before-record, then validate the envelope") in the same commit; `internal/org/spawn.go:990-1001` (`reject` writes `Details: cause.Error()`). | Move the dry-run gate to *after* `ValidateSpawn` and `permissionArgsForDriver` so both paths reject on the same first cause, then fix the doc comment at `:248-251` to describe the actual order and drop "unchanged". If the current order is deliberate, say why at `:299` and delete the contradicting claim at `:248-251` — leaving the two comments contradictory is the worst option. |
| MEDIUM | maintainability | Phase 2 re-reads the manifest under a re-acquired lock but re-runs **only** the capacity check — not the idempotent or envelope checks — against that fresh snapshot. The comment justifying the fresh read reasons explicitly that "another concurrent spawn could have raced in during that window, so the capacity decision must be made against a snapshot taken while the (re-acquired) lock is held". That argument applies with identical force to the *idempotent* decision, which was made against Phase 1's now-stale snapshot and is not revisited. Consequences of the newly unlocked detect→compensate window, neither of which the comment mentions: (a) two concurrent `Spawn` calls for the same stale seat can both detect it, both release, and both run `compensateStale` — two `pane send-keys C-c` round trips and two `spawn_failed` events for one stale saga, which the single-lock shape made impossible; (b) if a racer completes a full spawn during the window, Phase 2 appends a second `spawn_started` on top of a seat that is already `spawned`, because the `EventSpawned` idempotent return only exists in Phase 1. | `internal/org/spawn.go:351-356` (the fresh-read rationale, capacity-only); `:521-546` (Phase 2 body: `Manifest.Read()` → `checkCapacityAndStart()`, no `Roster`/idempotent/envelope re-check); `:421-431` (the `EventSpawned` idempotent return, Phase-1-only); `:500-509` (Phase 1 defers compensation by setting `staleExisting` and returning); `:527-530` ("the same capacity check + spawn_started append Phase 1 would have run had no stale seat been in the way" — Phase 1 would also have re-decided idempotency against its own lock-held read). | Either re-run the idempotent lookup at the top of Phase 2's closure (cheap — `events` is already re-read there, so it is a `Roster` scan plus the same `EventSpawned` early return), or document the residual window explicitly at `:351` and add a `docs/tech-debt/README.md` row. Silently narrowing a guarantee that the previous single-lock shape provided, inside a comment block that reasons about that exact window, is the gap worth closing. |
| LOW | maintainability | `permArgs`'s doc comment went stale when the AC-2b gate was moved below `permissionArgsForDriver`. It claims `permArgs` "stays nil on any early-return path (idempotent respawn, envelope/permission rejection)", but the gate rejection at `:494-498` is a new early-return path on which `permArgs` has already been assigned at `:460`. Nothing reads it on that path, so this is a documentation defect rather than a bug — but it is exactly the class of stale invariant a future reader would trust. | `internal/org/spawn.go:363-367` (the comment) vs `:454-460` (`permArgs = args`) and `:494-498` (the gate's `early = &r; return nil`). | Reword to "stays nil on any early-return path that precedes the `permArgs = args` assignment", or list the gate path as the documented exception. |
| LOW | readability | `checkCapacityAndStart` is a zero-argument, zero-return closure that communicates entirely through two captured variables — it reads the enclosing `events` and writes the enclosing `early`. Its doc comment says it "always sets `early` on any non-nil-worthy path", a phrase with no determinate meaning, and the claim is misleading: on the success path `early` stays nil. Reading `events` "most recently assigned to the enclosing events variable" makes the function's input invisible at the call site — `.claude/rules/architecture.md` calls out "opaque helper layers that hide control flow from search-based code reading". | `internal/org/spawn.go:376-396` (definition + comment); call sites at `:511` and `:537`, each of which must be read together with a preceding `events = rr.Events` assignment to know what is being validated. | Change the signature to `func(events []ManifestEvent) *SpawnResult` and have both call sites do `if r := checkCapacityAndStart(events); r != nil { early = r }`. That makes the input explicit, removes the "non-nil-worthy" phrasing, and keeps both call sites' `return nil` shape. |
| LOW | naming | The MEDIUM-4 fix put the enum in the package that structurally *cannot* be its single source. The dependency direction is `internal/org → internal/config` (permissions.go's own import), so defining `PermissionModeAutonomous/Edits/Guarded` in `internal/config` would have given one grep-able definition with zero cycle risk and let `internal/org` reference `config.PermissionModeAutonomous` directly. As shipped, the direction chosen forces `internal/config` to keep its own literals plus an eight-line "keep these three string values in sync by hand" comment — so the original complaint ("adding or renaming a mode requires coordinated edits with zero compiler help") is reduced from 6 sites to 2, not closed. | `internal/org/permissions.go:1-25` (constants defined here; the comment itself explains config cannot import org); `internal/config/config.go:252-268` (`orgPermissionModeAllowed`'s literals + hand-sync comment); `internal/org/permissions.go:9` (`"github.com/yoshpy-dev/ralph/internal/config"` — the import proving the safe direction is config→org, not org→config). | Move the three constants to `internal/config`, build `orgPermissionModeAllowed` from them, and either use `config.PermissionMode*` at `internal/org`'s call sites or keep thin `const PermissionModeAutonomous = config.PermissionModeAutonomous` aliases so `internal/org` call sites read unchanged. Both hand-sync comments then delete. |
| LOW | maintainability | The double-config-read fix widened `newOrgRuntime`'s signature, forcing `rt, _, err :=` at 9 of its 10 call sites and a six-line doc comment justifying the second return value — even though `org.Org.Config` is an exported field already assigned `orgCfg` in that very constructor. `rt.Config` would have fixed the double read with a one-line change at the single caller that needs it and zero churn elsewhere; the Cycle-1 report offered exactly this option ("or read it off `rt.Config`"). | `internal/cli/org.go:97-104` (`return &org.Org{Config: orgCfg, ...}, orgCfg, nil` — the same value returned twice); `internal/org/spawn.go:132-133` (`Config config.OrgConfig`, exported); the nine `rt, _, err := newOrgRuntime(...)` sites at `internal/cli/org.go:166`, `:313`, `:360`, `:398`, `:435`, `:464`, `:559`, `:600`; `internal/cli/org.go:7-16` (the doc comment added to justify the extra return). | Revert `newOrgRuntime` to `(*org.Org, error)` and read `rt.Config.Model...` in `newOrgStartCmd`. Nine `_` placeholders and a justifying comment are more surface than the problem they solve. |
| LOW | unnecessary-change | Cycle-1's plan-hygiene LOW named two things — the `Status:` line and the unticked checkboxes. The checkboxes were ticked (with good evidence pointers); the `Status: Draft` line was not touched, so the plan now reads "Draft" above twelve completed acceptance criteria and four completed artifact boxes. This is the multi-site-finding/single-site-fix shape. | `docs/plans/active/2026-08-02-org-runtime-lead.md:3` (`- Status: Draft`) vs `:68-81` (AC-1..AC-12 all `[x]` with evidence) and `:136-139` (Implementation/Review/Verification/Test artifact boxes all `[x]`). | Flip line 3 to `- Status: In review`. |
| LOW | naming | Cycle-1's frontmatter LOW (`/org` is the only skill with a Japanese `description:`) was neither fixed nor recorded in `docs/tech-debt/README.md`, so it now silently ages out of the review record. `description` is the auto-invocation matching surface and ships downstream through all four mirrors. | `.claude/skills/org/SKILL.md:3` unchanged across `9a22942`/`de4de50`; `docs/tech-debt/README.md` contains no row mentioning skill frontmatter or description language. | Either translate the `description:` line (a one-line change replicated to 3 mirrors via `scripts/sync-skills.sh`) or add a tech-debt row so the deferral is explicit. Leaving a deferred finding untracked is the one outcome the Cycle-1 report's own deferral discipline was trying to avoid. |

## Positive notes (Cycle 2)

- The HIGH-1 fix is genuinely multi-site, which is the failure mode this repo keeps hitting: flag default, flag help, a new `Long` on the command, the lead prompt, and all four skill mirrors were all updated, and a grep for `--until` across `internal/`, `.claude/skills/`, `.agents/skills/` and `templates/base/` turns up no surviving `idle`-only default or example.
- `--timeout-ms 0` was preserved as an explicit unbounded-wait opt-out rather than removed, and the `Long` string names the live-probe provenance (herdr v0.7.5 rests at `done`) at the definition site rather than only in a test — the "rationale at the definition, not in the test" shape this repo has gotten wrong before.
- The MEDIUM-3 fix chose the harder-but-correct option (restructure the lock) over the easier one (weaken the comment and raise `manifestLockTimeout` above the 60s spawn timeout), and Phase 1's new header comment states the invariant positively and checkably: "No herdr/agmsg call happens while this lock is held."
- `autonomousScopeGateErr` is a genuinely pure, grep-able extraction — one condition, one error string, two call sites — which is what makes the dry-run/real ordering divergence in M-1 a comment problem rather than a duplicated-logic problem.
- The AC-2b audit-trail fix updated both affected tests' *names and doc comments* (`..._RejectedNoEvents` → `..._RejectedWithEvent`) instead of only their assertions, so a future reader is not misled by a test name asserting the opposite of what the test checks.
- The `rejected`-event fix preserved the two properties that actually matter — zero driver calls and no `spawn_started` — and both are still asserted, so the fail-closed semantics were not traded away for auditability.
- The tech-debt evidence-redaction row was extended to name this PR's own new evidence file in both the description and the pay-down trigger, rather than being left to cover it implicitly.
- No secrets, credentials, debug statements, commented-out code, or `TODO`/`FIXME` markers were added anywhere in the delta.

## Tech debt identified (Cycle 2)

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| _(none proposed as new rows)_ | | | | |

M-1 and M-2 are recommended for fix in this cycle (both are comment-or-small-code changes in files this PR authored). If M-2's idempotent re-check is deferred rather than fixed, it is the one finding that warrants a `docs/tech-debt/README.md` row, since it narrows a concurrency guarantee that the pre-fix single-lock shape provided. L-5 (Japanese `description:`) should get a row if it is deferred a second time.

## Recommendation (Cycle 2)

- Merge: **Merge with follow-ups** — no CRITICAL and no HIGH findings in the delta, and every Cycle-1 HIGH/MEDIUM was addressed at the sites it named with no regressions. The two MEDIUMs are internal-consistency defects in newly written comments and one narrow concurrency window; neither blocks the pipeline.
- Follow-ups, in order:
  1. M-1 — make the dry-run gate ordering match the real path (or fix the doc comment at `internal/org/spawn.go:248-251`); the two comments currently contradict each other in the same file.
  2. M-2 — re-run the idempotent lookup at the top of Phase 2's closure, or document the window and open a tech-debt row.
  3. L-1/L-2 — the stale `permArgs` comment and `checkCapacityAndStart`'s signature; both are small and both are in the region this cycle rewrote.
  4. L-3/L-4 — enum placement (`internal/config`) and `newOrgRuntime`'s signature; batchable, no urgency.
  5. L-5/L-6 — plan `Status: Draft` line (one line) and the Japanese `description:` (translate or track).
