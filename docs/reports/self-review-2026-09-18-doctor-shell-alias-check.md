# Self-review report: doctor-shell-alias-check

- Date: 2026-09-18
- Plan: `docs/plans/active/2026-09-18-doctor-shell-alias-check.md` (issue #162)
- Reviewer: `reviewer` subagent (Claude Code), cycle 1
- Scope: diff quality only for `git diff main...HEAD` on `feat/doctor-shell-alias-check` (HEAD `9a6d971`, 6 commits, 10 files, +625/-6). No tests, no static analysis, no spec-compliance or doc-drift verdicts (those are `/verify` and `/test`).

## Evidence reviewed

- Whole-file read of `internal/cli/doctor_shell_alias.go` (220 lines) and `internal/cli/doctor_shell_alias_test.go` (271 lines), plus the 5-line registration hunk in `internal/cli/doctor.go` read in the context of all 13 checks.
- The flags ralph actually passes when it launches a seat, read at the producer rather than inferred: `internal/org/permissions.go:66-67` (`--sandbox workspace-write`, `--ask-for-approval never`), `:106-108` (`--permission-mode bypassPermissions|acceptEdits`), `internal/org/spawn.go:643` (`--model <p.Model>`). `agentArgs` is `permArgs + --model + prompt`, appended to the alias-expanded command in the pane.
- **Behavioral probes against the binary built from this branch** (`go build -o <scratch>/ralph-probe ./cmd/ralph`, run with `HOME=<temp>` / `ZDOTDIR=` and a one-line fixture rc). Nothing was written inside the repo and no test or linter was run. Results are quoted verbatim in the findings below.
- **Probes against the real `codex` CLI on this machine** to decide whether a given alias spelling actually collides: `codex -s read-only --sandbox workspace-write --help`, `codex -a never --ask-for-approval never --help`, `codex -m gpt-5 --model gpt-5 --help`, and the same for `codex exec`. All three short/long pairs answer `error: the argument '--<flag>' cannot be used multiple times`. `--yolo` and `--dangerously-bypass-approvals-and-sandbox` do **not** collide with `--sandbox` (help prints normally), so they are correctly out of the detection set.
- A 24-line table of realistic rc spellings replayed through byte-identical copies of `aliasShPattern`, `aliasFishPattern`, and `conflictingFlag` in a throwaway `main` (scratchpad, deleted), to separate "not matched as an alias line" from "matched but no flag found".
- Mirror equality: `cmp` of `.claude/skills/org/SKILL.md` against `.agents/skills/org/SKILL.md`, `templates/base/.claude/skills/org/SKILL.md`, `templates/base/.agents/skills/org/SKILL.md`, and of `docs/recipes/codex-seat-permissions.md` against its `templates/base/` copy -> all four byte-identical. The template copies carry no `internal/` or `cmd/` reference.
- Call-site sweep (not narrowed by line range) of every new symbol across `internal/`: `checkShellAliases` -> 1 production caller (`doctor.go:126`) + 9 test calls; `results[len(results)-1]` -> 1 occurrence in the whole tree, introduced by this diff.
- Which pre-existing tests now reach the new check: `grep -n 'runDoctor(\|runDoctorOpts(\|runDoctorFull(' internal/cli/*_test.go` -> 14 call sites (13 pre-existing, across 8 test functions), of which only the new `TestRunDoctorOpts_ShellAliasesCheck_AppearsInOutput` pins `HOME`.
- Exit-code contract re-read at the source: `doctor.go:156-166` (`allPass` flips on `"fail"` only), `countFailed` (`:184-192`), and the `--strict` flag help (`:38-51`, strict elevates only `checkScaffoldIntegrity`). warn/info from this check cannot change the exit code.
- `docs/tech-debt/README.md` cross-checked in both directions: no open row covers shell-alias detection, and this diff invalidates none.
- `git status --porcelain` in the worktree: empty before the report commit. No secrets, no debug statements, no commented-out code, no TODO/FIXME markers in either new file.

## Findings

<!-- Area recommended values: naming, readability, unnecessary-change, typo,
     null-safety, debug-code, secrets, exception-handling, security, maintainability -->

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | **H1: codex's short flags `-s` / `-a` are not detected, although they are the same clap arguments as `--sandbox` / `--ask-for-approval` and collide exactly like `-m`.** The check handles the short spelling of one of the four flags ralph passes (`-m`, incl. `-mVALUE`) and only the long spelling of the other three. On this machine `codex -s read-only --sandbox workspace-write` answers `error: the argument '--sandbox <SANDBOX_MODE>' cannot be used multiple times`, and `codex -a never --ask-for-approval never` answers the same for `--ask-for-approval` — i.e. `alias codex='codex -s danger-full-access'` produces exactly the `spawn_failed` this check exists to prevent, and doctor reports a confident pass. This is not a plan non-goal: the plan's design decision scopes detection to "the flags ralph actually passes", and `-s` *is* `--sandbox`. Verified end-to-end against the branch binary: `alias codex='codex -s danger-full-access'` -> `pass — no conflicting codex/claude alias in 1 scanned rc file(s) (1 alias line(s) seen)`; `alias codex='codex -a never'` -> same pass; `alias codex='codex --sandbox danger-full-access'` -> `warn — alias codex in ~/.zshrc:1 adds --sandbox`. | `internal/cli/doctor_shell_alias.go:140-149` (the switch: short form only for `-m`) vs `internal/org/permissions.go:66-67` (`codexAutonomousArgs = []string{"--sandbox", "workspace-write", "--ask-for-approval", "never"}`). Doc comment `:123-129` and the plan's Design decisions both enumerate the same asymmetry, so it is consistent-but-incomplete, not an implementation slip. | Add two cases mirroring the `-m` rule, then update the doc comment at `:123-129`, the plan's Design-decisions bullet, and AC-2: `case tok == "-s", strings.HasPrefix(tok, "-s") && len(tok) > 2 && tok[2] != '-': return "--sandbox (-s)"` and the same for `-a` -> `"--ask-for-approval (-a)"`. Keep the `-m` ordering (first match wins). `--yolo` / `--dangerously-bypass-approvals-and-sandbox` should stay out: probed above, they do not collide. |
| MEDIUM | readability | **M2: the doc comment says the Detail names the files that were scanned; no branch of the Detail names any file that has no finding.** `shellAliasRcCandidates`' comment presents that as the mitigation for the check's largest blind spot (`source`d files are not followed), and the plan's Non-goals repeat it (`Detail に「候補 rc のみ」と明記`). What ships is a bare count: `no codex/claude alias in 5 shell rc file(s)`. An oh-my-zsh user whose alias lives in `~/.oh-my-zsh/custom/aliases.zsh`, or any user with `source ~/.config/zsh/aliases.zsh`, gets an unqualified `pass` with nothing to tell them the file holding their alias was never opened. The warn branch names only the files that *did* match, so the blind spot is invisible in every branch. | `internal/cli/doctor_shell_alias.go:25` (`the Detail says which files were scanned`) vs the three Detail branches at `:195` (findings only), `:213`, `:218` (counts only). Branch-binary output: `pass — no codex/claude alias in 1 shell rc file(s)`. | Cheapest honest fix is the comment: drop the clause, or replace it with "the Detail reports how many candidates were read". The better fix is what the plan promised — append the scanned paths (home-collapsed) or at least `(candidate rc files only; `source`d files are not scanned)` to both pass branches, which is one `strings.Join` over a slice `checkShellAliases` already has. |
| MEDIUM | exception-handling | **M3: a scan error silently voids an entire file's findings and drops it from the scanned count, and the comment names only the causes that cannot do that.** `os.Open` failures and `bufio.Scanner` failures flow into the same `continue`. For an open failure nothing is lost; for a scanner failure the findings already collected in that file are thrown away — `scanShellAliasFile` deliberately returns them (`return findings, aliasLines, scanErr`) and the caller discards them. The reachable trigger is `bufio.MaxScanTokenSize` (64 KiB) with no `scanner.Buffer` call, and the resulting message is indistinguishable from "you have no rc files". Verified on the branch binary: a `.zshrc` whose first line is a 70 KB `export` and whose second line is `alias codex='codex -m gpt-6-astra'` reports `pass — no codex/claude alias in 0 shell rc file(s)`. A `chmod 000 ~/.zshrc` (uid 502, not root) reports the identical string. The named result `err` in the signature at `:81` is never assigned, which is what makes the discarded third return easy to miss. | `internal/cli/doctor_shell_alias.go:173-178` (single `continue`, comment claims "permissions, race with candidate listing"), `:117-118` (partial return), `:81` (unused named result). | Split the two paths: keep `fileFindings`/`lines` when `scanErr != nil`, count the file as skipped, and surface it — `1 rc file skipped (unreadable)` in the Detail — so a silent fail-open becomes a visible one. Optionally `scanner.Buffer(make([]byte, 0, 64*1024), 1<<20)` to remove the long-line trigger. Then correct the comment to name the scanner-error case. Dropping the unused named `err` from the signature is a free readability win. |
| MEDIUM | maintainability | **M4: everything after `alias <name>=` on the line is tokenized, so a trailing `#` comment produces a false warn and a second alias on the line is attributed to the first.** The comment rule is whole-line only (`strings.HasPrefix(trimmed, "#")`), and the one-layer quote strip at `:132-136` no-ops when the line does not *end* in a quote, so the comment text becomes tokens. Verified end-to-end: `alias codex='codex'  # never add --model here` -> `warn — alias codex in ~/.zshrc:1 adds --model`, naming a line that adds no flag at all. The same mechanism makes `alias codex="codex" && alias claude="claude -m x"` report the flag under `alias codex`. The plan accepts erring toward false positives, but this FP names the wrong line and the wrong reason, which is the kind that makes an operator stop trusting the check. | `internal/cli/doctor_shell_alias.go:93-96` (whole-line comment skip), `:131-138` (strip-then-`strings.Fields`). Probe table: both spellings warn. | After the quote strip, cut the value at the first ` #` (or `\t#`) that is not inside the stripped quotes, and say so in `conflictingFlag`'s doc comment. Attribution across a second `alias` on the same line is rarer; documenting it in the same comment ("the first matching name on the line owns every flag found after it") is enough. |
| LOW | maintainability | **L5: `checkShellAliases(results[len(results)-1].Status == "pass")` reads its argument out of the slice by position.** It is correct today only because `checkHerdrAvailable` is appended immediately above and returns exactly `pass`/`info`. Anyone inserting a check between 8 and 8b silently repoints the argument at their own result, and the failure is invisible (warn quietly becomes info). This is the only `results[len(results)-1]` in the tree, and the plan's own Scope item 3 writes the named-variable form (`checkShellAliases(herdrOK)`). | `internal/cli/doctor.go:126`; `checkHerdrAvailable` at `doctor.go:703-713`. | Two lines: `herdr := checkHerdrAvailable(); results = append(results, herdr)` then `checkShellAliases(herdr.Status == "pass")`. Behaviour identical, coupling explicit. |
| LOW | naming | **L6: `conflictingFlag` is a package-scope name that does not say what it conflicts with.** `internal/cli` is a large package; the sibling helpers in this same file are all prefixed (`shellAliasRcCandidates`, `scanShellAliasFile`, `shellAliasFinding`, `checkShellAliases`). A bare `conflictingFlag` is the one name a future reader cannot place from a grep hit, and it is the one most likely to be wanted by a second caller under a different meaning. | `internal/cli/doctor_shell_alias.go:130`; sibling naming at `:26`, `:70`, `:81`, `:158`. | Rename to `shellAliasConflictingFlag` (or `conflictingSeatLaunchFlag`). Test references at `doctor_shell_alias_test.go:201-224` (`TestConflictingFlag`) update with it. |
| LOW | maintainability | **L7: 8 pre-existing doctor test functions (13 `runDoctor*` call sites) now read the developer's real `$HOME` rc files.** Every `runDoctor*` test reaches the new check, and only the new integration test pins `HOME` (it documents exactly why). The assertions are safe — warn/info never touch the exit code — but `TestRunDoctorOpts_HerdrAgmsgAbsent_ExitCodeUnaffected` pins `RALPH_ORG_AGMSG_HOME` two lines above its call with the comment "so this test is deterministic regardless of ... the machine running it", and this diff reintroduces the same class of machine dependence into that test. `go test ./internal/cli/...` now opens up to 11 files under the user's home per call site. | `internal/cli/doctor_org_test.go:186`, `:236` (no `HOME` pin; the `Setenv("HOME", "")` at `:588` belongs to a different test), `internal/cli/cli_test.go:344`, `internal/cli/doctor_scaffold_test.go:147/153/452/456/511/515/702/706/756/760`. Pinned counter-example: `doctor_shell_alias_test.go:253-255`. | Either give `checkShellAliases` a `home string` parameter (it already takes one parameter, and `shellAliasRcCandidates` already accepts `home`) so the check is hermetic by construction, or add the two `t.Setenv` lines to the shared doctor test setup. The first also removes the need for `t.Setenv` in most of the new unit tests. |
| LOW | readability | **L8: two `ZDOTDIR`/symlink behaviours that a reader will want are in neither the comment nor the Detail.** (a) A relative `ZDOTDIR` is silently skipped (`filepath.IsAbs` guard) — the plan lists it as edge case (5) but nothing in the code says it, so it reads like an oversight. (b) When `ZDOTDIR` is set, `~/.zshrc` is still scanned even though zsh will not read it, so an abandoned `~/.zshrc` can raise a warn for a file the user's shell never loads. (c) The symlink dedup keeps the first-seen *candidate* path, so the reported `file:line` can be the symlink rather than where the alias physically lives (`~/.zshrc:1` for an alias stored in `~/.config/zsh/.zshrc`). Editing through the symlink works, so (c) is benign — but the comment says only "deduplicated by its real path", which suggests the opposite. | `internal/cli/doctor_shell_alias.go:28` (`filepath.IsAbs`), `:33-43` (home paths always appended), `:47-62` + `:20-25` (dedup, display path); test at `doctor_shell_alias_test.go:119-137` asserts the count only. | One sentence in the doc comment covering all three: a relative `ZDOTDIR` is ignored, home rc files are scanned even when `ZDOTDIR` is set (superset on purpose), and the reported path is the candidate, not the symlink target. No code change. |
| LOW | readability | **L9: the sentence added to the `/org` skill is 84 display columns in a paragraph that wraps at 73-78, in all four mirrors.** The surrounding bullet's lines measure 73, 78, 75, 78, 64, 76; the new second line measures 84. Mechanically harmless, visible in review diffs of four files. | `.claude/skills/org/SKILL.md:44` and the three byte-identical mirrors; widths measured with East-Asian-width counting. | Re-wrap the two added lines to the block's width, then re-run `scripts/sync-skills.sh` + the template copies so the four files stay identical. |

No CRITICAL findings. H1 is a merge-blocking-quality call for the orchestrator, not a correctness risk to anything outside this check: the failure mode is a missing advisory warn.

## Notes for `/verify` (out of this review's scope, recorded so they are not lost)

Two places where the implementation and the plan text disagree. Judging them is spec compliance, so no severity is assigned here.

- The plan's Assumptions say the alias value's quotes are **not** stripped and matching is substring-based (`引用符…は剥がさず、部分文字列として判定する`). What shipped strips one layer of quotes and tokenizes on whitespace (`:131-138`). The tokenizing version is the better design — it is why `--models-dir` and `--m` do not false-positive — but the change is not in Deviation notes.
- The plan's Scope item 2 specifies the "alias present, no conflicting flag" Detail as `alias codex in <file> has no conflicting flags`; what ships is `no conflicting codex/claude alias in N scanned rc file(s) (M alias line(s) seen)`, which names no alias. The corresponding test asserts only the substring `alias line(s) seen`, so it does not pin either wording.

## Positive notes

- **No alias value ever reaches the output.** The Detail is built from `f.Name`, `displayPath(f.File)`, `f.Line`, and a fixed flag string (`:195`), never from the matched line. An rc file holding `alias claude='ANTHROPIC_API_KEY=sk-... claude --model fable'` produces `alias claude in ~/.zshrc:12 adds --model` and nothing else, so doctor output remains safe to paste into `docs/evidence/`. Home is collapsed to `~` (`:184-190`), matching the repo's redaction convention for committed evidence.
- **The check is read-only and process-free.** `os.Stat` + `os.Open` only; no shell is started, which is the plan's first non-goal and the reason the check stays deterministic. The candidate list is static, so the file set cannot be steered by repo content.
- **The exit-code claim holds where it matters.** `countFailed` counts `"fail"` only, `allPass` flips on `"fail"` only, and `--strict` elevates `checkScaffoldIntegrity` alone — so no rc file on any machine can make `ralph doctor` exit non-zero through this check. I checked this at the source rather than trusting the plan.
- **The herdr-absent downgrade is the right default and is implemented symmetrically.** Same Detail text, `info` instead of `warn`, plus one clause explaining why (`:205-206`), so the finding is still greppable in the output of a machine without herdr.
- **The two structural tests discriminate.** Remove the `seen` map and `TestCheckShellAliases_SymlinkedRc_DedupedToOneFinding` counts 2 instead of 1; remove the `ZDOTDIR` branch and `TestCheckShellAliases_Zdotdir_Detected` drops from warn to pass. `TestConflictingFlag`'s negative rows (`--models-dir`, `--m`, `-c a=b --effort xhigh`) pin the three near-miss spellings that the prefix rules could plausibly over-match, and the `-mgpt-5.5` row pins the concatenated form the Codex plan advisory contributed.
- **Fixture hygiene in the new tests.** Every filesystem-touching test goes through `setShellAliasTestHome`, which pins `HOME` **and** clears `ZDOTDIR`; the one test that needs an unresolvable home skips on Windows with the reason stated. No `t.Parallel()` anywhere in `internal/cli`, so the `t.Setenv` usage cannot race.
- **Mirror and template discipline.** All four `/org` skill copies and both recipe copies are byte-identical, and the added sentence names only shipped surfaces (`ralph doctor`, the check name, the recipe path) — nothing that would trip template purity.
- **Registration is minimal and numbering-stable.** One append plus a three-line comment; "8b" keeps every later check number unchanged, which is what the surrounding comments promised.
- **`checkShellAliases` is 63 lines including comments** — above the 50-line guidance in the global review rules, but linear with no nesting beyond two levels, so I am not filing it. If M2 adds the scanned-path list, extracting the Detail builder is the natural place to split.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| _(none — see Follow-ups)_ | | | | |

No tech-debt rows are proposed. H1 is roughly four lines plus comment/plan updates, M2-M4 are one to six lines each, and L5-L9 are one- to two-line changes — all comfortably inside cycle 1 of a 2-cycle cap. If the orchestrator elects to defer any of them, they should land as a **single batched row** naming this report, not as five rows and not silently.

## Recommendation

- Merge: **no-merge as-is; merge after H1, and preferably M2-M4.** Nothing here is CRITICAL and nothing risks the exit code, data, or secrets. The blocker is that the new check answers `pass` for a verified instance of the exact failure it was built to catch (`alias codex='codex -s …'`), and a confident pass is worse than no check because it sends the operator looking elsewhere. M2 and M3 are the two places where the output overstates what was actually inspected; both are small and both are worth fixing while this code is fresh.
- Follow-ups: after fixing H1, re-check the one-sentence claims added to `.claude/skills/org/SKILL.md` (+3 mirrors) and `docs/recipes/codex-seat-permissions.md` (+template) — they promise the check warns about "such aliases", which the short-form gap currently makes partly untrue. The four skill copies must stay byte-identical (`scripts/sync-skills.sh`), and the recipe pair is copied by hand.

## Revalidation (cycle 1, fix round)

Reviewed `git diff 00d3435..HEAD` (report commit → `d4ef9ee`; 13 files, +907/-258). Substantive commits: `c12169f` (Go, implementer), `8202d58` (orchestrator refactor), `fa7f1eb` (docs), `2cfbcb9` + `d4ef9ee` (plan). No tests, linters, or static analysis were run for this round either.

Evidence method, same as cycle 1: whole-file read of the rewritten `internal/cli/doctor_shell_alias.go` (473 lines) and `internal/cli/doctor_shell_alias_test.go` (604 lines); a binary built from `d4ef9ee` into the scratchpad and run against 19 synthetic rc fixtures with a fake `HOME`; independent CLI probes of `claude` 2.1.274; and `zsh -c` / `bash -c` replays of the two rc spellings the new parser documents as unsupported. Every quoted Detail below is verbatim output from that binary.

**The claude severity change is sound and I could reproduce its premise.** `claude --permission-mode acceptEdits --permission-mode plan --output-format bogus2 -p x` and `claude --model haiku --model sonnet --output-format bogus2 -p x` both fail only on the deliberately invalid `--output-format`, so parsing walks past both occurrences of the repeated flag without a duplicate error (`claude --permission-mode bogus …` does error, which shows each occurrence is validated, so the absence of an error is not the parser giving up). That, plus the `modelUsage` measurement recorded in the plan, supports "accepts a repeat, ralph's later value wins". Grading codex `warn` and claude `info` follows from it, and the hedge that ships in the docs ("claude 2.1.274 CLI; not verified on a live seat") is the right strength.

| Finding | Status | Evidence |
| --- | --- | --- |
| H1 (HIGH) | **Resolved** | `shellAliasConflictingFlags` (`internal/cli/doctor_shell_alias.go:309-347`) now keys on the driver and adds `-s`/`-a` with the same exact-or-concatenated rule as `-m`. Binary output: `alias codex='codex -s danger-full-access'` → `warn — alias codex in ~/.zshrc:1 adds --sandbox (-s)`, `-sread-only` → same, `-a never` → `--ask-for-approval (-a)`. Per-driver gating is pinned by three negative rows in `TestShellAliasConflictingFlags` (`doctor_shell_alias_test.go:475`): claude `-m x` → nil, claude `--sandbox x` → nil, codex `--permission-mode x` → nil. The doc comment now records the codex-cli 0.154.0 measurement inline (`:294-302`). |
| M2 (MEDIUM) | **Resolved** | Every branch ends with a scanned clause that names files: `pass — alias codex in ~/.zshrc:1 has no conflicting flags. scanned 1 shell rc file(s): ~/.zshrc (files they source are not followed)`; two rc files → `scanned 2 shell rc file(s): ~/.zshrc, ~/.bashrc`; none → `pass — no shell rc file found to scan`. The comment that made the false claim is rewritten and now says what the code does (`:43-57`). See N2 for the new edge this creates. |
| M3 (MEDIUM) | **Resolved** | `scanShellAliasFile` (`:259-291`) dropped the named returns, sets a 1 MiB line cap, and returns `defs` with the error; the caller appends `fileDefs` **before** testing `scanErr` (`:388-396`). Binary output: a 70 KB line followed by a conflicting alias now warns (was a silent `pass`), and a `chmod 000` rc gives `info — could not read: ~/.zshrc (permission denied) — aliases there were not checked. scanned 0 shell rc file(s)`, where cycle 1 produced a bare `pass`. `shellAliasUnreadableReason` (`:348-355`) unwraps `*fs.PathError` so the path is not printed twice. See N3 for the wording when the same file yields both. |
| M4 (MEDIUM) | **Resolved** | Both regexes are gone. `shellAliasWords` (`:121-199`) is a three-state reader and `parseShellAliasLine` (`:201-243`) handles option words and several `NAME=VALUE` words per statement. Binary output: `alias codex='codex'  # never add --model here` → `pass` (was a false `warn`); `alias ll='ls -l' codex='codex -m gpt'` → `warn` attributed to codex (was not matched at all); `alias -g codex='codex -m gpt'` → `warn`. `TestShellAliasWords` (`:442`) pins 11 tokenizer cases including `'a'\''b'` concatenation and an unterminated quote. |
| L5 (LOW) | **Resolved** | `internal/cli/doctor.go:121-128`: `herdrResult := checkHerdrAvailable()` then `checkShellAliases(doctorShellAliasEnv, herdrResult.Status == "pass")`. No `results[len(results)-1]` remains in the tree. |
| L6 (LOW) | **Resolved** | Renamed to `shellAliasConflictingFlags`; `grep -rn 'conflictingFlag'` outside this report hits only the plan's dated Slice-A deviation note (`docs/plans/active/2026-09-18-doctor-shell-alias-check.md:115`), which describes the commit where that name existed. See N5 for a naming collision the rewrite introduced. |
| L7 (LOW) | **Resolved** | `internal/cli/main_test.go` pins `doctorShellAliasEnv` for the whole package to a non-existent home, and `checkShellAliases` takes the resolver as a parameter (`:365`) so unit tests pass closures instead of mutating `HOME`. Two tests make the seam falsifiable rather than assumed: `TestRunDoctorOpts_ShellAliasesCheck_HermeticByDefault` (`:519`) asserts the pinned `no shell rc file found to scan` reaches stdout, and `TestRunDoctorOpts_ShellAliasesCheck_WarnsThroughTheSeam` (`:562`) overrides the variable and asserts a `warn` line, which is what proves `runDoctorFull` reads the package variable at all. `TestShellAliasEnvFromOS` (`:211`) keeps the real `HOME`/`ZDOTDIR` resolver covered. |
| L8 (LOW) | **Resolved** | All three behaviours are now in `shellAliasRcCandidates`' doc comment (`:43-57`): the reported path is the symlink candidate rather than the resolved target, a relative `$ZDOTDIR` is ignored, and home rc files are scanned even when `$ZDOTDIR` is set — with the reason (this process's `$ZDOTDIR` need not match the one herdr's login-zsh pane resolves from `/etc/zshenv`, so over-approximate). |
| L9 (LOW) | **Resolved** | The rewritten bullet measures 68-78 display columns (block range 71-78); `cmp` confirms `.agents/skills/org/SKILL.md` and both `templates/base/` copies are byte-identical to `.claude/skills/org/SKILL.md`, and the recipe matches its template copy. |
| /verify note 1 (quoting) | Addressed in the plan | Assumptions now describes the word reader and says why the substring approach was dropped (self-review M4). |
| /verify note 2 (harmless Detail) | Addressed in the plan | Scope 改訂 item 3 specifies `alias codex in <file>:<line> has no conflicting flags`, which is what ships. |

### New findings introduced by the fix round

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | maintainability | **N1: both new Detail sentences assume ralph always passes the same flag; in `guarded` permission mode it passes none of the permission flags, which inverts what each sentence promises.** The codex sentence asserts `codex rejects the repeated flag ("cannot be used multiple times"), so ralph org spawn fails`, and the claude sentence asserts `the value ralph org spawn passes last wins, so the seat still starts`. Both hold for `--model`, which `spawn.go:643` always appends. They do not hold for `--sandbox` / `--ask-for-approval` / `--permission-mode`: `permissionArgsForDriver` returns `nil, nil` for `guarded` on both drivers, so there is no second occurrence. A guarded codex seat with `alias codex='codex -s danger-full-access'` therefore **spawns successfully with a widened sandbox** while doctor says the spawn will fail; a guarded claude seat with `alias claude='claude --permission-mode bypassPermissions'` runs **with the permission dialog removed** while doctor grades it `info` and says the seat is fine. `guarded` is first-class config (`[org.permissions].default`, `[org.permissions].roles`), not a hypothetical; it is not the shipped default (`autonomous`), which is why this is MEDIUM and not HIGH. | `internal/cli/doctor_shell_alias.go:418` and `:427` (the two sentences) vs `internal/org/permissions.go:109` and `:116` (`case PermissionModeGuarded: return nil, nil`) and `internal/org/spawn.go:643` (`--model` always appended). Same overstatement in the shipped docs: `.claude/skills/org/SKILL.md:41-42` (`--sandbox` / `--ask-for-approval` と短縮形 … も同じ) +3 mirrors, and `docs/recipes/codex-seat-permissions.md:24-25` (`The same happens for --sandbox / --ask-for-approval`). | Wording, both sentences plus the two doc surfaces. Codex: `…so ralph org spawn fails when the seat's permission mode passes the same flag (--model always; --sandbox / --ask-for-approval only for autonomous/edits) — otherwise the alias's value applies to the seat unopposed`. Claude: add the same caveat, since the unopposed case is the one worth acting on. Making the check read `[org.permissions]` and grade accordingly is the thorough fix and is worth a tech-debt row rather than this cycle. |
| MEDIUM | maintainability | **N2: M2's fix turned two silent misses into positive safety claims, and both spellings really do define the alias.** The Detail now asserts `alias codex in ~/.zshrc:1 has no conflicting flags` and, when nothing parses, `no codex/claude alias found` — statements about a specific alias and about the file as a whole. The parser documents two shapes it does not read, and for each the binary emits one of those claims: a continued value (`alias codex="codex \` / `  -m gpt"`) → `pass — alias codex in ~/.zshrc:1 has no conflicting flags`, and a second statement on the line (`alias ll='ls -l'; alias codex='codex -m gpt'`) → `pass — no codex/claude alias found`. Both fixtures were replayed through the real shells: `zsh -c 'source rc; alias codex'` prints `codex='codex   -m gpt'` for the first and `codex='codex -m gpt'` for the second, and `bash` agrees. Cycle 1's output was equally blind but said only "no codex/claude alias in N rc file(s)"; the fix upgraded a missing statement into a wrong one, and `TestCheckShellAliases_SecondStatementOnSameLine_NotDetected` (`doctor_shell_alias_test.go:327`) pins the `pass` as intent. | Claims at `internal/cli/doctor_shell_alias.go:440` and `:442`; documented limitations at `:116-120`; real-shell replay above. | Cheapest: have `shellAliasWords` report that it stopped early (ended inside a quote, on a trailing backslash, or at an unquoted `;`/`|`/`&` with `alias` still ahead) and add a clause in the same family as the unreadable one — `not fully parsed: ~/.zshrc:1 — the alias continues beyond what was read`. Failing that, soften both sentences to describe the line rather than the alias (`adds no seat-launch flag on that line`, `no codex/claude alias statement recognized`), so a miss stops reading as an all-clear. |
| LOW | readability | **N3: the partial-read Detail contradicts itself three ways in one line.** With a finding before an over-long line, the binary prints: `warn — alias codex in ~/.zshrc:1 adds --model (-m) — …. could not read: ~/.zshrc (bufio.Scanner: token too long) — aliases there were not checked. scanned 0 shell rc file(s) (files they source are not followed)`. The same file is named as a source of a finding, as not checked, and as not scanned. `TestCheckShellAliases_OverLongLine_KeepsPartialFindingsAndReportsUnreadable` (`doctor_shell_alias_test.go:414`) asserts exactly this combination, so the wording is pinned as intent. | `internal/cli/doctor_shell_alias.go:388-396` (a file with a scan error never enters `scannedOK` even when it contributed defs), `:459` (the fixed "aliases there were not checked" text). | Distinguish the two cases where the clause is built: if the file contributed any definition, say `partially read: <file> (<reason>) — aliases after the failure were not checked` and count it in the scanned list; otherwise keep the current wording. Roughly one `if` plus a second format string. |
| LOW | typo | **N4: "claude has no short flags" is false; claude 2.1.274 has eight.** `claude --help` lists `-c, --continue`, `-d, --debug`, `-h, --help`, `-n, --name`, `-p, --print`, `-r, --resume`, `-v, --version`, `-w, --worktree`. What the code means, and what the plan states correctly (`claude 2.1.274 の --help に該当する短縮形はない`), is that neither `--model` nor `--permission-mode` has a short form. As written the comment invites a future maintainer to skip the check the plan actually performed. | `internal/cli/doctor_shell_alias.go:304` vs `claude --help` output; correct phrasing at `docs/plans/active/2026-09-18-doctor-shell-alias-check.md:64`. | One clause: `claude has no short form for either flag (claude 2.1.274)`. |
| LOW | naming | **N5: `shellAliasWord` is not an element of `shellAliasWords`.** `shellAliasWords(rest string) []string` returns raw shell words; `type shellAliasWord struct { Name, Value string }` is a parsed `NAME=VALUE` pair produced one layer up by `parseShellAliasLine`. The singular/plural pairing states a containment relationship that does not exist, in a file that otherwise names its types for what they hold (`shellAliasDef`, `shellAliasEnv`). | `internal/cli/doctor_shell_alias.go:101-105` (the type) and `:121` (the function). | Rename the type to `shellAliasAssignment` (or `shellAliasNameValue`); `parseShellAliasLine`'s signature and the three uses inside `scanShellAliasFile` are the whole blast radius. |
| LOW | unnecessary-change | **N6: the plan's Assumptions still pins a Detail string this round deleted.** `Windows では候補 rc が存在しないため pass(`no codex/claude alias in 0 shell rc file(s)`)` names a string that no longer exists anywhere; the zero-candidate branch now emits `no shell rc file found to scan`. The adjacent Assumptions bullet (quoting) was rewritten in this same round, so the sweep stopped one line short. The Scope table's old Detail examples at `:20` are explicitly superseded by the 改訂 paragraph and need no edit. | `docs/plans/active/2026-09-18-doctor-shell-alias-check.md:48` vs `internal/cli/doctor_shell_alias.go:448`. | Replace the parenthetical with the shipped string. Optionally add `(現 shellAliasConflictingFlags)` to the dated deviation note at `:115` so the old name stays greppable to its replacement. |

### Notes that are not findings

- **The three artifacts that record the claude reversal all keep their provenance.** The evidence file's original "claude 座席も同じ衝突が起きるはず(未検証)" bullet is left in place with a dated `追記(2026-09-18、#162)` correction underneath naming the measurement and stating that a live seat was still not tried (`docs/evidence/codex-seat-permissions-2026-09-18.md:39`). Correcting a record in place would have been worse; this is the right shape.
- **The `-s`/`-a` additions cannot over-match a long flag.** `isShort` requires the token's third byte to exist and not be `-`, and `--sandbox` fails `HasPrefix(tok, "-s")` on its second byte, so the long forms never take the short-form branch. `TestShellAliasConflictingFlags` keeps the `--models-dir` and `--m` negative rows from cycle 1 and adds `codex -m x -s y` → `--model (-m), --sandbox (-s)` for ordering.
- **Hermeticity is now provable rather than assumed**, and the `chmod 0o000` test skips under root, so it cannot silently pass as a no-op.
- **No secrets, debug code, TODO markers, or unrelated edits** in the fix range. `TestCheckShellAliases_AliasValueNeverAppearsInDetail` (`doctor_shell_alias_test.go:432`) now pins the no-leak property that cycle 1 verified only by reading.

### Updated recommendation

- Merge: **merge after N1's wording fix and N2's; N3-N6 are one-liners worth taking in the same commit.** All nine cycle-1 findings are resolved, and the two that mattered most (H1's false `pass`, M3's silent fail-open) are verifiably gone. What is left is that the fix round's more assertive output now states things the parser and the permission model do not guarantee: N1 misdescribes the mechanism for three of the four flags whenever a seat is guarded, and N2 turns two documented parser blind spots into explicit all-clears. Neither can change an exit code, and neither is a regression against `main` in behaviour — only in what the output claims.
- Follow-ups: if N1's wording fix lands but the config-aware grading does not, that is worth one tech-debt row (doctor grades shell-alias findings without knowing the seat's resolved permission mode). Nothing else here is deferral material.
