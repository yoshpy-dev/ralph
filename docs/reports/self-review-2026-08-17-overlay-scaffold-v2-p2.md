# Self-review report: overlay-scaffold-v2 Phase 2

- Date: 2026-08-17
- Plan: `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md`
- Reviewer: `reviewer` subagent (Claude Code, Opus 5)
- Scope: diff quality only for `git diff main...HEAD` on `feat/overlay-scaffold-v2-p2` (base `main`, HEAD `f1c9ab4`, 124 files). No tests, static analysis, spec-compliance, or doc-drift checks were run.

## Evidence reviewed

Read in full or in the changed region:

- `templates/base/.claude/hooks/ralph-dispatch.sh` (and the byte-identical root mirror `.claude/hooks/ralph-dispatch.sh`)
- all 8 `<event>.d/*.sh` shims at root + `templates/base/` (verified byte-identical pairs)
- `.claude/hooks/lib_json.sh`, `.claude/settings.json`, `.codex/config.toml`
- `internal/cli/init.go` (`handleExistingProjectInit`, `reconcileBlockSurfaces`, `extractBlockInterior`, `ownerForScaffoldPath`, `executeInit` steps 1b/3b)
- `internal/scaffold/render.go` `RenderFS` (to confirm `hashes` is populated before the skip branch)
- `internal/upgrade/block.go`, `internal/cli/upgrade.go`, `internal/scaffold/manifest.go` (`SetOwner`)
- `internal/cli/language_pack.go`, `internal/cli/pack.go`
- `scripts/check-sync.sh` (`extract_agents_md_block` + section 1b), `scripts/run-verify.sh`, `scripts/check-pipeline-sync.sh`
- `tests/test-ralph-dispatch.sh`, `tests/test-hook-wiring.sh`
- `.gitignore` / `templates/base/.gitignore`, root + template `AGENTS.md` managed-block boundaries, `.ralph/core/AGENTS.core.md`
- `docs/tech-debt/README.md` diff (1 row closed, 2 rows added)
- diff-wide greps for TODO/FIXME/debug output, hardcoded credentials, and surviving `.claude/rules/<name>.md` (non-`ralph/`) references

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH | maintainability | The pack/rule-path rename swept the *positive* assertion in `TestOrgSpawn_RoleAndScopeFlags_ExpandTemplateAndRecordScope` but not the two *negative* guards in the same file. Both now search for a string the prompt templates no longer emit, so neither can ever fire again — they pass vacuously and no longer guard anything. | `internal/cli/org_test.go:850` and `:902` still read `strings.Contains(logText, ".claude/rules/agent-messaging.md")`; the diff updates only line 857 to `.claude/rules/ralph/agent-messaging.md`. `internal/org/prompts/{lead,qa,reviewer}.md` now emit only `.claude/rules/ralph/agent-messaging.md`, so the old substring is absent from any rendered template. The guards assert "the rendered template body must NOT appear inline in argv" (850) and "no role-template markers for an unknown role" (902) — both regressions are now undetectable. | Update both to `.claude/rules/ralph/agent-messaging.md`. Consider hoisting the marker string into one package-level test constant so a future rename cannot half-land again. |
| MEDIUM | security | `ralph-dispatch.sh` installs only `trap cleanup EXIT`. POSIX `sh` does not run an EXIT trap when the process dies from an untrapped fatal signal, and Claude Code kills hooks that exceed the hook timeout. On that path the buffered payload — which carries the full prompt / `tool_input` (including Bash command lines and file contents) — is left in `$TMPDIR` indefinitely. | `templates/base/.claude/hooks/ralph-dispatch.sh:66-74`: `stdin_buf="$(mktemp ...)"; cat > "$stdin_buf"` … `trap cleanup EXIT`. Same in the root mirror. | Add `trap cleanup INT TERM HUP` alongside the EXIT trap (per `.claude/rules/`-adjacent convention: separate signal trap from EXIT trap). Also move the `trap` above the `mktemp` calls so a failure of the second/third `mktemp` under `set -e` does not leak the first file. |
| MEDIUM | maintainability | Codex is left on the old wiring. `.codex/config.toml` (root and `templates/base/`, both untouched by this PR) still invokes `./.claude/hooks/check_mojibake.sh` directly, so under Codex the `<event>.d` layering and the new `.ralph/local/hooks/<event>.d/` extension point never execute. `tests/test-hook-wiring.sh` passes only because the pre-dispatcher scripts still exist at their old paths. Nothing in the diff records the gap. | `.codex/config.toml:76,80` are the only `command =` entries and both name `check_mojibake.sh`; `git diff main...HEAD --stat -- .codex/ templates/base/.codex/` shows only `AGENTS.override.md` changed. `AGENTS.md`'s new repo-map bullet describes `.ralph/local/` as extension points "that `scripts/run-verify.sh` and the hooks dispatcher execute" with no CLI qualifier. The three new `docs/tech-debt/README.md` rows do not cover it. | Either wire `.codex/config.toml` through `ralph-dispatch.sh` in this PR, or qualify the `AGENTS.md` bullet ("Claude Code only today") and add a tech-debt row naming the Codex-side dispatcher wiring as Phase 3 work. |
| MEDIUM | maintainability | The `.gitignore` managed block runs to the last line of the file: `# END RALPH MANAGED` *is* the final line. The header comment tells users to "add yours outside the managed block", but the only free area is the two comment lines above `BEGIN`. Appending — the universal habit for `.gitignore` — silently lands inside the ralph-owned region that Phase 3's block engine will rewrite. | `.gitignore:1-3` (header + `# BEGIN RALPH MANAGED (ralph:gitignore)`) and the trailing `# END RALPH MANAGED`; `templates/base/.gitignore` is byte-identical. `tail -8 .gitignore` confirms `END` is last. | Close the block before the project-specific tail, or append a labelled free area after `END` (e.g. `# Your project ignores below this line`) so the "outside the block" instruction has somewhere obvious to point. |
| MEDIUM | typo | The new comment above `REFS` claims the list "mirrors the 'Where this order is referenced' list in `$CANONICAL` itself", but the same hunk deletes `CLAUDE.md` from `REFS` while the canonical doc's own list still names `CLAUDE.md`. The comment is false as of the commit that introduces it. | `scripts/check-pipeline-sync.sh:26-33` (comment + `REFS` with `CLAUDE.md` removed) vs. `.claude/rules/ralph/post-implementation-pipeline.md` "Where this order is referenced" → still lists ``- `CLAUDE.md` (Default behavior)``. `templates/base/.claude/rules/ralph/post-implementation-pipeline.md` carries the same stale entry. | Pick one: drop `CLAUDE.md` from the canonical doc's reference list (matching the new "CLAUDE.md carries no pipeline detail" rationale), or restore the `REFS` entry. Do not leave the comment asserting a mirror that does not hold. |
| MEDIUM | maintainability | `ralph-dispatch.sh`'s header says "lib_json.sh is reused for jq-availability detection", but the script sources `lib_json.sh` and then runs its own `command -v jq` probe; `extract_json_field` is never called. The `.` is a hard `set -e` dependency, so a missing or unreadable `lib_json.sh` now aborts *every* hook event rather than just the hooks that parse JSON. | `templates/base/.claude/hooks/ralph-dispatch.sh:47` (comment), `:52-53` (`. "$HOOK_DIR/lib_json.sh"`), `:61-64` (independent `command -v jq` probe). `lib_json.sh` exports only `extract_json_field`, which the dispatcher does not use. | Drop the `source` line and the header claim, or actually route the jq probe through a helper in `lib_json.sh`. As written it is an unused dependency that widens the blast radius of a missing file. |
| MEDIUM | maintainability | The Codex-config check's comment says it matches ``command = [ "..." ]`` TOML array entries, but the grep scans the whole file for any double-quoted `./`-prefixed string. A future `cwd = "./x"`, an example path in a comment, or any unrelated relative path would be asserted to be an executable hook command and fail the test with a misleading message. | `tests/test-hook-wiring.sh:98-101`: `commands="$(grep -oE '"\./[^"]+"' "$config" | tr -d '"' \|\| true)"` under a comment describing `command = [...]` matching. | Anchor the grep to command lines (e.g. `grep -E '^[[:space:]]*command[[:space:]]*=' "$config" \| grep -oE ...`) so the extraction matches the documented intent. |
| LOW | maintainability | `ownerForScaffoldPath` classifies OS-separator paths, but manifest keys are `fs.FS` slash paths. On Unix the two coincide; on Windows `filepath.Separator` is `\`, so `.github/workflows/verify.yml` and every `docs/...` entry would silently fall through to `OwnerCore` instead of `OwnerSeed`. | `internal/cli/init.go:290-301`: `filepath.Join(".github", "workflows", "verify.yml")` and `strings.HasPrefix(relPath, "docs"+string(filepath.Separator))`, compared against keys produced by `fs.WalkDir` in `internal/scaffold/render.go:41-72` (always `/`). | Use literal `"/"` (or the `path` package) in the new ownership classifier so it matches the key space it is switching on. `packRuleRelPath` (`internal/cli/language_pack.go:114`) has the same pre-existing shape and is worth aligning at the same time. |
| LOW | security | `$event` is taken from `$1` and interpolated into an unquoted, word-split directory list, so an event name containing whitespace or a glob metacharacter word-splits or pathname-expands. Values come from `settings.json` today, so this is a latent contract gap rather than a live bug. | `templates/base/.claude/hooks/ralph-dispatch.sh:82` (`dirs="./.claude/hooks/${event}.d ..."`) and `:84` (`for d in $dirs`); the only validation is the non-empty check at `:56-59`. | Add a one-line charset guard after the non-empty check (e.g. `case "$event" in *[!A-Za-z]*) echo "..." >&2; exit 2;; esac`) to make the "event names are bare identifiers" contract explicit. |
| LOW | maintainability | The 5-clause blank-line-collapsing `awk` program is duplicated verbatim in the jq and no-jq merge branches; a fix to one silently diverges from the other. | `templates/base/.claude/hooks/ralph-dispatch.sh:150` and `:154` are character-identical `awk` programs. | Extract to a shell function (e.g. `collapse_blank_runs()`) called from both branches. |
| LOW | maintainability | `ralph-dispatch.sh:116` writes the first script's output via `cp`, which uses the process umask (typically `0644`), whereas every other temp file in the script comes from `mktemp` (`0600`). The path is unpredictable, so this is a readability/consistency issue rather than an exploitable one. | `first_output="$out_tmp.first"; cp "$out_tmp" "$first_output"` at `templates/base/.claude/hooks/ralph-dispatch.sh:115-117`. | Either `mktemp` the file or `chmod 600` it, so all payload-derived temp files carry the same permissions. |
| LOW | naming | When `templates/base/AGENTS.md` is absent, section 1b prints `ROOT_ONLY templates/base/AGENTS.md (file missing)` and increments `root_only` — the label directly contradicts the path it names. | `scripts/check-sync.sh:196-200` (the `for agents_file in "AGENTS.md" "templates/base/AGENTS.md"` loop's missing-file branch). | Use a neutral label/counter for this branch (e.g. `MISSING`), or split the loop so each side gets the correct classification. |
| LOW | maintainability | Section 1b increments the same `identical` counter that section 1 uses for whole-file pairs, so the summary line now mixes "files compared byte-for-byte" with "managed blocks compared", double-counting `AGENTS.md` as two comparisons. | `scripts/check-sync.sh:207` (`identical=$((identical + 1))` inside the block-aware loop) vs. the section-1 usage at `:170`-ish. | Track block comparisons in their own counter and report them on a separate summary line. |
| LOW | readability | The free-area content that follows the `AGENTS.md` managed block opens at H3 (`### Org runtime pointers (meta-repo specific)`) immediately after the block, whose last heading is `## Hard rules` — so it renders as a subsection of "Hard rules", which it is not. It also begins "Steps 4–7 above", a reference into the ralph-owned block's numbered list; if upstream renumbers that list the free-area text goes stale with nothing to catch it. | `AGENTS.md:68-76` (`<!-- END RALPH MANAGED -->` then `### Org runtime pointers`, then "Steps 4–7 above run through..."). | Promote the section to `##`, and phrase the cross-reference by name ("the self-review / verify / test / sync-docs steps") rather than by the block's list numbering. |
| LOW | maintainability | All 8 `<event>.d/*.sh` shims are `exec "$(dirname "$0")/../<name>.sh"` with no `"$@"` forwarding. Harmless today (the dispatcher passes no arguments), but it is an eight-fold copy-paste where adding `"$@"` costs nothing and removes the future trap. | e.g. `.claude/hooks/PostToolUse.d/10-post-edit-verify.sh:2`; identical shape in all 8 root shims and their 8 `templates/base/` mirrors. | Append `"$@"` to each `exec` line. |

## Positive notes

- **Mirror discipline is clean.** `.claude/hooks/ralph-dispatch.sh` and all 8 `<event>.d` shims are byte-identical to their `templates/base/` counterparts (verified with `diff -q`), and `.gitignore` matches `templates/base/.gitignore` exactly. The `KNOWN_DIFFS` entry rename (`.claude/rules/model-routing.md` → `.claude/rules/ralph/model-routing.md`) tracked the move.
- **The `AGENTS.md` whole-file suppression was genuinely retired, not papered over.** `AGENTS.md` was removed from `KNOWN_DIFFS` and replaced with a block-aware comparison against `templates/base/.ralph/core/AGENTS.core.md`, and the tech-debt row was closed with an accurate description of what actually landed. `extract_agents_md_block` fails closed: a file with no markers yields empty output and diffs as DRIFTED against the non-empty source.
- **No skipped-file bug in the block reconciliation path.** `reconcileBlockSurfaces` keys off `result.Skipped`, and `RenderFS` populates `hashes[path]` at `render.go:71-72` *before* the skip branch at `:75-79`, so a pre-existing `AGENTS.md`/`.gitignore` still gets a manifest entry for step 3b's `SetFileOwned` call to attach the disk hash to. I specifically checked for the "disk hash computed but never recorded" failure mode and it does not occur.
- **The legacy-upgrade guard is placed correctly.** `internal/cli/upgrade.go`'s `LayoutV2` check sits before any diff computation or write and its comment accurately states that it applies to `--force` and `--dry-run` alike; `handleExistingProjectInit` closes the matching `ralph init`-on-existing-project entry point at both call sites.
- **`block.go`'s style parameterization preserved the old API honestly.** `UpdateManagedBlock`/`BeginMarker`/`EndMarker` are thin, correctly-documented delegations to the `*Styled` forms rather than reimplementations, and the marker scan uses the style-specific end marker throughout (no residual use of the HTML constant in the scan loop).
- **Test cleanup scope is safe.** `tests/test-ralph-dispatch.sh` confines every write and removal to a `mktemp -d` workdir, including the `run-verify.sh` fixture, so a parallel real session's `.harness/state/` is never touched.
- **No secrets, debug output, TODO/FIXME markers, or commented-out code** in the added lines (diff-wide grep).

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| The Codex hook surface (`.codex/config.toml`, root and `templates/base/`) was not migrated to `ralph-dispatch.sh`: it still invokes `./.claude/hooks/check_mojibake.sh` directly, so `<event>.d` layering and the `.ralph/local/hooks/<event>.d/` extension point are Claude-Code-only. `tests/test-hook-wiring.sh` cannot catch this — it asserts each referenced path exists and is executable, and the pre-dispatcher scripts still exist at their old paths. | A downstream project that adds a `.ralph/local/hooks/<event>.d/` drop-in gets it under Claude Code and silently not under Codex, contradicting `AGENTS.md`'s unqualified "the hooks dispatcher" wording. | Phase 2's slice-2 scope was the Claude-side dispatcher plus the `.d` migration; the Codex `[hooks]` surface is a distinct config with its own event-name mapping and was not in the slice's file list. | Phase 3 (`ralph upgrade` wiring for v2), which touches hook surfaces anyway — or the next `.codex/config.toml` change, whichever is first. | `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (Slice 2), `.codex/config.toml:76,80`, `templates/base/.codex/config.toml`, `tests/test-hook-wiring.sh` |
| The `.gitignore` managed block extends to EOF (`# END RALPH MANAGED` is the last line), so the file's own "add yours outside the managed block" instruction has no free area below the block. User additions appended in the normal way land inside the ralph-owned region. | Once Phase 3's block engine starts rewriting block interiors on upgrade, a downstream project's appended ignore rules are silently discarded. Today it is only a latent trap (nothing rewrites the block yet). | Phase 2 only seeds the block markers; the rewrite-on-upgrade behavior that makes this destructive is Phase 3's. Choosing where the block ends is a layout decision better made alongside the engine that consumes it. | Phase 3, when the block engine is wired into `ralph upgrade` — before the first release that actually rewrites a `.gitignore` block. | `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (Slice 1/5), `.gitignore`, `templates/base/.gitignore`, `internal/upgrade/block.go` (`BlockMarkerHash`) |

_(Both rows appended to `docs/tech-debt/README.md`.)_

## Recommendation

- Merge: **not yet** — no CRITICAL findings, so the pipeline is not blocked, but the HIGH finding should be fixed before `/pr`. It is a two-line change in `internal/cli/org_test.go` and leaving it turns two live regression guards into permanent no-ops.
- Fix before merge: HIGH-1 (`org_test.go:850,902`). Strongly recommended in the same pass: MEDIUM-1 (dispatcher signal trap — one line) and MEDIUM-5 (`check-pipeline-sync.sh` comment vs. canonical list — pick one side).
- Follow-ups (fine to defer): the two tech-debt rows above; MEDIUM-6 (`lib_json.sh` unused source), MEDIUM-7 (`test-hook-wiring.sh` grep scope), and the LOW list.
- Known gaps in this review: no tests were run, no static analysis, no spec-compliance or documentation-drift assessment — `/verify` and `/test` own those. Test *coverage* questions I noticed but did not pursue (the dispatcher's no-jq fallback path and the third `.claude/hooks/local/<event>.d/` layer are unexercised by `tests/test-ralph-dispatch.sh`) belong to `/test`.

---

# Cycle 2 (fix-and-revalidate re-run)

- Date: 2026-08-17
- Pipeline cycle: 2 of 2 (`RALPH_STANDARD_MAX_PIPELINE_CYCLES` default) — **this is the last cycle**
- Scope: `git diff 02c5328..HEAD` (HEAD `f80e60f`), i.e. everything since the cycle-1 review. Commits: `c2d501e` (cycle-1 self-review fixes), `4d4bdfc` / `0132d9f` / `5283ee0` (verify + test + sync-docs artifacts, the tester's three-layer dispatcher case, a test-hermeticity fix, README/repo-map doc sync), `6e915f5` (cross-review triage), `f80e60f` (cross-review ACTION_REQUIRED fixes).
- Method: every cycle-1 finding re-checked against the post-fix tree (each cited path re-read or re-grepped individually), plus a fresh diff-quality pass over the delta. Diff quality only — no tests, static analysis, spec compliance, or doc-drift checks.

## Cycle-1 finding re-classification

| # | Cycle-1 finding | Status | Evidence |
| --- | --- | --- | --- |
| HIGH-1 | `org_test.go` negative guards search a string the templates no longer emit | **Fixed** | Both sites updated (`c2d501e`: `org_test.go:850`, `:902` → `.claude/rules/ralph/agent-messaging.md`). Swept for stragglers: `grep -rn 'rules/agent-messaging' --include="*.go" .` returns nothing, so the rename left no other vacuous assertion. The suggested test constant was not extracted — cosmetic, not tracked. |
| MEDIUM-1 | Dispatcher installs only `trap cleanup EXIT`; payload leaks on signal death | **Fixed, new defect introduced** | Trap is now `EXIT INT TERM HUP` and sits above the `mktemp` calls (`ralph-dispatch.sh:68-77`). The leak is closed, but the rework has its own problems — see C2-1. |
| MEDIUM-2 | Codex left on the old wiring; nothing in the diff records the gap | **Deferred, row landed — but the gap widened** | `docs/tech-debt/README.md:101` carries a Phase-3 trigger. `.codex/config.toml` still untouched. However the AGENTS.md bullet was never qualified and `/sync-docs` added two more unqualified claims — see C2-2. |
| MEDIUM-3 | `.gitignore` managed block runs to EOF; no free area below | **Fixed in code, register row now false** | `c2d501e` appended a blank line + `# Project-specific ignores go below this line.` after `# END RALPH MANAGED` in both `.gitignore` and `templates/base/.gitignore` (still byte-identical). The tech-debt row that describes the now-fixed state was left open — see C2-3. |
| MEDIUM-4 | `check-pipeline-sync.sh` comment claims a mirror that does not hold | **Fixed** | `CLAUDE.md` removed from the canonical doc's "Where this order is referenced" list in both root and `templates/base/` copies. `REFS` (`scripts/check-pipeline-sync.sh:29-37`) and the canonical list now hold the same six entries in the same order, so the comment is true as written. |
| MEDIUM-5 | `lib_json.sh` sourced but unused; widens blast radius | **Fixed** | The `. "$HOOK_DIR/lib_json.sh"` line and the now-orphaned `HOOK_DIR` assignment are both gone; the header claim was rewritten to "jq availability is detected directly below via `command -v jq`" (`ralph-dispatch.sh:47`), which matches `:63-66`. |
| MEDIUM-6 | `test-hook-wiring.sh` grep scans the whole TOML, not command lines | **Fixed** | `tests/test-hook-wiring.sh:101` now pre-filters with `grep -E '^[[:space:]]*command[[:space:]]*='`, and the comment was rewritten to describe the two-stage extraction rather than the old single-stage claim. |
| LOW-1 | `ownerForScaffoldPath` classifies OS-separator paths against slash keys | **Half-fixed, remainder untracked** | The classifier is normalized (`filepath.ToSlash` at `init.go:296`, literal `/` comparisons). The second half of the same finding — "`packRuleRelPath` has the same pre-existing shape and is worth aligning at the same time" — was not done: `internal/cli/language_pack.go:113-115` still returns `filepath.Join(".claude", "rules", "ralph", pack+".md")`, so `ralph pack add` still writes OS-separator manifest keys on Windows while `ralph init` writes slash keys. Nothing tracks it. See C2-6. |
| LOW-2 | `$event` word-splits / glob-expands into the directory list | **Fixed** | `case "$event" in *[!A-Za-z]*)` guard at `ralph-dispatch.sh:56-61`. Verified non-breaking against the real caller: all seven event names in `.claude/settings.json` (`PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `UserPromptSubmit`, `SessionStart`, `SessionEnd`, `PreCompact`) are pure `[A-Za-z]`. |
| LOW-3 | Five-clause `awk` program duplicated verbatim in both merge branches | **Unfixed, untracked** | Still character-identical at `ralph-dispatch.sh:157` and `:161`. |
| LOW-4 | `cp`-created `$out_tmp.first` uses the umask, not `mktemp`'s 0600 | **Fixed** | `chmod 600 "$first_output"` at `ralph-dispatch.sh:123`. (But C2-1 describes a path that re-creates a payload temp file under the umask again.) |
| LOW-5 | `ROOT_ONLY templates/base/AGENTS.md (file missing)` label contradicts its path | **Unfixed, untracked** | `scripts/check-sync.sh:205-209` unchanged; `git diff --stat 02c5328..HEAD -- scripts/` is empty. |
| LOW-6 | Section 1b increments the whole-file `identical` counter | **Unfixed, untracked** | Same evidence as LOW-5. |
| LOW-7 | Free-area section opens at H3 and cites the block's list numbering | **Fixed** | `AGENTS.md:70` is now `## Org runtime pointers (meta-repo specific)` and the body says "The self-review, verify, test, and sync-docs steps" instead of "Steps 4–7 above", so it no longer depends on the managed block's numbering. |
| LOW-8 | Eight `.d` shims `exec` without `"$@"` | **Fixed** | All 8 root shims and all 8 `templates/base/` mirrors carry `"$@"`; every pair still `diff -q`-identical. |

Summary: 1 HIGH fixed, 5 of 6 MEDIUMs fixed (1 properly deferred with a register row), 4 of 8 LOWs fixed, 1 half-fixed, 3 unfixed and untracked.

## Cycle-2 findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | security | **C2-1.** The MEDIUM-1 fix closed the leak but opened two new holes. (a) `cleanup` is now installed before the `mktemp` calls with `stdin_buf`/`out_tmp`/`merged_tmp` pre-set to `""`, so if the trap fires in that window `rm -f "$out_tmp.first"` expands to `rm -f .first` — a relative path in the hook's cwd (the project root), outside the script's own temp namespace. (b) `trap cleanup INT TERM HUP` has no `exit` in the handler, and POSIX `sh` **resumes** the script after a trap action. So a hook-timeout SIGTERM now deletes all three temp files and then keeps running: the next iteration re-creates `$out_tmp` via `: > "$out_tmp"` (umask perms — re-opening exactly the payload-permission hole that LOW-4's `chmod 600` was closing), then `"$script" < "$stdin_buf"` fails on the missing file. Verified by probe under `/bin/sh`: the script printed `RESUMED after TERM`, a pre-existing `.first` in cwd was deleted, and the final exit status was `0` (not `128+15`); a separate probe confirmed a failed stdin redirect returns rc=1 with empty stdout, which the dispatcher would surface as `cat "$out_tmp"` (empty) + `exit 1` — a blocking hook failure with no message. | `templates/base/.claude/hooks/ralph-dispatch.sh:68-77` (empty initializers, `cleanup`, `trap ... EXIT INT TERM HUP`), `:96-99` (`: > "$out_tmp"` then the redirect), `:120-124`; root mirror identical. | Make the signal handler terminate: `trap 'cleanup; exit 130' INT; trap 'cleanup; exit 143' TERM; trap 'cleanup; exit 129' HUP` (keep `trap cleanup EXIT`). Guard the `.first` removal with `[ -n "$out_tmp" ] && rm -f "$out_tmp.first"`, or drop the separate `.first` file and track it in its own variable that `cleanup` tests before removing. |
| MEDIUM | maintainability | **C2-2.** The MEDIUM-2 deferral chose "add a tech-debt row" over "qualify the AGENTS.md bullet", but the bullet was never qualified — and `/sync-docs` (`5283ee0`) then added two more unqualified claims, one of them an outright instruction. The register row cites only the AGENTS.md wording, so the two new sites are outside anything that tracks the gap. Under Codex, `.ralph/local/hooks/<event>.d/` drop-ins do not run at all. | Unqualified today at three sites: `AGENTS.md:106` ("that `scripts/run-verify.sh` and the hooks dispatcher execute after core processing"), `docs/architecture/repo-map.md:20` (same claim, reworded), and `README.md:229` ("Add your own hook by dropping a script into `.ralph/local/hooks/<event>.d/` — no `settings.json` edits needed"). `docs/tech-debt/README.md:101` names only "`AGENTS.md`'s unqualified ... wording in the `.ralph/local/` repo-map bullet". `.codex/config.toml` is still unchanged in this PR. | Add "(Claude Code today; Codex wiring is Phase 3)" to the README sentence at minimum — it is the one a downstream reader will act on — and widen the tech-debt row's file list to all three sites so the Phase-3 payoff covers them. |
| MEDIUM | maintainability | **C2-3.** The `.gitignore` tech-debt row is now false in its own words. `c2d501e` fixed exactly what the row describes, in exactly the way the cycle-1 recommendation proposed, but the row was left open and unedited — so the register asserts a state the same PR removed two commits later. | `docs/tech-debt/README.md:102` still reads "extends to end-of-file (`# END RALPH MANAGED` is the last line of both `.gitignore` and `templates/base/.gitignore`) ... has no free area below the block", while `tail -3 .gitignore` is `# END RALPH MANAGED` / blank / `# Project-specific ignores go below this line.` (and `templates/base/.gitignore` is byte-identical, END at line 63 of 65). | Either strike the row through with a note pointing at `c2d501e`, or rewrite it to the residual risk only (Phase 3's block engine must not rewrite past `END`), which is a different and much narrower claim than what the row says today. |
| MEDIUM | maintainability | **C2-4.** This is cycle 2 of 2, so "recommended for in-cycle fix but not fixed" is now a silent deferral: the plan is archived at `/pr` and this report is the only thing that carries the residue. Four cycle-1 items are open and in no register — LOW-3 (duplicated `awk`), LOW-5 (`ROOT_ONLY` label), LOW-6 (`identical` counter), and the unfixed half of LOW-1 (`packRuleRelPath`). | `docs/tech-debt/README.md` has four rows; none covers `scripts/check-sync.sh` counters/labels, the dispatcher `awk` duplication, or `packRuleRelPath`. `git diff --stat 02c5328..HEAD -- scripts/` is empty. | Add one batched tech-debt row covering all four, triggered by "the next change to `scripts/check-sync.sh` or `ralph-dispatch.sh`". They are individually trivial; the risk is that they vanish from the record entirely at `/pr`. |
| MEDIUM | maintainability | **C2-5.** The pack-ownership fix hardcodes `scaffold.OwnerCore` while its own comment names the function that owns that decision — `ownerForScaffoldPath`, which lives in the same `package cli` and is directly callable. The two classifiers agree for today's pack payloads (`packs/languages/<lang>/**` and `.claude/rules/ralph/<lang>.md` both resolve to core), so this is a latent divergence, not a live bug: a pack that ever ships a `docs/` or `.ralph/local/` path gets `core` from `pack add` and `seed` from `ralph init` for the same path. Calling the function is a one-line change that is strictly better — it also picks up the `filepath.ToSlash` normalization the same PR just added. | `internal/cli/pack.go:84-95` — comment reads "same classification ralph init gives every non-seed/non-block path — see ownerForScaffoldPath in init.go", body calls `manifest.SetOwner(path, scaffold.OwnerCore)`. `internal/cli/init.go:295-309` is the classifier, unexported but same package. | Replace the constant with `ownerForScaffoldPath(path)` and shorten the comment to say the classification is shared rather than mirrored. |
| LOW | maintainability | **C2-6.** Carryover from LOW-1: `packRuleRelPath` still builds manifest keys with `filepath.Join`, so `ralph pack add` writes `\`-separated keys on Windows into a manifest whose `ralph init` entries are `/`-separated — and `init.go`'s new doc comment now asserts "Manifest keys are always fs.FS slash paths" without qualification. The new tests key off `filepath.Join` too, so they would follow the OS rather than catch the mismatch. | `internal/cli/language_pack.go:113-115`; `internal/cli/init.go:291-294` (the absolute claim); `internal/cli/cli_test.go:2371-2373` and `internal/cli/init_v2_test.go:85` build expected keys with `filepath.Join`. | Fold into the C2-4 batched row, or align `packRelDir`/`packRuleRelPath` on `path.Join` now (they feed manifest keys, not filesystem operations). |
| LOW | readability | **C2-7.** The `/sync-docs` commit re-indented five README tree lines it had no reason to touch, and left seven lines' comment columns misaligned. The `#` column lands at offset 22 for most rows but 23–24 for `skills/`, `agents/` (both `.claude/` and `.codex/`), `.codex/README.md`, `.agents/skills/`, and the two new `.ralph/` rows. | `5283ee0` in `README.md` contains whitespace-only changes to `.claude/skills/`, `.claude/agents/`, `.codex/agents/`, `.codex/README.md`, and `.agents/skills/` — none of which changed content. Confirmed with `cat -A` on the tree block at `README.md:146-172`. | Re-align all rows to a single `#` column and drop the five whitespace-only line changes from the diff. |
| LOW | readability | **C2-8.** The `.gitignore` fix established a convention — a labelled free area after `# END RALPH MANAGED` — and applied it to only one of the two block surfaces. `templates/base/AGENTS.md`, the file `ralph init` actually ships downstream, still ends at `<!-- END RALPH MANAGED -->` (line 64 of 64), so it has the exact shape cycle-1 flagged for `.gitignore`. Its designated free area is the `## Project notes` stub above the block, which is fine but undocumented at the point where a user would append. | `grep -n "RALPH MANAGED" templates/base/AGENTS.md` → `7`, `64`; `wc -l` → `64`. Root `AGENTS.md` has free content after `END` (lines 69-149) but no label either. | Add the same one-line marker below `END` in `templates/base/AGENTS.md` so the convention is consistent across both managed-block surfaces the scaffold ships. |
| LOW | maintainability | **C2-9.** `pack.go` now runs two consecutive loops over the same `pr.hashes` map, and the second one's error branch cannot fire: `SetOwner` fails only on an invalid owner (a constant here) or a missing manifest entry, and the immediately preceding loop just called `SetFile`/`SetFileWithBaseline` for every one of those keys. The `⚠ Could not set owner` warning is unreachable code that reads as if it guards something. | `internal/cli/pack.go:77-95`; `internal/scaffold/manifest.go:213-224` (`SetOwner`'s only two error paths). | Merge the owner assignment into the existing loop. The `⚠`-and-continue style matches the surrounding `ReadManifest`/`Write` handling, so keeping it is defensible — but only if it can actually fire. |
| LOW | readability | **C2-10.** Two artifact nits. `TestAddPack_LegacyManifest_StaysOwnerless` reports its central behavioral assertion — that `addPack` must not upgrade `Meta.Layout` — via `t.Fatalf("precondition failed: ...")`, which sends whoever hits it looking for a broken fixture instead of a regression. Separately, the triage report's `file:line` pointers were invalidated by the fix commit that followed it: `init.go:296-299` now lands on the `docs/` branch (the `.ralph/local/` branch is at `:306-308`) and `pack.go:55` lands on a doc-comment line (the fix is at `:84-95`). | `internal/cli/cli_test.go:2415-2417`; `docs/reports/cross-review-triage-overlay-scaffold-v2-p2.md:25-26`. | Reword the test failure to "addPack upgraded Meta.Layout to v2, want unchanged". For the triage report, prefer function names over line numbers, or re-resolve the two pointers post-fix. |

No CRITICAL findings.

## Cycle-2 positive notes

- **The rename sweep is genuinely complete this time.** `grep -rn 'rules/agent-messaging' --include="*.go" .` returns nothing after the HIGH-1 fix, so no third site survived with the old string.
- **The event-name guard was validated against its real caller, not assumed.** All seven `ralph-dispatch.sh <event>` invocations in `.claude/settings.json` are pure `[A-Za-z]`, so the new `*[!A-Za-z]*` rejection cannot break a shipped event today.
- **Mirror discipline held through both fix commits.** `.claude/hooks/ralph-dispatch.sh`, all 8 `<event>.d` shims, and `.gitignore` are still byte-identical to their `templates/base/` counterparts after `c2d501e` (`diff -q` on every pair).
- **The `MEDIUM-4` fix picked a side instead of splitting the difference.** `REFS` in `check-pipeline-sync.sh` and the canonical doc's reference list now hold the same six entries in the same order, in both the root and `templates/base/` copies — the comment asserting the mirror is true as written.
- **The cross-review fix is scoped to what triage actually found.** Both ACTION_REQUIRED items map 1:1 onto `f80e60f`, and the `.ralph/local/` branch is live rather than dead code: `templates.go`'s `//go:embed all:templates` includes dotfiles, and `templates/base/.ralph/local/{hooks,verify.d,test.d}/.gitkeep` really exist, so `ownerForScaffoldPath` sees those keys at render time. The `init_v2_test.go` fixture path matches a real template file rather than an invented one.
- **The new regression tests assert the negative case too.** `TestAddPack_LegacyManifest_StaysOwnerless` pins that a legacy manifest stays ownerless, not just that a v2 manifest gains owners — so the guard is a real contract, not a change-detector.

## Cycle-2 process gaps

- `docs/insights/events/2026-08-17-overlay-scaffold-v2-p2.jsonl` has four events (`self_review`, `test`, `sync_docs`, `cross_review`) but no `verify` event, even though `docs/reports/verify-2026-08-17-overlay-scaffold-v2-p2.md` was committed in `4d4bdfc`. `ralph insights` will under-report the pipeline for this slug. Append the missing `verify` row via `scripts/insights-append.sh`.
- The plan's artifact checklist ticks review / verification / test / sync-docs but `- [ ] Plan reviewed` is still unchecked, and there is no cross-review checkbox despite the triage report existing.

## Cycle-2 recommendation

- Merge: **yes, with fixes** — no CRITICAL findings, and the cycle-1 HIGH is genuinely closed. C2-1 is the one worth fixing before `/pr`: it is a security-adjacent regression introduced by a fix, and the correction is three `trap` lines plus one guard.
- Fix before merge: C2-1. Cheap and worth doing in the same pass: C2-3 (edit or strike one register row) and C2-4 (add one batched register row) — both are bookkeeping that this last cycle is the final chance to get right.
- Follow-ups (fine to defer, provided C2-4's row lands): C2-2, C2-5, and the LOW list.
- Known gaps in this review: unchanged from cycle 1 — no tests, static analysis, spec compliance, or doc-drift assessment. The `/verify` and `/test` reports in this delta were read for factual claims about the diff, not re-executed.

---

# Cycle 3 (final pass after cap raise)

- Date: 2026-08-17
- Pipeline cycle: 3 of 3 (`RALPH_STANDARD_MAX_PIPELINE_CYCLES` raised from 2) — **this is the last cycle**
- Scope: `git diff 98d184f..HEAD` (HEAD `c289875`), i.e. everything since the cycle-2 review. Commits: `bd2d867` (cycle-2 self-review fixes), `54f97ac` / `d21273b` / `0be339a` (verify + test + sync-docs cycle-2 artifacts), `d5b478e` (cross-review triage cycle 2), `c289875` (cycle-2 cross-review ACTION_REQUIRED fixes: `doctor` command-token split + `init` symlink guard + 4 test files).
- Method: every cycle-2 finding re-checked against the post-fix tree, plus a fresh diff-quality pass over the delta. Two claims were settled by isolated probe rather than inspection (recorded inline below). Diff quality only — no tests, static analysis, spec compliance, or doc-drift checks.

## Cycle-2 finding re-classification

| # | Cycle-2 finding | Status | Evidence |
| --- | --- | --- | --- |
| C2-1 | Signal handler resumed instead of exiting; empty `$out_tmp` made `rm -f "$out_tmp.first"` a cwd-relative `.first` | **Fixed** | `ralph-dispatch.sh:90-93` is now `trap cleanup EXIT` plus `trap 'cleanup; exit 130' INT` / `143 TERM` / `129 HUP` (each `128+signo`). `cleanup` (`:72-85`) guards all four removals with `if [ -n ... ]` rather than `&&`, with a comment explaining that the `&&` form would make `cleanup`'s own status the EXIT trap's exit code. `first_output=""` was hoisted from `:102` to `:71` so the guard has a variable to test. The fix itself is correct; the regression test added alongside it is not — see C3-2. |
| C2-2 | Codex-parity claim unqualified at three sites; register row cited only one | **Fixed** | All three sites now carry the qualifier: `AGENTS.md:106`, `docs/architecture/repo-map.md:20`, and `README.md:229` (the imperative one, which now spells out that `.ralph/local/hooks/<event>.d/` drop-ins do not run under Codex yet). `docs/tech-debt/README.md`'s Codex row widened its file list to name all three plus `AGENTS.md`/`repo-map.md`/`README.md` explicitly. |
| C2-3 | `.gitignore` register row asserted a state the same PR had already removed | **Fixed** | The row is struck through with `(RESOLVED 2026-08-17 in c2d501e)`, and its "Trigger" cell is struck too with a note naming the labelled free area that landed. Verified against the tree: `.gitignore:63` is `# END RALPH MANAGED`, `:65` is `# Project-specific ignores go below this line.` |
| C2-4 | Last-cycle deferrals (LOW-3/LOW-5/LOW-6 + LOW-1's remainder) in no register | **Fixed** | One batched row landed covering the dispatcher `awk` duplication and both `check-sync.sh` nits, with an explicit note that LOW-1's `packRuleRelPath` half was fixed rather than deferred. Nit: the row's prose opens "Two small ... findings" while its own reference cell cites "Cycle 1 LOW-3/LOW-5/LOW-6" (three) — the count treats the two `check-sync.sh` items as one. |
| C2-5 | `pack.go` hardcoded `scaffold.OwnerCore` while its comment named the shared classifier | **Fixed** | `internal/cli/pack.go:98` now calls `ownerForScaffoldPath(path)`; the comment was rewritten from "same classification ... see ownerForScaffoldPath" to "shared with ralph init via ownerForScaffoldPath ... rather than mirrored". No import shadowing: `pack.go` does not import `path`, and Go import names are file-scoped. |
| C2-6 | `packRuleRelPath` built manifest keys with `filepath.Join` | **Fixed** | `internal/cli/language_pack.go:121` uses `path.Join`, with a doc comment naming `init.go`'s "always fs.FS slash paths" invariant as the reason. Checked every `path`-shaped identifier in the file: the new import cannot be shadowed (the only nearby binding is the `pack string` parameter). |
| C2-7 | `/sync-docs` left five whitespace-only README tree lines and seven misaligned `#` columns | **Fixed** | `bd2d867` re-indented `.claude/skills/`, `.claude/agents/`, `.codex/agents/`, `.codex/README.md`, `.agents/skills/`, and both `.ralph/` rows back to a single comment column. |
| C2-8 | `templates/base/AGENTS.md` had no labelled free area below its block | **Fixed** | `templates/base/AGENTS.md:66` is `<!-- Project-specific notes go below this line. -->`. Note the file now designates two free areas (the `## Project notes` heading at `:5` and this one), and unlike `.gitignore`'s `#` comment this label is invisible in rendered Markdown — cosmetic, not tracked. |
| C2-9 | `pack.go`'s `⚠ Could not set owner` branch is unreachable | **Addressed by comment, not removed** | `pack.go:99-104` now documents both `SetOwner` error paths and why neither can fire, and states the branch is a deliberate defensive guard matching the surrounding style. Acceptable resolution — the branch no longer reads as if it guards something real. |
| C2-10 | Test `Fatalf` misattributes a behavioral assertion; triage `file:line` pointers invalidated by the fix commit | **Half-fixed** | Triage half: both pointers were replaced (`internal/cli/doctor.go(checkHooks)`, `internal/cli/init.go:373(reconcileBlockSurfaces)`), but `:373` was resolved against the pre-fix file — post-`c289875` it lands on the closing `}` of the new `Lstat` error branch, not on the write path it describes. Test half: unfixed and untracked — `internal/cli/cli_test.go:2416` still reports the `Meta.Layout` contract as `t.Fatalf("precondition failed: ...")`, and `cli_test.go` is absent from `git diff --stat 98d184f..HEAD`. |
| Process gap 1 | Missing `verify` insight event | **Fixed, then re-broken** | The cycle-1 backfill landed (`bd2d867`, `13:32:51Z`, `cycle:1`), but the two events appended after it are mis-stamped — see C3-4. |
| Process gap 2 | Plan's `- [ ] Plan reviewed` unchecked; no cross-review checkbox | **Half-fixed** | `- [x] Plan reviewed` is now ticked (plan `:150`). The eleven AC checkboxes (`:86-96`) are all still `[ ]` despite `/verify` reporting them met, and there is still no cross-review artifact checkbox. |

Summary of cycle 2: 8 of 10 findings fully fixed, 1 addressed by documentation, 1 half-fixed; both process gaps partially closed.

## Cycle-3 findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | security | **C3-1.** The symlink guard closes one of two write paths. `reconcileBlockSurfaces` now refuses non-regular files, but it only ever sees paths that `RenderFS` *skipped*, and `RenderFS` decides that with `os.Stat`, which follows symlinks. A **dangling** `AGENTS.md` symlink therefore stats as absent, is classified `Created`, and `os.WriteFile` writes the scaffold content *through* the link — landing outside `targetDir`, which is exactly the containment failure the guard's own comment says it prevents. `RenderFS`'s boundary check does not help: it is a lexical `strings.HasPrefix` on the joined template path and never resolves the link. This is pre-existing `RenderFS` behavior, not introduced here, but the fix commit adopted the threat model and left the sibling gate open. Applies to every scaffold path, not just block surfaces. | `internal/cli/init.go:366-381` (the guard) vs. `internal/scaffold/render.go:75` (`os.Stat(target)`), `:82` (`Created`), `:56-58` (lexical boundary check), `:95` (`os.WriteFile(target, ...)`). Confirmed by probe: a dangling symlink `target/AGENTS.md` → `../OUTSIDE.md` makes `os.Stat` return `no such file or directory` and the subsequent `os.WriteFile` creates `OUTSIDE.md` outside the target dir with the scaffold content. The cycle-2 triage's own rationale — "従来は既存ファイル skip で発生しなかった" — holds only for resolvable symlinks. | Add the same `Lstat`/`IsRegular` refusal to `RenderFS`'s existence check (treat a non-regular target as skip-with-warning), or `O_NOFOLLOW` the write. If that is judged Phase-3 work, this is the last cycle — it needs a `docs/tech-debt/README.md` row, because the plan is archived at `/pr` and this report is otherwise the only record. |
| MEDIUM | maintainability | **C3-2.** The SIGTERM regression test added for C2-1 cannot fail. It backgrounds a *subshell* (`( cd ... \| dispatcher ) &`) and kills `$!` — the subshell's PID, not the dispatcher's. The subshell dies from the default TERM action, `wait` reports 143, and the dispatcher is never signaled at all: it keeps running to completion, reparented to PID 1. So `assert_exit ... 143 "$i_rc"` measures POSIX `kill` semantics on a background subshell, not the trap the commit added; the `.first` assertion is likewise unreachable, since `cleanup` runs normally at EXIT with every variable set. The test also leaks an orphaned `sleep 5` past the case. | `tests/test-ralph-dispatch.sh:325-339`. Confirmed by probe with a script carrying **no signal traps at all** (the pre-fix shape) under the identical construct: `rc from wait on subshell = 143` and `ps` showed the inner script alive with `PPID 1` after the subshell died. A trap-free dispatcher passes this test unchanged. | Signal the dispatcher directly: drop the wrapping subshell and background the dispatcher itself (`printf '{}' \| ... &` will not work — use a FIFO or a `sh -c` whose `exec` replaces the shell), or have the drop-in script record `getppid()`/`$PPID` to a marker file and `kill -TERM` that PID. Then assert on the dispatcher's own exit status and on the absence of its `ralph-dispatch-*` temp files in `$TMPDIR`. |
| LOW | maintainability | **C3-3.** `tests/test-ralph-dispatch.sh`'s header enumerates its cases `a.` through `h.`; `bd2d867` added Case I without extending the list, so the file's own index is one case short of its body. | `tests/test-ralph-dispatch.sh:5-21` (cases a–h) vs. `:314` (`Case I`). | Add the one-line `i.` entry, or drop the header enumeration in favor of the inline `── Case X ──` banners that already exist. |
| LOW | maintainability | **C3-4.** Two insight events in this delta are stamped with the wrong cycle, and cycle 2 has no `self_review` event at all. `ralph insights` will report this slug as having three cycle-1 verify/cross-review runs and a single self-review across the whole PR. | `docs/insights/events/2026-08-17-overlay-scaffold-v2-p2.jsonl`: `13:42:10Z` `phase:"verify"` `cycle:1` (appended by `54f97ac`, the *cycle-2* verify) and `14:02:20Z` `phase:"cross_review"` `cycle:1` (appended by `d5b478e`, the *cycle-2* triage). No line has `phase:"self_review"` with `cycle:2`. | Correct the two `cycle` values to `2` and append the missing cycle-2 `self_review` row via `scripts/insights-append.sh`, alongside this cycle's own events. |
| LOW | readability | **C3-5.** Carryover from C2-10, both halves. `cli_test.go:2416` still frames "addPack must not upgrade `Meta.Layout` to v2" as `precondition failed:`, which sends whoever hits it looking for a broken fixture rather than the regression the test exists to catch. The triage report's re-resolved `init.go:373` pointer now lands on a closing brace. | `internal/cli/cli_test.go:2416`; `docs/reports/cross-review-triage-overlay-scaffold-v2-p2.md` ACTION_REQUIRED row 2; `internal/cli/init.go:371-373`. | Reword the `Fatalf` to state the regression, and prefer the function name over the line number in the triage row (`reconcileBlockSurfaces` is already named there — the `:373` adds nothing but decay). |

No CRITICAL findings.

## Cycle-3 positive notes

- **The C2-1 trap rework is correct on its own terms.** Exit codes are `128+signo` for all three signals, `cleanup` is idempotent under `rm -f`, and the comment explaining why the guards are `if` blocks rather than `&&` chains records a real POSIX subtlety (`&&`'s false-branch status would become the EXIT trap's exit code) at the definition site rather than in a test.
- **Mirror discipline held through both fix commits.** `.claude/hooks/ralph-dispatch.sh` vs. `templates/base/.claude/hooks/ralph-dispatch.sh` and `.gitignore` vs. `templates/base/.gitignore` are still byte-identical (`diff -q`) after `bd2d867`.
- **The tech-debt register was diffed in both directions this time.** The `.gitignore` row was struck with a commit pointer rather than deleted, the Codex row's file list was widened to match the widened claim, and the batched deferral row landed — the exact three bookkeeping moves cycle 2 asked for.
- **The `doctor` tests cover both directions.** Present-script → `pass`, absent-script → `fail` with the exact detail string, and an arg-less command → `pass`. The middle case is what stops the whitespace split from masking a genuinely missing hook, and it is asserted rather than assumed.
- **The symlink test asserts absence of side effects, not just the warning.** It checks that the link is still a link, that `Readlink` still resolves to the external path, that the external target's bytes are unchanged, and that the manifest entry has an empty `DiskHash` — a real containment contract rather than a string match on stdout.
- **The `path.Join` change was checked for shadowing, not assumed safe.** `language_pack.go` has no local binding named `path`, and the import is file-scoped so `pack.go`'s `for path := range pr.hashes` is unaffected.

## Cycle-3 recommendation

- Merge: **yes** — no CRITICAL findings, both cycle-2 cross-review ACTION_REQUIRED items are genuinely closed, and every cycle-2 MEDIUM is fixed. Nothing here blocks `/pr`.
- Strongly recommended before `/pr`, in priority order: **C3-1** (either extend the `Lstat` refusal to `RenderFS` or file the register row) and **C3-2** (a regression test that cannot fail is worse than no test — it advertises coverage the dispatcher does not have).
- This is the last cycle, so anything not fixed here is a silent deferral: the plan is archived at `/pr` and this report is the only carrier. If C3-1, C3-2, C3-3, C3-4, and C3-5 are all deferred, they need **one batched `docs/tech-debt/README.md` row**, triggered by "the next change to `internal/scaffold/render.go` or `tests/test-ralph-dispatch.sh`".
- Known gaps in this review: unchanged from cycles 1 and 2 — no tests, static analysis, spec compliance, or doc-drift assessment. The two probes recorded above were isolated shell/Go snippets run in a scratch directory to settle specific factual claims; no repo test suite was executed. The cycle-2 triage report's header still reads `Cycle: 2/2 (cap reached)`, which the cap raise has made stale — that artifact belongs to `/cross-review`, not to this report.
