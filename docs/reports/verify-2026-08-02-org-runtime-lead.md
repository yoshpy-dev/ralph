# Verify report: org-runtime-lead

- Date: 2026-08-02
- Plan: `docs/plans/active/2026-08-02-org-runtime-lead.md`
- Verifier: `verifier` subagent (`/verify`)
- Scope: `git diff main...HEAD` (merge-base `8ec4a48`, HEAD `0bb2b75`) — 38 files, +4192/−185. Spec compliance + static analysis + documentation drift only. No tests were run (that is `/test`'s responsibility); self-review fix commit `9a22942` (post-review response to `docs/reports/self-review-2026-08-02-org-runtime-lead.md`) is included in the diff being verified.
- Evidence: `docs/evidence/verify-2026-08-02-org-runtime-lead.log` (also: `docs/evidence/verify-2026-08-02-124249.log`, the raw `run-static-verify.sh` auto-log)

## Spec compliance

| Acceptance criterion | Status | Evidence |
| --- | --- | --- |
| AC-1 (`[org.permissions]` 3-way lockstep) | Met | `internal/config/config.go:128-232,406-420` (`OrgPermissionsConfig`, `orgPermissionModeAllowed`, role-override validation), `templates/base/ralph.toml:93-107`, `scripts/ralph-config.sh:83-88` (`RALPH_ORG_PERMISSION_DEFAULT`, deliberately unexported — see Positive notes), `internal/config/defaults_sync_test.go:188-189` (`check("org.permissions.default", "RALPH_ORG_PERMISSION_DEFAULT", ...)`, 3-way comparison via the shared `check()` helper) |
| AC-2 (mode → driver-native argv, codex fail-closed) | Met | `internal/org/permissions.go:9-14,77-95` (`permissionArgsForDriver`: claude → `--permission-mode bypassPermissions/acceptEdits/nil`; codex → error unless `guarded`), constants added in fix commit `9a22942` (`PermissionModeAutonomous/Edits/Guarded`) |
| AC-2b (autonomous fail-closed on missing `--scope`) | Met | `internal/org/spawn.go:275-290` (gate runs before `DryRun` branch; after `9a22942` routed through `o.reject(...)` so a `rejected` manifest event + receipt is appended — this is a **documented, self-review-driven behavior change** from the plan's original "no `rejected` event" text; the plan's "fail-closed" claim still holds, only the audit trail improved) |
| AC-3 (`ralph org start`) | Met | `internal/cli/org.go:222-` (`newOrgStartCmd`, spawns `SeatID=Role=LeadIdentity`), `internal/org/prompts/lead.md` (new template), post-fix `newOrgStartCmd` now reuses `newOrgRuntime`'s single `resolveOrgConfig` call (fixed self-review LOW finding on double config reads) |
| AC-4 (`ralph org report`) | Met | `internal/org/report.go` (207 lines, new), `internal/cli/org.go:585-` (`newOrgReportCmd`), live-generated sample confirms output shape (see evidence file, "generated report (full...)" section) |
| AC-5 (`/org` skill + mirrors + CLAUDE.md) | Met | `./scripts/check-skill-sync.sh` → `[ok] check-skill-sync: 14 skill(s) in lock-step`; `./scripts/check-sync.sh` → `DRIFTED: 0, PASS`; `CLAUDE.md` and `templates/base/CLAUDE.md` both list `org` in the auto-invoked skill set (verified byte-for-byte pair diff, see Documentation drift) |
| AC-6 (tech-debt #1: announce-failure `Leave` compensation) | Met | `internal/org/spawn.go` `compensateLeave` call in the `agmsg_announce` failure path; `docs/tech-debt/README.md` row struck through, `RESOLVED 2026-08-02 in feat/org-runtime-lead (Slice 1 tech-debt batch, AC-6)` |
| AC-7 (tech-debt #2: `LeadIdentity` const + driver-derived agmsg type) | Met | `internal/org/spawn.go:39-49` (`const LeadIdentity = "lead"`, doc comment mandating its use at every call site), `agmsgTypeForDriver(leadDriver)` at line 752, `docs/tech-debt/README.md` row RESOLVED |
| AC-8 (tech-debt #3: `herdr_agent_name` persistence + fallback) | Met | `internal/org/manifest.go` `HerdrAgentName` field (`omitempty`), `internal/org/verbs.go` `resolvedHerdrAgentName` prefers recorded value, falls back to derivation; `docs/tech-debt/README.md` row RESOLVED with test names listed |
| AC-9 (tech-debt #4: flock-serialized spawn) | Met | `internal/org/lockfile.go` (`withManifestLock`, `syscall.Flock(LOCK_EX\|LOCK_NB)` polled, 5s bounded timeout); `docs/tech-debt/README.md` row RESOLVED; fix commit `9a22942` additionally reshaped the lock into two phases (Phase 1 detect-stale / Phase 2 post-compensation re-check) to resolve self-review MEDIUM-3 (see below) |
| AC-10 (tech-debt #5/#6: doctor agmsg-home display, dry-run error propagation) | Met | `internal/cli/doctor.go:617-` (`checkAgmsgAvailable` names `home` in both branches), `internal/org/spawn.go` `dryRunSpawn` now propagates `RenderRolePrompt`/`promptFilePath` errors via `failStep`; `docs/tech-debt/README.md` row RESOLVED |
| AC-11 (live smoke: bypass-dialog-free bash, headless lead runs) | Met (evidenced) | `docs/evidence/org-lead-smoke-2026-08-02.txt` lines 2-84 (Smoke A: `reviewer` seat runs a bash-backed task through a full protocol round trip after the one-time per-machine bypass dialog is accepted; no further dialog appears), lines 85-113 (Smoke B: `ralph org start` spawns a `lead` seat that reaches `interactive_ready` and executes) |
| AC-11b (Lead minimal E2E: spawn → send → status → disband) | Met (evidenced) | `docs/evidence/org-lead-smoke-2026-08-02.txt` lines 88-157: lead transcript shows dry-run spawn of `sample`/`qa`, a send attempt, `status` (roster line for `sample`), `report` generation (`docs/reports/org-manifest-leadsmoke-b-2026-08-02.md`), `SMOKE-B-DONE`, then operator-steered stop/disband; cleanup lines 150-159 show `stopped seat "lead"`, agmsg teams removed, "(end)" with no residue reported |
| AC-12 (`go test ./...` / `run-verify.sh` green; tech-debt rows RESOLVED) | Partially verified here | Static half confirmed: `./scripts/run-static-verify.sh` exits 0 (gofmt ok, `go vet` silent, `golangci-lint run` → "0 issues.", `staticcheck` present on PATH and silent — see Static analysis); all 6 named tech-debt rows in `docs/tech-debt/README.md` are struck through and marked `RESOLVED 2026-08-02 in feat/org-runtime-lead`. The `go test ./...` half of this AC is `/test`'s responsibility, not re-run here per the verify/test division of labor. |

## Self-review fix-commit crosscheck (`9a22942`)

Walked the self-review's findings table (`docs/reports/self-review-2026-08-02-org-runtime-lead.md`) row-by-row against `git show 9a22942`:

| Finding | Fixed in `9a22942`? | Evidence |
| --- | --- | --- |
| HIGH-1 (`wait` unbounded on herdr `done` state) | Yes | `internal/cli/org.go` `newOrgWaitCmd`: `--until` default changed `"idle"` → `"idle,done"`, `--timeout-ms` default `0` → `60000` (bounded), plus a `Long` doc string explaining why; `.claude/skills/org/SKILL.md` and `internal/org/prompts/lead.md` examples updated to match |
| MEDIUM-2 (`org start` examples omit `--scope`, contradicting AC-2b) | Yes | `.claude/skills/org/SKILL.md`: both `start` examples (verb table + headless-lead section) now include `--scope "..."` |
| MEDIUM-3 (`compensateStale`'s driver round trip ran inside `withManifestLock`, contradicting the lock's own doc comment) | Yes | `internal/org/spawn.go`: two-phase lock — Phase 1 detects a stale in-flight seat and releases the lock without compensating; `compensateStale` runs lock-free; Phase 2 re-acquires the lock for a fresh read + capacity check + `spawn_started` append (`checkCapacityAndStart` extracted as a shared closure) |
| MEDIUM-4 (permission-mode enum re-spelled as bare literals, 4+ sites) | Yes | `internal/org/permissions.go:9-14` (`PermissionModeAutonomous/Edits/Guarded` constants), used at `permissions.go`'s switch and `spawn.go:281`'s gate; `internal/config/config.go` keeps its own literals with a comment pointing back at the canonical constants (documented as unavoidable — `internal/config` cannot import `internal/org`, which already imports `internal/config`) |
| MEDIUM-5 (shipped skill/lead.md reference `internal/org/...` paths invalid downstream) | Yes | `.claude/skills/org/SKILL.md` and `internal/org/prompts/lead.md`: all three `internal/org/prompts/` / `internal/org/protocol` references reworded to "the role prompt templates embedded in the `ralph` binary" / "the `ralph` CLI validates this at runtime", matching the precedent already set in `.claude/rules/agent-messaging.md` |
| LOW (AC-2b rejection bypassed `reject()`, no audit trail) | Yes | `internal/org/spawn.go:275-290`: gate now calls `o.reject(p, ...)`; comment explains the audit-trail improvement and confirms fail-closed semantics are unchanged (see AC-2b row above) |
| LOW (`newOrgStartCmd` double `resolveOrgConfig` read) | Yes | `internal/cli/org.go`: `newOrgRuntime` now returns `(*org.Org, config.OrgConfig, error)`; `newOrgStartCmd` uses the returned config instead of a second `resolveOrgConfig` call; all other `newOrgRuntime` call sites updated to discard the second return value |
| LOW (evidence file unredacted paths/UUID) | Deferred, as recommended | Self-review said "No action required this cycle"; still open, see `docs/tech-debt/README.md`'s standalone evidence-redaction row (unchanged, correctly not claimed as resolved by this commit) |
| LOW (`/org` skill `description:` is Japanese, all other skills English) | Not fixed, not previously required | `.claude/skills/org/SKILL.md:3` still Japanese. Self-review flagged this as LOW/optional and it was not in the "Merge: No-merge as-is" blocking list — this is a real, minor, still-open item, not a regression |
| LOW (plan `Status: Draft`, AC checkboxes unticked despite complete work) | Not fixed | See Documentation drift below — this is the same class of drift already known from prior verify cycles (plan AC checklists lag behind implementation) |

No fix-commit finding was silently dropped; the two "not fixed" rows above are both LOW, both explicitly optional/deferred in the self-review's own recommendation, and neither blocks AC-1..AC-12.

## Static analysis

| Command | Result | Notes |
| --- | --- | --- |
| `./scripts/run-static-verify.sh` (changed-language scope, golang pack selected) | PASS (exit 0) | `gofmt: ok`; `go vet ./...` silent (pass); `golangci-lint run ./...` → `0 issues.`; `staticcheck` present on PATH (`/Users/hiroki.yoshioka/go/bin/staticcheck`) and silent (pass, per this verifier's own convention: staticcheck emits no "0 issues" line on a clean run) |
| `./scripts/check-sync.sh` | PASS | `IDENTICAL: 179, DRIFTED: 0, ROOT_ONLY: 0, TEMPLATE_ONLY: 10, KNOWN_DIFF: 3` — the new `/org` skill's 4 mirrored copies (`.claude/`, `.agents/`, both `templates/base/`) are all counted in `IDENTICAL`; no new `DRIFTED` or `ROOT_ONLY` entries introduced by this branch |
| `./scripts/check-skill-sync.sh` | PASS | `[ok] check-skill-sync: 14 skill(s) in lock-step` (13 pre-existing + `org`) |
| `./scripts/check-pipeline-sync.sh` (run as part of `run-static-verify.sh`) | PASS | all 8 canonical-order reference sites still consistent — this branch does not touch the post-implementation pipeline order, so this is a non-regression confirmation |

## Documentation drift

| Doc / contract | In sync? | Notes |
| --- | --- | --- |
| `CLAUDE.md` (root) ↔ `templates/base/CLAUDE.md` | Yes | Both add `org` to the auto-invoked skill list in the same diff (`git diff main...HEAD -- CLAUDE.md templates/base/CLAUDE.md`), consistent with the meta-repo's manual-trigger-skills wording (root) vs. the scaffold's "no manual-trigger skill" wording (template) — pre-existing divergence in phrasing, not introduced here |
| `.claude/skills/org/SKILL.md` ↔ `.agents/skills/org/SKILL.md` ↔ `templates/base/.claude/skills/org/SKILL.md` ↔ `templates/base/.agents/skills/org/SKILL.md` | Yes | 4-way byte-identical (`check-skill-sync.sh` + `check-sync.sh` both green; self-review's `diff -q` sweep confirmed the same before the fix commit, and the fix commit's SKILL.md edits were applied identically to all 4 copies — confirmed via `git show 9a22942 --stat`, all 4 files changed with the same insertion/deletion counts) |
| `docs/tech-debt/README.md` | Yes, with one addition | 6 named rows (AC-6..AC-10, split a/b/c) struck through and marked `RESOLVED 2026-08-02 in feat/org-runtime-lead`; 1 new row added for the cwd-relative state-dir finding (Implementation notes #3), correctly *not* marked resolved (deferred to PR④); the pre-existing evidence-redaction row (from `org-runtime-seats`) remains open and unmodified — this branch's own new evidence file (`org-lead-smoke-2026-08-02.txt`) is not yet covered by that row's text but self-review LOW already flagged this as a known, deliberately-deferred gap |
| `docs/specs/2026-08-01-org-runtime.md` FR-2/FR-5/FR-9/FR-10 | Content matches; checkboxes stale | The spec's prose for FR-2 (`[org.permissions]`-shaped envelope extension), FR-5 (Lead current-session + headless dual path), FR-9 (manifest saga + `docs/reports/` artifact), FR-10 (`/org` skill + Codex mirror) all match what shipped in this branch. The spec's own `- [ ]` checkboxes for these four items are still unchecked — this is a spec-level (not plan-level) checklist that spans the whole PR①-⑤ series and is out of this PR's unilateral control to flip; flagged as observed, not blocking |
| Plan `docs/plans/active/2026-08-02-org-runtime-lead.md`: `Status: Draft`, AC-1..AC-12 all `[ ]`, Progress checklist mostly `[ ]` | Stale (known pattern) | The plan's own "Implementation notes (deviations)" section documents completed Slices 1-5 and both live smokes, contradicting the unticked AC/Progress checkboxes above it. This is the same recurring drift pattern flagged in prior verify cycles on this repo (plan AC checklists lag behind implementation) — not a spec-compliance failure, but a doc-drift item worth flipping before `/pr` archives the plan. Self-review already flagged this as its own LOW finding and it was not fixed in `9a22942`. |
| AC-2b wording ("fail-closed" with "no rejected event") vs. shipped behavior (fail-closed **with** a rejected event + receipt) | Diverges narrowly, non-blocking | The plan's AC-2b checkbox text only asserts fail-closed semantics, which still holds after `9a22942`; the *implementation-outline-level* commentary in the pre-fix code (and the self-review's own framing) described "no `rejected` manifest event" as part of the design — that description is now stale post-fix. The plan text itself was not edited to record this; flagged here so `/sync-docs` can note it. |

## Observational checks

- `docs/evidence/org-lead-smoke-2026-08-02.txt` is a committed, human-readable transcript (159 lines) of two live runs against a real herdr/agmsg install (not stubs): Smoke A shows the one-time per-machine `bypassPermissions` acceptance dialog, then a clean pane tail with **no further dialog** on a typed `TASK`/`RESULT` round trip after the `d35f157` idle/done fix; Smoke B shows a headless `lead` seat (sonnet driver) autonomously completing dry-run spawn → send attempt → status → `org report` generation → `SMOKE-B-DONE` in ~59s, then being steered by the operator to stop/disband, with a clean "(end)" and no reported herdr/agmsg residue.
- The generated sample report embedded in the evidence file (`# org report: leadsmoke-b`) matches `internal/org/report.go`'s documented output shape (Roster / Event timeline / Model receipts / Known residuals tables) — this is a real artifact, not a fixture.
- I did not re-run the live smoke myself (herdr/agmsg require a live pane environment this verify pass does not have); this is "evidenced by a committed transcript", not independently re-executed. Flag as the one item that would benefit most from a fresh live re-run if the operator wants stronger confidence before merge (see Coverage gaps).

## Coverage gaps

- `go test ./...` was intentionally not run here — that is `/test`'s scope. AC-12's test half remains unverified by this report.
- AC-2 / AC-2b / AC-9's argv- and concurrency-level unit tests (`internal/cli/org_test.go`, `internal/org/spawn_test.go`) were read for shape but not executed in this pass; static analysis (`go vet`, `golangci-lint`, `staticcheck`) confirms the code compiles and lints clean, which is a weaker guarantee than the tests actually passing.
- AC-11/AC-11b are evidenced by a single committed transcript from one live run on one operator's machine; no second independent live run exists in this branch to cross-check reproducibility (e.g. a flaky herdr timing edge, as already surfaced once mid-smoke: `org: send: ... wait for seat "reviewer" idle: herdr: timed out` before the `d35f157` fix landed).
- The plan's own AC/Progress checkboxes and `Status: Draft` were not updated by this verify pass (out of scope for `/verify`, which observes rather than edits plan state) — left for `/sync-docs` or the PR step to reconcile.

## Verdict

- **PASS** — spec compliance: AC-1 through AC-11b confirmed met with code + evidence; AC-12's static half green (`run-static-verify.sh` exit 0), test half deferred to `/test`. All self-review HIGH/MEDIUM findings from `docs/reports/self-review-2026-08-02-org-runtime-lead.md` are fixed in commit `9a22942`; the two deferred LOW findings (evidence redaction, Japanese `description:`) were explicitly optional and remain correctly tracked/open. `check-sync.sh` and `check-skill-sync.sh` both green with zero drift.
- Verified: AC-1, AC-2, AC-2b, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9, AC-10, static half of AC-12; all 7 fixed self-review findings in `9a22942`; skill-mirror and tech-debt-row documentation drift.
- Partially verified: AC-11 / AC-11b (evidenced by a single committed live-smoke transcript, not independently re-executed in this pass); AC-12 (test half belongs to `/test`).
- Not verified (out of scope for `/verify`): behavioral test suite results (`/test`'s responsibility); a second independent live-smoke run.
- Documentation drift found (non-blocking): plan `Status`/AC checkboxes/Progress checklist stale vs. completed work; spec FR-2/5/9/10 checkboxes stale (spans the whole PR①-⑤ series); AC-2b's "no rejected event" framing is stale post-fix; the new evidence file is not yet covered by the existing evidence-redaction tech-debt row's literal text (though its intent clearly applies).
