# Self-review report: org-runtime-seats (PR② seat activation)

- Date: 2026-08-02
- Plan: `docs/plans/active/2026-08-02-org-runtime-seats.md`
- Reviewer: `reviewer` subagent (Claude Code, `/self-review`)
- Scope: diff quality only for `git diff ffda48f...544bddb` (17 commits, 34 files, +4102/-212). No spec-compliance verdict, no static analysis, no test runs, no documentation-drift audit — those belong to `/verify` and `/test`.

## Evidence reviewed

- Full diff: `git diff ffda48f...HEAD` (all 34 files); targeted reads of `internal/org/spawn.go`, `internal/org/verbs.go`, `internal/org/prompts.go`, `internal/org/protocol/protocol.go`, `internal/org/driver/{agmsg,herdr,driver}.go`, `internal/cli/{org,doctor}.go`, `internal/config/config.go`, `scripts/ralph-config.sh`, `.claude/rules/agent-messaging.md`, `internal/org/prompts/{reviewer,qa}.md`.
- Plan "Implementation notes (deviations)" — the 4 live-smoke-driven fixes (herdr JSON envelope, prompt-file handoff, `agent_pane_busy` retry, `leave.sh` instead of `despawn.sh`) were read as intentional design and are **not** reported as findings.
- Symbol greps: `ensureLeadJoined`, `agmsgTeam`, `"lead"`, `PlanPath`, `RALPH_ORG_AGMSG_HOME`, `method=C-c`.
- Mirror check: `cmp .claude/rules/agent-messaging.md templates/base/.claude/rules/agent-messaging.md` → identical (same blob `cd85a49`), both mode 100644.
- Runtime probes (read-only, no test suite): `sh -c '. ./scripts/ralph-config.sh; printenv RALPH_ORG_AGMSG_HOME'` → `~/.agents/skills/agmsg`; `posixpath.normpath` reproduction of `promptFilePath`'s `filepath.Join` behaviour.
- Debug-code sweep over the test diff (`t.Skip` / `fmt.Print` / `TODO` / `FIXME`): **no hits**.
- Secret sweep: no credentials, tokens, or keys introduced anywhere in the diff.

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| MEDIUM | maintainability | `ralph-config.sh` **unconditionally exports** `RALPH_ORG_AGMSG_HOME` with the default value, which inverts the precedence this PR documents (`env > [org].agmsg_home > default`). Any `ralph org …` process launched from a shell that sourced `ralph-config.sh` sees the env var set and therefore **never reads `[org].agmsg_home` from `ralph.toml`** — a user-configured agmsg home is silently dead in that context. The diff's own comment acknowledges this var is "read directly by the Go binary" yet still adds it to the `export` line, unlike the other (inert) `RALPH_ORG_*` vars. Latent today (no shell script invokes `ralph org` yet); becomes live in PR③. | `scripts/ralph-config.sh:83,95` + `templates/base/scripts/ralph-config.sh:83,95`; `internal/org/driver/agmsg.go` `ResolveAgmsgHome` (env checked first); probe: `env -u RALPH_ORG_AGMSG_HOME sh -c '. ./scripts/ralph-config.sh; printenv RALPH_ORG_AGMSG_HOME'` → `~/.agents/skills/agmsg` | Drop `RALPH_ORG_AGMSG_HOME` from the `export` list (keep the `VAR="${VAR:-…}"` assignment line so `defaults_sync_test.go`, which parses the file text via regex rather than the environment, stays green — see `internal/config/defaults_sync_test.go:63` `parseShellDefaults`). An operator who genuinely wants the override still exports it themselves. |
| MEDIUM | security | Unvalidated `--id` / `--org-id` now reach the filesystem. `promptFilePath` builds `filepath.Join(stateDir, "prompts", fmt.Sprintf("%s-%s.md", orgID, seatID))` and `writePromptFile` does `MkdirAll` + `WriteFile(0644)`. The CLI only checks these flags are non-blank, and `ValidateSpawnEnvelope` validates driver/model/role — never the ids. `--id '../../../../../../../../tmp/pwn'` normalizes out of the state dir (reproduced: `join('/Users/x/.harness/state/org','prompts','myorg-../../../../../../tmp/pwn.md')` → `/Users/x/tmp/pwn.md`). Operator-supplied today, but PR③ makes seat ids **Lead-LLM-supplied**, and the spec's own threat model requires the mechanism layer to reject over-reach without LLM judgement. This is the first code path in `internal/org` that turns an id into a path. | `internal/org/spawn.go:540-546` (`promptFilePath`) and `:553-561` (`writePromptFile`); `internal/cli/org.go:133-137` (blank-check only); `internal/org/envelope.go:51-62`; `docs/specs/2026-08-01-org-runtime.md` "Security considerations" | Add an id shape check to `ValidateSpawnEnvelope` (e.g. `^[A-Za-z0-9][A-Za-z0-9._-]*$` for `OrgID`/`SeatID`) so it runs before any side effect, or at minimum reject ids containing `/`, `\`, or `..` inside `promptFilePath`. A single regexp + one table test closes it. |
| MEDIUM | naming | `ensureLeadJoined` is referenced as a named symbol in three code comments **and in the shipped rule doc**, but no such function exists — the lead-join is 13 lines inlined in `Spawn`. A reader (or a future agent) grepping `ensureLeadJoined` finds only prose. This is exactly what `.claude/rules/architecture.md` ("prefer grep-able names", "avoid opaque helper layers that hide control flow from search-based code reading") targets, and PR① already established the counter-pattern with `herdrAgentName` / `agmsgTeam` as single grep-able definitions. | `internal/org/spawn.go:70,366,710`; `.claude/rules/agent-messaging.md:20` + template mirror; grep shows zero `func ensureLeadJoined` | Extract the block at `spawn.go:366-386` into `func (o *Org) ensureLeadJoined(ctx, team, cwd) (note string, err error)` returning the `leadJoinNote`, so the name the docs promise actually resolves. (Cheaper alternative: reword all five references — but the extraction also shortens `Spawn`, which is now ~170 lines.) |
| MEDIUM | maintainability | The scaffolded copy `templates/base/.claude/rules/agent-messaging.md` ships **7 references to `internal/org/protocol…`** to downstream projects, where no such package exists (`ralph init` scaffolds a project, not the ralph source tree). It is the only shipped rule that references `internal/`. The established convention is the opposite: `.claude/rules/model-routing.md` is deliberately registered in `check-sync.sh`'s `KNOWN_DIFFS` with the comment *"root documents the Go-layer default (internal/config) which does not exist in scaffolded projects"*, and the shipped copy has those references stripped. | `templates/base/.claude/rules/agent-messaging.md:3,5,45,46,61,72,93`; `scripts/check-sync.sh:88-96` (`KNOWN_DIFFS` + rationale comment); `grep -ln "internal/" templates/base/.claude/rules/*.md` → only `agent-messaging.md` | Either (a) add `.claude/rules/agent-messaging.md` to `KNOWN_DIFFS` and strip/soften the `internal/org/protocol` pointers in the template copy (matching the model-routing.md precedent), or (b) add one caveat line to both copies: "`internal/org/protocol` lives in the ralph source repo, not in scaffolded projects." |
| MEDIUM | null-safety | `RolePromptVars.PlanPath` is **never populated by any production caller** — both `Spawn` and `dryRunSpawn` construct `RolePromptVars{OrgID, SeatID, Team, Role, Scope}` with no `PlanPath`, and `org spawn` has no `--plan` flag. Every rendered prompt therefore ships a literal `- plan: ` with nothing after it. `{{SCOPE}}` has the same shape when `--scope` is omitted (it is optional): the seat is told *"scope:  の範囲外は読むだけに留め…"* — an instruction that references an empty scope, which is worse than omitting the line. | `internal/org/prompts.go:28,52`; `internal/org/spawn.go:314-316,610-612` (both call sites omit `PlanPath`); `internal/org/prompts/reviewer.md:5,52` and `qa.md:5,49` | Either wire `--plan` / `--scope` through to the template, or make `RenderRolePrompt` substitute an explicit placeholder for empty vars (e.g. `(未指定)`) so the rendered instruction reads coherently. At minimum drop `PlanPath` from `RolePromptVars` until a caller supplies it, rather than shipping a field no path sets. |
| LOW | naming | `agmsgTeam := seat.AgmsgTeam` shadows the package-level function `func agmsgTeam(orgID string) string`. It compiles only because `Stop` happens not to call the function; a later edit that needs `agmsgTeam(p.OrgID)` inside `Stop` (e.g. a fallback when the roster row has no recorded team) hits a confusing type error. | `internal/org/verbs.go:232,248,250,262` vs `internal/org/spawn.go:446` | Rename the local to `team`, matching the name `Spawn` already uses for the same value (`spawn.go:307`). |
| LOW | maintainability | The `"lead"` identity is a bare string literal at two executable sites, while its two sibling conventions got single grep-able definitions in PR① (`herdrAgentName`, `agmsgTeam`). Relatedly, lead's agmsg type is hardcoded `agmsgTypeForDriver("claude")` → `claude-code`, so an operator running Codex as the coordinating identity registers a mistyped roster entry. | `internal/org/spawn.go:378` (`Join(ctx, team, "lead", agmsgTypeForDriver("claude"), …)`), `:404` (`Send(…, "lead", msg)`); `.claude/rules/agent-messaging.md` "Star topology" | Add `const leadIdentity = "lead"` next to `agmsgTeam`. The lead driver type is a real (if small) gap — worth either a `TODO`-free comment stating the assumption or threading the primary CLI through. |
| LOW | exception-handling | Retry accounting is lost exactly where it matters. `agentStartWithRetry` returns the retry count only on the success path (`agent_started agent_start_retries=N`); on ctx-deadline or exhaustion the count is discarded and the `spawn_failed` Details carries only the last `agent_pane_busy` error, with no indication that 20 attempts were made or that the saga deadline (not herdr) ended it. Separately, the exhaustion return is off by one: it returns `maxAgentStartAttempts` (20) though only 19 retries followed the first attempt. | `internal/org/spawn.go:477-497` (loop), `:356-358` (success-only annotation), `:714-728` (`failStepWithNote` — already exists and is not used at the `agent_start` failure site) | Pass the retry count (and a `deadline=true` marker when `ctx.Done()` fired) into the existing `failStepWithNote` at the `agent_start` failure site; return `attempt` rather than `maxAgentStartAttempts` on exhaustion. |
| LOW | readability | `checkAgmsgAvailable` discards the one piece of information an operator needs when agmsg is misconfigured: `driver.AgmsgAvailable` formats `"(expected <home>/scripts/send.sh)"` into its error, and doctor replaces it with a fixed string that never names the resolved home. With `agmsg_home` now configurable from three surfaces, "which directory did it look in?" is the whole question. | `internal/cli/doctor.go:625-631`; `internal/org/driver/driver.go:82-86` | Include the resolved home in the info detail, e.g. `"agmsg not installed at <home> — org runtime seats unavailable (solo execution unaffected)"`. |
| LOW | maintainability | The shipped role template embeds a ralph-branch-specific example: `EVIDENCE: internal/org/spawn.go:236 (commit 71511ea)`. Rendered into a downstream seat's prompt, that commit does not exist and the line number is meaningless — it reads as real provenance rather than illustration. `qa.md` has a milder version (`docs/reports/verify-2026-08-02-org-runtime-seats.md`). | `internal/org/prompts/reviewer.md:45`, `internal/org/prompts/qa.md:42` | Use obviously-synthetic placeholders (`internal/foo/bar.go:42 (commit abc1234)`), as the `TASK_ID: t-42` in the same block already does. |
| LOW | exception-handling | `dryRunSpawn` silently swallows two errors that the real path treats as fatal: `RenderRolePrompt`'s error (`if rendered, ok, err := …; err == nil && ok`) and `promptFilePath`'s (`if promptPath, perr := …; perr == nil`). A dry run therefore reports a clean trail for a spawn that a real run would `failStep` at `agent_start`/`prompt_file`, weakening dry-run's purpose as a pre-flight check. | `internal/org/spawn.go:610-624` vs the fatal handling at `:314-334` | Mirror the real path: on either error, surface it (a `dry_run` `spawn_failed` step, or at minimum `agent_started prompt_file=<error>`) instead of falling through to the inline-prompt branch. |
| LOW | readability | `protocol.go` uses `sort.Strings` while this same branch spent two dedicated refactor commits modernising to `slices.Contains` (`71511ea`) and range-over-int (`c5b8d6c`). Minor inconsistency in brand-new code. | `internal/org/protocol/protocol.go:18,187` | `slices.Sort(names)` and drop the `sort` import. |
| LOW | security | The committed smoke evidence embeds the developer's home path (~14 occurrences of `/Users/hiroki.yoshioka/…`) and a herdr/claude agent session UUID (`b982c05c-…`) plus terminal ids. Not credentials, and there is precedent for absolute paths in committed docs (`docs/reports/test-2026-07-11-standard-flow-orchestrator.md`), but `docs/evidence/` is a new artifact class and this is the moment to set a redaction convention. | `docs/evidence/org-seats-smoke-2026-08-02.txt:4,5,19,28-29,54,66-67,81-82,96` | Optional: sed the home prefix to `<repo>` when saving evidence, and note the convention where `docs/evidence/` is introduced. |

## Positive notes

- **The four live-smoke deviations are implemented with unusual discipline.** Each one carries a doc comment that states the observed real-CLI behaviour, the version it was observed on, and a pointer back to the plan bullet — e.g. `AgmsgClient.Leave`'s comment explains *why* `despawn.sh` is a no-op for `join.sh`'d members rather than just asserting that `Leave` is correct. That is exactly the "evidence beats confidence" contract in AGENTS.md.
- **`parseHerdrEnvelope` fails closed in the right direction.** Non-JSON → fall back to trimmed raw (keeps `pane read` and test fakes working); `{`-prefixed but malformed → hard error rather than fallback, explicitly so a truncated envelope "never gets mistaken for a bare ID and fed downstream as e.g. a workspace_id". That is the precise failure mode the smoke test hit, and it is now structurally impossible.
- **AC-10's phantom-seat fix is in the right place.** `Stop` resolves the seat from the roster *before* the dry-run branch, so `--dry-run` cannot fabricate a `stopped` event either — a subtler variant that would have been easy to leave open.
- **`Send` fails closed and validates before the manifest append**, including in dry-run, and `--raw` bypasses are recorded as `raw=true` in Details rather than being invisible.
- **Test naming is exemplary** — `TestOrgStop_UnknownSeat_DryRun_AlsoErrorsWithoutAppendingEvent`, `TestOrgSpawn_AgentStart_NonBusyError_FailsImmediatelyWithoutRetry`, `TestValidate_BodySizeCap_CountsRunesNotBytes` all explain intent, not mechanics (`.claude/rules/testing.md`). No `t.Skip`, no debug prints, no commented-out code anywhere in the diff.
- **Tech-debt hygiene:** both closed rows are struck through *and* preserved with a `RESOLVED` HTML comment naming the commit and the concrete evidence, matching the convention PR① established.
- **Mirror discipline held:** `.claude/rules/agent-messaging.md` and its `templates/base/` copy are byte-identical (same git blob) with matching modes; the three-surface `agmsg_home` lock-step is covered by the existing `defaults_sync_test.go` tripwire.

## Tech debt identified

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| _(none newly deferred by this review)_ | — | — | — | — |

All findings above are fixable inside this branch. If the MEDIUM path-traversal finding (`--id`/`--org-id` id-shape validation) is deferred rather than fixed here, it must be added to `docs/tech-debt/README.md` with **PR③ (Lead 自律編成, where seat ids become LLM-supplied)** as the trigger — that is the point where it stops being an operator-shoots-own-foot issue.

## Recommendation

- Merge: **Yes, with fixes** — no CRITICAL findings; nothing blocks the pipeline from proceeding to `/verify`. Recommend fixing the five MEDIUM findings before `/pr`; each is a small, local change:
  1. drop `RALPH_ORG_AGMSG_HOME` from the `export` line (2 files) — restores the documented config precedence;
  2. validate `OrgID`/`SeatID` shape in `ValidateSpawnEnvelope` — closes the prompt-file traversal;
  3. extract `ensureLeadJoined` so the name five docs promise actually resolves;
  4. resolve the `internal/org/protocol` references in the scaffolded rule copy (KNOWN_DIFFS entry or caveat line);
  5. wire or remove `PlanPath`, and give empty `{{SCOPE}}`/`{{PLAN_PATH}}` a coherent rendering.
- Follow-ups (LOW, safe to batch or defer): `agmsgTeam` local shadowing; `leadIdentity` constant + lead driver-type assumption; retry-count/deadline reporting on the `agent_start` failure path; doctor detail naming the resolved home; synthetic example evidence in `reviewer.md`/`qa.md`; dry-run error swallowing; `slices.Sort`; evidence-file redaction convention.
