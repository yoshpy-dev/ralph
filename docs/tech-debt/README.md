# Tech debt

Record debt that should not disappear into chat history.

Recommended fields:
- debt item
- impact
- why it was deferred
- trigger for paying it down
- related plan or report

## Entries

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 4): Ralph Loop's per-slice pipeline (`ralph-pipeline.sh`) was fully removed (Slice 2). The standard `/work` flow's CRITICAL-findings-block behavior is now the only post-implementation pipeline; there is no longer a second behavior to diverge from. Row preserved for traceability. -->
| ~~Per-slice pipelines do NOT stop on CRITICAL self-review findings~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | Differs from standard `/work` flow behavior | Autonomous pipelines benefit from letting verify/test confirm true positives before halting | If false-negative CRITICAL findings slip through to merge | `.claude/rules/post-implementation-pipeline.md` |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop: `ralph status --json` now emits a real JSON payload via `json.Encoder` (internal/cli/status.go's printStatusJSONAllOrgs/printStatusEmpty, unified into one `statusJSON` schema). AC11's machine-consumption gap no longer applies. Row preserved for traceability. -->
| ~~`ralph status --json` outputs plain text, not JSON~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | AC11 partially met; machine consumption broken | Phase 6a scope — table/JSON rendering deferred to Phase 6b Go native | Phase 6b implementation | `docs/plans/archive/2026-04-16-ralph-cli-tool.md` |
| `ralph init/upgrade` lack transactional safety (journal.toml, staging) | AC17/18/21 not met; interrupted init can leave inconsistent state | Development cost vs. shipping core CLI functionality first | Before v1.0 release or after first user report of interrupted init corruption | `docs/plans/archive/2026-04-16-ralph-cli-tool.md` |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop: superseded, not completed as originally scoped. The Ralph Loop shell pipeline (`ralph-orchestrator.sh`, `ralph-pipeline.sh`, `ralph-cli-driver.sh`) that this row's "Go native pipeline migration" targeted was removed outright rather than ported to Go -- autonomous multi-seat execution is now the org runtime (`internal/org/`, `ralph org spawn/send/wait/...`), a from-scratch Go mechanism layer, not a parity port of the shell Loop. The row's premise (shell pipeline survives pending a Go rewrite) no longer holds. Row preserved for traceability. -->
| ~~Phase 6b: Go native pipeline migration pending~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop -- superseded by org runtime, not completed as a parity port) | Pipeline runs via shell wrapper (Phase 6a) | 3400 lines of shell → Go is a major effort requiring parity tests | Next PR; parity tests must pass before shell scripts are removed | `docs/plans/archive/2026-04-16-ralph-cli-tool.md` | Bridge hardening landed 2026-07-13 in `refactor/core-hardening` (PR #128): `set -euo pipefail` in orchestrator/pipeline/ralph-CLI, source guards for testability, `ckpt_update` and parser function tests, `parse_slices` pipe-safe US-delimiter records. Shell core is now safe until Go migration. |
| `filePerm()` in upgrade.go duplicates permission logic from render.go with different strategy (name-based vs FS-metadata-based) | A new extensionless executable in templates needs updates in two places with different fix patterns | Scope of fix-template-distribution-gaps PR is distribution gaps, not refactoring permissions | Adding a second extensionless executable to templates | `docs/reports/self-review-2026-04-16-fix-template-distribution-gaps.md` (GC'd by 30-day retention 2026-07-13; recover via git history) |
| `.claude/hooks/check_mojibake.sh` + `mojibake-allowlist` + `tests/test-check-mojibake.sh` + `tests/fixtures/payloads/` + settings.json entry + AGENTS.md note + check-sync.sh exclusions | Every `Edit\|Write\|MultiEdit` in every session runs the scan; carrying a mitigation for a fixed upstream bug becomes dead weight | Temporary mitigation for upstream Claude Code Issue #43746 (U+FFFD injection at SSE chunk boundaries); permanent fix must land upstream | Upstream Issue #43746 closes in a released Claude Code version AND no local recurrences observed for 1 week | `docs/plans/archive/2026-04-17-mojibake-postedit-guard.md`, `docs/reports/self-review-mojibake-postedit-guard.md` |
<!-- RESOLVED-by-removal 2026-08-18 in feat/overlay-scaffold-v2-p3: this row's premise (an interactive `skip` prompt pinning entries `Managed=false`, and a `--force` flag that tree-wide re-adopts them) no longer exists — Phase 3 removed the interactive upgrade engine and `--force` (FR-3) entirely in favor of a fully non-interactive v2 engine (fork/drift/preserve classification, no per-entry prompting). The underlying need (a manual re-adoption path for a fork/drift entry, narrower than a full re-scaffold) resurfaces as Phase 5's planned `adopt` command (plan `docs/plans/active/2026-08-18-overlay-scaffold-v2-p3.md`, Non-goals: "`eject` / `adopt` ... (Phase 5)"; until then, manual recovery is "make disk match the new template, then let refresh converge"). Row preserved for traceability. -->
| ~~`ralph upgrade --resync <path>` / `--adopt` per-path escape hatch for `Managed=false` entries~~ (RESOLVED-by-removal 2026-08-18 in feat/overlay-scaffold-v2-p3 — mechanism replaced by Phase 5's planned `adopt` command) | Once a user selects `skip` on a conflict, the entry is pinned unmanaged and silently skipped on subsequent runs. Tree-wide re-adoption is available via `ralph upgrade --force` (which now re-adopts all `Managed=false` entries back to `{Hash: newHash, Managed: true}` — see `docs/specs/2026-04-16-ralph-cli-tool.md` §冪等性と自動修復), but there is no per-path or per-glob escape hatch short of hand-editing `.ralph/manifest.toml`. `--force` is all-or-nothing and also overwrites any other legitimately-conflicting files, which is often heavier than users want. | Out of scope for the "detect local edits + unified diff" PR — shipping the convergence contract first was the priority so users stop being prompt-stormed. The per-path escape hatch needs UX design (per-path vs. glob, force-template-overwrite vs. keep-local-but-track) | ~~First user report of being stuck on an unmanaged entry they want back under management without the `--force` blast radius, OR before v1.0 release~~ — superseded by Phase 5's `adopt` command design | `docs/plans/archive/2026-04-22-upgrade-detect-local-edits.md` (Open Questions), `docs/reports/self-review-2026-04-22-upgrade-detect-local-edits.md` (GC'd by 30-day retention 2026-07-13; recover via git history), `docs/plans/active/2026-08-18-overlay-scaffold-v2-p3.md` (Non-goals) |
| `runUpgradeIO` positional-parameter creep (now 6 params; trailing `bool colorize`) | Callers must remember argument order; risk grows linearly with each new display flag. Trailing `bool` at call sites (`..., true)` vs `..., false)`) is a known anti-pattern. | Out of scope for the colorize-upgrade-diff UX change; refactor would touch every test in `internal/cli/cli_test.go` for no current behavior gain. | Next time another display/IO knob lands (theme, paging, width override) — collapse into an `upgradeIOOpts` struct then. | `docs/plans/archive/2026-04-23-colorize-upgrade-diff.md`, `docs/reports/self-review-2026-04-23-colorize-upgrade-diff.md` (GC'd by 30-day retention 2026-07-13; recover via git history) |
| ~~`ralph-pipeline.sh` Outer Loop still hard-codes `codex exec review` and `_codex_*` variables / log messages despite the cross-review skill body being bidirectional~~ (RESOLVED 2026-05-08 in feat/44/ralph-loop-codex-driver: dispatcher routes through `pick_reviewer`, variables renamed to `_xreview_log`/`_reviewer`, log line is "Running cross-review (driver=…, reviewer=…)…") | Codex-driven Loop runs will recursively invoke `codex exec review` instead of `claude -p`; operator log output ("Running codex review...") is misleading after the rename | Loop driver work is out of scope for the codex-cli-parity PR per Non-goals; tracked in yoshpy-dev/ralph#44 | When #44 lands, or first time `/loop` is run under Codex and the wrong reviewer is invoked | `docs/reports/self-review-2026-05-07-codex-cli-parity.md` (GC'd by 30-day retention 2026-07-13; recover via git history), `docs/plans/archive/2026-05-07-codex-cli-parity.md` (Non-goals), yoshpy-dev/ralph#44 |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 2): `scripts/ralph-pipeline.sh` (the only caller of this Loop-driver preflight probe) was removed outright as part of the Ralph Loop retirement. Row preserved for traceability. -->
| ~~Preflight Probe 1 / Probe 7 detect Loop-driver CLIs via `command -v` only (regressed from a `--version` round-trip via `scripts/codex-check.sh`)~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | A broken-but-PATH'd `claude`/`codex` binary now passes preflight and fails opaquely at first agent turn instead of producing an early, named probe failure | Phase 2 self-review flagged this as MEDIUM and recommended restoring the `--version` shape; deferred because the broken-binary case is rare on developer machines and the simpler probe shape removed the special-case path through `scripts/codex-check.sh` | First operator report of "preflight passed but the first slice failed with a cryptic error from claude/codex" | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (MEDIUM #1), `scripts/ralph-pipeline.sh:253, 391-401` (removed) |
| `pick_reviewer` (moved 2026-08-03 in refactor/org-runtime-retire-loop from `scripts/ralph-cli-driver.sh` to `scripts/xreview-helpers.sh:66-73`) third-arm fallback silently coerces an unknown driver to `codex` reviewer | Ralph Loop's own `validate_loop_driver` call site (which used to run before every `pick_reviewer` call) was removed along with the rest of `ralph-cli-driver.sh`; the only surviving caller is `/cross-review`'s auto-detect (step 2 of `.claude/skills/cross-review/SKILL.md`), which only ever passes `claude`/`codex`/unset, so the fallback stays unreachable today, but the removed upstream validation makes the risk marginally more real if a future caller passes an arbitrary value | Phase 2 self-review flagged this as MEDIUM and recommended either dropping the arm or making it `return 1`; deferred because there is no current bypass path and the comment documents the intent | Adding a second `pick_reviewer` call site that does not validate its driver argument first | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (MEDIUM #4), `scripts/xreview-helpers.sh:66-73` |
| `count_triage_findings`'s awk fallback (moved 2026-08-03 in refactor/org-runtime-retire-loop from `scripts/ralph-cli-driver.sh` to `scripts/xreview-helpers.sh:86-`) excludes header / separator rows by matching `!/^\| *# /` and `!/^\| *-+/`, so it is coupled to the literal column header `#` shipped by `docs/reports/templates/cross-review-triage-report.md`. A future template author who renames column 1 from `#` to `No.` (or any other label) will silently start counting the header row as a real finding — off-by-one in every category — without any test failure | Cycle-2 self-review flagged this as LOW #2 and recommended skipping the first `^\|` row after each `## <CATEGORY>` heading unconditionally instead of substring-excluding `# `; declined as defensive over-engineering for cycle-2 since the template column is stable today | The next time the cross-review triage template is restructured (e.g. column rename, extra leading column, or template rewrite), or the next time the awk fallback is touched for any other reason — replace the substring exclusion with a row-index skip | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (LOW #2), `scripts/xreview-helpers.sh:86-`, `docs/reports/templates/cross-review-triage-report.md` |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 2): both `tests/test-ralph-cli-driver.sh` and `scripts/ralph-pipeline.sh` were removed outright as part of the Ralph Loop retirement; the surviving `xreview-helpers.sh` functions used by `/cross-review` (`detect_base_branch`/`pick_reviewer`/`count_triage_findings`) were carried over with their own tests. Row preserved for traceability. -->
| ~~Test 6b in `tests/test-ralph-cli-driver.sh` is a dead regression guard for the cross-review claude-reviewer dispatcher~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | Future flag changes in the dispatcher will not break Test 6b unless someone remembers to also update the test's hand-rolled `claude -p ...` line. Bugs of the same shape as cycle-2 cross-review HIGH P1 will silently slip past this regression guard. | Cycle-3 scope was the production fix (`plan` → `auto`) and the test-line modernization to match; refactoring Test 6b to source the dispatcher's actual command from `ralph-pipeline.sh` (or from a refactored `dispatch_cross_review` function extracted from the dispatcher block at lines 778-803) would have been an out-of-scope abstraction landing during a fix-and-revalidate cycle | Next time the cross-review dispatcher block is touched for any reason, OR before the next cross-review surface change (e.g. permission flag, output format, model knob) — extract `dispatch_cross_review` as a sourced bash function and have Test 6b call that function with fakes on PATH, so flag changes propagate automatically into the assertion | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (LOW #2 / cycle-3 finding), `tests/test-ralph-cli-driver.sh:206-214` (removed), `scripts/ralph-pipeline.sh:778-803` (removed) |
<!-- RESOLVED 2026-07-12 in fix/known-breakage: ReadPipelineCheckpoint now probes the raw JSON map; when cross_review_triage is absent (all zeros) and codex_review_triage is present, the legacy value is unmarshalled into CrossReviewTriage at read time. New key present → legacy ignored. File is never rewritten. Three new tests added: legacy-only fixture, both-keys fixture (new key wins), and existing-fixtures regression. Row preserved for traceability. -->
| ~~`internal/state/PipelineCheckpoint` has no migration path from the pre-rename `codex_review_triage` JSON key to `cross_review_triage`~~ (RESOLVED 2026-07-12 in fix/known-breakage) | Existing per-project `checkpoint.json` files become silently un-readable for the renamed field; resume drops triage counts to zero with no warning | Migration plumbing was not in the plan's Slice list; risk is small because checkpoints regenerate on the next pipeline run | ~~First user reports a stale resume losing triage state, or whenever `schema_version` is bumped for any other reason — handle migration in the same hop~~ | `docs/reports/self-review-2026-05-07-codex-cli-parity.md`, `internal/state/types.go` (`CrossReviewTriage`), `internal/state/reader.go` (`ReadPipelineCheckpoint`) |
| `ralph doctor`'s `checkHooks` only walks `.claude/settings.json` `hooks[*].command` paths; there is no equivalent integrity check for `.codex/config.toml` `[hooks.*] command = ["./..."]` paths | A typo or stale path in `.codex/config.toml` ships green through doctor today (the cycle-2 CRITICALs in this report would have been caught at doctor time if the same on-disk-existence walk applied to Codex hooks) | Cycle-2 fix was scoped to closing the doctor warning loop, not extending hook-integrity coverage to the Codex side | When a third Codex-side hook lands, or as part of fixing the cycle-2 CRITICAL hook-path bugs (whichever comes first) | `docs/reports/self-review-2026-05-07-codex-cli-parity.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Cycle 2 section), `internal/cli/doctor.go:225-294` (`checkHooks`) |
<!-- RESOLVED 2026-05-07 in commit 79d7a73: probeBinary now wraps the `<bin> --version` exec in a 5s context.WithTimeout, returns the first non-empty line of output, and surfaces both LookPath and run errors symmetrically. The hang scenario described below is no longer reachable. Row preserved for traceability. -->
| ~~`probeBinary` in `internal/cli/doctor.go` runs `<bin> --version` with no timeout via `exec.Command(...).CombinedOutput()`~~ (RESOLVED 79d7a73) | A hung Claude/Codex binary (waiting on stdin, slow npm shim re-installing on first run, blocked network call) wedges `ralph doctor` indefinitely with no signal to the operator | Cycle-2 was scoped to fixing the `LookPath`-only false-pass bug; the orthogonal hang case wasn't in the cross-review finding | ~~Next time a CI runner or sandboxed environment causes a slow CLI binary to wedge doctor, OR when the cycle-2 CRITICAL hook-path bugs are fixed and a follow-up commit touches `probeBinary` anyway~~ — fixed 79d7a73 | `docs/reports/self-review-2026-05-07-codex-cli-parity.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Cycle 2 section), `internal/cli/doctor.go:111-138` (`probeBinary`), commit `79d7a73` |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 2): all three duplicated copies (`scripts/ralph-pipeline.sh`, `tests/test-xreview-prompt-render.sh`, `tests/test-xreview-gate-regression.sh`) were the Ralph Loop `pipeline-outer.md` adversarial-prompt renderer and its tests; all were removed outright as part of the Ralph Loop retirement (the `/cross-review` skill's own reviewer-prompt construction is a separate, non-Loop code path). Row preserved for traceability. -->
| ~~Adversarial-prompt awk renderer (`lreplace` + ENVIRON-based literal substitution) is duplicated three places~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | If a future maintainer fixes a renderer bug on one side and not the others, the unit test would keep reporting green while the production renderer was broken. Today's risk is low because the renderer body is small and the drift guard is loud, but the pattern is fragile. | Out of scope for the issue #50 fix; extracting `render_adversarial_prompt` into `scripts/ralph-cli-driver.sh` next to `count_triage_findings` would cleanly de-duplicate but adds surface area unrelated to the silent-bypass fix. | Next time the renderer grows (new placeholder, new metacharacter edge case, or another caller besides the cross-review phase), pull the awk body out into a single sourced shell function. | `docs/reports/self-review-2026-05-13-fix-xreview-placeholder-substitution.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (MEDIUM #1), `scripts/ralph-pipeline.sh:808-826` (removed), `tests/test-xreview-prompt-render.sh` (removed), `tests/test-xreview-gate-regression.sh` (removed) |
<!-- RESOLVED 2026-07-12 in fix/known-breakage: renderPackInto helper extracted to language_pack.go; pack.go and init.go both use it, so they cannot diverge. Pack payload files render under packs/languages/<lang>/, rule.md maps to .claude/rules/<lang>.md, manifest keys namespaced, Meta.Packs updated. Three tests added: AC1/AC2 assertions, no-duplicate-packs, unknown-lang error. Row preserved for traceability. -->
| ~~`ralph pack add <lang>` writes pack files to the project root instead of `packs/languages/<lang>/`~~ (RESOLVED 2026-07-12 in fix/known-breakage) | `ralph pack add` is effectively broken for every language pack. Workaround: pack files are auto-distributed via `ralph init` with the right `packs:` array, so the `pack add` CLI path is mostly cosmetic today; users who hit it report confusion rather than blocking failures. | Out of scope for the Terraform language pack PR (Codex advisory HIGH#1; plan Non-goals and R6 explicitly defer this). The Terraform PR's acceptance only required `PackFS("terraform")` to resolve and `ralph pack list` to enumerate `terraform` — both verified. | ~~Fix `pack.go:64-67` to compute `filepath.Join(absDir, "packs", "languages", lang)` (matching `init.go:158`'s layout) and add an integration test that runs `ralph pack add <lang>` in a tempdir and asserts files land under `packs/languages/<lang>/`. File a follow-up issue at `/pr` time.~~ | `docs/plans/archive/2026-05-13-add-terraform-language-pack.md` (Non-goals, Risks R6), `docs/reports/self-review-2026-05-13-add-terraform-language-pack.md` (GC'd by 30-day retention 2026-07-13; recover via git history) ("Tech debt identified"), `internal/cli/pack.go`, `internal/cli/language_pack.go` (shared `renderPackInto`), `internal/cli/init.go` |
| `tfsec` is upstream-archived (announced by Aqua in 2024); `trivy config` is the recommended successor. `packs/languages/terraform/verify.sh:65-71` runs `tfsec` first and falls back to `trivy config` to preserve behavior for the existing tfsec install base. | Optional security check will eventually 404 or refuse new platforms as Aqua winds down hosting. Users with `tfsec` installed continue to work today, but the path is on borrowed time. The pack already falls back to `trivy config` when `tfsec` is absent, so the failure mode is graceful. | Ripping out the `tfsec` arm today would be churn for users who still rely on it; the dual-path keeps everyone working. Plan R2 explicitly chose this dual-arm strategy. | When `tfsec` install starts failing on a supported OS (macOS/Linux), OR `trivy config` ergonomics catch up enough that the fallback can become the default. At that point, drop the `tfsec` arm and keep only `trivy config`. | `docs/plans/archive/2026-05-13-add-terraform-language-pack.md` (R2), `docs/reports/self-review-2026-05-13-add-terraform-language-pack.md` (GC'd by 30-day retention 2026-07-13; recover via git history) ("Tech debt identified"), `packs/languages/terraform/verify.sh:65-71` |
| The hermetic-PATH coreutils symlink loop in `tests/test-terraform-pack-verify.sh:95-102` includes shell builtins (`printf`, `test`, `true`, `false`). Under `/bin/sh` these resolve via `command -v` to their bare builtin names (e.g. the literal string `"printf"`), so `ln -s "printf" "$coreutils_dir/printf"` creates a relative self-referential symlink that resolves to nothing. Four of the fifteen entries are broken symlinks on the current host. Tests pass anyway because `verify.sh` and the test stubs invoke these as builtins, not via PATH lookup. | Cosmetic noise only — no test depends on the broken symlinks resolving. Could mislead a future debugger who `ls -la`s the hermetic dir and assumes the fixture is corrupted. No behavior risk. | Cycle-2 self-review LR-1 flagged as LOW; tests pass; fixing is a five-minute follow-up not worth scoping into a fix-and-revalidate cycle. | Next time anyone touches that loop, OR if `ls -la` of the hermetic dir surfaces in a CI debug log and confuses a reader. Cleanest fix: drop `printf test true false` from the loop with a one-line comment that they are `/bin/sh` builtins; alternative is to guard `ln -s` on `case "$_resolved" in /*) ln -s ...;; esac`. | `docs/reports/self-review-2026-05-13-add-terraform-language-pack-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Finding LR-1), `tests/test-terraform-pack-verify.sh:95-102` |
| The IaC-tool leak-guard in `tests/test-terraform-pack-verify.sh:106-110` uses `[ -e "$coreutils_dir/$_banned" ]`, which is false for broken symlinks under POSIX `test`. So if a future maintainer added `terraform` to the coreutils symlink loop on a host where `terraform` is absent, `command -v terraform` would return empty, the per-tool `[ -z "$_resolved" ]` check would fire first ("FAIL: required coreutil 'terraform' not found"), and the leak-guard would never even run. The intended threat model — a host where terraform actually leaks is a host where terraform is installed, so the symlink would be non-broken — holds, but the guard is one layer thinner than it could be. | Defense-in-depth gap, not a live bug. The "non-broken symlink leak" case is fully covered today; the "broken symlink leak" case is caught a step earlier by a different check with a less helpful error. | Cycle-2 self-review LR-2 flagged as LOW; current behavior fails fast and loud, just via the wrong error path for the typo case. | Next time the leak-guard is touched, OR when a maintainer hits the "required coreutil 'terraform' not found" error and is confused about why a banned tool name landed in the coreutils loop. Fix: change `[ -e ... ]` to `[ -L ... ] \|\| [ -e ... ]` (true for any symlink including broken, OR a real file) so the leak-guard's specific error message wins over the generic coreutil-missing one. | `docs/reports/self-review-2026-05-13-add-terraform-language-pack-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Finding LR-2), `tests/test-terraform-pack-verify.sh:106-110` |
| Terraform ignore set in `.gitignore` / `templates/base/.gitignore` (block added by `03c5598`) diverges from GitHub's canonical `Terraform.gitignore` on four minor points: (a) `*.tfstate.*.backup` is narrower than canonical `*.tfstate.*` and misses workspace state (`terraform.tfstate.d/<workspace>/*.tfstate.backup`) plus `.terraform.tfstate.lock.info`; (b) only `*.auto.tfvars*` is ignored, not the broader `*.tfvars` / `*.tfvars.json` (defensible: explicit `-var-file=` fixtures often live in repo, but `prod.tfvars` with credentials is not protected); (c) `.terraformrc` and `terraform.rc` (Terraform Cloud token files) are not ignored — usually `$HOME`-scoped so risk is low; (d) the block-header comment "state and cache" understates scope (block also covers plan output, overrides, crash logs). | Low — current set covers the literal patterns the rule file (`terraform.md:11`) names and the most common state-leak vectors. Workspace state, project-local `.terraformrc`, and explicit-`-var-file` credential files are uncommon in scaffold-time setups. Comment wording is cosmetic. | Cycle-3 self-review surfaced all four as LOW maintainability findings (no CRITICAL/HIGH/MEDIUM); broadening to canonical alignment would have required scope expansion (`!example.tfvars` negation design, additional template-mirror commits) during a fix-and-revalidate cycle with the pipeline cap already raised to 3. Defensible-narrow choice matches the rules file. | Adoption of `terraform workspace` in any scaffolded project; first report of a contributor accidentally committing a project-local `.terraformrc` or a `prod.tfvars` with credentials; OR when a secret-scanning CI hook is added — broaden to canonical `*.tfstate.*` / `*.tfvars` with explicit `!fixture.tfvars` negation patterns, add `terraform.tfstate.d/` / `.terraform.tfstate.lock.info` / `.terraformrc` / `terraform.rc`, and consider retitling the comment to "Terraform / OpenTofu (state, plans, overrides, crash logs)". | `docs/reports/self-review-2026-05-13-add-terraform-language-pack-cycle3.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (all four LOW findings), `.gitignore:15-28`, `templates/base/.gitignore:15-28`, canonical reference `https://raw.githubusercontent.com/github/gitignore/main/Terraform.gitignore` |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 2/4): `scripts/ralph-cli-driver.sh`, `resolve_phase_model`, and the per-phase `RALPH_*_MODEL` env vars were removed outright as part of the Ralph Loop retirement; `.claude/rules/model-routing.md` no longer documents a per-phase routing table. The equivalent concern for org runtime is tracked separately: `internal/org/receipts.go`'s own `Honored` tri-state already models "commanded vs. reported" per seat, and the org spec is the place to record any future Codex-model-override gap for `ralph org spawn`. Row preserved for traceability. -->
| ~~Codex driver ignores per-phase model arg passed via `run_agent` 4th argument (`scripts/ralph-cli-driver.sh`)~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | Loop runs under `RALPH_LOOP_DRIVER=codex` silently use the Codex default model for every phase (implement, self-review, verify, test, sync-docs, pr), ignoring RALPH_SELF_REVIEW_MODEL=opus and RALPH_IMPLEMENT_MODEL. The quality-floor gate (verify/test/cross-review) and cycle-2 escalation receipts still record the gap, so auditing is unaffected — the honor=false fields prevent false audit evidence. | Codex CLI does not expose a stable per-invocation model flag (unlike `claude -p --model <alias>`). Fixing this requires either a Codex API change or routing through a wrapper that edits `.codex/config.toml` on the fly, which is fragile and not thread-safe under parallel slices. Recorded as non-fixable without upstream Codex API support (plan Design Decision 3, AC3b). | When Codex CLI adds a stable per-invocation model override flag (e.g. `codex exec -m <model>`), update `_run_agent_codex` in `scripts/ralph-cli-driver.sh` to pass the 4th arg through and flip `honored=true` in receipt writes. Also update `model-routing.md` Receipts section and this entry. | `docs/plans/archive/2026-07-11-loop-model-routing.md` (Non-goals, Design Decision 3, AC3b), `scripts/ralph-cli-driver.sh` (removed), `.claude/rules/model-routing.md` (Receipts section removed) |
<!-- RESOLVED 2026-07-12 in fix/unify-permission-mode-default: toml/Go Default() changed to bypassPermissions; run.go env-export changed from unconditional append to appendEnvIfMissing for MODEL/EFFORT/PERMISSION_MODE (env > toml); MAX_* use Flags().Changed() for CLI > env > toml priority. Row preserved for traceability. -->
| ~~`RALPH_PERMISSION_MODE` default divergence: shell default is `bypassPermissions` (`scripts/ralph-config.sh:20`); toml/Go default is `auto` (`templates/base/ralph.toml` `[pipeline] permission_mode`, `internal/config/config.go` `Default()`)~~ (RESOLVED 2026-07-12 in fix/unify-permission-mode-default: all three sources now default to `bypassPermissions`; env > toml priority enforced in `run.go`) | Same pipeline runs with different permission posture depending on entry point — direct shell invocation bypasses all permission prompts while `ralph run` uses auto-approval, which is more restrictive | Which default is correct is an operator/product decision (autonomous loop ergonomics vs. safety); resolving it is out of scope for a docs-drift fix | Next change to permission handling, or first operator surprise report of unexpected permission prompts (or unexpected bypass) depending on entry point | This audit (2026-07-12); `scripts/ralph-config.sh:20`, `templates/base/ralph.toml` (`[pipeline] permission_mode = "auto"`), `internal/config/config.go` `Default()` |
<!-- RESOLVED 2026-07-12 in fix/cli-stub-stdin-hang: both stubs now drain stdin only when `[ -f /dev/stdin ]` (regular file, EOF guaranteed); TTYs and open pipes fall through to the safe no-drain branch. Call-site `< /dev/null` added to test 6a. Elapsed-time regression test (Test 13) using FIFO + background sleep writer added to test-ralph-cli-driver.sh. Row preserved for traceability. -->
| ~~`tests/fixtures/cli-stubs/{codex,claude}` drain stdin via `head -c 4096` guarded only by `[ -t 0 ]` (skip iff TTY)~~ (RESOLVED 2026-07-12 in fix/cli-stub-stdin-hang) | When stdin is a non-TTY descriptor that never reaches EOF (an inherited open pipe from a background Claude Code Bash task), `head` blocks forever and the entire verify chain (`test-ralph-cli-driver.sh` → `verify.local.sh` → `run-verify.sh`) hangs at 0% CPU with no output — observed during PR #116 | Discovered post-PR-#114; root cause was the TTY-only guard not covering the open-pipe case; the fix is a one-line condition change per stub | ~~On recurrence of `run-verify.sh` hanging at 0% CPU with no output when run from a Claude Code background Bash task~~ — RESOLVED | `docs/plans/archive/2026-07-12-cli-stub-stdin-hang.md`, `tests/fixtures/cli-stubs/codex`, `tests/fixtures/cli-stubs/claude`, `tests/test-ralph-cli-driver.sh` (Test 13) |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 2): `scripts/ralph` (legacy shell CLI, deprecation-notice block) and `tests/test-ralph-deprecation-notice.sh` were removed outright as part of the Ralph Loop autonomous-execution retirement — the shell CLI's remaining reason to exist (`plan`/`run`/`status`/`abort`/`retry`/`cleanup` for environments without the Go binary) no longer applies once `run`/`abort`/`retry` themselves are retired from the Go CLI too. AGENTS.md repo map updated. Row preserved for traceability. -->
| ~~Legacy shell CLI (`scripts/ralph`) retirement~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | Prints a deprecation notice when a different `ralph` binary is on PATH (suppressed when `scripts/` itself is on PATH via inode self-check, fix #134); two codebases to maintain (shell + Go). Commands `doctor`, `init`, `upgrade` are Go-only; `cleanup` command missing from Go CLI. | Go CLI not yet at full shell-CLI command parity (`cleanup` subcommand unimplemented). Shell CLI still needed for `plan`/`run`/`status`/`abort`/`retry`/`cleanup` in environments where the Go binary is not installed. Retirement deferred until Go CLI reaches parity. | Go CLI gains `cleanup` command (the last missing subcommand) and command-parity tests pass for all subcommands listed in `print_usage`. At that point: remove `scripts/ralph`, remove the deprecation-notice block and test, and update `AGENTS.md` repo map. | `docs/plans/archive/2026-07-13-dual-cli-consolidation.md`, `docs/plans/active/2026-07-24-deprecation-notice-self-detect.md`, `scripts/ralph` (deprecation block, removed), `tests/test-ralph-deprecation-notice.sh` (removed) |
<!-- RESOLVED 2026-07-12 in fix/xreview-base-detection: detect_base_branch() helper added to ralph-cli-driver.sh (resolution order: RALPH_XREVIEW_BASE > symbolic-ref origin/HEAD > main/master fallback); ralph-pipeline.sh gate replaced HEAD@{upstream} with detect_base_branch(); orchestrator exports RALPH_XREVIEW_BASE="$_base_branch" before slice/integration pipeline invocations; SKILL.md x4 updated; gate-proving tests added to test-ralph-cli-driver.sh. Row preserved for traceability. -->
| ~~Cross-review base detection still uses `git rev-parse --abbrev-ref 'HEAD@{upstream}'` at the two sites that actually gate cross-review: `scripts/ralph-pipeline.sh:807` (+ `templates/base/` mirror) and `.claude/skills/cross-review/SKILL.md:51` (+ `.agents/` mirror). On a pushed feature branch the upstream ref resolves to `origin/<same-branch>`, so `git diff "${_base}...HEAD"` is empty and cross-review is silently skipped (pipeline line 809 gates on `--quiet`). The repo-wide-drift-fixes PR fixed the 4 `pipeline-outer.md` prompt copies to `git symbolic-ref --short refs/remotes/origin/HEAD` but left these production call sites untouched, so the prompt now documents a strategy that diverges from the shell that runs. `scripts/ralph:490` and `scripts/ralph-worktree.sh:85` already use the correct `symbolic-ref` form.~~ (RESOLVED 2026-07-12 in fix/xreview-base-detection) | On any branch tracked by a remote (the common case after `git push -u`), the Ralph Loop cross-review gate and the standard-flow cross-review base both collapse to an empty diff — cross-review can be skipped or scoped to nothing without a loud failure | Out of scope for repo-wide-drift-fixes, whose Scope 2 covered only the prompt copies; porting the fix to the pipeline script + skill body (+ mirrors) is a behavior change to the running gate, not a docs/drift fix | Next touch to the cross-review dispatch block in `ralph-pipeline.sh`, OR first operator report of cross-review producing no findings on a pushed feature branch — replace both `HEAD@{upstream}` sites (+ mirrors) with `git symbolic-ref --short refs/remotes/origin/HEAD 2>/dev/null \| sed 's\|^origin/\|\|'` and add an elapsed/diff-nonempty regression assertion | `docs/reports/self-review-2026-07-12-repo-wide-drift-fixes.md` (Cycle 2 addendum, HIGH follow-up), `scripts/ralph-pipeline.sh:807-809`, `.claude/skills/cross-review/SKILL.md:51`, `templates/base/scripts/ralph-pipeline.sh:807`, `.agents/skills/cross-review/SKILL.md:51` |
<!-- RESOLVED 2026-08-01 in commit 9bfe07e (fix: address self-review findings): herdrAgentName(orgID, seatID) added to internal/org/spawn.go as the single grep-able definition of the herdr agent-name convention; used at AgentStart (spawn.go:189) and at Send's/Wait's AgentWait call sites (verbs.go:83,138). WaitParams gained an OrgID field, wired from the CLI's --org-id flag (internal/cli/org.go newOrgWaitCmd:239). Two regression tests added: TestOrgSpawn_HerdrAgentNameNamespacedByOrgID, TestHerdrAgentName_NamespacesBySeatAndOrg (internal/org/spawn_test.go). Confirmed still fixed under -race by the /test pass (docs/reports/test-2026-08-01-org-runtime-mechanism.md, Regression checks). Row preserved for traceability. -->
| ~~herdr agent names are not `org_id`-namespaced, while agmsg team names are (`ralph-<org_id>`). `internal/org/spawn.go` registers the herdr agent under the bare `seat_id`; `internal/org/verbs.go` `Send`/`Wait` target the bare `seat_id` too. `org wait` requires `--org-id` and then discards it (`WaitParams` has no `OrgID` field).~~ (RESOLVED 2026-08-01 in commit 9bfe07e) | Two concurrent orgs that both spawn a seat with the same id (e.g. `reviewer`) collide in herdr's global agent namespace — `ralph org send` / `ralph org wait` can act on the wrong org's live seat. Manifest accounting (`ActiveSeatCount`, `Roster`, `disband`) stays correctly isolated by `org_id`; only the external herdr resource namespace leaks. | PR① is a mechanism layer with operator-driven, typically single-org usage; the seat-naming decision belongs with the seat model in PR② (座席化) | ~~Before PR③ (`ralph org start` / Lead 自律編成) can run two orgs concurrently — namespace the herdr agent name (e.g. `ralph-<org_id>-<seat_id>`), persist it on the manifest event, and have send/wait/read/stop resolve it from the seat record instead of re-deriving from the bare seat id. Add a two-org concurrency regression test.~~ — fixed ahead of schedule within PR① itself | `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md` (HIGH finding, resolved), `internal/org/spawn.go:189,249-251`, `internal/org/verbs.go:83,117,138`, `internal/cli/org.go:239`, `internal/org/spawn_test.go` (regression tests), commit `9bfe07e` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-lead (Slice 1 tech-debt batch, AC-9): added internal/org/lockfile.go withManifestLock(dir, fn) -- an OS advisory flock (syscall.Flock, LOCK_EX|LOCK_NB polled with a 5s bounded timeout) on <manifest dir>/manifest.lock. Spawn's idempotent-check/envelope-validate/stale-compensation/capacity-validate/spawn_started-append critical section now runs inside withManifestLock with a fresh manifest read taken under the lock, closing the TOCTOU window; the subsequent herdr/agmsg round trip stays outside the lock (rollout note's "read→validate→append 最小区間に限定" scoping). Regression tests: TestOrgSpawn_ConcurrentSpawns_MaxSeatsNeverExceeded (10 goroutines racing against max_seats=3, in-process Org with fake clients) and TestWithManifestLock_SerializesConcurrentCallers (unit-level pin on lockfile.go itself). Row preserved for traceability. -->
| ~~`max_seats` is enforced across an unlocked read-then-append window in the spawn saga: `Manifest.Read()` → `ActiveSeatCount` → `ValidateSpawn` → `appendEvent`, with no advisory lock in `internal/org/manifest.go`.~~ (RESOLVED 2026-08-02 in feat/org-runtime-lead) | Two concurrent `ralph org spawn` processes can both observe `activeSeats = N`, both pass the `max_seats` gate, and both append — exceeding the `[org].max_seats` envelope. The plan's edge-case list covers append *line integrity* (`O_APPEND` single write) but not this count race. | Spawns are operator-driven and serial in PR①; a lock file is only load-bearing once a Lead issues concurrent spawns programmatically | ~~PR③ (Lead autonomy issuing concurrent spawns), or first observed `max_seats` overshoot — take a lock file around read+validate+append~~ | `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md` (LOW finding), `internal/org/spawn.go:120-130`, `internal/org/manifest.go:222-241`, `internal/org/lockfile.go` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-seats commit 67220ef (Slice 1, PR②): the agmsg adapter was rewritten from the assumed single-binary `--team/--as` flag shape to the verified real interface -- a script collection under `<agmsg_home>/scripts/` (send.sh/join.sh/team.sh/history.sh/despawn.sh/whoami.sh/delivery.sh), invoked as `bash <script> <positional args>`. `agmsgArgs` and the package-level ASSUMPTION note no longer exist in internal/org/driver/agmsg.go. Row preserved for traceability. -->
| ~~agmsg CLI flag shape (`--team TEAM --as FROM <verb>`) is an unverified assumption, centralised in `agmsgArgs` in `internal/org/driver/agmsg.go` and documented as such in a package-level ASSUMPTION note. Team name convention `ralph-<org_id>` in `internal/org/spawn.go` `agmsgTeam()` is likewise provisional.~~ (RESOLVED 2026-08-02 in feat/org-runtime-seats commit 67220ef) | First live `ralph org spawn` against a real agmsg install may fail at the `agmsg_announce` saga step (which does compensate and record `spawn_failed`, so the failure is auditable rather than silent). | The real agmsg CLI was not available during PR①; the assumption is deliberately isolated to one function so a correction touches one place | ~~First live use against a real agmsg install — validate `agmsgArgs` argv shape and the team-name convention, then remove the ASSUMPTION note~~ — validated against agmsg v1.1.13's real script interface in PR②'s Slice 1 | `docs/plans/active/2026-08-01-org-runtime-mechanism.md` (Open questions), `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md`, `internal/org/driver/agmsg.go`, `docs/plans/active/2026-08-02-org-runtime-seats.md` (Slice 1), commit `67220ef` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-seats (Slice 4, PR②): unit tests added to internal/org/verbs_test.go for Send (dry-run valid message, unknown seat, seat-without-pane-id) and Stop (dry-run existing seat, existing seat with no pane_id/agmsg_team) branches, plus new Wait/Read test suites (happy path, timeout/default-lines branches, unknown-seat and no-pane-id error paths); CLI wiring tests added to internal/cli/org_test.go for `ralph org wait`/`ralph org read` (happy path + unknown-seat paths) against the herdr stub. Post-change internal/org coverage: Send 83.3%, Wait 100.0%, Read 91.7%, Stop 92.0% (was Send 70.0%, Wait 0.0%, Read 0.0%, Stop 80.0%). Row preserved for traceability. -->
<!-- (a)/(b)/(c) RESOLVED 2026-08-02 in feat/org-runtime-lead (Slice 1 tech-debt batch): (a) AC-7 -- leadIdentity const added (spawn.go), ensureLeadJoined now derives the lead agmsg type via agmsgTypeForDriver(p.LeadDriver) with defaultLeadDriver="claude" as the zero-value fallback; SpawnParams.LeadDriver is wired from a new `ralph org spawn --lead-driver` flag (default "claude"). (b) AC-10a -- checkAgmsgAvailable's Detail now names the resolved home in both the not-installed and available branches. (c) AC-10b -- dryRunSpawn now propagates RenderRolePrompt and promptFilePath errors as a failed result (via failStep), matching the real Spawn path; promptFilePath's filepath.Abs call was made testable via a new absPath package var seam (spawn.go, mirroring driver.go's lookPath pattern). (d) evidence redaction is unrelated to this slice's scope and is carried forward unresolved in the row below. Original bundled row preserved for traceability. -->
| ~~4 deferred LOW findings from org-runtime-seats self-review, not fixed and not previously tracked: (a) lead identity is a bare `"lead"` string literal (no `leadIdentity` const) and its agmsg type is hardcoded `agmsgTypeForDriver("claude")`, so an operator running Codex as the coordinating identity registers a mistyped roster entry; (b) `internal/cli/doctor.go` `checkAgmsgAvailable` discards the resolved agmsg home from `driver.AgmsgAvailable`'s error, replacing it with a fixed string, so a misconfigured 3-surface `agmsg_home` gives no clue which directory doctor actually checked; (c) `dryRunSpawn` silently swallows `RenderRolePrompt`'s and `promptFilePath`'s errors (`err == nil && ok` / `perr == nil` guards), so a dry run can report a clean trail for a spawn that the real path would `failStep` at `agent_start`/`prompt_file`~~ (RESOLVED 2026-08-02 in feat/org-runtime-lead); (d) `docs/evidence/` committed smoke logs have no redaction convention — `org-seats-smoke-2026-08-02.txt` embeds ~14 occurrences of the developer's home path plus a herdr/claude session UUID (still open, see the row below). | (a)-(b) are operator-facing rough edges with a safe default today (single-driver, single-org usage); (c) weakens dry-run's value as a pre-flight check but does not affect the real spawn path; (d) is not credential leakage, just unredacted local-path noise in a new artifact class. None block AC-1..AC-11. | Self-review flagged all 4 as LOW ("safe to batch or defer"), not MEDIUM; verify report confirmed they remain open and untracked, explicitly asking `/sync-docs` to record them rather than silently letting them age out. | ~~PR③ (Lead 自律編成) is the first point where multi-driver/multi-identity leads make (a) live, and where an LLM-driven dry-run pre-flight makes (c) load-bearing; pay down opportunistically alongside either~~, or on first operator complaint about (b) or (d). | `docs/reports/self-review-2026-08-02-org-runtime-seats.md` (LOW findings: `leadIdentity`/lead driver-type, doctor detail naming, `dryRunSpawn` error swallowing, evidence redaction), `docs/reports/verify-2026-08-02-org-runtime-seats.md` (Documentation drift + Coverage gaps, "Deferred LOW self-review findings"), `internal/org/spawn.go:378,404`, `internal/cli/doctor.go:625-631`, `internal/org/driver/driver.go:82-86`, `internal/org/spawn.go:610-624`, `docs/evidence/org-seats-smoke-2026-08-02.txt` |
| `docs/evidence/` committed smoke logs have no redaction convention — `org-seats-smoke-2026-08-02.txt` embeds ~14 occurrences of the developer's home path plus a herdr/claude session UUID (split out of the bundled 4-LOW-findings row above; (a)-(c) of that row resolved 2026-08-02 in feat/org-runtime-lead, this item did not). `docs/evidence/org-lead-smoke-2026-08-02.txt` (feat/org-runtime-lead's own live-smoke transcript, AC-11/AC-11b) carries the same unredacted local-path/UUID pattern and is likewise covered by this row. | Not credential leakage, just unredacted local-path noise in a new artifact class; does not block AC-1..AC-11(b). | Out of scope for the org-runtime-lead tech-debt batch (Slice 1), which closed the 6 items docs/plans/active/2026-08-02-org-runtime-lead.md AC-6..AC-10 name explicitly -- evidence redaction was not among them. | First operator complaint about a committed evidence file leaking local-path noise, or when a `docs/evidence/` redaction convention is defined for any other reason -- apply it retroactively to `org-seats-smoke-2026-08-02.txt`, `org-lead-smoke-2026-08-02.txt`, and any other pre-convention evidence files. | `docs/reports/self-review-2026-08-02-org-runtime-seats.md` (LOW finding, evidence redaction), `docs/reports/verify-2026-08-02-org-runtime-lead.md` (Documentation drift row noting the new file is not yet literally named here), `docs/evidence/org-seats-smoke-2026-08-02.txt`, `docs/evidence/org-lead-smoke-2026-08-02.txt` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-lead (Slice 1 tech-debt batch, AC-8): ManifestEvent gained HerdrAgentName string `json:"herdr_agent_name,omitempty"` (manifest.go), written on the `spawned` event as herdrAgentName(OrgID, SeatID) (spawn.go); SeatStatus gained the mirroring field and Roster now propagates it. verbs.go's Send now resolves the target via a new resolvedHerdrAgentName(seat) helper that prefers the persisted value and falls back to herdrAgentName() only when it is empty (legacy events). Wait deliberately keeps deriving directly -- it is documented and regression-tested (TestOrgWait_UnknownSeat_StillDrivesHerdr_NoManifestCheck) as never touching the manifest at all, so extending it would break that pinned contract; noted as a scoped deviation in Wait's own doc comment. Regression tests: TestOrgSpawn_SpawnedEventRecordsHerdrAgentName (write side), TestOrgSend_PrefersRecordedHerdrAgentName_OverDerivedConvention + TestOrgSend_LegacySpawnedEvent_FallsBackToDerivedHerdrAgentName + TestResolvedHerdrAgentName_EmptyVsSet (read side, including the omitempty compat path), TestManifestEvent_HerdrAgentName_JSONRoundTrip (wire shape). Row preserved for traceability. -->
| ~~The herdr agent name is **derived** at every call site (`herdrAgentName(orgID, seatID)`) instead of being **persisted** on the `spawned` manifest event, unlike its sibling external id `PaneID` (`internal/org/manifest.go:29`, "herdr external id, persisted as soon as known"). The `-`→`_` separator change in `f0cbf11` is the first demonstration of the cost.~~ (RESOLVED 2026-08-02 in feat/org-runtime-lead) | Any future change to the name format silently orphans live external resources: a seat recorded by a pre-`f0cbf11` build is unreachable by `send`/`wait`/`stop` (they target `<org>_<seat>`, herdr holds `<org>-<seat>`), pane compensation is best-effort, and the manifest still records `stopped` — so `ralph org status` reports a stopped seat while the real herdr agent survives. Confirmed to affect **no released build**: `git tag --contains 9bfe07e` is empty and `internal/org` post-dates `v3.9.1`, so the blast radius is main-tracking developer machines only, including this branch's own AC-8 smoke run. | The rename itself was a correctness fix demanded by cross-review (hyphen joins are ambiguous when ids may contain hyphens); persisting the name is a separate `ManifestEvent` schema change that would widen this PR's diff into the manifest format, and no released user is affected. | ~~PR③ (Lead 自律編成), where seat lifecycles become LLM-driven and long-lived — or any subsequent change to `herdrAgentName`/`agmsgTeam`'s format, whichever comes first. Fix shape: add `herdr_agent_name` to `ManifestEvent`, write it on `spawned`, and have `Send`/`Wait`/`Stop` prefer the recorded value with `herdrAgentName()` as the fallback (the same shape `PaneID` already uses).~~ | `docs/reports/self-review-2026-08-02-org-runtime-seats.md` (Cycle 2, LOW "derived, not persisted"), `internal/org/manifest.go:29`, `internal/org/verbs.go:107,162`, `internal/org/spawn.go:378,571`, `docs/plans/active/2026-08-02-org-runtime-seats.md:136` (deviations bullet documents the rename but not the compat break), commit `f0cbf11` |
| ~~`Verbs.Send`, `Verbs.Wait`, `Verbs.Read` (`internal/org/verbs.go:54,131,156`) and their CLI wiring (`internal/cli/org.go` `newOrgSendCmd`/`newOrgWaitCmd`/`newOrgReadCmd`) have 0.0% test coverage — confirmed under both a package-local `internal/org` run and a combined `-coverpkg=./internal/org/...,./internal/cli/...` run from `internal/cli`; no unit or CLI-level test exercises these three verbs at all.~~ (RESOLVED 2026-08-02 in feat/org-runtime-seats, Slice 4) | 3 of the 7 verbs in PR①'s scope ship with no behavioral proof; a regression in `send`/`wait`/`read` (e.g. a future refactor of `herdrAgentName` call sites, or the `--dry-run` seat-lookup gap already noted below) would not be caught by `go test ./...` today. | Out of scope for PR① — the `/test` pass focused on proving the 10 plan ACs (spawn saga, status, dry-run, config lockstep), none of which name send/wait/read directly; adding coverage was flagged as a recommendation, not a blocking gap, in the test report. | ~~Alongside PR② (seat operation phase), which will exercise send/wait/read live once seats run resident sessions — add at minimum: `Send` happy-path + stub-agmsg-failure case, `Wait` happy-path + timeout case, `Read` happy-path + seat-not-found case, reusing the stub herdr/agmsg pattern already established in `spawn_test.go`/`org_test.go`~~ — added in PR②'s Slice 4 | `docs/reports/test-2026-08-01-org-runtime-mechanism.md` (Coverage, Test gaps), `docs/specs/2026-08-01-org-runtime.md` (PR②), `internal/org/verbs.go`, `internal/cli/org.go`, `internal/org/verbs_test.go`, `internal/cli/org_test.go`, `docs/plans/active/2026-08-02-org-runtime-seats.md` (Slice 4) |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-lead (Slice 1 tech-debt batch, AC-6): Spawn's agmsg_announce (HELLO Send) failure path now calls a new compensateLeave(o.Agmsg, team, p.SeatID) helper (spawn.go) alongside the existing pane C-c compensation, best-effort agmsg-Leave-ing the seat's own already-succeeded Join back out. The outcome ("leave=ok" / "leave=failed: <err>" / "skipped: ...") is folded into the spawn_failed event's Details next to the existing lead_join= note. Regression tests: TestOrgSpawn_FailureInjection_AgmsgSend_LeavesJoinedSeat + TestOrgSpawn_FailureInjection_AgmsgSend_LeaveFailure_RecordedInDetails (internal/org, failure injection via fakeAgmsg), TestOrgSpawn_FailureInjection_AgmsgSend_CompensatesPane (internal/cli, extended to assert leave=ok against the real leave.sh stub). Row preserved for traceability. -->
| ~~`spawn` の agmsg_announce(HELLO send)失敗パスの補償が herdr pane の C-c のみで、直前の `Join` で登録済みの座席 roster エントリを `Leave` しない(`internal/org/spawn.go` agmsg_announce 失敗パス)。`spawn_failed` 座席は stop/disband の対象外のため、agmsg team に stale roster エントリが残留する。~~ (RESOLVED 2026-08-02 in feat/org-runtime-lead) | 失敗パス限定。残留は `team.sh` で可視・`leave.sh <team> <seat>` で手動回復可・team 削除で自然消滅。manifest は `spawn_failed` を正しく記録するため監査は破綻しない。 | cross-review cycle-2(cap 到達)の ACTION_REQUIRED を「記録して PR」で処理したもの(docs/reports/cross-review-triage-org-runtime-seats.md Cycle 2 #2)。修正は announce 失敗パスへの best-effort `Leave` 追加のみ。 | ~~PR③(Lead 自律編成)で agmsg クリーンアップ群(lead identity 定数化ほか)と同時に対応。~~ | `docs/reports/cross-review-triage-org-runtime-seats.md`(Cycle 2 #2)、`internal/org/spawn.go`(agmsg_announce 失敗パス) |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-watchdog (Slice 1, AC-7): added internal/org/statedir.go's ResolveOrgStateDir(explicit, explicitSet) (dir, source) -- precedence --state-dir flag (detected via cmd.Flags().Changed("state-dir"); the flag's own default is now "" so a merely-defaulted value can never be confused with an explicit one) > env RALPH_ORG_STATE_DIR > `git rev-parse --show-toplevel` + .harness/state/org > cwd fallback. internal/cli/org.go's newOrgRuntime resolves through it before constructing every org.Org, so lead and operator invocations from different cwds inside the same repository now converge on the same manifest -- the /org skill's same-cwd operating convention is no longer load-bearing within a single repo. 5 unit tests added (internal/org/statedir_test.go): explicit relative flag, env, git subdirectory, git root, non-git cwd. Row preserved for traceability. -->
| ~~org runtime の state-dir が cwd 相対のため、lead 座席と operator が異なる cwd で `ralph org` を実行すると manifest が分裂する(PR③ 実機スモークで実測)。運用規約(/org skill 前提)で同一 cwd を要求して回避中。~~ (RESOLVED 2026-08-02 in feat/org-runtime-watchdog) | 状態分裂時は status/report が不完全になるが、両側の manifest は各々監査可能で破壊はない。 | org_id 単位の状態共有解決(例: git-common-dir 配下への集約)は状態レイアウトの設計変更を要するため、運用規約で十分な現段階では繰延。 | ~~PR④(Watchdog がクロス cwd で状態を読む必要が出た時点)で設計。~~ | docs/plans/archive 予定 2026-08-02-org-runtime-lead.md(実機発見 #3)、.claude/skills/org/SKILL.md(前提)、docs/plans/active/2026-08-02-org-runtime-watchdog.md(Slice 1)、`internal/org/statedir.go`、`internal/org/statedir_test.go` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-watchdog (Slice 1, AC-9): frontmatter description translated to English (content parity preserved), replicated to all four mirrors (.claude/skills/org/SKILL.md, .agents/skills/org/SKILL.md, templates/base/.claude/skills/org/SKILL.md, templates/base/.agents/skills/org/SKILL.md) via ./scripts/sync-skills.sh (both the default CLAUDE_ROOT/CODEX_ROOT invocation and the templates/base-scoped invocation); check-skill-sync.sh confirms the four copies are in lock-step. Row preserved for traceability. -->
| ~~`.claude/skills/org/SKILL.md`'s frontmatter `description:` is written in Japanese, unlike all other 13 skills' `description:` (the field used for auto-invocation matching), and ships downstream through all four mirrors (`.agents/skills/org/`, both `templates/base/` copies).~~ (RESOLVED 2026-08-02 in feat/org-runtime-watchdog) | `description:` is the matching surface for auto-invocation; a lone Japanese-language description among otherwise-English descriptions is a consistency gap in a field that governs skill discoverability, though no functional breakage has been observed. | Cycle-1 self-review flagged this as LOW and deferred it; Cycle-2 self-review (finding L-5) re-flagged it as still unfixed and, per its own escalation rule ("a deferred finding untracked is the one outcome the deferral discipline was trying to avoid"), called for tracking after a second deferral. | ~~Next touch to `.claude/skills/org/SKILL.md` frontmatter for any reason, or when description-language consistency across skills is next audited — translate the line (one-line change, replicate via `scripts/sync-skills.sh` to the 3 mirrors) or record here why Japanese is intentional for this skill.~~ | `docs/reports/self-review-2026-08-02-org-runtime-lead.md` (Cycle 2, finding L-5), `.claude/skills/org/SKILL.md:3`, `docs/plans/active/2026-08-02-org-runtime-watchdog.md` (Slice 1) |
<!-- (a)/(c) RESOLVED 2026-08-02 in feat/org-runtime-watchdog (Slice 1, AC-9); (b) intentionally left as-is, final: (a) checkCapacityAndStart (internal/org/spawn.go) is now a package-level function taking (o *Org, p SpawnParams, req SpawnRequest, events []ManifestEvent) and returning *SpawnResult, replacing the closure that read/wrote enclosing events/early captured variables; both call sites assign `early = checkCapacityAndStart(o, p, req, events)`. (c) newOrgRuntime (internal/cli/org.go) reverted to `(*org.Org, error)`; the one caller that needed config (newOrgStartCmd) now reads it off `rt.Config` instead of a second return value; all 9 call sites updated. (b) permission-mode enum placement in internal/org/permissions.go is kept as-is per this plan's design decision -- internal/org already cross-references internal/config's mirrored literals in a hand-sync comment, and moving the enum crosses a package boundary with no new call site demanding it; row closed as "final, not moved" rather than "moved". Row preserved for traceability. -->
| ~~3 batchable LOW findings from org-runtime-lead Cycle-2 self-review, deferred without individual fixes: (a) `checkCapacityAndStart` (`internal/org/spawn.go:376-396`) is a zero-argument, zero-return closure that reads/writes the enclosing `events`/`early` captured variables instead of taking `events` as a parameter and returning `*SpawnResult`, hiding its input from call-site reading; (b) the permission-mode enum constants (`PermissionModeAutonomous`/`Edits`/`Guarded`, `internal/org/permissions.go:20-24`) live in `internal/org` even though the safe dependency direction is `internal/config` (which `internal/org` already imports), so `internal/config`'s own `orgPermissionModeAllowed` literals still require a hand-sync comment instead of referencing the constants directly; (c) `newOrgRuntime` (`internal/cli/org.go:91`) returns `(*org.Org, config.OrgConfig, error)` to avoid a second `ralph.toml` read, forcing `rt, _, err :=` at 9 of its 10 call sites, when `org.Org.Config` is already an exported field carrying the same value.~~ (RESOLVED 2026-08-02 in feat/org-runtime-watchdog: (a)/(c) fixed, (b) kept as final placement per plan) | (a) reduces search-based readability at two call sites per `.claude/rules/architecture.md`'s "avoid opaque helper layers" guidance, no behavior risk; (b) keeps the coordinated-edit risk the original Cycle-1 MEDIUM-4 finding named, just narrowed from 6 sites to 2; (c) adds 9 sites of `_` placeholder churn plus a justifying doc comment for a problem `rt.Config` already solves. None affect correctness or block any AC. | Cycle-2 self-review flagged all three as LOW ("batchable, no urgency") and recommended fixing them alongside L-5/L-6, but bundling them into the fix-and-revalidate cycle already spent on the two MEDIUM findings (M-1/M-2) was judged unnecessary churn for a cycle already near the pipeline-cycle cap. | ~~Next time `internal/org/spawn.go`'s Spawn/DryRun region, `internal/org/permissions.go`, or `internal/cli/org.go`'s `newOrgRuntime` is touched for any other reason — apply the three self-review-recommended fixes in the same pass: parameterize `checkCapacityAndStart` for (a), relocate the enum constants to `internal/config` for (b), revert `newOrgRuntime` to `(*org.Org, error)` + read `rt.Config` for (c).~~ | `docs/reports/self-review-2026-08-02-org-runtime-lead.md` (Cycle 2, findings L-2/L-3/L-4), `internal/org/spawn.go`, `internal/org/permissions.go:20-24`, `internal/cli/org.go`, `docs/plans/active/2026-08-02-org-runtime-watchdog.md` (Slice 1) |
| ~~`leadActivityEventCount` (`internal/org/watch.go`, deadman's manifest-growth "lead activity" source) only excludes the watchdog's own cutoff-reason events; it is not scoped to `org_id` or to lead-attributable seats (`ev.SeatID == LeadIdentity`). Since `w.org.Manifest.Read()` reads the whole state-dir manifest, any unrelated seat's own event — or, in a shared state dir, a different org's event — still counts as "lead activity" and can prematurely clear a pending deadman alert.~~ (RESOLVED 2026-08-03 in feat/org-runtime-watchdog: cross-review AR-1 first added the `orgID` filter — skipping any event whose `OrgID` does not match. Self-review cycle-2 (finding M2-1) then added an `ev.SeatID == LeadIdentity` branch, but its own closure note ("fully closes this row") overclaimed correctness: self-review cycle-3 (finding H3-1) found that branch backwards — `ev.SeatID` on a `sent` event is the *recipient* (`Send` writes `SeatID: p.To`, `internal/org/verbs.go`), not the author, so `ev.SeatID == LeadIdentity` excluded the star topology's mandated seat→lead `sent` traffic while counting lead→seat sends as nothing. The cycle-3 fix corrects the model: every `sent` event is lead-authored BY CONSTRUCTION (only `ralph org send`, driven by lead/the operator, ever appends one — a seat's reply travels over the agmsg skill, which never touches this manifest), so `ev.Event == EventSent` now counts unconditionally, and the non-watchdog lifecycle allowance was widened from `stopped`/`disbanded` to the full lead-driven set (`spawned`, `spawn_started`, `stopped`, `disbanded`, `rejected`), closing a related cycle-3 gap (M3-1: lead spawning a *replacement* seat, not itself, in response to a stall ALERT). This row is now fully closed against the corrected model.) | A genuinely unresponsive Lead facing a real anomaly could have its deadman escalation silently cleared by unrelated seat/org manifest growth, weakening the human-escalation safety net AC-5 exists to guarantee. Not a regression from pre-PR behavior (deadman is new in this PR). | Self-review (M-4) recommended filtering to `ev.OrgID == status.OrgID && (ev.SeatID == LeadIdentity \|\| ev.SeatID == "")`, or dropping this source entirely in favor of the herdr/agmsg-derived sources (which are already lead-scoped). Cross-review AR-1 closed the `org_id` half; self-review cycle-2 (M2-1) closed the seat-attribution half with an inverted predicate; self-review cycle-3 (H3-1) corrected that predicate to the `sent`-is-lead-by-construction model and extended the lifecycle set (M3-1). | ~~PR⑤ (旧系撤去・ドキュメント全面改稿), or the first live multi-seat/multi-org deadman incident where a pending alert clears without genuine Lead activity — fix shape: scope `leadActivityEventCount` to `status.OrgID` and lead-attributable `SeatID`s.~~ Closed by cross-review AR-1 (org-scope), self-review cycle-2 M2-1 (seat-scope, later corrected) + cycle-3 H3-1/M3-1 (corrected model + spawn-side lead actions), all in feat/org-runtime-watchdog. | `docs/reports/self-review-2026-08-02-org-runtime-watchdog.md` (M-4), `docs/reports/verify-2026-08-02-org-runtime-watchdog.md` (AC-5 caveat), `docs/reports/test-2026-08-02-org-runtime-watchdog.md` (Test gaps), `docs/reports/cross-review-triage-org-runtime-watchdog.md` (AR-1), `docs/reports/self-review-2026-08-02-org-runtime-watchdog.md` (Cycle 3, findings H3-1/M3-1), `internal/org/watch.go` (`leadActivityEventCount`, `checkDeadman`), `internal/org/watch_test.go` (`TestWatch_Deadman_SeatSentEvent_ClearsPendingAlert_WatchdogCutoffDoesNot`, `TestWatch_Deadman_LeadSpawnsReplacementSeat_ClearsPendingAlert`) |
<!-- RESOLVED 2026-08-03 in chore/org-debt-batch (Slice 4, commit ab4e9da): all four fixed. (1) `ResolveOrgStateDir`'s `source` is now surfaced in the `ralph org watch` startup banner and in `ralph status`'s state-dir line; `doctor` was left unchanged -- it has no org state-dir diagnostic line to annotate (its org-related checks (`checkOrgEnvelope`, herdr/agmsg availability, opt-in model probes) report config and tool availability, not state-dir resolution), so the doctor half of sub-item (1) was closed as not-applicable, not implemented. (2) `git status --porcelain` output is truncated to a bounded head (first 20 lines + "… N more") before building the scope-change ALERT body; the protocol-validation-failure fallback path now includes a `SEAT` header so Lead can identify the busy seat even when the full ALERT is rejected. (3) The unreachable `status.Escalated[alertID]` guard in `checkDeadman` was removed with a comment explaining why (`escalateAlert` already deletes the pending alert in the same statement); `Escalated` now prunes to the most recent 100 entries so `watch-status-<org_id>.json` no longer grows unbounded. (4) `evaluateTotalBudget`'s zero-active-seats guard now clears `Conditions[key].Active` when it fires, so a resumed org re-alerts on its next cutoff; existing regression tests were updated to the new semantics and a new resume→re-spawn→re-cutoff test asserts the re-alert. Row preserved for traceability. -->
| ~~4 deferred LOW findings from org-runtime-watchdog self-review, not fixed in the cycle each was first raised: (1) `ResolveOrgStateDir`'s `source` return is discarded (`_`) at both production call sites (`internal/cli/org.go:103,674`), so the resolved-precedence tier (flag/env/git-toplevel/cwd) is never surfaced in the `ralph org watch` startup banner or `status`/`doctor`, the one place it would earn its keep as a diagnostic; (2) an oversized scope-change ALERT — `git status --porcelain` output interpolated whole into the message body — silently degrades to a content-free protocol-validation-failure fallback (no `SEAT` header, no scope text) once it exceeds `DefaultMaxBodyChars`, so Lead sees only "message failed protocol validation" for a busy seat; (3) `checkDeadman`'s `status.Escalated[alertID]` guard is unreachable dead code (`escalateAlert` already deletes the pending alert in the same statement it sets `Escalated`, and new alert ids embed a nanosecond timestamp so they never collide), and `status.Escalated` is persisted forever with no pruning/expiry, so a long-running `ralph org watch` process grows `watch-status-<org_id>.json` without bound; (4) (added cycle-3, was self-review cycle-2 finding L2-6) the AC-5 zero-active-seats guard (`evaluateTotalBudget`, `internal/org/watch.go:548-559`) returns early and leaves `watchConditionRecord.Active` untouched once every seat in an org ends, so a later-spawned seat that trips the same total-budget condition is cut off silently: `alreadyAlerted` (derived from the never-cleared `Active` flag) gates the ALERT, and the existing regression test only covers the case where the guard fires before any alert was ever sent.~~ (RESOLVED 2026-08-03 in chore/org-debt-batch) | (1) reduces operator diagnosability of state-dir resolution with no correctness impact; (2) a busy seat's scope-change ALERT can reach Lead with zero actionable content; (3) is dead code (no behavior risk) plus an unbounded-growth concern for very long-running watch processes; (4) a resumed org's first post-resume cutoff produces no ALERT at all, so Lead has no signal that enforcement fired against a seat it may not know is budget-constrained. | Self-review flagged (1)-(3) as LOW ("batchable, no urgency") in cycle 1/2; the in-cycle fix-and-revalidate pass (commit `79d9f40`) closed 4 of the 7 cycle-1 LOW findings but not these three, and bundling them into an already-near-cap pipeline cycle was judged unnecessary churn each time. (4) was flagged in self-review cycle 2 (L2-6) and reconfirmed still open and untracked in cycle 3 — folded into this row per the cycle-3 report's own recommendation rather than left to survive a fourth cycle unregistered. | ~~Next time `internal/org/watch.go` or `internal/cli/org.go`'s watch-banner code is touched for any other reason — apply: print `source` in the watch banner/`status`/`doctor`, truncate `git status --porcelain` output to a bounded head (e.g. first 20 lines + "… N more") before building the scope-change ALERT (with `SEAT` in the fallback), drop/comment the unreachable `Escalated` guard while capping or expiring `Escalated` entries, and either clear `Active` when the zero-active-seats guard fires (so a resumed org re-alerts on its next cutoff) or document in the guard's comment that a resumed cutoff is intentionally silent.~~ | `docs/reports/self-review-2026-08-02-org-runtime-watchdog.md` (LOW findings, Cycle 2 finding L2-6), `docs/reports/verify-2026-08-02-org-runtime-watchdog.md`, `internal/cli/org.go`, `internal/cli/status.go`, `internal/org/watch.go` (`sendAlert`, `checkDeadman`, `escalateAlert`, `evaluateTotalBudget`), `docs/plans/active/2026-08-03-org-debt-batch.md` (Slice 4) |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 1, AC-7): checkDeadman's two probe-based deadman sources (leadProbeSnapshot / historyLeadLineCount) now only count as "lead activity" when the ALERT-time baseline itself was a valid, comparable snapshot -- i.e. `pending.LeadAgentGet != ""` / `pending.HistoryLeadLines >= 0` (reusing the fields' own existing "unavailable" sentinels, per each field's doc comment, rather than adding new booleans) gates each source's comparison. A pending alert recorded while a probe was down can still clear via the other, unaffected sources (manifest events, or a probe/history source whose baseline was valid). Regression tests: TestWatch_Deadman_ProbeOutageRecoveryAlone_DoesNotClearPendingAlert (negative: probe recovery alone, unavailable baseline, does not clear) + TestWatch_Deadman_ProbeOutageThenGenuineManifestActivity_ClearsPendingAlert (positive: genuine lead activity via a different source still clears). Row preserved for traceability. -->
| ~~watchdog デッドマンのプローブ復旧誤クリア: alert 記録時にプローブ不可(`LeadAgentGet==""`/`HistoryLeadLines==-1`)だと、復旧しただけで lead 無応答でも pending が解消される(`internal/org/watch.go` checkDeadman)。~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | プローブ全滅中に alert が発生した場合のみの限定窓。エスカレーション遅延であり誤エスカレーションではない。 | cross-review cycle-3 #5(cap 到達につき記録繰延)。可用性状態を pending record に明示保持すれば閉じる。 | ~~PR⑤ または初回の実運用デッドマン事象時。~~ | docs/reports/cross-review-triage-org-runtime-watchdog.md(Cycle 3 #5)、`internal/org/watch.go`(checkDeadman)、`internal/org/watch_test.go`(`TestWatch_Deadman_ProbeOutageRecoveryAlone_DoesNotClearPendingAlert`, `TestWatch_Deadman_ProbeOutageThenGenuineManifestActivity_ClearsPendingAlert`) |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (Slice 1, AC-7): ensureWatchdogJoined now sets status.WatchdogJoined = true only inside the `if err := ...Join(...); err == nil` branch, so a transient Join failure leaves the flag false and the next cycle retries -- ALERT delivery itself (SendWatchdogAlert) is unaffected either way, since it never depended on Join's own outcome. Regression test: TestWatch_EnsureWatchdogJoined_TransientFailure_RetriesUntilSuccess (cycle 1 Join fails -> flag stays false -> cycle 3's re-raised ALERT retries Join and succeeds -> flag set). Row preserved for traceability. -->
| ~~watchdog の agmsg join が一時失敗しても `WatchdogJoined=true` が永続化され、以後 rejoin されず ALERT 配送が失敗し続ける(`internal/org/watch.go` ensureWatchdogJoined)。~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | transient join 失敗時のみ。watch-status ファイル削除で手動回復可。 | cross-review cycle-3 #6(cap 到達につき記録繰延)。join 成功時のみフラグ設定にすれば 1 行で閉じる。 | ~~PR⑤ または初回の配送失敗観測時。~~ | docs/reports/cross-review-triage-org-runtime-watchdog.md(Cycle 3 #6)、`internal/org/watch.go`(ensureWatchdogJoined)、`internal/org/watch_test.go`(`TestWatch_EnsureWatchdogJoined_TransientFailure_RetriesUntilSuccess`) |
<!-- RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop (cycle-2 fix 746b35b): `/cross-review`'s SKILL.md now sources `scripts/xreview-helpers.sh` and calls both helpers for real — Step 2.b (`. scripts/xreview-helpers.sh; REVIEWER=$(pick_reviewer "$DRIVER")`) and Step 6's count-verification pass (`count_triage_findings <report> ACTION_REQUIRED|WORTH_CONSIDERING|DISMISSED` re-derived against the canonical `- After triage: ...` header line). All three functions and the file header's "/cross-review is the only consumer" claim are now accurate. Row preserved for traceability. -->
| ~~`scripts/xreview-helpers.sh`'s `count_triage_findings` and `pick_reviewer` have no executable caller~~ (RESOLVED 2026-08-03 in refactor/org-runtime-retire-loop) | A future editor reading either claim will assume the SKILL.md path is covered by `tests/test-xreview-helpers.sh` when it is not; ~110 lines of tests defend an unreachable helper | Wiring SKILL.md to source the helpers is a behavior change to `/cross-review`, outside the retirement PR's "move, do not modify" contract for these three functions | ~~Next edit to `/cross-review` Step 2 or Step 5~~ | `docs/reports/self-review-2026-08-03-org-runtime-retire-loop.md` (MEDIUM, cycle 1; Cycle 2 §closed), `scripts/xreview-helpers.sh:9-16`, `.claude/skills/cross-review/SKILL.md:44,105` |
| 6 batchable LOW findings from org-debt-batch cycle-1 self-review, deferred at merge (run 2/2 under the default cycle cap): (1) write-only `status.Escalated` still carries pruning + tests for state nothing reads; (2) test fixtures at `internal/org/report_test.go:137,216` / `spawn_test.go:193` / `watch_test.go:276` still build receipts paths with `filepath.Join(dir, "receipts.jsonl")` (non-production filename) instead of `org.ReceiptsPathIn`; (3) `SEAT: ` header with empty value in the org-level ALERT fallback (`internal/org/watch.go:917`) vs no header on the normal path; (4) `printStateDirLine` used at 1 of 4 formatting sites plus a dead `omitempty` on `state_dir_source` (`internal/cli/status.go:263`); (5) test globals mutated without `t.Cleanup` in some new watch tests; (6) minor comment nits from the cycle-1 report's LOW list; (7) `scripts/xreview-helpers.sh:9-16` header rewording (Slice 2) went wider than the guard PATTERN required -- restoring the more specific provenance wording is optional polish. | Cosmetic/maintainability only; no behavior or correctness impact. | Cycle-2 was the final run under the default pipeline cap and all six are doc/test-hygiene edits judged not worth another fix-and-revalidate cycle. | Next time the corresponding file is touched for any other reason — apply the matching cleanup in the same pass. | `docs/reports/self-review-2026-08-03-org-debt-batch.md` (Cycle 1 LOW list, Cycle 2 C2-2) |
<!-- RESOLVED 2026-08-03 in chore/org-debt-batch (Slice 2, commit 57effc3): `PATTERN` widened to the full 9-token knob set (`RALPH_(FORCE|IMPLEMENT|SELF_REVIEW|VERIFY|TEST|SYNC_DOCS|PR|PROBE|ESCALATION)_MODEL`), matching the header's "per-phase `RALPH_*_MODEL` knobs" claim exactly. `EXCLUDE_REGEX`'s file-wide exemption for `scripts/xreview-helpers.sh` (+ mirror) was narrowed to match only the provenance comment lines, so a live loop reference newly added to that file's executable body is no longer silently exempted. Row preserved for traceability. -->
| ~~`tests/test-no-loop-references.sh`'s `PATTERN` still under-claims relative to its own header. The header now says it guards "the per-phase `RALPH_*_MODEL` knobs", but the pattern only covers `RALPH_(FORCE\|IMPLEMENT\|SELF_REVIEW\|PROBE\|ESCALATION)_MODEL` — 5 of the 8 knobs the retired `model-routing.md` table defined. `RALPH_VERIFY_MODEL`, `RALPH_TEST_MODEL`, `RALPH_SYNC_DOCS_MODEL` and `RALPH_PR_MODEL` are unguarded. Separately, `EXCLUDE_REGEX` now exempts all of `scripts/xreview-helpers.sh` (+ mirror) rather than just its provenance comment.~~ (RESOLVED 2026-08-03 in chore/org-debt-batch) | The residual after the cycle-2 widening: a new dangling reference to one of the 4 unlisted knobs, or any live loop reference added inside `xreview-helpers.sh`, still lands silently. Verified 2026-08-03 that all 4 knobs currently have zero live hits, so widening is free today and only gets harder later. | The cycle-2 fix (746b35b) widened `PATTERN` using the cycle-1 report's suggested token list verbatim rather than the actual 8-knob set, and fixed all four leftovers that list surfaced | ~~Next touch to `tests/test-no-loop-references.sh` for any reason — complete the knob set (`RALPH_(FORCE\|IMPLEMENT\|SELF_REVIEW\|VERIFY\|TEST\|SYNC_DOCS\|PR\|PROBE\|ESCALATION)_MODEL`) and narrow the `xreview-helpers.sh` exclusion to the provenance lines~~ | `docs/reports/self-review-2026-08-03-org-runtime-retire-loop.md` (Cycle 2, C2-5), `tests/test-no-loop-references.sh`, `docs/plans/active/2026-08-03-org-debt-batch.md` (Slice 2) |
<!-- RESOLVED 2026-08-03 in chore/org-debt-batch (Slice 1, commit 3b91875 + 0e510a7): `org.NewManifestStore`/`ManifestRelPath` and `org.NewReceiptStore`/`ReceiptsRelPath` deleted outright. Path derivation is now centralized in `internal/org` via new `ManifestPathIn(stateDir)` / `ReceiptsPathIn(stateDir)` helpers; `internal/cli/org.go`'s `orgManifestPath` was deleted, and both the write path (`newOrgRuntimeAt`) and the read path (`runStatus`) now call `org.ManifestPathIn` directly. Tests that used the root-relative constructor pattern were replaced with the `...AtPath` + new `*PathIn` helpers. AC-1 grep (`grep -rn 'NewManifestStore\b|ManifestRelPath|NewReceiptStore\b|ReceiptsRelPath' --include='*.go' .`) returns zero hits, confirmed 2026-08-03 (docs/evidence/org-debt-batch-2026-08-03.txt). Row preserved for traceability. -->
| ~~`org.NewManifestStore(root)` / `org.ManifestRelPath` and `org.NewReceiptStore(root)` / `org.ReceiptsRelPath` are exported with zero callers. The AR-1 fix (fea2ee6) removed the last `NewManifestStore` call site (`internal/cli/status.go`) in favour of `orgManifestPath`, and every runtime path now goes through the `...AtPath` constructors from an already-resolved state dir.~~ (RESOLVED 2026-08-03 in chore/org-debt-batch) | The root-relative constructor left in the API is precisely the footgun that caused AR-1 (`ralph status` blind to every real manifest because `.harness/state/org/manifest.jsonl` was re-appended to an already-resolved dir). Any future caller that reaches for the obvious-looking `NewManifestStore(stateDir)` reproduces the bug, and the fix's own doc comment on `orgManifestPath` is the only thing warning them. | Deleting exported API is out of scope for a cross-review fix commit whose contract was "make status read the path spawn writes" | ~~Next edit to `internal/org/manifest.go` or `receipts.go` — delete both root-relative constructors and their `*RelPath` constants, or move `orgManifestPath`/`orgReceiptsPath` into `internal/org` as the single grep-able derivation~~ | `docs/reports/self-review-2026-08-03-org-runtime-retire-loop.md` (Cycle 2, C2-3), `internal/org/manifest.go` (`ManifestPathIn`), `internal/org/receipts.go` (`ReceiptsPathIn`), `internal/cli/org.go` (`newOrgRuntimeAt`), `internal/cli/status.go` (`runStatus`), `docs/plans/active/2026-08-03-org-debt-batch.md` (Slice 1) |
<!-- RESOLVED 2026-08-03 in chore/org-debt-batch (Slice 3, commit e00e301 + 4ddd8e7): `ralph insights --receipts` now defaults to the org runtime's receipts (`org.ReceiptsPathIn(stateDir)`, resolved via `org.ResolveOrgStateDir`), reading the tri-state `honored` (true/false/unknown) org receipt schema via a new reader in `internal/insights`. The Receipts section groups by `org_id` x `seat_id`, reports `honored: true=N false=N unknown=N`, and computes rate as true/(true+false) with unknown excluded from the denominator (both text and `--json` output). The old `.harness/state/pipeline/` schema reader was deleted (its writer was already retired, no back-compat needed). Real-render evidence: docs/evidence/org-debt-batch-2026-08-03.txt. Row preserved for traceability. -->
| ~~`ralph insights` still defaults `--receipts` to `.harness/state/pipeline/model-receipts.jsonl`, a path whose only writer was `write_model_receipt` in the deleted `scripts/ralph-cli-driver.sh`. The org runtime writes its receipts to `.harness/state/org/model-receipts.jsonl` (`org.ReceiptsRelPath`) with a different schema.~~ (RESOLVED 2026-08-03 in chore/org-debt-batch) | `ralph insights`'s receipt-diagnostics section is now permanently empty on a fresh repo, and both the flag help and `internal/insights/receipts.go`'s doc comment present the retired path as a live source. `tests/test-no-loop-references.sh` cannot catch this — it is a state-dir path string, not one of the guarded tokens. | Retirement PR scoped its deletion sweep to script/symbol names; the receipts *reader* survived because `ralph insights` itself survived | ~~Next edit to `internal/cli/insights.go` — either repoint the default at the org runtime's receipts (requires a schema decision: `internal/insights.Receipt` vs `internal/org` receipt fields) or document the flag as historical-only~~ | `docs/reports/self-review-2026-08-03-org-runtime-retire-loop.md` (Cycle 2, C2-6), `internal/cli/insights.go`, `internal/insights/receipts.go`, `docs/plans/active/2026-08-03-org-debt-batch.md` (Slice 3), `docs/evidence/org-debt-batch-2026-08-03.txt` |
| `evaluateTotalBudget`'s `Cutoff` ratchet (`internal/org/watch.go`, `watchConditionRecord.Cutoff`) is scoped to the org-level condition key (`conditionKey(orgID, "", condTotalBudget)`), not per-seat, and is a one-way flag that is never cleared once set. Slice 4 of this batch (commit `ab4e9da`) fixed the *re-alert* half of the resumed-org gap (clearing `Conditions[key].Active` when the zero-active-seats guard fires, so a resumed org gets an ALERT on its next over-budget cutoff), but deliberately left `Cutoff` itself untouched (see `watch.go:635-637`'s own comment: "this only clears `Active`, not `rec.Cutoff` — the separate 'one cutoff per key, ever' ratchet above is unaffected, so a prior successful cutoff still permanently disables Stop for this key regardless"). So for a resumed org whose total-budget cutoff previously SUCCEEDED (`Cutoff=true`), `evaluateTotalBudget`'s `if rec != nil && rec.Cutoff { return cutSeats }` early-return fires before both the ALERT path and Stop — the resume is entirely silent and unenforced (confirmed by codex cross-review cycle-2 P2, 2026-08-03). The Slice 4 re-alert fix only takes effect when the prior cutoff FAILED (`Cutoff=false`), which is also why its regression test uses the failed-Stop technique. | After a successful cutoff, a resumed over-budget org produces neither ALERT nor Stop for this condition — Lead has no signal at all, and an operator would reasonably assume the watchdog is still enforcing. Not a regression from pre-Slice-4 behavior (pre-fix, the org didn't even re-ALERT); it is the boundary of what Slice 4's fix scope (re-alert only, per its own plan Scope item 4d: "同一条件で再アラートされるようにする") covered. | Out of scope for the re-alert fix in `docs/plans/active/2026-08-03-org-debt-batch.md` Scope item 4(d), which named only the re-alert gap, not the Cutoff ratchet's own scope. Widening `Cutoff` to also clear (or to become per-seat, or per-resume-epoch) is a distinct semantic decision — the ratchet exists specifically because self-review's original H-2 fix wanted "one cutoff per key, ever" as a safety property against re-triggering Stop mid-cutoff, and undoing that needs its own design pass, not a follow-on to the re-alert fix. | Next time `evaluateTotalBudget`'s semantics are touched for any other reason, or the first real org resume that needs the total-budget condition to actually cut off (not just re-alert) a second time — decide whether `Cutoff` should clear alongside `Active` on the zero-active-seats guard, or move to a resume-epoch-scoped key so a new resume gets a fresh ratchet. | `internal/org/watch.go` (`evaluateTotalBudget`, `watchConditionRecord.Cutoff`, lines ~606-680), `docs/plans/active/2026-08-03-org-debt-batch.md` (Slice 4, Scope item 4(d)) |
<!-- RESOLVED 2026-08-17 in feat/overlay-scaffold-v2-p2 (Slice 5): `scripts/check-sync.sh` now compares `AGENTS.md` block-aware instead of whole-file — it extracts the `ralph:agents-md` managed-block interior from both root and `templates/base/AGENTS.md` and asserts each equals `templates/base/.ralph/core/AGENTS.core.md`; content outside the block is free-area per owner (root: meta-repo Repo map/Planning/Review/escalation sections; template: downstream skeleton framing) and is not compared. The `AGENTS.md` entry was removed from `KNOWN_DIFFS` entirely. Row preserved for traceability. -->
| ~~`AGENTS.md` was added to `scripts/check-sync.sh`'s `KNOWN_DIFFS` for the overlay-scaffold-v2 Phase 1 repo-map two-line extension. `KNOWN_DIFFS` is a whole-file suppression (`is_known_diff` matches the path, not a hunk), so this permanently stops `check-sync.sh` from reporting *any* future divergence between root `AGENTS.md` and `templates/base/AGENTS.md` — the most structurally important synced file in the repo — not just the two-line delta it was added to accommodate.~~ (RESOLVED 2026-08-17 in feat/overlay-scaffold-v2-p2) | Root ↔ `templates/base/` drift in `AGENTS.md` can accumulate unreported for reasons unrelated to this feature until the entry is removed. | Phase 1 of overlay-scaffold-v2 must not touch `templates/`, per the plan's scope, so a matching template-facing update could not land in the same PR as the repo-map extension. | ~~Remove the `AGENTS.md` entry from `scripts/check-sync.sh`'s `KNOWN_DIFFS` when Phase 2 lands the template-facing rollout of overlay-scaffold-v2.~~ — RESOLVED | `docs/reports/self-review-2026-08-17-overlay-scaffold-v2.md` (Cycle 2, C2-3), `scripts/check-sync.sh` (`KNOWN_DIFFS`, `extract_agents_md_block`), `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (Scope), `.ralph/core/AGENTS.core.md` |
| 5 unfixed cycle-1 LOW findings from overlay-scaffold-v2 self-review, not fixed at merge (pipeline cap 2/2 reached; a 6th, the root-runnable `chmod 0444` failure-injection test, was fixed in the cycle-2 cleanup slice): (1) `classifyFork` reports a stale `DiskHash` (falls back to the manifest's recorded hash) when the fork file is absent from disk instead of leaving it empty; (2) `ReplacePlan.LegacySkipped`'s doc says "owner=block or is a legacy (unattributed) entry" but omits the unknown/future-owner producer (the `default` branch in `classifyPath`); (3) `WriteUpgradeReport` hardcodes `0644`/`0755` instead of using `scaffold.FilePerm`, a permission-decision site now joined by a fifth in Phase 3's `internal/cli/upgrade_v2.go` `writeFileV2` (hardcoded `0755` for `os.MkdirAll`); (4) `getObjPath` (variadic) and `getArrayPath` (slice) take the same kind of argument in two different shapes; (5) `TestExistingConstructorsWriteNoV3Fields` reads an unset map key (`m.Files["with-hash.md"]`, zero value) without a comment explaining that the zero-value start is deliberate. ~~A 6th item joined from the Phase-3 cycle-2 self-review (C2-4): `runUpgradeIOWithOptions`'s unused `in io.Reader` parameter (`internal/cli/upgrade.go`) is kept only for signature stability across ~25 same-package call sites — drop it on the next signature-touching change.~~ (RESOLVED 2026-08-18 in feat/overlay-scaffold-v2-p4: `in` is now read by `runMigrateLegacy`'s y/N migration confirmation prompt, `internal/cli/migrate.go`.) | Readability/consistency debt on API surfaces Phase 3 inherits; none are correctness bugs and none block merge. | Pipeline cap (2/2) reached at overlay-scaffold-v2 merge; each finding is individually small and was judged not worth a third fix-and-revalidate cycle. | Phase 3 (`feat/overlay-scaffold-v2-p3`) wired these primitives into `internal/cli` without paying any of the 5 items down; next touch to any of the listed call sites, or a dedicated permission-handling sweep for item (3)'s now-5 hardcoded-permission sites. | `docs/reports/self-review-2026-08-17-overlay-scaffold-v2.md` (Cycle 1 LOW list; Cycle 2, C2-4), `docs/reports/self-review-2026-08-18-overlay-scaffold-v2-p3.md` (LOW, `writeFileV2` fifth permission site), `internal/upgrade/replaceplan.go` (`classifyFork`, `LegacySkipped` doc), `internal/upgrade/report.go` (`WriteUpgradeReport`), `internal/upgrade/settingsmerge.go` (`getObjPath`/`getArrayPath`), `internal/scaffold/manifest_test.go` (`TestExistingConstructorsWriteNoV3Fields`), `internal/cli/upgrade_v2.go` (`writeFileV2`) |
| `PlanCoreReplace` cannot handle template path shape transitions (old core file `foo` → new template file `foo/bar`, or the reverse): `readDiskFile` gets `ENOTDIR`/`EISDIR` (not `os.ErrNotExist`), the error bubbles, and planning aborts before emitting the delete/create ops that would resolve the transition. Failure mode is a non-destructive error abort, not misclassification or data loss. Known at slice-4 implementation time; re-surfaced by codex cross-review cycle 2 (P2). | A scaffold upgrade whose template renames a path across the file/directory boundary errors out instead of completing; the operator must resolve the on-disk shape manually before upgrade can proceed. | Pipeline cap (2/2) reached at overlay-scaffold-v2 Phase 1 merge; the right fix is a classification decision (e.g. shape mismatch → drift or a dedicated plan entry) that belongs to Phase 3's error-UX design for the wired upgrade flow. | Phase 4's legacy→v2 migration classifier (plan `docs/plans/active/2026-08-18-overlay-scaffold-v2-p3.md` Non-goals: "ファイル⇔ディレクトリ形状遷移の分類対応... 分類設計は Phase 4 の移行分類器と合流") — Phase 3 (`feat/overlay-scaffold-v2-p3`) wired `PlanCoreReplaceDesired` into `internal/cli` but left this classification decision (and its planned "clear error message" UX polish) untouched; `readDiskFile` still bubbles the raw `ENOTDIR`/`EISDIR` os error. | `docs/reports/cross-review-triage-overlay-scaffold-v2.md` (Cycle 2, WC#1), `internal/upgrade/replaceplan.go` (`readDiskFile`) |
| `pre_bash_guard.sh` calls `extract_json_field "$payload" "command"` (a top-level-only path for `lib_json.sh`'s `jq -r ".${_field} // empty"` branch), but real Claude Code PreToolUse payloads nest the Bash tool's argument under `.tool_input.command`. When `jq` is present (the function's preferred path), `.command` resolves to empty on the real payload shape, so `$command` is always empty and every `case "$command" in ...` deny/ask rule silently never matches. The guard only works today via the sed fallback (`command -v jq` absent), which happens to match the leaf key `command` anywhere in the payload regardless of nesting — an accidental correctness dependency on `jq` being unavailable, the opposite of the function's documented preference. Identical in both `.claude/hooks/pre_bash_guard.sh` and `templates/base/.claude/hooks/pre_bash_guard.sh` (kept in sync via `check-sync.sh`). Discovered during Phase-2 slice-5 hook dispatcher smoke testing. | On any environment with `jq` installed (the common case `lib_json.sh` optimizes for), the guard's deny/ask rules (dangerous `rm`, force-push, hard reset, `.git`/`.env` writes, unsafe commit-message quoting) provide no real protection against actual Claude Code Bash tool invocations. | Out of scope for overlay-scaffold-v2 Phase 2, whose slice-5 root migration only moved and dispatcher-wired the hook, and did not change its payload-parsing logic. | Next hooks change: call `extract_json_field "$payload" "tool_input.command"` (or add a `.tool_input.command // .command` jq fallback) so the `jq` path resolves the real payload shape. (Phase 3, `feat/overlay-scaffold-v2-p3`, wired `ralph upgrade` for the v2 layout without touching hook payload handling — that expired trigger branch is dropped.) | `.claude/hooks/pre_bash_guard.sh`, `.claude/hooks/lib_json.sh`, `templates/base/.claude/hooks/pre_bash_guard.sh`, `templates/base/.claude/hooks/lib_json.sh`, docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md (Slice 6) |
<!-- UPDATED 2026-08-18 in feat/overlay-scaffold-v2-p3: the legacy `ralph upgrade` engine (`internal/upgrade/diff.go`/`merge.go`) that this row originally described was deleted in Phase 3, along with `internal/cli/cli_test.go`'s cited dual-path fixtures. The underlying property — no rename/move detection, so a template path rename shows up as an unrelated delete+create pair — carries over unchanged to the engine's Phase-3 successor (`PlanCoreReplaceDesired`, `internal/upgrade/replaceplan.go`), which classifies purely by path presence/absence with no cross-path correlation. Row content rewritten to describe the current mechanism rather than the deleted one. -->
| `PlanCoreReplaceDesired` (`internal/upgrade/replaceplan.go`), the core replace planner behind `ralph upgrade`'s v2 engine (`internal/cli/upgrade_v2.go`), has no rename/move detection: a template path rename between versions (e.g. the pack-rule relocation `.claude/rules/<lang>.md` → `.claude/rules/ralph/<lang>.md`, Phase 2 Slice 4) classifies as an unrelated delete-at-old-path plus create-at-new-path, because classification is purely per-path presence/absence with no cross-path correlation. | A downstream v2 project that upgrades across a template path rename sees one extra remove + one extra add in its upgrade report instead of a single clean rename; no data loss (both ops classify correctly on their own), but the report is noisier than the actual change. | Rename/move detection was out of scope for both Phase 2 (which introduced the relocation this row originally cited) and Phase 3 (which wired the planner into `internal/cli` without changing its classification model); adding it is a planner-level design change, not incidental to either phase's file list. | Next template path rename that ships in a release, or the next time `PlanCoreReplaceDesired`'s classification logic is touched for any other reason — add path-rename detection (e.g. correlate a delete/create pair by content hash) so the report collapses them into a single rename op. | `internal/upgrade/replaceplan.go` (`PlanCoreReplaceDesired`), `internal/cli/language_pack.go` (`packRuleRelPath`), `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (Slice 4, Slice 6), `docs/reports/self-review-2026-08-18-overlay-scaffold-v2-p3.md` (M4, row reconciliation) |
| The Codex hook surface (`.codex/config.toml`, root and `templates/base/`) was not migrated to `ralph-dispatch.sh` in Phase 2: it still invokes `./.claude/hooks/check_mojibake.sh` directly, so the `<event>.d` layering and the new `.ralph/local/hooks/<event>.d/` extension point are Claude-Code-only. `tests/test-hook-wiring.sh` cannot catch this — it asserts each referenced path exists and is executable, and the pre-dispatcher scripts still exist at their old paths, so the test passes on the un-migrated config. `AGENTS.md`, `docs/architecture/repo-map.md`, and `README.md`'s `.ralph/local/`/Hooks bullets are now qualified ("Claude Code today; Codex wiring is Phase 3") so a downstream reader is not misled while this row is open. | A downstream project that adds a `.ralph/local/hooks/<event>.d/` drop-in gets it under Claude Code and silently not under Codex. | Phase 2 slice-2's scope was the Claude-side dispatcher plus the `.d` migration; the Codex `[hooks]` surface is a distinct config with its own event-name mapping and was not in the slice's file list. | Phase 5 (plan `docs/plans/active/2026-08-18-overlay-scaffold-v2-p3.md` Non-goals: "`.codex/` hooks の dispatcher パリティ(Phase 5)") — or the next `.codex/config.toml` change, whichever comes first. Phase 3 (`feat/overlay-scaffold-v2-p3`) wired `ralph upgrade` for the v2 layout without touching `.codex/config.toml`, so that expired trigger branch is replaced. | `docs/reports/self-review-2026-08-17-overlay-scaffold-v2-p2.md` (MEDIUM, Codex parity; Cycle 2, C2-2), `.codex/config.toml` (lines 76, 80), `templates/base/.codex/config.toml`, `tests/test-hook-wiring.sh`, `AGENTS.md`, `docs/architecture/repo-map.md`, `README.md`, `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (Slice 2) |
| ~~The `.gitignore` managed block extends to end-of-file (`# END RALPH MANAGED` is the last line of both `.gitignore` and `templates/base/.gitignore`), so the file's own header instruction — "add yours outside the managed block" — has no free area below the block. Appending to `.gitignore`, the universal habit, lands inside the ralph-owned region.~~ (RESOLVED 2026-08-17 in `c2d501e`) | Once Phase 3's block engine rewrites block interiors during `ralph upgrade`, a downstream project's appended ignore rules are silently discarded. Today it is a latent trap only: nothing rewrites the block yet. | Phase 2 only seeds the block markers; the rewrite-on-upgrade behavior that makes this destructive belongs to Phase 3. Where the block should end is a layout decision better made alongside the engine that consumes it. | ~~Phase 3, when the block engine is wired into `ralph upgrade` — before the first release that actually rewrites a `.gitignore` managed block.~~ — RESOLVED: `c2d501e` appended a labelled free area (`# Project-specific ignores go below this line.`) after `# END RALPH MANAGED` in both `.gitignore` and `templates/base/.gitignore` (still byte-identical). | `docs/reports/self-review-2026-08-17-overlay-scaffold-v2-p2.md` (MEDIUM, .gitignore block extent; Cycle 2, C2-3), `.gitignore`, `templates/base/.gitignore`, `internal/upgrade/block.go` (`BlockMarkerHash`), `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (Slice 1/5) |
| Two small, individually-trivial cycle-1 self-review findings for overlay-scaffold-v2 Phase 2 were left unfixed and untracked at the pipeline cap (2/2): (1) `ralph-dispatch.sh`'s five-clause blank-line-collapsing `awk` program is duplicated verbatim in the jq and no-jq merge branches, so a fix to one silently diverges from the other; (2) `scripts/check-sync.sh` prints `ROOT_ONLY templates/base/AGENTS.md (file missing)` for the missing-template-side branch, a label that contradicts the path it names, and the same section 1b block-aware comparison increments the whole-file `identical` counter, double-counting `AGENTS.md` as two comparisons in the summary line. (The third cycle-1 LOW in this group, `packRuleRelPath` building manifest keys with `filepath.Join` instead of a slash-safe join, was fixed in the cycle-2 cleanup slice rather than deferred — see `internal/cli/language_pack.go`.) | Readability/consistency debt; none are correctness bugs on the current (Unix) CI/dev path and none block merge. | Pipeline cap (2/2) reached at overlay-scaffold-v2 Phase 2 merge; each finding is individually small and was judged not worth a third fix-and-revalidate cycle. | Phase 3 wiring, or the next touch of `ralph-dispatch.sh` / `scripts/check-sync.sh` for any other reason. | `docs/reports/self-review-2026-08-17-overlay-scaffold-v2-p2.md` (Cycle 1 LOW-3/LOW-5/LOW-6; Cycle 2, C2-4), `templates/base/.claude/hooks/ralph-dispatch.sh`, `scripts/check-sync.sh` |
| `scripts/check-sync.sh`'s block-aware `AGENTS.md` comparison (`extract_agents_md_block` + the `DRIFTED` branch, AC-7) has no isolated fixture test. It is only exercised by running `check-sync.sh` against the real repo tree, which currently has zero drift — the drift-detected code path (`diff -q` mismatch against `.ralph/core/AGENTS.core.md`) is never exercised, only the happy path. | If a future edit breaks `extract_agents_md_block` or the `DRIFTED` comparison itself, nothing in the test suite would catch it — only a live, accidental drift in the repo would surface the bug, and by then the gate has already failed silently for some other reason. | Out of scope for overlay-scaffold-v2 Phase 2's `/test` pass; the plan's test plan did not call for a fixture repo with an intentionally mutated managed block. | The next change to `scripts/check-sync.sh`'s block-aware comparison — add a fixture repo with an intentionally mutated managed block and assert `check-sync.sh` reports `DRIFTED` with a non-zero exit. | `docs/reports/test-2026-08-17-overlay-scaffold-v2-p2.md` (Test gaps), `scripts/check-sync.sh` (`extract_agents_md_block`), `docs/plans/active/2026-08-17-overlay-scaffold-v2-p2.md` (AC-7) |
| Two cycle-3 self-review findings for overlay-scaffold-v2 Phase 2 were left unfixed at the pipeline cap (3/3, cap raised from 2): ~~(1) two entries in `docs/insights/events/2026-08-17-overlay-scaffold-v2-p2.jsonl` are stamped `cycle:1` although they were appended by cycle-2 fix commits (`54f97ac`'s `verify` event, `d5b478e`'s `cross_review` event), and cycle 2 has no `self_review` event at all~~ (RESOLVED 2026-08-18 in feat/overlay-scaffold-v2-p2, cycle-3 cross-review fix); (2) `internal/cli/cli_test.go` (`TestAddPack_LegacyManifest_StaysOwnerless`) frames the `addPack`-must-not-upgrade-`Meta.Layout` regression assertion as `t.Fatalf("precondition failed: ...")`, which reads as a broken test fixture rather than the regression the test exists to catch. ~~(3) stale triage-report pointer/header~~ (RESOLVED 2026-08-18: 756aac8 rewrote the triage report; its header now reads Cycle: 3/3 and the decayed `init.go:373` pointer no longer exists). | ~~`ralph insights` will under/mis-report this slug's pipeline cadence (three cycle-1 verify/cross-review runs, one self-review across the whole PR)~~ (RESOLVED 2026-08-18); the misleading `Fatalf` message sends a future debugger looking for a broken fixture instead of the real regression. | This is the last pipeline cycle for overlay-scaffold-v2 Phase 2 (3 of 3, cap raised from 2); a fourth fix-and-revalidate cycle was judged not worth the churn for individually cosmetic findings, so they are batched here per the cycle-3 self-review's own recommendation rather than left to silently vanish once the plan is archived at `/pr`. | ~~The next change to `docs/insights/events/2026-08-17-overlay-scaffold-v2-p2.jsonl` or any `ralph insights` schema/aggregation work — correct the two mis-stamped `cycle` values to `2` and append the missing cycle-2 `self_review` event via `scripts/insights-append.sh`.~~ (RESOLVED 2026-08-18) Separately, the next touch of `internal/cli/cli_test.go` near `TestAddPack_LegacyManifest_StaysOwnerless` — reword the `Fatalf` to state the regression directly ("addPack upgraded Meta.Layout to v2, want unchanged"). | `docs/reports/self-review-2026-08-17-overlay-scaffold-v2-p2.md` (Cycle 3, C3-4/C3-5), `docs/insights/events/2026-08-17-overlay-scaffold-v2-p2.jsonl`, `internal/cli/cli_test.go` (`TestAddPack_LegacyManifest_StaysOwnerless`) |
| Phase 3's legacy-engine deletion removed the only tests covering three still-live surfaces: (1) `installGitHook`'s user-hook chaining branch (`internal/cli/git_hooks.go`) now has no test anywhere; (2) the `--pager` flag on `ralph upgrade` survives as a user-facing flag with zero coverage; (3) `buildDesiredStateV2`'s `AvailablePacks` failure branch (`apErr` path in `internal/cli/upgrade_v2.go`) is untested. | Regressions in git-hook chaining, pager fallback, or the pack-listing failure path would ship silently. | The deleted legacy tests exercised these incidentally; Phase 3's replacement suite covers the v2 engine's own contracts, and adding dedicated coverage for these three was judged out of the fix-slice's scope at the pipeline stage it was found. | Next touch of `internal/cli/git_hooks.go`, the upgrade pager path, or `buildDesiredStateV2` — add the dedicated test then. | `docs/reports/self-review-2026-08-18-overlay-scaffold-v2-p3.md` (deleted-test inventory / tech-debt notes), `docs/reports/verify-2026-08-18-overlay-scaffold-v2-p3.md` (LOW finding) |
| ~~Manifests produced by pre-b3c6307 commits of the Phase-3 branch (and any v2 manifest from Phase-2-era `ralph init`) record `.codex/AGENTS.override.md` with `owner=core`; the cycle-3 cross-review fix (b3c6307) reclassified it as seed for newly-generated manifests only — existing v2 manifests keep the stale core attribution until reclassified. Blast radius today is zero: `git tag --contains` on the first LayoutV2 commit returns no tags, so no released binary ever produced a v2 manifest.~~ (RESOLVED 2026-08-18 in feat/overlay-scaffold-v2-p4) | A hypothetical pre-release v2 manifest would still treat downstream edits to the Codex override as unresolved drift (exit 3) until its owner attribution is corrected. | Owner re-attribution of existing manifests is the migration classifier's job (spec Phase 4), not a Phase-3 upgrade-path concern; only test fixtures within this repo ever produced such manifests. | ~~Phase 4's migration classifier — include an owner-correction pass for `.codex/AGENTS.override.md` (and any future L3 reclassifications) when migrating or touching v2 manifests.~~ — RESOLVED: `classifyLegacyPath`'s `pathCodexOverride` case (`internal/cli/migrate.go`) always reattributes `owner=seed` on every legacy-to-v2 migration, regardless of modified state; no released binary ever shipped a v2 manifest with the stale `owner=core` attribution for this path to correct. | `docs/reports/cross-review-triage-overlay-scaffold-v2-p3.md` (cycle 3, AR#1), `docs/reports/self-review-2026-08-18-overlay-scaffold-v2-p3.md` (Cycle 4, residue), `internal/cli/init.go` (`ownerForScaffoldPath`), `internal/cli/migrate.go` (`classifyLegacyPath`, `buildMigratedManifest`) |
<!-- RESOLVED 2026-08-18 in feat/overlay-scaffold-v2-p4: `ReplaceOptions.OwnerForPath` (internal/upgrade/replaceplan.go) lets `runUpgradeV2` inject `ownerForScaffoldPath` into the planner. `classifyUntracked` now checks it: an untracked, desired, disk-present, content-diverging path whose resolved owner is `scaffold.OwnerSeed` is no longer classified as drift — it is left untouched (no op) and recorded as a seed `AdvisoryEntry` instead, so `exit 0` and the manifest rebuild (`rebuildManifestV2`'s existing per-path sweep, unchanged) records `owner=seed`/`DiskHash=<disk content>` on the same run. A nil `OwnerForPath` (the zero value) keeps prior behavior. Row preserved for traceability. -->
| ~~`PlanCoreReplaceDesired` の untracked パス分類(manifest 未登録 + desired に存在)は owner 非考慮で、新テンプレートリリースが seed 所有パス(docs/ 等)を新規追加し下流に同パスのローカルファイルが既存する場合、L5 契約(欠落時のみ生成・advisory)に反して「未解決 drift」(恒久 exit 3)に分類する(codex cross-review Phase 3 cycle 4, P2)。~~ (RESOLVED 2026-08-18 in feat/overlay-scaffold-v2-p4) | 将来リリース後、新 seed ファイルと衝突するローカルファイルを持つ下流で upgrade が毎回 exit 3 になる(据え置きのため非破壊、ただし機械ゲートのノイズ)。 | 発火条件が「リリース(Phase 5 後)+ 新 seed パス + 下流同パス既存」の 3 条件で現時点の露出ゼロ。owner 帰属分類は Phase 4 移行分類器の設計上の本籍地であり、cap 4/4 到達時点で操作者判断により Known gap 化。 | ~~Phase 4 の移行分類器実装 — untracked パスの分類に `ownerForScaffoldPath` 相当の owner 解決を通し、seed パスは「既存ローカル → seed として採用 + advisory」に分類する(リリース前必須)。~~ — RESOLVED: `ReplaceOptions.OwnerForPath` + `classifyUntracked`'s seed-aware branch | `docs/reports/cross-review-triage-overlay-scaffold-v2-p3.md`(Cycle 4, WC#1)、`internal/cli/upgrade_v2.go`、`internal/upgrade/replaceplan.go`(classifyUntracked) |
