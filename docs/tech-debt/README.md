# Tech debt

Record debt that should not disappear into chat history.

Recommended fields:
- debt item
- impact
- why it was deferred
- trigger for paying it down
- related plan or report

## Entries

| Debt item | Impact | Why deferred | Trigger to pay down | Related plan/report |
| --- | --- | --- | --- | --- |
| Per-slice pipelines do NOT stop on CRITICAL self-review findings | Differs from standard `/work` flow behavior | Autonomous pipelines benefit from letting verify/test confirm true positives before halting | If false-negative CRITICAL findings slip through to merge | `.claude/rules/post-implementation-pipeline.md` |
| `ralph status --json` outputs plain text, not JSON | AC11 partially met; machine consumption broken | Phase 6a scope — table/JSON rendering deferred to Phase 6b Go native | Phase 6b implementation | `docs/plans/archive/2026-04-16-ralph-cli-tool.md` |
| `ralph init/upgrade` lack transactional safety (journal.toml, staging) | AC17/18/21 not met; interrupted init can leave inconsistent state | Development cost vs. shipping core CLI functionality first | Before v1.0 release or after first user report of interrupted init corruption | `docs/plans/archive/2026-04-16-ralph-cli-tool.md` |
| Phase 6b: Go native pipeline migration pending | Pipeline runs via shell wrapper (Phase 6a) | 3400 lines of shell → Go is a major effort requiring parity tests | Next PR; parity tests must pass before shell scripts are removed | `docs/plans/archive/2026-04-16-ralph-cli-tool.md` | Bridge hardening landed 2026-07-13 in `refactor/core-hardening` (PR #128): `set -euo pipefail` in orchestrator/pipeline/ralph-CLI, source guards for testability, `ckpt_update` and parser function tests, `parse_slices` pipe-safe US-delimiter records. Shell core is now safe until Go migration. |
| `filePerm()` in upgrade.go duplicates permission logic from render.go with different strategy (name-based vs FS-metadata-based) | A new extensionless executable in templates needs updates in two places with different fix patterns | Scope of fix-template-distribution-gaps PR is distribution gaps, not refactoring permissions | Adding a second extensionless executable to templates | `docs/reports/self-review-2026-04-16-fix-template-distribution-gaps.md` (GC'd by 30-day retention 2026-07-13; recover via git history) |
| `.claude/hooks/check_mojibake.sh` + `mojibake-allowlist` + `tests/test-check-mojibake.sh` + `tests/fixtures/payloads/` + settings.json entry + AGENTS.md note + check-sync.sh exclusions | Every `Edit\|Write\|MultiEdit` in every session runs the scan; carrying a mitigation for a fixed upstream bug becomes dead weight | Temporary mitigation for upstream Claude Code Issue #43746 (U+FFFD injection at SSE chunk boundaries); permanent fix must land upstream | Upstream Issue #43746 closes in a released Claude Code version AND no local recurrences observed for 1 week | `docs/plans/archive/2026-04-17-mojibake-postedit-guard.md`, `docs/reports/self-review-mojibake-postedit-guard.md` |
| `ralph upgrade --resync <path>` / `--adopt` per-path escape hatch for `Managed=false` entries | Once a user selects `skip` on a conflict, the entry is pinned unmanaged and silently skipped on subsequent runs. Tree-wide re-adoption is available via `ralph upgrade --force` (which now re-adopts all `Managed=false` entries back to `{Hash: newHash, Managed: true}` — see `docs/specs/2026-04-16-ralph-cli-tool.md` §冪等性と自動修復), but there is no per-path or per-glob escape hatch short of hand-editing `.ralph/manifest.toml`. `--force` is all-or-nothing and also overwrites any other legitimately-conflicting files, which is often heavier than users want. | Out of scope for the "detect local edits + unified diff" PR — shipping the convergence contract first was the priority so users stop being prompt-stormed. The per-path escape hatch needs UX design (per-path vs. glob, force-template-overwrite vs. keep-local-but-track) | First user report of being stuck on an unmanaged entry they want back under management without the `--force` blast radius, OR before v1.0 release | `docs/plans/archive/2026-04-22-upgrade-detect-local-edits.md` (Open Questions), `docs/reports/self-review-2026-04-22-upgrade-detect-local-edits.md` (GC'd by 30-day retention 2026-07-13; recover via git history) |
| `runUpgradeIO` positional-parameter creep (now 6 params; trailing `bool colorize`) | Callers must remember argument order; risk grows linearly with each new display flag. Trailing `bool` at call sites (`..., true)` vs `..., false)`) is a known anti-pattern. | Out of scope for the colorize-upgrade-diff UX change; refactor would touch every test in `internal/cli/cli_test.go` for no current behavior gain. | Next time another display/IO knob lands (theme, paging, width override) — collapse into an `upgradeIOOpts` struct then. | `docs/plans/archive/2026-04-23-colorize-upgrade-diff.md`, `docs/reports/self-review-2026-04-23-colorize-upgrade-diff.md` (GC'd by 30-day retention 2026-07-13; recover via git history) |
| ~~`ralph-pipeline.sh` Outer Loop still hard-codes `codex exec review` and `_codex_*` variables / log messages despite the cross-review skill body being bidirectional~~ (RESOLVED 2026-05-08 in feat/44/ralph-loop-codex-driver: dispatcher routes through `pick_reviewer`, variables renamed to `_xreview_log`/`_reviewer`, log line is "Running cross-review (driver=…, reviewer=…)…") | Codex-driven Loop runs will recursively invoke `codex exec review` instead of `claude -p`; operator log output ("Running codex review...") is misleading after the rename | Loop driver work is out of scope for the codex-cli-parity PR per Non-goals; tracked in yoshpy-dev/ralph#44 | When #44 lands, or first time `/loop` is run under Codex and the wrong reviewer is invoked | `docs/reports/self-review-2026-05-07-codex-cli-parity.md` (GC'd by 30-day retention 2026-07-13; recover via git history), `docs/plans/archive/2026-05-07-codex-cli-parity.md` (Non-goals), yoshpy-dev/ralph#44 |
| Preflight Probe 1 / Probe 7 detect Loop-driver CLIs via `command -v` only (regressed from a `--version` round-trip via `scripts/codex-check.sh`) | A broken-but-PATH'd `claude`/`codex` binary now passes preflight and fails opaquely at first agent turn instead of producing an early, named probe failure | Phase 2 self-review flagged this as MEDIUM and recommended restoring the `--version` shape; deferred because the broken-binary case is rare on developer machines and the simpler probe shape removed the special-case path through `scripts/codex-check.sh` | First operator report of "preflight passed but the first slice failed with a cryptic error from claude/codex" | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (MEDIUM #1), `scripts/ralph-pipeline.sh:253, 391-401` |
| `pick_reviewer` (`scripts/ralph-cli-driver.sh:184-189`) third-arm fallback silently coerces an unknown driver to `codex` reviewer ("safe fallback; validate_loop_driver should have caught this") | Fallback is unreachable in production because every call site goes through `validate_loop_driver` first, but if a future caller bypasses validation the fallback masks the bug instead of failing loudly | Phase 2 self-review flagged this as MEDIUM and recommended either dropping the arm or making it `return 1`; deferred because there is no current bypass path and the comment documents the intent | Adding a second `pick_reviewer` call site that does not run `validate_loop_driver` first | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (MEDIUM #4), `scripts/ralph-cli-driver.sh:184-189` |
| `count_triage_findings`'s awk fallback (`scripts/ralph-cli-driver.sh:155-178`) excludes header / separator rows by matching `!/^\| *# /` and `!/^\| *-+/`, so it is coupled to the literal column header `#` shipped by `docs/reports/templates/cross-review-triage-report.md`. A future template author who renames column 1 from `#` to `No.` (or any other label) will silently start counting the header row as a real finding — off-by-one in every category — without any test failure | Cycle-2 self-review flagged this as LOW #2 and recommended skipping the first `^\|` row after each `## <CATEGORY>` heading unconditionally instead of substring-excluding `# `; declined as defensive over-engineering for cycle-2 since the template column is stable today | The next time the cross-review triage template is restructured (e.g. column rename, extra leading column, or template rewrite), or the next time the awk fallback is touched for any other reason — replace the substring exclusion with a row-index skip | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (LOW #2), `scripts/ralph-cli-driver.sh:155-178`, `docs/reports/templates/cross-review-triage-report.md` |
| Test 6b in `tests/test-ralph-cli-driver.sh` is a dead regression guard for the cross-review claude-reviewer dispatcher: it invokes the fake `claude` CLI directly with the `--permission-mode <value>` literal that its own assertion expects, instead of re-playing what `scripts/ralph-pipeline.sh:794-797` actually executes. The cycle-2 → cycle-3 production fix flipped the dispatcher flag from `plan` to `auto` (commit `094f964`), and the test continued to pass on cycle 2 even though the production flag was wrong (HIGH bug masked); cycle 3 had to update both production and the test in lock-step. The test cannot detect a future drift between the dispatcher invocation and its own canned argv | Future flag changes in the dispatcher will not break Test 6b unless someone remembers to also update the test's hand-rolled `claude -p ...` line. Bugs of the same shape as cycle-2 cross-review HIGH P1 will silently slip past this regression guard. | Cycle-3 scope was the production fix (`plan` → `auto`) and the test-line modernization to match; refactoring Test 6b to source the dispatcher's actual command from `ralph-pipeline.sh` (or from a refactored `dispatch_cross_review` function extracted from the dispatcher block at lines 778-803) would have been an out-of-scope abstraction landing during a fix-and-revalidate cycle | Next time the cross-review dispatcher block is touched for any reason, OR before the next cross-review surface change (e.g. permission flag, output format, model knob) — extract `dispatch_cross_review` as a sourced bash function and have Test 6b call that function with fakes on PATH, so flag changes propagate automatically into the assertion | `docs/reports/self-review-2026-05-08-ralph-loop-codex-driver-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (LOW #2 / cycle-3 finding), `tests/test-ralph-cli-driver.sh:206-214`, `scripts/ralph-pipeline.sh:778-803` |
<!-- RESOLVED 2026-07-12 in fix/known-breakage: ReadPipelineCheckpoint now probes the raw JSON map; when cross_review_triage is absent (all zeros) and codex_review_triage is present, the legacy value is unmarshalled into CrossReviewTriage at read time. New key present → legacy ignored. File is never rewritten. Three new tests added: legacy-only fixture, both-keys fixture (new key wins), and existing-fixtures regression. Row preserved for traceability. -->
| ~~`internal/state/PipelineCheckpoint` has no migration path from the pre-rename `codex_review_triage` JSON key to `cross_review_triage`~~ (RESOLVED 2026-07-12 in fix/known-breakage) | Existing per-project `checkpoint.json` files become silently un-readable for the renamed field; resume drops triage counts to zero with no warning | Migration plumbing was not in the plan's Slice list; risk is small because checkpoints regenerate on the next pipeline run | ~~First user reports a stale resume losing triage state, or whenever `schema_version` is bumped for any other reason — handle migration in the same hop~~ | `docs/reports/self-review-2026-05-07-codex-cli-parity.md`, `internal/state/types.go` (`CrossReviewTriage`), `internal/state/reader.go` (`ReadPipelineCheckpoint`) |
| `ralph doctor`'s `checkHooks` only walks `.claude/settings.json` `hooks[*].command` paths; there is no equivalent integrity check for `.codex/config.toml` `[hooks.*] command = ["./..."]` paths | A typo or stale path in `.codex/config.toml` ships green through doctor today (the cycle-2 CRITICALs in this report would have been caught at doctor time if the same on-disk-existence walk applied to Codex hooks) | Cycle-2 fix was scoped to closing the doctor warning loop, not extending hook-integrity coverage to the Codex side | When a third Codex-side hook lands, or as part of fixing the cycle-2 CRITICAL hook-path bugs (whichever comes first) | `docs/reports/self-review-2026-05-07-codex-cli-parity.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Cycle 2 section), `internal/cli/doctor.go:225-294` (`checkHooks`) |
<!-- RESOLVED 2026-05-07 in commit 79d7a73: probeBinary now wraps the `<bin> --version` exec in a 5s context.WithTimeout, returns the first non-empty line of output, and surfaces both LookPath and run errors symmetrically. The hang scenario described below is no longer reachable. Row preserved for traceability. -->
| ~~`probeBinary` in `internal/cli/doctor.go` runs `<bin> --version` with no timeout via `exec.Command(...).CombinedOutput()`~~ (RESOLVED 79d7a73) | A hung Claude/Codex binary (waiting on stdin, slow npm shim re-installing on first run, blocked network call) wedges `ralph doctor` indefinitely with no signal to the operator | Cycle-2 was scoped to fixing the `LookPath`-only false-pass bug; the orthogonal hang case wasn't in the cross-review finding | ~~Next time a CI runner or sandboxed environment causes a slow CLI binary to wedge doctor, OR when the cycle-2 CRITICAL hook-path bugs are fixed and a follow-up commit touches `probeBinary` anyway~~ — fixed 79d7a73 | `docs/reports/self-review-2026-05-07-codex-cli-parity.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Cycle 2 section), `internal/cli/doctor.go:111-138` (`probeBinary`), commit `79d7a73` |
| Adversarial-prompt awk renderer (`lreplace` + ENVIRON-based literal substitution) is duplicated three places: `scripts/ralph-pipeline.sh` cross-review phase, `tests/test-xreview-prompt-render.sh`, and `tests/test-xreview-gate-regression.sh`. A drift guard (grep for `function lreplace`) keeps them named alike, but a divergent implementation could pass the name check while silently changing behavior. | If a future maintainer fixes a renderer bug on one side and not the others, the unit test would keep reporting green while the production renderer was broken. Today's risk is low because the renderer body is small and the drift guard is loud, but the pattern is fragile. | Out of scope for the issue #50 fix; extracting `render_adversarial_prompt` into `scripts/ralph-cli-driver.sh` next to `count_triage_findings` would cleanly de-duplicate but adds surface area unrelated to the silent-bypass fix. | Next time the renderer grows (new placeholder, new metacharacter edge case, or another caller besides the cross-review phase), pull the awk body out into a single sourced shell function. | `docs/reports/self-review-2026-05-13-fix-xreview-placeholder-substitution.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (MEDIUM #1), `scripts/ralph-pipeline.sh:808-826`, `tests/test-xreview-prompt-render.sh`, `tests/test-xreview-gate-regression.sh` |
<!-- RESOLVED 2026-07-12 in fix/known-breakage: renderPackInto helper extracted to language_pack.go; pack.go and init.go both use it, so they cannot diverge. Pack payload files render under packs/languages/<lang>/, rule.md maps to .claude/rules/<lang>.md, manifest keys namespaced, Meta.Packs updated. Three tests added: AC1/AC2 assertions, no-duplicate-packs, unknown-lang error. Row preserved for traceability. -->
| ~~`ralph pack add <lang>` writes pack files to the project root instead of `packs/languages/<lang>/`~~ (RESOLVED 2026-07-12 in fix/known-breakage) | `ralph pack add` is effectively broken for every language pack. Workaround: pack files are auto-distributed via `ralph init` with the right `packs:` array, so the `pack add` CLI path is mostly cosmetic today; users who hit it report confusion rather than blocking failures. | Out of scope for the Terraform language pack PR (Codex advisory HIGH#1; plan Non-goals and R6 explicitly defer this). The Terraform PR's acceptance only required `PackFS("terraform")` to resolve and `ralph pack list` to enumerate `terraform` — both verified. | ~~Fix `pack.go:64-67` to compute `filepath.Join(absDir, "packs", "languages", lang)` (matching `init.go:158`'s layout) and add an integration test that runs `ralph pack add <lang>` in a tempdir and asserts files land under `packs/languages/<lang>/`. File a follow-up issue at `/pr` time.~~ | `docs/plans/archive/2026-05-13-add-terraform-language-pack.md` (Non-goals, Risks R6), `docs/reports/self-review-2026-05-13-add-terraform-language-pack.md` (GC'd by 30-day retention 2026-07-13; recover via git history) ("Tech debt identified"), `internal/cli/pack.go`, `internal/cli/language_pack.go` (shared `renderPackInto`), `internal/cli/init.go` |
| `tfsec` is upstream-archived (announced by Aqua in 2024); `trivy config` is the recommended successor. `packs/languages/terraform/verify.sh:65-71` runs `tfsec` first and falls back to `trivy config` to preserve behavior for the existing tfsec install base. | Optional security check will eventually 404 or refuse new platforms as Aqua winds down hosting. Users with `tfsec` installed continue to work today, but the path is on borrowed time. The pack already falls back to `trivy config` when `tfsec` is absent, so the failure mode is graceful. | Ripping out the `tfsec` arm today would be churn for users who still rely on it; the dual-path keeps everyone working. Plan R2 explicitly chose this dual-arm strategy. | When `tfsec` install starts failing on a supported OS (macOS/Linux), OR `trivy config` ergonomics catch up enough that the fallback can become the default. At that point, drop the `tfsec` arm and keep only `trivy config`. | `docs/plans/archive/2026-05-13-add-terraform-language-pack.md` (R2), `docs/reports/self-review-2026-05-13-add-terraform-language-pack.md` (GC'd by 30-day retention 2026-07-13; recover via git history) ("Tech debt identified"), `packs/languages/terraform/verify.sh:65-71` |
| The hermetic-PATH coreutils symlink loop in `tests/test-terraform-pack-verify.sh:95-102` includes shell builtins (`printf`, `test`, `true`, `false`). Under `/bin/sh` these resolve via `command -v` to their bare builtin names (e.g. the literal string `"printf"`), so `ln -s "printf" "$coreutils_dir/printf"` creates a relative self-referential symlink that resolves to nothing. Four of the fifteen entries are broken symlinks on the current host. Tests pass anyway because `verify.sh` and the test stubs invoke these as builtins, not via PATH lookup. | Cosmetic noise only — no test depends on the broken symlinks resolving. Could mislead a future debugger who `ls -la`s the hermetic dir and assumes the fixture is corrupted. No behavior risk. | Cycle-2 self-review LR-1 flagged as LOW; tests pass; fixing is a five-minute follow-up not worth scoping into a fix-and-revalidate cycle. | Next time anyone touches that loop, OR if `ls -la` of the hermetic dir surfaces in a CI debug log and confuses a reader. Cleanest fix: drop `printf test true false` from the loop with a one-line comment that they are `/bin/sh` builtins; alternative is to guard `ln -s` on `case "$_resolved" in /*) ln -s ...;; esac`. | `docs/reports/self-review-2026-05-13-add-terraform-language-pack-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Finding LR-1), `tests/test-terraform-pack-verify.sh:95-102` |
| The IaC-tool leak-guard in `tests/test-terraform-pack-verify.sh:106-110` uses `[ -e "$coreutils_dir/$_banned" ]`, which is false for broken symlinks under POSIX `test`. So if a future maintainer added `terraform` to the coreutils symlink loop on a host where `terraform` is absent, `command -v terraform` would return empty, the per-tool `[ -z "$_resolved" ]` check would fire first ("FAIL: required coreutil 'terraform' not found"), and the leak-guard would never even run. The intended threat model — a host where terraform actually leaks is a host where terraform is installed, so the symlink would be non-broken — holds, but the guard is one layer thinner than it could be. | Defense-in-depth gap, not a live bug. The "non-broken symlink leak" case is fully covered today; the "broken symlink leak" case is caught a step earlier by a different check with a less helpful error. | Cycle-2 self-review LR-2 flagged as LOW; current behavior fails fast and loud, just via the wrong error path for the typo case. | Next time the leak-guard is touched, OR when a maintainer hits the "required coreutil 'terraform' not found" error and is confused about why a banned tool name landed in the coreutils loop. Fix: change `[ -e ... ]` to `[ -L ... ] \|\| [ -e ... ]` (true for any symlink including broken, OR a real file) so the leak-guard's specific error message wins over the generic coreutil-missing one. | `docs/reports/self-review-2026-05-13-add-terraform-language-pack-cycle2.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (Finding LR-2), `tests/test-terraform-pack-verify.sh:106-110` |
| Terraform ignore set in `.gitignore` / `templates/base/.gitignore` (block added by `03c5598`) diverges from GitHub's canonical `Terraform.gitignore` on four minor points: (a) `*.tfstate.*.backup` is narrower than canonical `*.tfstate.*` and misses workspace state (`terraform.tfstate.d/<workspace>/*.tfstate.backup`) plus `.terraform.tfstate.lock.info`; (b) only `*.auto.tfvars*` is ignored, not the broader `*.tfvars` / `*.tfvars.json` (defensible: explicit `-var-file=` fixtures often live in repo, but `prod.tfvars` with credentials is not protected); (c) `.terraformrc` and `terraform.rc` (Terraform Cloud token files) are not ignored — usually `$HOME`-scoped so risk is low; (d) the block-header comment "state and cache" understates scope (block also covers plan output, overrides, crash logs). | Low — current set covers the literal patterns the rule file (`terraform.md:11`) names and the most common state-leak vectors. Workspace state, project-local `.terraformrc`, and explicit-`-var-file` credential files are uncommon in scaffold-time setups. Comment wording is cosmetic. | Cycle-3 self-review surfaced all four as LOW maintainability findings (no CRITICAL/HIGH/MEDIUM); broadening to canonical alignment would have required scope expansion (`!example.tfvars` negation design, additional template-mirror commits) during a fix-and-revalidate cycle with the pipeline cap already raised to 3. Defensible-narrow choice matches the rules file. | Adoption of `terraform workspace` in any scaffolded project; first report of a contributor accidentally committing a project-local `.terraformrc` or a `prod.tfvars` with credentials; OR when a secret-scanning CI hook is added — broaden to canonical `*.tfstate.*` / `*.tfvars` with explicit `!fixture.tfvars` negation patterns, add `terraform.tfstate.d/` / `.terraform.tfstate.lock.info` / `.terraformrc` / `terraform.rc`, and consider retitling the comment to "Terraform / OpenTofu (state, plans, overrides, crash logs)". | `docs/reports/self-review-2026-05-13-add-terraform-language-pack-cycle3.md` (GC'd by 30-day retention 2026-07-13; recover via git history) (all four LOW findings), `.gitignore:15-28`, `templates/base/.gitignore:15-28`, canonical reference `https://raw.githubusercontent.com/github/gitignore/main/Terraform.gitignore` |
| Codex driver ignores per-phase model arg passed via `run_agent` 4th argument (`scripts/ralph-cli-driver.sh`). The `_run_agent_codex` function does not accept a model override because `codex exec` has no stable `-m <model>` flag equivalent. Model receipts for Codex-driven runs record `effective_model="codex-default"` and `honored=false` — the model used is whatever is configured in `.codex/config.toml`, not the per-phase routing decision from `resolve_phase_model`. | Loop runs under `RALPH_LOOP_DRIVER=codex` silently use the Codex default model for every phase (implement, self-review, verify, test, sync-docs, pr), ignoring RALPH_SELF_REVIEW_MODEL=opus and RALPH_IMPLEMENT_MODEL. The quality-floor gate (verify/test/cross-review) and cycle-2 escalation receipts still record the gap, so auditing is unaffected — the honor=false fields prevent false audit evidence. | Codex CLI does not expose a stable per-invocation model flag (unlike `claude -p --model <alias>`). Fixing this requires either a Codex API change or routing through a wrapper that edits `.codex/config.toml` on the fly, which is fragile and not thread-safe under parallel slices. Recorded as non-fixable without upstream Codex API support (plan Design Decision 3, AC3b). | When Codex CLI adds a stable per-invocation model override flag (e.g. `codex exec -m <model>`), update `_run_agent_codex` in `scripts/ralph-cli-driver.sh` to pass the 4th arg through and flip `honored=true` in receipt writes. Also update `model-routing.md` Receipts section and this entry. | `docs/plans/archive/2026-07-11-loop-model-routing.md` (Non-goals, Design Decision 3, AC3b), `scripts/ralph-cli-driver.sh` (`_run_agent_codex`), `.claude/rules/model-routing.md` (Receipts paragraph) |
<!-- RESOLVED 2026-07-12 in fix/unify-permission-mode-default: toml/Go Default() changed to bypassPermissions; run.go env-export changed from unconditional append to appendEnvIfMissing for MODEL/EFFORT/PERMISSION_MODE (env > toml); MAX_* use Flags().Changed() for CLI > env > toml priority. Row preserved for traceability. -->
| ~~`RALPH_PERMISSION_MODE` default divergence: shell default is `bypassPermissions` (`scripts/ralph-config.sh:20`); toml/Go default is `auto` (`templates/base/ralph.toml` `[pipeline] permission_mode`, `internal/config/config.go` `Default()`)~~ (RESOLVED 2026-07-12 in fix/unify-permission-mode-default: all three sources now default to `bypassPermissions`; env > toml priority enforced in `run.go`) | Same pipeline runs with different permission posture depending on entry point — direct shell invocation bypasses all permission prompts while `ralph run` uses auto-approval, which is more restrictive | Which default is correct is an operator/product decision (autonomous loop ergonomics vs. safety); resolving it is out of scope for a docs-drift fix | Next change to permission handling, or first operator surprise report of unexpected permission prompts (or unexpected bypass) depending on entry point | This audit (2026-07-12); `scripts/ralph-config.sh:20`, `templates/base/ralph.toml` (`[pipeline] permission_mode = "auto"`), `internal/config/config.go` `Default()` |
<!-- RESOLVED 2026-07-12 in fix/cli-stub-stdin-hang: both stubs now drain stdin only when `[ -f /dev/stdin ]` (regular file, EOF guaranteed); TTYs and open pipes fall through to the safe no-drain branch. Call-site `< /dev/null` added to test 6a. Elapsed-time regression test (Test 13) using FIFO + background sleep writer added to test-ralph-cli-driver.sh. Row preserved for traceability. -->
| ~~`tests/fixtures/cli-stubs/{codex,claude}` drain stdin via `head -c 4096` guarded only by `[ -t 0 ]` (skip iff TTY)~~ (RESOLVED 2026-07-12 in fix/cli-stub-stdin-hang) | When stdin is a non-TTY descriptor that never reaches EOF (an inherited open pipe from a background Claude Code Bash task), `head` blocks forever and the entire verify chain (`test-ralph-cli-driver.sh` → `verify.local.sh` → `run-verify.sh`) hangs at 0% CPU with no output — observed during PR #116 | Discovered post-PR-#114; root cause was the TTY-only guard not covering the open-pipe case; the fix is a one-line condition change per stub | ~~On recurrence of `run-verify.sh` hanging at 0% CPU with no output when run from a Claude Code background Bash task~~ — RESOLVED | `docs/plans/archive/2026-07-12-cli-stub-stdin-hang.md`, `tests/fixtures/cli-stubs/codex`, `tests/fixtures/cli-stubs/claude`, `tests/test-ralph-cli-driver.sh` (Test 13) |
| Legacy shell CLI (`scripts/ralph`) retirement | Prints a deprecation notice when a different `ralph` binary is on PATH (suppressed when `scripts/` itself is on PATH via inode self-check, fix #134); two codebases to maintain (shell + Go). Commands `doctor`, `init`, `upgrade` are Go-only; `cleanup` command missing from Go CLI. | Go CLI not yet at full shell-CLI command parity (`cleanup` subcommand unimplemented). Shell CLI still needed for `plan`/`run`/`status`/`abort`/`retry`/`cleanup` in environments where the Go binary is not installed. Retirement deferred until Go CLI reaches parity. | Go CLI gains `cleanup` command (the last missing subcommand) and command-parity tests pass for all subcommands listed in `print_usage`. At that point: remove `scripts/ralph`, remove the deprecation-notice block and test, and update `AGENTS.md` repo map. | `docs/plans/archive/2026-07-13-dual-cli-consolidation.md`, `docs/plans/active/2026-07-24-deprecation-notice-self-detect.md`, `scripts/ralph` (deprecation block), `tests/test-ralph-deprecation-notice.sh` |
<!-- RESOLVED 2026-07-12 in fix/xreview-base-detection: detect_base_branch() helper added to ralph-cli-driver.sh (resolution order: RALPH_XREVIEW_BASE > symbolic-ref origin/HEAD > main/master fallback); ralph-pipeline.sh gate replaced HEAD@{upstream} with detect_base_branch(); orchestrator exports RALPH_XREVIEW_BASE="$_base_branch" before slice/integration pipeline invocations; SKILL.md x4 updated; gate-proving tests added to test-ralph-cli-driver.sh. Row preserved for traceability. -->
| ~~Cross-review base detection still uses `git rev-parse --abbrev-ref 'HEAD@{upstream}'` at the two sites that actually gate cross-review: `scripts/ralph-pipeline.sh:807` (+ `templates/base/` mirror) and `.claude/skills/cross-review/SKILL.md:51` (+ `.agents/` mirror). On a pushed feature branch the upstream ref resolves to `origin/<same-branch>`, so `git diff "${_base}...HEAD"` is empty and cross-review is silently skipped (pipeline line 809 gates on `--quiet`). The repo-wide-drift-fixes PR fixed the 4 `pipeline-outer.md` prompt copies to `git symbolic-ref --short refs/remotes/origin/HEAD` but left these production call sites untouched, so the prompt now documents a strategy that diverges from the shell that runs. `scripts/ralph:490` and `scripts/ralph-worktree.sh:85` already use the correct `symbolic-ref` form.~~ (RESOLVED 2026-07-12 in fix/xreview-base-detection) | On any branch tracked by a remote (the common case after `git push -u`), the Ralph Loop cross-review gate and the standard-flow cross-review base both collapse to an empty diff — cross-review can be skipped or scoped to nothing without a loud failure | Out of scope for repo-wide-drift-fixes, whose Scope 2 covered only the prompt copies; porting the fix to the pipeline script + skill body (+ mirrors) is a behavior change to the running gate, not a docs/drift fix | Next touch to the cross-review dispatch block in `ralph-pipeline.sh`, OR first operator report of cross-review producing no findings on a pushed feature branch — replace both `HEAD@{upstream}` sites (+ mirrors) with `git symbolic-ref --short refs/remotes/origin/HEAD 2>/dev/null \| sed 's\|^origin/\|\|'` and add an elapsed/diff-nonempty regression assertion | `docs/reports/self-review-2026-07-12-repo-wide-drift-fixes.md` (Cycle 2 addendum, HIGH follow-up), `scripts/ralph-pipeline.sh:807-809`, `.claude/skills/cross-review/SKILL.md:51`, `templates/base/scripts/ralph-pipeline.sh:807`, `.agents/skills/cross-review/SKILL.md:51` |
<!-- RESOLVED 2026-08-01 in commit 9bfe07e (fix: address self-review findings): herdrAgentName(orgID, seatID) added to internal/org/spawn.go as the single grep-able definition of the herdr agent-name convention; used at AgentStart (spawn.go:189) and at Send's/Wait's AgentWait call sites (verbs.go:83,138). WaitParams gained an OrgID field, wired from the CLI's --org-id flag (internal/cli/org.go newOrgWaitCmd:239). Two regression tests added: TestOrgSpawn_HerdrAgentNameNamespacedByOrgID, TestHerdrAgentName_NamespacesBySeatAndOrg (internal/org/spawn_test.go). Confirmed still fixed under -race by the /test pass (docs/reports/test-2026-08-01-org-runtime-mechanism.md, Regression checks). Row preserved for traceability. -->
| ~~herdr agent names are not `org_id`-namespaced, while agmsg team names are (`ralph-<org_id>`). `internal/org/spawn.go` registers the herdr agent under the bare `seat_id`; `internal/org/verbs.go` `Send`/`Wait` target the bare `seat_id` too. `org wait` requires `--org-id` and then discards it (`WaitParams` has no `OrgID` field).~~ (RESOLVED 2026-08-01 in commit 9bfe07e) | Two concurrent orgs that both spawn a seat with the same id (e.g. `reviewer`) collide in herdr's global agent namespace — `ralph org send` / `ralph org wait` can act on the wrong org's live seat. Manifest accounting (`ActiveSeatCount`, `Roster`, `disband`) stays correctly isolated by `org_id`; only the external herdr resource namespace leaks. | PR① is a mechanism layer with operator-driven, typically single-org usage; the seat-naming decision belongs with the seat model in PR② (座席化) | ~~Before PR③ (`ralph org start` / Lead 自律編成) can run two orgs concurrently — namespace the herdr agent name (e.g. `ralph-<org_id>-<seat_id>`), persist it on the manifest event, and have send/wait/read/stop resolve it from the seat record instead of re-deriving from the bare seat id. Add a two-org concurrency regression test.~~ — fixed ahead of schedule within PR① itself | `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md` (HIGH finding, resolved), `internal/org/spawn.go:189,249-251`, `internal/org/verbs.go:83,117,138`, `internal/cli/org.go:239`, `internal/org/spawn_test.go` (regression tests), commit `9bfe07e` |
| `max_seats` is enforced across an unlocked read-then-append window in the spawn saga: `Manifest.Read()` → `ActiveSeatCount` → `ValidateSpawn` → `appendEvent`, with no advisory lock in `internal/org/manifest.go`. | Two concurrent `ralph org spawn` processes can both observe `activeSeats = N`, both pass the `max_seats` gate, and both append — exceeding the `[org].max_seats` envelope. The plan's edge-case list covers append *line integrity* (`O_APPEND` single write) but not this count race. | Spawns are operator-driven and serial in PR①; a lock file is only load-bearing once a Lead issues concurrent spawns programmatically | PR③ (Lead autonomy issuing concurrent spawns), or first observed `max_seats` overshoot — take a lock file around read+validate+append | `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md` (LOW finding), `internal/org/spawn.go:120-130`, `internal/org/manifest.go:222-241` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-seats commit 67220ef (Slice 1, PR②): the agmsg adapter was rewritten from the assumed single-binary `--team/--as` flag shape to the verified real interface -- a script collection under `<agmsg_home>/scripts/` (send.sh/join.sh/team.sh/history.sh/despawn.sh/whoami.sh/delivery.sh), invoked as `bash <script> <positional args>`. `agmsgArgs` and the package-level ASSUMPTION note no longer exist in internal/org/driver/agmsg.go. Row preserved for traceability. -->
| ~~agmsg CLI flag shape (`--team TEAM --as FROM <verb>`) is an unverified assumption, centralised in `agmsgArgs` in `internal/org/driver/agmsg.go` and documented as such in a package-level ASSUMPTION note. Team name convention `ralph-<org_id>` in `internal/org/spawn.go` `agmsgTeam()` is likewise provisional.~~ (RESOLVED 2026-08-02 in feat/org-runtime-seats commit 67220ef) | First live `ralph org spawn` against a real agmsg install may fail at the `agmsg_announce` saga step (which does compensate and record `spawn_failed`, so the failure is auditable rather than silent). | The real agmsg CLI was not available during PR①; the assumption is deliberately isolated to one function so a correction touches one place | ~~First live use against a real agmsg install — validate `agmsgArgs` argv shape and the team-name convention, then remove the ASSUMPTION note~~ — validated against agmsg v1.1.13's real script interface in PR②'s Slice 1 | `docs/plans/active/2026-08-01-org-runtime-mechanism.md` (Open questions), `docs/reports/self-review-2026-08-01-org-runtime-mechanism.md`, `internal/org/driver/agmsg.go`, `docs/plans/active/2026-08-02-org-runtime-seats.md` (Slice 1), commit `67220ef` |
<!-- RESOLVED 2026-08-02 in feat/org-runtime-seats (Slice 4, PR②): unit tests added to internal/org/verbs_test.go for Send (dry-run valid message, unknown seat, seat-without-pane-id) and Stop (dry-run existing seat, existing seat with no pane_id/agmsg_team) branches, plus new Wait/Read test suites (happy path, timeout/default-lines branches, unknown-seat and no-pane-id error paths); CLI wiring tests added to internal/cli/org_test.go for `ralph org wait`/`ralph org read` (happy path + unknown-seat paths) against the herdr stub. Post-change internal/org coverage: Send 83.3%, Wait 100.0%, Read 91.7%, Stop 92.0% (was Send 70.0%, Wait 0.0%, Read 0.0%, Stop 80.0%). Row preserved for traceability. -->
| ~~`Verbs.Send`, `Verbs.Wait`, `Verbs.Read` (`internal/org/verbs.go:54,131,156`) and their CLI wiring (`internal/cli/org.go` `newOrgSendCmd`/`newOrgWaitCmd`/`newOrgReadCmd`) have 0.0% test coverage — confirmed under both a package-local `internal/org` run and a combined `-coverpkg=./internal/org/...,./internal/cli/...` run from `internal/cli`; no unit or CLI-level test exercises these three verbs at all.~~ (RESOLVED 2026-08-02 in feat/org-runtime-seats, Slice 4) | 3 of the 7 verbs in PR①'s scope ship with no behavioral proof; a regression in `send`/`wait`/`read` (e.g. a future refactor of `herdrAgentName` call sites, or the `--dry-run` seat-lookup gap already noted below) would not be caught by `go test ./...` today. | Out of scope for PR① — the `/test` pass focused on proving the 10 plan ACs (spawn saga, status, dry-run, config lockstep), none of which name send/wait/read directly; adding coverage was flagged as a recommendation, not a blocking gap, in the test report. | ~~Alongside PR② (seat operation phase), which will exercise send/wait/read live once seats run resident sessions — add at minimum: `Send` happy-path + stub-agmsg-failure case, `Wait` happy-path + timeout case, `Read` happy-path + seat-not-found case, reusing the stub herdr/agmsg pattern already established in `spawn_test.go`/`org_test.go`~~ — added in PR②'s Slice 4 | `docs/reports/test-2026-08-01-org-runtime-mechanism.md` (Coverage, Test gaps), `docs/specs/2026-08-01-org-runtime.md` (PR②), `internal/org/verbs.go`, `internal/cli/org.go`, `internal/org/verbs_test.go`, `internal/cli/org_test.go`, `docs/plans/active/2026-08-02-org-runtime-seats.md` (Slice 4) |
