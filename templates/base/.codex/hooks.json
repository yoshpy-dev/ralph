{
  "description": "Codex hook wiring for ralph: routes PostToolUse through ralph-dispatch.sh so the same layered .d dispatcher Claude Code uses also runs under Codex.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && ./.claude/hooks/ralph-dispatch.sh PostToolUse"
          }
        ]
      }
    ]
  }
}
